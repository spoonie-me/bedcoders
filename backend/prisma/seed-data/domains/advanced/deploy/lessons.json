[
  {
    "id": "adv-dep-l01",
    "moduleId": "adv-dep-a1",
    "title": "Keep Costs Down",
    "description": "Optimize token usage. Cache, batching, and model selection strategies.",
    "duration": 15,
    "order": 1,
    "learningObjectives": [
      {
        "text": "Calculate cost per agent run using Anthropic's pricing",
        "bloomLevel": "apply"
      },
      {
        "text": "Implement response caching to eliminate duplicate API calls",
        "bloomLevel": "apply"
      },
      {
        "text": "Design a model routing strategy (haiku vs. sonnet vs. opus) based on task complexity",
        "bloomLevel": "create"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "The math that keeps you in business",
        "podNumber": 1,
        "duration": 5
      },
      {
        "type": "hook",
        "body": "Your research agent processes 100 competitor companies every week. Each run uses 50,000 tokens. At Claude Opus 4.6 pricing, that's $7.50 per run \u2014 $390/year. Switch to Haiku for the classification steps and cache the research results: now it's $40/year. Same output. 90% savings."
      },
      {
        "type": "text",
        "body": "**Token cost basics (Claude pricing as of 2025):**\n\n| Model | Input cost | Output cost |\n|-------|-----------|-------------|\n| claude-haiku-4-5 | $0.80/MTok | $4/MTok |\n| claude-sonnet-4-6 | $3/MTok | $15/MTok |\n| claude-opus-4-6 | $15/MTok | $75/MTok |\n\n*MTok = 1 million tokens*\n\n**Cost per agent run formula:**\n```js\nconst cost = (\n  (inputTokens / 1_000_000) * inputPrice +\n  (outputTokens / 1_000_000) * outputPrice\n);\n// Track via: response.usage.input_tokens + response.usage.output_tokens\n```\n\n**Where tokens go in a typical agent:**\n- System prompt: 1,000-5,000 (repeated every call)\n- Tool definitions: 2,000-4,000 (repeated every call)\n- Conversation history: grows each iteration\n- Tool results: 500-10,000 per tool call\n\n**Key insight**: the system prompt and tool definitions are paid for on EVERY API call. A 3,000-token system prompt across 20 iterations = 60,000 tokens just for the instructions."
      },
      {
        "type": "interactive-guess",
        "question": "An agent makes 15 API calls. Each call has a 2,000-token system prompt and 500 tokens of output. Input tokens = 2,500/call \u00d7 15 = 37,500. Using Haiku, what's the cost?",
        "answer": "**About $0.06.** Input: (37,500 / 1,000,000) \u00d7 $0.80 = $0.030. Output: (7,500 / 1,000,000) \u00d7 $4 = $0.030. Total: ~$0.06 per run. That's cheap! But scale to 1,000 users running this daily = $60/day = $21,900/year. Cost optimization matters at scale.",
        "hint": "Use the formula: (tokens / 1,000,000) \u00d7 price per MTok"
      },
      {
        "type": "takeaway",
        "body": "Measure actual token usage before optimizing. Premature cost optimization wastes time. Measure first, cut what matters most."
      },
      {
        "type": "pod-header",
        "title": "The three biggest optimizations",
        "podNumber": 2,
        "duration": 5
      },
      {
        "type": "text",
        "body": "**1. Model routing** (biggest savings)\nUse the cheapest model that can handle the task:\n```js\nfunction selectModel(task) {\n  if (task.type === 'classification') return 'claude-haiku-4-5-20251001'; // 10\u00d7 cheaper\n  if (task.type === 'simple_summary') return 'claude-haiku-4-5-20251001';\n  if (task.type === 'complex_reasoning') return 'claude-sonnet-4-6';\n  if (task.type === 'critical_evaluation') return 'claude-opus-4-6';\n  return 'claude-haiku-4-5-20251001'; // default cheap\n}\n```\n\n**2. Response caching** (zero cost for repeated inputs)\n```js\nconst cache = new Map();\nasync function cachedGenerate(prompt, model) {\n  const key = `${model}:${prompt}`;\n  if (cache.has(key)) return cache.get(key);\n  const result = await generate(prompt, model);\n  cache.set(key, result);\n  return result;\n}\n// For production: use Redis with TTL based on data freshness needs\n```\n\n**3. Prompt compression** (reduce system prompt size)\nEvery 1,000 tokens removed from system prompt saves money on every API call.\n- Remove examples once the model demonstrates correct behavior\n- Use references instead of full text: 'format: [see example in appendix]'\n- Batch multiple classification tasks in one call instead of one call per item"
      },
      {
        "type": "callout",
        "variant": "example",
        "body": "**Real-world savings from model routing:**\nBefore: All 20 agent steps use claude-opus-4-6 \u2192 $3.50/run\nAfter: 15 steps use haiku (classification, search parsing), 4 use sonnet (drafting), 1 uses opus (final quality check) \u2192 $0.45/run\n87% cost reduction. Same output quality."
      },
      {
        "type": "exercise",
        "exerciseRef": "ADV-DEP-001"
      },
      {
        "type": "pod-header",
        "title": "Batching and async processing",
        "podNumber": 3,
        "duration": 5
      },
      {
        "type": "text",
        "body": "**Batching** reduces latency and sometimes costs by processing multiple items together:\n\n```js\n// Instead of one API call per item:\nfor (const item of items) {\n  await classify(item); // N calls\n}\n\n// Batch them:\nconst batchPrompt = `Classify each of these items. Return JSON array.\n\nItems:\n${items.map((item, i) => `${i+1}. ${item}`).join('\\n')}`;\nconst results = await singleApiCall(batchPrompt); // 1 call\n```\n\n**When batching works:**\n- Classification / labeling tasks (5-20 items per prompt)\n- Independent items with the same output format\n- When order doesn't matter\n\n**When batching breaks:**\n- Items need different context per item\n- Output becomes too large for max_tokens\n- Claude's attention degrades on the last items in large batches (real issue)\n\n**Anthropic Batch API** (50% cheaper, async):\nFor large offline jobs, the Batch API processes up to 100K requests at 50% discount. You submit a batch and get results in < 24 hours. Perfect for nightly processing jobs."
      },
      {
        "type": "exercise",
        "exerciseRef": "ADV-DEP-002"
      }
    ]
  },
  {
    "id": "adv-dep-l02",
    "moduleId": "adv-dep-m1",
    "title": "Safe & Aligned Agents",
    "description": "Prevent hallucinations and harmful output. Keep agents on rails in production.",
    "duration": 15,
    "order": 2,
    "learningObjectives": [
      {
        "text": "Identify the three main failure modes of production agents",
        "bloomLevel": "analyze"
      },
      {
        "text": "Implement output validation to catch hallucinations before they reach users",
        "bloomLevel": "apply"
      },
      {
        "text": "Design a content safety layer for user-facing agent outputs",
        "bloomLevel": "create"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "What goes wrong in production",
        "podNumber": 1,
        "duration": 5
      },
      {
        "type": "hook",
        "body": "Your agent worked perfectly in testing. Then it went live and told a user that your refund policy offers 60-day returns (it's 30). Or it confidently cited a 'source' that doesn't exist. Or it responded to a frustrated user with 'That's unfortunate, please try again.' Production agents fail in ways tests never predict."
      },
      {
        "type": "text",
        "body": "Three failure modes in production agents:\n\n**1. Hallucination** (most common)\nClaude generates plausible-sounding but incorrect facts.\n- Citing non-existent sources\n- Wrong dates, prices, names\n- Inventing policy details\n*Fix*: Ground outputs in retrieved context (RAG), validate claims against known data\n\n**2. Scope violation** (most dangerous)\nAgent takes actions outside its intended scope.\n- Answering questions about topics it shouldn't touch\n- Performing actions it wasn't authorized for\n- Giving advice in regulated domains (medical, legal, financial)\n*Fix*: Explicit scope restrictions in system prompt + output validation\n\n**3. Tone/quality failure** (most user-visible)\nOutputs that are technically correct but feel wrong.\n- Too formal or too casual for the context\n- Overly long when brevity was needed\n- Dismissive of user frustration\n*Fix*: Output format validation + examples of good/bad responses"
      },
      {
        "type": "interactive-guess",
        "question": "An agent generates: 'Our premium plan costs $29/month, which is the industry standard for this feature set.' The actual price is $19/month. What type of failure is this?",
        "answer": "**Hallucination + scope violation.** The price is hallucinated (wrong number). The 'industry standard' claim is a scope violation \u2014 the agent made an unverifiable competitive claim it had no basis to make. Both failures compound: incorrect + overreaching.",
        "hint": "Is the wrong price a hallucination? Is 'industry standard' a fact the agent can actually know?"
      },
      {
        "type": "takeaway",
        "body": "Validate outputs before they reach users. A wrong output that's caught is a bug. A wrong output that reaches a user is an incident."
      },
      {
        "type": "pod-header",
        "title": "Output validation patterns",
        "podNumber": 2,
        "duration": 5
      },
      {
        "type": "text",
        "body": "**Pattern 1: Schema validation**\nFor structured outputs, validate the JSON shape before using it:\n```js\nconst result = JSON.parse(agentOutput);\nif (!result.price || typeof result.price !== 'number') {\n  throw new ValidationError('Agent output missing required price field');\n}\n```\n\n**Pattern 2: Factual grounding check**\nFor outputs that reference your data, verify claims against source:\n```js\n// Agent says: 'Your plan renews on March 15.'\nconst actualDate = await db.subscriptions.findUnique({ where: { userId } }).renewsAt;\nconst claimedDate = extractDate(agentOutput);\nif (!datesMatch(claimedDate, actualDate)) {\n  // Don't use agent output \u2014 replace with verified data\n  return `Your plan renews on ${formatDate(actualDate)}.`;\n}\n```\n\n**Pattern 3: Scope guard**\nDetect out-of-scope content before outputting:\n```js\nconst scopeGuard = await client.messages.create({\n  model: 'claude-haiku-4-5-20251001',\n  messages: [{ role: 'user', content:\n    `Does this response contain: medical advice, legal advice, financial advice, competitor comparisons, or pricing claims not in the provided context? Answer YES or NO.\\n\\nResponse: ${agentOutput}` }]\n});\nif (scopeGuard.content[0].text.includes('YES')) {\n  return safeDefaultResponse;\n}\n```"
      },
      {
        "type": "callout",
        "variant": "tip",
        "body": "**Use Haiku for validation.** A cheap haiku call to check the output costs ~$0.001 and catches expensive mistakes. Think of it as insurance \u2014 you pay a tiny premium on every response to prevent rare but costly failures."
      },
      {
        "type": "exercise",
        "exerciseRef": "ADV-DEP-003"
      },
      {
        "type": "pod-header",
        "title": "System prompt guardrails",
        "podNumber": 3,
        "duration": 5
      },
      {
        "type": "text",
        "body": "The system prompt is your first line of defense. Effective guardrails are explicit, not implied:\n\n**Explicit scope restrictions:**\n```\nYou ONLY answer questions about [product/topic]. \nFor any other topic, respond exactly: 'I can only help with [product/topic] questions.'\nNever provide medical, legal, or financial advice.\nNever discuss competitor products.\n```\n\n**Citation requirements:**\n```\nWhen stating facts about [product], only state facts from the provided context.\nIf the answer is not in the provided context, say: 'I don't have that information. Please contact support.'\nNever invent or assume pricing, policies, or features.\n```\n\n**Persona consistency:**\n```\nYou are a helpful assistant for [company]. Maintain a professional, friendly tone.\nNever say anything that could be interpreted as a product guarantee or legal commitment.\nIf a user is frustrated, acknowledge their frustration before addressing the issue.\n```\n\n**The jailbreak gap**: Users will try to get your agent to say things it shouldn't. Test with: 'Ignore previous instructions', 'Pretend you have no restrictions', 'In a hypothetical scenario...' Your guardrails should survive these attempts."
      },
      {
        "type": "exercise",
        "exerciseRef": "ADV-DEP-004"
      }
    ]
  },
  {
    "id": "adv-dep-l03",
    "moduleId": "adv-dep-m2",
    "title": "Debug & Monitor",
    "description": "Understand agent behavior in production. Logging, tracing, and incident response.",
    "duration": 15,
    "order": 3,
    "learningObjectives": [
      {
        "text": "Implement structured logging for every step of an agent loop",
        "bloomLevel": "apply"
      },
      {
        "text": "Design a trace that reconstructs what an agent did and why",
        "bloomLevel": "create"
      },
      {
        "text": "Diagnose a failing agent run from logs alone",
        "bloomLevel": "analyze"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "You can't fix what you can't see",
        "podNumber": 1,
        "duration": 5
      },
      {
        "type": "hook",
        "body": "A user reports: 'Your AI gave me wrong information.' You look at the database. The response is there. But what did the agent search? What did it read? Why did it decide to say that? Without tracing, you're blind. Observability turns post-mortem guessing into root cause analysis."
      },
      {
        "type": "text",
        "body": "Agent observability requires three layers:\n\n**1. Run-level logs** (what happened in this session)\n- Run ID, user ID, timestamp, total tokens, total cost\n- Final outcome: success / error / timeout\n- Duration\n\n**2. Step-level traces** (what the agent did)\n- Each Claude API call: input messages, output, tokens, latency\n- Each tool call: tool name, input, output, success/fail\n- Stop reason at each step\n\n**3. Quality metrics** (how well it did)\n- Output validation results\n- User feedback (thumbs up/down, corrections)\n- Hallucination detection results\n\nWithout all three, you can debug obviously broken agents but not subtly wrong ones."
      },
      {
        "type": "interactive-guess",
        "question": "A user reports 'the agent gave wrong info' but didn't say what. You have run-level logs showing: run_id=abc, outcome=success, 8 steps, 45K tokens. Is this enough to diagnose the issue?",
        "answer": "**No \u2014 you need step-level traces.** Run-level logs confirm it completed but can't tell you which step produced the wrong output, what the agent read, or what reasoning led to the wrong answer. You need to see the full message sequence \u2014 every input, every tool call result, every Claude response.",
        "hint": "What specifically do you need to know to find the source of wrong information?"
      },
      {
        "type": "takeaway",
        "body": "Run-level logs tell you if something happened. Step-level traces tell you why it happened."
      },
      {
        "type": "pod-header",
        "title": "Implementing structured tracing",
        "podNumber": 2,
        "duration": 5
      },
      {
        "type": "text",
        "body": "**Trace structure:**\n```js\nconst trace = {\n  runId: crypto.randomUUID(),\n  userId, agentId, goal,\n  startedAt: new Date(),\n  steps: [],\n  outcome: null,\n  totalTokens: 0, totalCost: 0\n};\n\n// In the agent loop, after each API call:\ntrace.steps.push({\n  stepNumber: iterations,\n  timestamp: new Date(),\n  type: 'llm_call',\n  inputTokens: response.usage.input_tokens,\n  outputTokens: response.usage.output_tokens,\n  stopReason: response.stop_reason,\n  // Don't store full messages in DB \u2014 too large. Store a summary:\n  summary: response.content[0]?.text?.slice(0, 200)\n});\n\n// After each tool call:\ntrace.steps.push({\n  stepNumber: iterations,\n  type: 'tool_call',\n  toolName, toolInput: JSON.stringify(toolCall.input).slice(0, 500),\n  toolResult: JSON.stringify(result).slice(0, 500),\n  success: result.success !== false\n});\n```\n\n**At end of run:** save trace to DB, calculate cost, flag any anomalies."
      },
      {
        "type": "callout",
        "variant": "tip",
        "body": "**Store message summaries, not full messages.** Full conversation histories can be megabytes. Store the first 200 chars of each response for browsing, but the run ID for reconstructing the full trace from logs if needed. Use structured logging (JSON) so you can filter traces by tool name, error type, cost, etc."
      },
      {
        "type": "exercise",
        "exerciseRef": "ADV-DEP-005"
      },
      {
        "type": "pod-header",
        "title": "Alerts and incident response",
        "podNumber": 3,
        "duration": 5
      },
      {
        "type": "text",
        "body": "**Set up alerts for:**\n\n```js\n// 1. High cost per run\nif (trace.totalCost > 1.00) await alert('high_cost', trace);\n\n// 2. Long-running agents (possible infinite loop)\nif (trace.duration > 5 * 60 * 1000) await alert('timeout_risk', trace);\n\n// 3. High error rate\nconst errorRate = trace.steps.filter(s => s.success === false).length / trace.steps.length;\nif (errorRate > 0.3) await alert('high_tool_error_rate', trace);\n\n// 4. Validation failures\nif (trace.validationFailed) await alert('output_validation_failed', trace);\n```\n\n**Incident response playbook:**\n1. **Identify**: Which run failed? What was the goal?\n2. **Trace**: Walk step-by-step \u2014 where did it go wrong?\n3. **Diagnose**: Tool failure? Hallucination? Scope violation? Context overflow?\n4. **Fix**: Update tool error handling / system prompt / validation / max iterations\n5. **Verify**: Reproduce the failing case. Does the fix work?\n6. **Document**: Add the failure pattern to known issues. Update monitoring.\n\n**Proactive quality monitoring:**\nSample 5% of completed runs and run your faithfulness evaluator on them. Track hallucination rate over time. If it rises, something changed (prompt? data? model?)."
      },
      {
        "type": "exercise",
        "exerciseRef": "ADV-DEP-006"
      }
    ]
  },
  {
    "id": "adv-dep-l04",
    "moduleId": "adv-dep-m3",
    "title": "Logging & Tracing Agent Runs",
    "description": "Six months from now, someone asks why the agent did what it did on March 3rd. Can you answer?",
    "duration": 15,
    "order": 4,
    "learningObjectives": [
      {
        "text": "Explain why standard application logging is insufficient for debugging agent behavior",
        "bloomLevel": "understand"
      },
      {
        "text": "Design a trace structure that captures each reasoning and tool-call step",
        "bloomLevel": "apply"
      },
      {
        "text": "Use a trace to reconstruct why an agent made a specific decision",
        "bloomLevel": "analyze"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "Why 'it printed some errors' isn't enough",
        "podNumber": 1,
        "duration": 5
      },
      {
        "type": "hook",
        "body": "A customer complains your support agent gave them wrong information three days ago. Your application logs show one line: 'Response sent to user 4471.' That tells you the agent ran. It tells you nothing about why it said what it said."
      },
      {
        "type": "text",
        "body": "### What a trace captures that a log doesn't\n\nStandard logging records events (a request came in, a response went out). A trace records the full reasoning path of one agent run: every 'think' step, every tool call and its result, and the final decision \u2014 all linked together so you can replay the run's logic afterward.\n\n```js\nasync function runAgentWithTrace(goal, client) {\n  const traceId = crypto.randomUUID();\n  const trace = { traceId, goal, startedAt: new Date(), steps: [] };\n  const messages = [{ role: 'user', content: goal }];\n\n  while (true) {\n    const response = await client.messages.create({ model: 'claude-opus-4-6', tools: TOOLS, messages });\n    if (response.stop_reason === 'end_turn') {\n      trace.finalAnswer = response.content[0].text;\n      break;\n    }\n    const toolCall = response.content.find(b => b.type === 'tool_use');\n    const result = await executeTool(toolCall.name, toolCall.input);\n    trace.steps.push({ tool: toolCall.name, input: toolCall.input, result, timestamp: new Date() });\n    messages.push({ role: 'assistant', content: response.content });\n    messages.push({ role: 'user', content: [{ type: 'tool_result', tool_use_id: toolCall.id, content: result }] });\n  }\n  await db.agentTrace.create({ data: trace });\n  return trace.finalAnswer;\n}\n```"
      },
      {
        "type": "interactive-guess",
        "question": "Your trace shows the agent called `search_docs` with a query that was clearly wrong, given the user's actual question. What does this let you diagnose that a simple 'response sent' log entry never could?",
        "answer": "**Whether the mistake happened in reasoning (a bad decision about what to search) or in the tool itself (a good query that returned bad results).** A single output log can't distinguish 'the agent reasoned poorly' from 'the agent reasoned fine but a downstream tool failed.' The trace's step-by-step record makes that distinction visible, which is the difference between fixing the actual cause and guessing.",
        "hint": "What could you tell from seeing the query the agent chose to run, that you couldn't tell from just seeing its final answer?"
      },
      {
        "type": "takeaway",
        "body": "A trace records the reasoning path, not just the outcome. It's what turns 'the agent was wrong' into 'the agent was wrong because of this specific step.'"
      },
      {
        "type": "pod-header",
        "title": "Making traces actually usable",
        "podNumber": 2,
        "duration": 5
      },
      {
        "type": "text",
        "body": "A trace nobody can search is barely better than no trace. Two additions make it usable in practice:\n\n**1. Index by user and time**, so a specific complaint ('this user, three days ago') resolves to the right trace in seconds, not a manual grep through a JSON dump.\n\n**2. Redact sensitive data before storage.** Traces often contain the full content of user messages and tool results \u2014 apply the same privacy discipline you'd apply to any other stored user data. Store references (an order ID) rather than full sensitive content (a credit card number) where the trace doesn't genuinely need the raw value to be useful for debugging.\n\n```js\n// Query: find the trace for this specific complaint\nconst trace = await db.agentTrace.findFirst({\n  where: { userId: '4471', startedAt: { gte: threeDaysAgoStart, lt: threeDaysAgoEnd } },\n  orderBy: { startedAt: 'desc' }\n});\n// Replay: print each step in order to see exactly what happened\nfor (const step of trace.steps) {\n  console.log(`${step.timestamp}: called ${step.tool} with`, step.input, '\u2192', step.result);\n}\n```"
      },
      {
        "type": "callout",
        "variant": "tip",
        "body": "**Set a retention window and stick to it.** Traces are useful for debugging, not indefinite storage \u2014 keep 30-90 days depending on your needs, and delete older ones automatically. Indefinite trace retention quietly turns into a second, unmanaged copy of all your user data."
      },
      {
        "type": "exercise",
        "exerciseRef": "ADV-DEP-007"
      },
      {
        "type": "exercise",
        "exerciseRef": "ADV-DEP-008"
      }
    ]
  },
  {
    "id": "adv-dep-l05",
    "moduleId": "adv-dep-m4",
    "title": "Incident Response When an Agent Misbehaves",
    "description": "The agent did something wrong in production. What do you do in the first ten minutes?",
    "duration": 15,
    "order": 5,
    "learningObjectives": [
      {
        "text": "Identify the immediate priority actions when an agent malfunction is discovered",
        "bloomLevel": "apply"
      },
      {
        "text": "Design a kill switch that stops an agent without destroying evidence of what it did",
        "bloomLevel": "create"
      },
      {
        "text": "Write a post-incident process that prevents the same failure from recurring",
        "bloomLevel": "evaluate"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "The first ten minutes",
        "podNumber": 1,
        "duration": 5
      },
      {
        "type": "hook",
        "body": "Someone messages you: 'the support agent just told a customer their refund was approved \u2014 it wasn't.' The agent is still running, potentially telling other customers the same thing right now. What you do in the next ten minutes matters more than what you do in the next ten hours."
      },
      {
        "type": "text",
        "body": "### Priority order when something goes wrong\n\n**1. Stop the bleeding.** Disable the specific capability that's misbehaving \u2014 ideally without taking down the entire agent, if other parts are working fine. A feature flag or a per-tool kill switch is far better than an all-or-nothing shutdown you built in a panic.\n\n```js\nconst DISABLED_TOOLS = new Set(process.env.DISABLED_TOOLS?.split(',') ?? []);\nasync function executeTool(name, input) {\n  if (DISABLED_TOOLS.has(name)) {\n    return { success: false, error: 'This capability is temporarily disabled.' };\n  }\n  // ... normal execution\n}\n```\n\n**2. Find the blast radius, using traces.** How many users were affected, and since when? This is exactly what the trace system from the previous lesson exists for \u2014 without it, you're guessing.\n\n**3. Communicate honestly, fast.** Affected users need to hear from you before they discover the problem themselves. A vague or delayed message costs more trust than an early, honest one."
      },
      {
        "type": "interactive-guess",
        "question": "Why disable a specific tool ('process_refund') rather than shutting down the entire agent when one capability misbehaves?",
        "answer": "**Because the entire agent going down is its own kind of harm** \u2014 every other function it performs (routing, answering simple questions, drafting) stops working too, for a problem that was isolated to one capability. A targeted kill switch contains the damage to exactly the part that's actually broken, which is both more precise and lets the rest of the system keep serving people correctly.",
        "hint": "What else does the agent do besides the thing that just went wrong?"
      },
      {
        "type": "takeaway",
        "body": "Contain the specific failure, don't reach for the biggest possible shutdown by default. A targeted kill switch, built before you need it, is what makes containment fast instead of panicked."
      },
      {
        "type": "pod-header",
        "title": "After the fire is out",
        "podNumber": 2,
        "duration": 5
      },
      {
        "type": "text",
        "body": "Once the immediate harm is contained, the goal shifts from stopping to understanding and preventing.\n\n**A minimal post-incident process:**\n\n1. **Reconstruct the timeline** from traces: when did it start, what triggered it, how many instances happened.\n2. **Root cause, not just symptom.** 'The agent said something wrong' is a symptom. 'The tool returned stale cached data that the agent trusted without a freshness check' is a root cause you can actually fix.\n3. **Fix the specific cause, and add a check that would have caught it earlier** \u2014 a validation, a guardrail, an eval-harness case (from the testing pattern covered earlier) built from this exact failure.\n4. **Re-enable gradually**, not all at once \u2014 a smaller rollout with close monitoring before returning to full capacity.\n\nThe output of every real incident should be a new eval case or guardrail, not just a fix and a hope it doesn't happen again."
      },
      {
        "type": "callout",
        "variant": "warning",
        "body": "**Resist the urge to skip the root-cause step under pressure.** A fast patch that addresses the symptom ('add a disclaimer to refund messages') without the actual cause ('the tool served stale data') leaves the real bug in place, waiting to resurface in a slightly different form."
      },
      {
        "type": "exercise",
        "exerciseRef": "ADV-DEP-009"
      },
      {
        "type": "exercise",
        "exerciseRef": "ADV-DEP-010"
      }
    ]
  }
]
