[
  {
    "id": "aod-rc1-l01",
    "moduleId": "aod-rc1",
    "order": 1,
    "duration": 20,
    "difficulty": "easy",
    "title": "Read the signature, then the return, then the middle",
    "description": "A generated diff lands in front of you with no explanation. Reading top to bottom wastes the fastest information you have: what goes in, what comes out, and only then how it gets there.",
    "learningObjectives": [
      {
        "text": "Read a function's signature and return statement(s) before its body to form a testable hypothesis about its purpose",
        "bloomLevel": "apply"
      },
      {
        "text": "Use that hypothesis to read the body as a check rather than an open-ended search",
        "bloomLevel": "apply"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "Signature first",
        "podNumber": 1,
        "duration": 10
      },
      {
        "type": "hook",
        "body": "A pull request lands on your desk. It's a Claude Code diff, no description beyond the ticket number. The new function:\n\n```python\ndef reconcile_ledger(entries: list[dict], as_of: date) -> ReconciliationResult:\n```\n\nYou haven't read a single line of the body yet, and you already know more than it looks like. `entries: list[dict]` and `as_of: date` in, a `ReconciliationResult` out — not a bool, not a list, a named result type. That's three real facts about what this function is for, sitting in one line, before you've spent a second reading logic."
      },
      {
        "type": "text",
        "body": "Most people read code top to bottom, the order it's written in — which is also the order that's slowest to give you the information you actually need. The signature and the return statement carry disproportionately more of a function's *intent* than its middle does, because they're the interface: what it promises to accept and what it promises to hand back. The middle is just the mechanism for keeping that promise.\n\nRead in this order instead: signature, then every `return` in the function (there may be more than one — each one is a distinct way the function can conclude), then the body. By the time you reach the body you're not reading to discover what the function does, you're reading to check whether the mechanism actually delivers what the signature and returns already told you to expect. That's a much smaller, much faster task than open-ended comprehension — you're verifying a specific hypothesis, not building one from scratch line by line.\n\nThis matters more for generated code than hand-written code, not less. A human author's variable names and comments often leak intent as you go; a fluent generated function can look equally confident regardless of whether the returns actually match what the name and signature promise — which is exactly the gap this order is built to catch fast."
      },
      {
        "type": "interactive-guess",
        "question": "Before reading the body: given only `def reconcile_ledger(entries: list[dict], as_of: date) -> ReconciliationResult`, and that `ReconciliationResult` has fields `matched`, `unmatched`, `as_of` — what is this function most likely doing with `as_of`?",
        "hint": "The parameter is threaded through to the output type unchanged. What would a reconciliation process need a cutoff date for?",
        "answer": "It's almost certainly filtering `entries` to only those relevant as of that date — reconciliation is normally a snapshot operation, not an all-time one, and the fact that `as_of` reappears unchanged on the result (rather than being consumed into a computed value) suggests it's being carried through as a record of what cutoff was used, not just used internally and discarded. You can't confirm this without the body, but you now have something specific to check for, instead of reading the body cold."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC1-001"
      },
      {
        "type": "takeaway",
        "body": "The signature and the return type are the fastest, densest source of intent in a function — read them before the body, and let the body answer a specific question instead of an open one."
      },
      {
        "type": "pod-header",
        "title": "The body is a check, not a discovery",
        "podNumber": 2,
        "duration": 10
      },
      {
        "type": "text",
        "body": "Once you've read the signature and every return, the body reading changes shape. You're not asking \"what does this do?\" from nothing — you're asking \"does this actually get from the inputs to the outputs I was already told to expect?\" That's a narrower, faster question, and it surfaces mismatches a top-to-bottom read tends to miss, because a top-to-bottom read has no expectation yet to check against.\n\nThis is especially sharp when a function has more than one `return`. Each early return is a distinct promise: a distinct condition under which the function concludes, and a distinct value it hands back for that condition. Reading all the returns before the body means you walk in already knowing how many endings to expect and roughly what should trigger each one — so when the body's logic doesn't cleanly map to one of them, that's the first thing worth flagging, not something you'd have caught by accident on your third re-read."
      },
      {
        "type": "worked-example",
        "problem": "A generated JavaScript function for a shipping calculator. Read it signature-first, then returns, then the body, to determine whether the body actually delivers on what those two things promise.\n\n```javascript\nfunction computeShippingCost(order, destination) {\n  if (!destination.isServiceable) {\n    return { cost: null, reason: 'unserviceable' };\n  }\n  const weight = order.items.reduce((sum, item) => sum + item.weightKg, 0);\n  if (weight === 0) {\n    return { cost: 0, reason: 'no_physical_items' };\n  }\n  const base = weight * destination.ratePerKg;\n  const surcharge = order.isPriority ? base * 0.4 : 0;\n  return { cost: Math.round((base + surcharge) * 100) / 100, reason: 'computed' };\n}\n```",
        "steps": [
          {
            "label": "Read the signature",
            "work": "`computeShippingCost(order, destination)` — two inputs, no declared types (plain JS). Name promises a cost comes out.",
            "why": "Even without types, the name and parameter names narrow the space: this is a pure calculation over an order and a destination, not something that mutates either."
          },
          {
            "label": "Read every return, before the body's logic",
            "work": "Three returns: `{cost: null, reason: 'unserviceable'}`, `{cost: 0, reason: 'no_physical_items'}`, `{cost: <number>, reason: 'computed'}`. So the shape is always `{cost, reason}`, and there are exactly three distinct endings.",
            "why": "This tells you the function was designed to explain itself via `reason`, not just return a bare number — and it tells you there are precisely three cases to hold the body accountable to, before you've read a line of the logic that produces them."
          },
          {
            "label": "Read the body as a check against those three cases",
            "work": "Case 1 (unserviceable) is checked first and returns immediately — consistent. Case 2 (zero weight) is checked next — consistent. Case 3 sums weight × rate, adds a 40% priority surcharge, rounds to two decimals — the 'computed' case. All three returns are reachable and mutually exclusive.",
            "why": "Because you already knew the three cases going in, verifying the body took one pass, checking each branch against an expectation, rather than building the whole mental model from scratch while also trying to spot problems."
          }
        ],
        "faded": {
          "problem": "A second, similar function — same technique, this time you fill in the reading.\n\n```python\ndef apply_late_fee(invoice: Invoice, today: date) -> FeeResult:\n    if invoice.paid:\n        return FeeResult(fee=Decimal('0'), waived=True, reason='already_paid')\n    days_late = (today - invoice.due_date).days\n    if days_late <= 0:\n        return FeeResult(fee=Decimal('0'), waived=False, reason='not_yet_due')\n    fee = min(invoice.balance * Decimal('0.015') * days_late, invoice.balance * Decimal('0.25'))\n    return FeeResult(fee=fee, waived=False, reason='late')\n```",
          "blanks": [
            {
              "prompt": "From the signature alone, before reading further: what does this function most likely compute, and for whom?",
              "answer": "A late fee for an invoice, as of a given date — the return type name (FeeResult) and the two inputs (an invoice, today's date) are enough to guess this even before reading a return statement.",
              "accept": [
                "a late payment fee for an invoice",
                "how much fee an invoice owes"
              ],
              "why": "The signature alone narrows this to 'something about lateness and money,' which is most of the way to correct before any logic is read."
            },
            {
              "prompt": "List the three distinct return cases, in the order they'd fire, based on reading only the return statements.",
              "answer": "1) already paid → fee 0, waived. 2) not yet due (days_late <= 0) → fee 0, not waived. 3) late → a computed fee, capped.",
              "accept": [
                "paid, not due, late",
                "already_paid, not_yet_due, late"
              ],
              "why": "Naming all three endings up front is what turns the body read into confirmation instead of discovery — you now know to look for exactly a cap and a per-day rate when you get to the third case."
            },
            {
              "prompt": "Now read the body: does the 'late' case's fee calculation match what a per-day, capped fee should look like, or is something off?",
              "answer": "It matches: 1.5% of the balance per day late, capped at 25% of the balance total (via `min(...)`) — both a growth mechanism and a ceiling, which is what 'capped daily late fee' implies.",
              "accept": [
                "it's correct",
                "matches — per-day rate with a cap"
              ],
              "why": "This is the payoff of the order: you're confirming a specific shape (rate × days, with a cap) rather than reverse-engineering the formula's intent from scratch."
            }
          ]
        },
        "procedure": "Signature narrows the space of what a function is for. Every return, read before the body, tells you how many distinct endings to expect and roughly what triggers each. The body read then becomes verification against a specific, small set of expectations — faster, and far more likely to catch a mismatch than a cold top-to-bottom read."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC1-002"
      },
      {
        "type": "takeaway",
        "body": "Signature, then every return, then the body — in that order, the body read becomes a check against a specific hypothesis instead of a search with no starting point."
      }
    ]
  },
  {
    "id": "aod-rc1-l02",
    "moduleId": "aod-rc1",
    "order": 2,
    "duration": 25,
    "difficulty": "medium",
    "title": "Naming as evidence — and as misdirection",
    "description": "A name is a claim about what code does, made by whoever wrote it — including a model that names things the same confident way whether the claim is true or not.",
    "learningObjectives": [
      {
        "text": "Use function and variable names as a first hypothesis about intent, while treating them as unverified",
        "bloomLevel": "analyze"
      },
      {
        "text": "Identify cases where a name overstates, understates, or misdescribes what the code beneath it actually does",
        "bloomLevel": "analyze"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "A name is a claim, not a fact",
        "podNumber": 1,
        "duration": 13
      },
      {
        "type": "hook",
        "body": "A generated function is named `sanitizeInput`. That name is doing work on you before you've read a single line — it's telling you this is safe to treat as cleaned, escaped, defended-against-injection input. Here's the body:\n\n```python\ndef sanitize_input(raw: str) -> str:\n    return raw.strip()\n```\n\nIt trims whitespace. That's all. Nothing about SQL, nothing about HTML, nothing about shell metacharacters. The name promised a security boundary; the code delivers a whitespace trim."
      },
      {
        "type": "text",
        "body": "Names are genuinely useful evidence — a well-named function usually does roughly what it says, and reading names first is part of the signature-first technique from the last lesson. But a name is a *claim*, made by whoever wrote the code, and claims can be wrong without anyone lying: the author (human or model) believed the name was accurate, or named it aspirationally for what it was supposed to become, or copied a name from a similar-looking function that did more.\n\nGenerated code has a specific version of this risk. A model produces a plausible name and a plausible implementation from the same generation pass — nothing checks that the two actually agree with each other, the same way nothing checks that a docstring matches the logic beneath it. `sanitizeInput` is exactly the kind of name a model reaches for when asked to \"handle user input safely,\" because it's a common, confident-sounding name for that class of task — regardless of whether the specific three lines underneath it actually constitute sanitization.\n\nThe practical habit: read the name as a hypothesis to check, not a fact to accept. For names that carry weight — anything implying validation, sanitization, authorization, or idempotency — go verify the body actually does what the name claims before you build anything else on top of the assumption that it does."
      },
      {
        "type": "spot-flaw",
        "code": "def is_authorized(user, resource):\n    \"\"\"Checks whether the user is authorized to access the resource.\"\"\"\n    return user is not None",
        "question": "The function is named `is_authorized` with a docstring confirming that name. What's actually wrong here?",
        "options": [
          {
            "value": "checks-login-not-permission",
            "label": "It only checks that a user object exists (is logged in) — it never checks whether that specific user has permission for that specific resource"
          },
          {
            "value": "should-return-none",
            "label": "It should return None instead of a boolean when the user is missing"
          },
          {
            "value": "resource-unused-is-fine",
            "label": "Nothing's wrong — `resource` being unused just means access is universal once logged in, which is a valid design"
          }
        ],
        "correctValue": "checks-login-not-permission",
        "feedback": {
          "checks-login-not-permission": "Correct. 'Authorized' means permitted to do this specific thing; this function only checks 'authenticated' — logged in at all. The parameter `resource` is accepted but never referenced in the body, which is the tell: a real authorization check would need to look at the resource to decide anything about it.",
          "should-return-none": "The return type isn't the problem here — a bool is a fine shape for an authorization check. The problem is what's being checked, not how the result is typed.",
          "resource-unused-is-fine": "An unused parameter in a function whose whole job is supposedly to reason about that parameter is a strong signal, not a neutral detail — 'universal access once logged in' is a real design some systems use, but nothing about the name `is_authorized` or the docstring suggests that's what's intended here; it reads as a gap, not a choice."
        },
        "flawExplanation": "The name and docstring both assert a permission check against a specific resource. The body checks only `user is not None` — authentication, not authorization — and the unused `resource` parameter is the giveaway that the function was never wired up to actually reason about the resource at all. Code that calls this expecting real per-resource authorization will silently let any logged-in user through to anything, which is the exact shape of bug a name-trusting review misses and a body-reading review catches in one line."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC1-003"
      },
      {
        "type": "takeaway",
        "body": "A name that implies a security or correctness property — authorized, sanitized, validated, safe — is a claim to verify against the body, not a fact to build on."
      },
      {
        "type": "pod-header",
        "title": "Names that undersell, and names copied from elsewhere",
        "podNumber": 2,
        "duration": 12
      },
      {
        "type": "text",
        "body": "Misdirection runs both ways. A name can overstate what code does (`sanitizeInput` that only trims) or understate it — a function called `formatDate` that also silently converts time zones is doing something a caller relying on the name alone won't expect. Both failure directions come from the same source: the name and the logic were written in the same pass, with nothing checking one against the other.\n\nThere's a third, specific pattern worth knowing: a name copied forward from a similar function nearby, where the logic was adapted but the name wasn't. This happens constantly when a model is shown one function as a pattern to follow and asked to write a related one — it often reuses naming conventions faithfully while the actual behavior diverges to fit the new case, and the result is a name that made perfect sense for the original and misleading sense for the copy."
      },
      {
        "type": "concept-flow",
        "scenario": "Two functions in the same generated file:\n\n```javascript\nfunction formatDate(iso) {\n  return new Date(iso).toLocaleDateString();\n}\n\nfunction formatDateWithOffset(iso, offsetHours) {\n  const d = new Date(iso);\n  d.setHours(d.getHours() + offsetHours);\n  return d.toLocaleDateString();\n}\n```\n\nA third function was generated later, apparently by copying `formatDateWithOffset` as a pattern:\n\n```javascript\nfunction formatDateForUser(iso, user) {\n  const d = new Date(iso);\n  d.setHours(d.getHours() + user.utcOffsetHours);\n  return d.toLocaleDateString();\n}\n```",
        "question": "What does the name `formatDateForUser` fail to signal, given what the function actually does?",
        "options": [
          {
            "label": "That it's applying a timezone shift before formatting, not just formatting — a caller reading only the name has no reason to expect the date to change, only how it's displayed",
            "value": "hides-shift"
          },
          {
            "label": "Nothing — the name accurately describes formatting a date for a user",
            "value": "name-fine"
          },
          {
            "label": "That it takes a `user` object as a parameter",
            "value": "hides-param"
          }
        ],
        "correctValue": "hides-shift",
        "feedback": {
          "hides-shift": "Right. 'Format' in the name promises a display transformation — same date, different string. The function actually shifts the date by the user's UTC offset before formatting, which can move the date itself across a day boundary near midnight. A caller who trusts the name and doesn't read the body could easily assume the underlying date value is untouched, which is exactly wrong.",
          "name-fine": "The name reads plausibly, which is the problem — it's plausible and incomplete. 'Formats a date for a user' undersells a real value change (the offset shift) as a cosmetic one (formatting), and that gap is invisible unless you read past the name.",
          "hides-param": "The parameter is visible right there in the signature — that's not hidden information. What's hidden is what the function *does* with that parameter, which the name doesn't disclose."
        },
        "concept": "A name inherited from a similar-looking function nearby can be accurate for the original and misleading for the copy, because the logic diverged while the naming convention didn't. Copied structure is exactly where this risk concentrates — check whether a name that looked safe two functions ago still describes what the new function actually does."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC1-004"
      },
      {
        "type": "takeaway",
        "body": "Misdirection isn't always overstatement — a name can undersell a real behavior change as cosmetic, especially when it was copied from a similar function whose logic has since diverged."
      }
    ]
  },
  {
    "id": "aod-rc1-l03",
    "moduleId": "aod-rc1",
    "order": 3,
    "duration": 20,
    "difficulty": "easy",
    "title": "Comments that describe code that no longer exists",
    "description": "A comment is a snapshot of what someone believed the code did at the moment they wrote it — and the code beneath it keeps changing after the comment stops.",
    "learningObjectives": [
      {
        "text": "Recognize when a comment describes an earlier version of the logic beneath it rather than the current one",
        "bloomLevel": "analyze"
      },
      {
        "text": "Treat comments as a hypothesis to check against the code, not as a substitute for reading it",
        "bloomLevel": "understand"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "The comment is a photograph",
        "podNumber": 1,
        "duration": 10
      },
      {
        "type": "hook",
        "body": "```python\n# Retry up to 3 times with exponential backoff before giving up\ndef fetch_with_retry(url):\n    response = requests.get(url, timeout=5)\n    if response.status_code >= 500:\n        response = requests.get(url, timeout=5)\n    return response\n```\n\nThe comment describes a retry loop with backoff. The code retries exactly once, with no backoff at all. Somewhere between whoever wrote that comment and the code as it stands now, the implementation changed and the comment didn't — or, in the AI-generated case, the comment and the code were never actually the same claim in the first place, just two plausible-sounding outputs from the same request."
      },
      {
        "type": "text",
        "body": "A comment is not a live description of the code — it's a snapshot, true at some earlier moment, that nothing forces to stay true as the code around it changes. This is a known problem with hand-written code (comments rot faster than logic, because nothing errors when they diverge), and it has a distinct version with generated code: a model can produce a comment that describes the *intended* behavior from the prompt — three retries, exponential backoff — while the actual generated logic falls short of implementing all of it, because generating an accurate summary and generating fully correct logic are not the same task, done by the same process, with no check between them.\n\nThe practical rule: read the comment as a claim about what the code is supposed to do, then verify it against the actual logic the same way you'd verify a name. Where they disagree, the code is what runs — the comment is not documentation you can trust instead of reading, it's a hypothesis with the same standing as a function name."
      },
      {
        "type": "diagnose-mechanism",
        "scenario": "The `fetch_with_retry` function above: comment claims 3 retries with exponential backoff, code shows one retry with no backoff and the same 5-second timeout both times.",
        "question": "What's the most accurate account of why the comment and the code disagree?",
        "options": [
          {
            "value": "code-was-edited",
            "label": "Someone edited the retry logic later and forgot to update the comment, the classic human-authored version of this problem"
          },
          {
            "value": "same-source-diff-fidelity",
            "label": "Comment and code both came from the same generation request — describing 'retry 3 times with exponential backoff' and fully implementing it are different tasks that don't automatically stay in sync just because one process produced both"
          },
          {
            "value": "backoff-implied",
            "label": "There's no real disagreement — a single retry with the same timeout could be read as a minimal form of backoff"
          }
        ],
        "correctValue": "same-source-diff-fidelity",
        "feedback": {
          "same-source-diff-fidelity": "Right, for generated code specifically: the comment and the logic were produced together, in response to a request that probably did ask for retries with backoff. Writing an accurate-sounding summary of that request is easy; actually implementing a full retry loop with a backoff schedule is more work, and there's no internal check that forces the two to match. The comment reflects the request more faithfully than the code does.",
          "code-was-edited": "This is the classic explanation for hand-written code, and it can't be ruled out in general — but nothing in this snippet shows evidence of a later edit (no removed lines, no version history shown), and the far more common cause in freshly generated code is the mismatch between summarizing intent and fully implementing it in one pass.",
          "backoff-implied": "A single retry with an identical timeout isn't a minimal form of exponential backoff — backoff specifically means increasing delay between attempts, and there's no delay of any kind here, let alone an increasing one. This reads the comment into the code rather than checking the code against the comment."
        },
        "mechanism": "Comments and logic can diverge in hand-written code through edits that miss the comment. In generated code there's an additional, more direct route: the comment can be a faithful echo of what was asked for, while the logic is a partial or approximate implementation of that same ask — two outputs of one generation pass, with nothing enforcing that they describe the same behavior. Either way, the fix is the same: read the comment as a claim, then check it against what the code actually does, not the other way around."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC1-005"
      },
      {
        "type": "takeaway",
        "body": "A comment is a claim made at some point about the code — possibly never fully true, possibly true once and no longer. Check it against the logic; don't read it as the logic."
      },
      {
        "type": "pod-header",
        "title": "When the comment is the only spec you have",
        "podNumber": 2,
        "duration": 10
      },
      {
        "type": "text",
        "body": "None of this means comments are useless — a comment that turns out to be wrong is still telling you something real: what someone (or some prompt) believed the requirement was. That's often the closest thing to a written spec you'll get for undocumented code, and a comment that contradicts its own code is a strong, specific signal of exactly where to look first — better than a comment that agrees with the code, which tells you nothing you couldn't get from reading the code alone.\n\nSo the useful move isn't to distrust comments generally, it's to treat a mismatch as a lead: the comment is evidence of an intended requirement the code doesn't fully meet, which is often more actionable than either the comment or the code would be alone. \"Retry 3 times with exponential backoff\" plus code that retries once is a much clearer bug report than either \"this comment might be stale\" or \"this code seems incomplete\" would be on their own."
      },
      {
        "type": "evidence-stack",
        "scenario": "Five comment/code pairs from a generated codebase you're reviewing.",
        "question": "Select every pair where the comment is worth treating as a lead pointing at a real gap in the code beneath it.",
        "items": [
          {
            "value": "cache-ttl",
            "label": "`# Cache results for 5 minutes` above code with no expiry logic at all — results are cached forever once set",
            "applicable": true
          },
          {
            "value": "matches-code",
            "label": "`# Returns the user's full name, or None if not set` above code that does exactly that, nothing more",
            "applicable": false
          },
          {
            "value": "sort-desc",
            "label": "`# Sort orders newest first` above `orders.sort(key=lambda o: o.created_at)` with no `reverse=True`",
            "applicable": true
          },
          {
            "value": "vague-praise",
            "label": "`# Efficiently processes the batch` above a straightforward for-loop with no particular optimization",
            "applicable": false
          },
          {
            "value": "idempotent-claim",
            "label": "`# Idempotent — safe to call multiple times` above code that increments a counter with no guard against duplicate calls",
            "applicable": true
          }
        ],
        "explanation": {
          "cache-ttl": "A real, checkable gap: the comment names a specific expiry duration, the code implements no expiry mechanism at all. This is a concrete lead — go check what happens to memory or staleness over time.",
          "matches-code": "No gap here — the comment and code agree, which means the comment isn't giving you information beyond what the code already shows. Not a lead, just confirmation.",
          "sort-desc": "A real gap: 'newest first' requires descending order, and a default `.sort()` on `created_at` produces ascending (oldest first) order. The missing `reverse=True` is exactly the kind of specific, checkable mismatch a comment-as-lead surfaces.",
          "vague-praise": "\"Efficiently\" isn't a checkable claim — there's no specific behavior to verify it against, so it's not a lead pointing anywhere in particular, just filler.",
          "idempotent-claim": "A real and serious gap: 'idempotent' has a specific technical meaning — calling it twice should produce the same result as calling it once — and an unguarded increment is the textbook counterexample. This is the highest-value kind of lead, because the claim is both specific and safety-relevant."
        },
        "synthesis": "The comments worth chasing make a specific, checkable claim that the code visibly doesn't meet — a duration, an order, a guarantee like idempotency. Vague comments and comments that just restate the code aren't leads either way; they're not lying, but they're also not telling you anything the code doesn't already say."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC1-006"
      },
      {
        "type": "takeaway",
        "body": "A comment that contradicts its code is a lead, not noise — it's often the closest thing you have to a written requirement, pointing exactly at the gap between what was intended and what shipped."
      }
    ]
  },
  {
    "id": "aod-rc1-l04",
    "moduleId": "aod-rc1",
    "order": 4,
    "duration": 25,
    "difficulty": "medium",
    "title": "Reconstructing the requirement from the implementation",
    "description": "When there's no ticket, no comment, and no one to ask, the specific numbers and branches in the code are themselves evidence of what the requirement was.",
    "learningObjectives": [
      {
        "text": "Infer an unstated business rule from specific constants, thresholds, and branch conditions in code",
        "bloomLevel": "analyze"
      },
      {
        "text": "Distinguish an inference well-supported by the code's specifics from a guess dressed up as one",
        "bloomLevel": "evaluate"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "Specific numbers are never accidents",
        "podNumber": 1,
        "duration": 13
      },
      {
        "type": "hook",
        "body": "No ticket, no comment, no one on the team remembers writing it:\n\n```python\ndef compute_late_fee(balance: Decimal, days_late: int) -> Decimal:\n    if days_late <= 0:\n        return Decimal('0')\n    fee = balance * Decimal('0.015') * days_late\n    return min(fee, balance * Decimal('0.25'))\n```\n\nThere's no comment explaining `0.015` or `0.25`. But those aren't arbitrary — nobody writes `0.015` by accident, and nobody caps at exactly a quarter of the balance by accident either. The numbers themselves are the requirement, encoded, whether or not anyone wrote it down anywhere else."
      },
      {
        "type": "text",
        "body": "When there's no written spec, the implementation is frequently the only record of what was actually decided — and it's a more reliable record than it looks, because specific constants and specific branch conditions are expensive to get by accident. Nobody types `0.015` instead of a round number like `0.01` or `0.02` unless that number came from somewhere: a contract term, a regulatory cap, a decision made in a meeting and never written down anywhere except in this one function.\n\nThe technique: when you hit code with no documented rationale, look specifically at what varies and what's fixed. A fixed cap (`min(fee, balance * 0.25)`) is a strong signal of an intentional ceiling — someone decided fees shouldn't exceed a quarter of the balance, for a reason that probably has nothing to do with this specific function and everything to do with a policy this function is one implementation of. A specific rate (`0.015`) that isn't a clean round number is a signal it was copied from an authoritative source — a contract, a rate table — rather than picked by whoever wrote this code.\n\nThis isn't guessing. It's treating the implementation as evidence and reasoning from what's specific and deliberate-looking to what requirement would produce exactly that shape — the same kind of inference a detective makes from a specific detail at a scene, not a shrug that says 'could be anything.'"
      },
      {
        "type": "predict-number",
        "scenario": "A generated function enforces a maximum discount on an order:\n\n```python\ndef apply_discount(order_total, discount_pct):\n    capped_pct = min(discount_pct, 0.30)\n    return order_total * (1 - capped_pct)\n```",
        "question": "Before being told anything else about this business: what's your best guess for the actual, real-world policy reason a cap of exactly 30% would exist, rather than 25% or 50%?",
        "unit": "% (your reasoning, not a literal further number)",
        "actualValue": 30,
        "explanation": "There's no single right story — the exercise is in noticing that 30% specifically, not 'a cap' generically, is what needs explaining. Plausible real reasons: a margin floor (the business can't discount past the point where a sale stops being profitable, and 30% is where that floor sits for this product line), a coupon-stacking policy (individual promotions cap lower, but this is the ceiling across all combined promotions), or a legal/contractual maximum from a supplier agreement. What matters is the habit: a specific number like 30% is evidence of one of these being true, and it's worth writing '30% cap — reason unknown, check with product/finance' into your review notes rather than treating the number as incidental."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC1-007"
      },
      {
        "type": "takeaway",
        "body": "A specific constant or a fixed cap in code with no comment isn't arbitrary — it's evidence of a decision made somewhere else, and worth naming as an open question rather than passing over."
      },
      {
        "type": "pod-header",
        "title": "Branches encode conditions someone thought mattered",
        "podNumber": 2,
        "duration": 12
      },
      {
        "type": "text",
        "body": "The same logic applies to branch conditions, not just numeric constants. An `if` statement checking a specific, oddly precise condition is evidence that someone (or some prompt) thought that exact condition mattered enough to special-case — and reconstructing *why* it mattered is often more useful for review than just confirming the branch works.\n\nThe risk on the other side: over-reading. Not every specific number or branch is meaningful — sometimes a threshold really is arbitrary, or copied from an unrelated example the model was shown. The discipline is to distinguish inference that's well-supported by the code's own specifics from a story you're telling because a story feels more satisfying than 'I don't know.' A well-supported inference points at *what to go verify* — ask someone, check a contract, look for the number elsewhere in the codebase — rather than stopping at a plausible-sounding guess and treating it as settled."
      },
      {
        "type": "case-sim",
        "title": "The unexplained branch",
        "opening": "You're reviewing a generated function with no ticket attached. It's part of an order-processing pipeline for a mid-size e-commerce company.\n\n```python\ndef requires_manual_review(order):\n    if order.total > 500 and order.shipping_country != order.billing_country:\n        return True\n    if order.item_count > 20:\n        return True\n    return False\n```\n\nNo comment. No linked ticket. Two branches, both with specific numbers.",
        "start": "start",
        "nodes": {
          "start": {
            "situation": "You need to decide how to handle this in review, since nobody can immediately explain the $500 and country-mismatch condition.",
            "question": "What's your first move?",
            "choices": [
              {
                "value": "accept-as-is",
                "label": "Approve it — the logic runs without error and the numbers are plausible enough for fraud-adjacent review triggers",
                "consequence": "Three weeks later, someone asks why orders exactly at $500 shipping domestically never get flagged, and nobody can explain the $500 figure or the international-shipping specifically. It ships anyway because nobody owns the question.",
                "ending": "unverified"
              },
              {
                "value": "guess-and-comment",
                "label": "Write a comment guessing it's a fraud-risk heuristic (mismatched billing/shipping country plus a high order value is a classic fraud signal) and move on",
                "consequence": "The guess is directionally reasonable — but you never actually confirmed whether $500 is this company's real threshold or a number the model reached for as a plausible-sounding default.",
                "nextNode": "confirm"
              },
              {
                "value": "flag-for-verification",
                "label": "Flag both thresholds explicitly in the review: 'these look like fraud/logistics review triggers based on the shape of the conditions — need to confirm the actual $500 and 20-item thresholds against whatever policy this implements before merging'",
                "consequence": "The reviewer chases it down before merge, not after.",
                "nextNode": "confirm"
              }
            ]
          },
          "confirm": {
            "situation": "Whoever wrote the ticket (found via a linked Slack thread) confirms: $500 + country mismatch is indeed a fraud heuristic pulled from the payment processor's own risk guidance, and 20+ items is a warehouse capacity trigger, unrelated to fraud at all — packages that large need a different fulfillment path.",
            "question": "Given that the two branches turned out to encode two completely unrelated policies bundled into one function, what's the useful next step?",
            "choices": [
              {
                "value": "leave-bundled",
                "label": "Leave it as one function — it works, and splitting it is just refactoring for its own sake",
                "consequence": "Six months later, someone touches the fraud threshold to update it for a policy change and doesn't realize they're now also affecting when large orders get flagged for warehouse routing — an unrelated system breaks.",
                "ending": "coupled"
              },
              {
                "value": "split-and-document",
                "label": "Split into two functions — `is_fraud_risk` and `needs_warehouse_review` — each with the confirmed policy source cited, even though the two happened to be combined in the original",
                "consequence": "The two concerns are now independently changeable, and the next person who touches either one has the actual policy source right there instead of having to reconstruct it again.",
                "ending": "resolved"
              }
            ]
          }
        },
        "endings": {
          "unverified": {
            "verdict": "costly",
            "summary": "An unexplained, specific threshold shipped unverified, and the review that could have caught it happened after the fact instead of before.",
            "lesson": "Plausible-looking numbers aren't the same as confirmed ones. 'Runs without error' answers a different question than 'is this the right threshold' — and only the second one is what review is actually for."
          },
          "coupled": {
            "verdict": "mixed",
            "summary": "The immediate review question got answered, but two unrelated policies stayed silently coupled in one function, and that coupling caused a real incident later.",
            "lesson": "Reconstructing *why* a branch exists is only half the job when the reconstruction reveals two unrelated concerns bundled together — the fix isn't just understanding the code, it's separating what shouldn't have been combined."
          },
          "resolved": {
            "verdict": "good",
            "summary": "Both thresholds got a confirmed source, and the accidental coupling between two unrelated policies got separated before it could cause a cross-system bug.",
            "lesson": "Reconstructing intent from specific numbers is the first step. Verifying the reconstruction with whoever actually owns the policy, and then acting on what that verification reveals — including untangling accidental coupling — is what turns a good guess into a real fix."
          }
        }
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC1-008"
      },
      {
        "type": "takeaway",
        "body": "A well-supported inference from specific code tells you what to go verify, not what to assume. Treat a plausible reconstruction as a question to confirm, not a conclusion to build on."
      }
    ]
  },
  {
    "id": "aod-rc1-l05",
    "moduleId": "aod-rc1",
    "order": 5,
    "duration": 20,
    "difficulty": "medium",
    "title": "Saying what a function does in one sentence",
    "description": "If you can't compress a function into one accurate sentence, you haven't actually finished reading it — and the compression itself is what exposes what you're still unsure about.",
    "learningObjectives": [
      {
        "text": "Compress a function's behavior into a single accurate sentence covering its normal case and its stated exceptions",
        "bloomLevel": "understand"
      },
      {
        "text": "Use the difficulty of writing that sentence as a signal that a function is doing more than one thing",
        "bloomLevel": "analyze"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "One sentence is a compression test",
        "podNumber": 1,
        "duration": 10
      },
      {
        "type": "hook",
        "body": "Four lessons in this module have been about gathering evidence — signatures, names, comments, unstated requirements. This one is about the payoff: can you say, in one sentence, what a function actually does? Not what it's named, not what its comment claims — what the code, as read, actually does.\n\n\"Computes the median of a sorted list, correctly averaging the two middle values for even-length lists, and returns `None` for an empty list\" is one sentence and it's precise: normal case, the tricky case (even-length), and the edge case (empty), all named. \"Handles the median calculation\" is also one sentence, and it says almost nothing — it would be equally true of a function with three known bugs."
      },
      {
        "type": "text",
        "body": "The one-sentence test isn't a communication exercise tacked on after the real work of reading — it's a check on whether the reading actually happened. A sentence that's accurate and specific requires you to have actually traced the returns, noticed the edge cases, and understood how the branches relate to each other — everything the last four lessons practiced. A sentence that's vague (\"handles X,\" \"processes the data,\" \"does the calculation\") is often a sign the reading stopped short, whether or not the reader realizes it.\n\nThe useful discipline: force the sentence to name the normal case *and* at least one exception or edge case the function specifically handles differently. If you can't find an exception to name, either the function genuinely has none (worth noting — is that actually complete?) or you haven't looked closely enough yet to find the one it does have."
      },
      {
        "type": "build-it",
        "intro": "Build an accurate one-sentence description of this function, piece by piece.\n\n```python\ndef next_billing_date(subscription):\n    if subscription.status == 'cancelled':\n        return None\n    if subscription.plan == 'annual':\n        return subscription.started_at.replace(year=subscription.started_at.year + 1)\n    return subscription.started_at + timedelta(days=30)\n```",
        "objectName": "one-sentence description",
        "fields": [
          {
            "key": "subject",
            "prompt": "What's the core action, in a few words?",
            "options": [
              "Computes the next billing date for a subscription",
              "Cancels a subscription",
              "Validates a subscription's plan type"
            ],
            "correctValue": "Computes the next billing date for a subscription",
            "feedback": "Right — that's what the function returns in its main cases, and the name confirms it."
          },
          {
            "key": "branching",
            "prompt": "What does it do differently for different plan types?",
            "options": [
              "Adds exactly one calendar year for annual plans, and 30 days for everything else",
              "Always adds exactly 30 days regardless of plan",
              "Asks the payment processor for the next date"
            ],
            "correctValue": "Adds exactly one calendar year for annual plans, and 30 days for everything else",
            "feedback": "Right — and note the distinction worth keeping in the sentence: a calendar year (which can land on a different day count depending on leap years) versus a fixed 30-day offset for non-annual plans, which isn't quite 'monthly' for every calendar month."
          },
          {
            "key": "exception",
            "prompt": "What's the one case where it doesn't compute a date at all?",
            "options": [
              "When the subscription is cancelled, it returns None instead",
              "When the subscription is annual, it returns None",
              "It never returns None"
            ],
            "correctValue": "When the subscription is cancelled, it returns None instead",
            "feedback": "Right — this is the exception the one-sentence description has to name, or it's silently claiming the function always returns a date, which it doesn't."
          }
        ],
        "synthesis": "Assembled: 'Computes a subscription's next billing date as one calendar year after its start date for annual plans or 30 days after for everything else, returning None instead if the subscription has been cancelled.' That's one sentence, and it names the normal case, the branch that changes behavior (annual vs. everything else), and the exception (cancelled → None) — which is exactly the shape a compression that actually required reading the whole function produces."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC1-009"
      },
      {
        "type": "takeaway",
        "body": "A one-sentence description that names the normal case and at least one real exception is evidence you actually read the function. A vague sentence usually means the reading stopped early."
      },
      {
        "type": "pod-header",
        "title": "When one sentence won't fit",
        "podNumber": 2,
        "duration": 10
      },
      {
        "type": "text",
        "body": "Sometimes the honest one-sentence attempt keeps growing a third and fourth clause, and no version of it feels like it's actually one coherent idea. That difficulty is itself useful information — not a failure of your compression skills, but a signal that the function is doing more than one job, stitched together because it was convenient to write it that way, not because the two jobs belong together.\n\nA function that genuinely does one thing compresses cleanly, almost always, once you've actually traced its branches. A function that resists compression — where every attempt at one sentence needs an 'and also' that isn't really related to the first clause — is a function whose responsibilities should probably be two functions. This is the same signal the case-sim in the previous lesson surfaced (the fraud-check-and-warehouse-routing function bundled into one) from a different angle: reading for intent doesn't just tell you what code does, it tells you when what it does shouldn't be one thing."
      },
      {
        "type": "interactive-guess",
        "question": "A function called `processOrder` has a one-sentence description that reads: 'Validates the order's payment method, charges the customer, updates inventory counts, sends a confirmation email, and logs the transaction for analytics.' Is that a sign of a well-compressed sentence, or a sign of something else?",
        "hint": "Count the number of genuinely distinct responsibilities in that sentence, connected by 'and.'",
        "answer": "It's a sign the function is doing five distinct jobs (validation, payment, inventory, notification, analytics) that happen to be sequenced together, not one job describable in one real sentence — the 'and, and, and' structure is the tell. A one-sentence description should read as one idea with at most one qualifying exception, the way the billing-date example did. When it reads as a list stitched together with conjunctions, that's usually evidence the function should be split — each responsibility independently readable, testable, and reviewable — rather than evidence you just need a longer sentence."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC1-010"
      },
      {
        "type": "takeaway",
        "body": "If your one-sentence attempt keeps needing another 'and' for an unrelated responsibility, that's not a compression failure — it's the function telling you it's actually several functions."
      }
    ]
  },
  {
    "id": "aod-rc1-p01",
    "moduleId": "aod-rc1",
    "order": 6,
    "duration": 30,
    "difficulty": "medium",
    "title": "Practice — Summarise Eight Unfamiliar Functions",
    "description": "Eight drills, no new concepts: read for intent using the signature, the returns, the names, the comments, and the specific numbers, then compress each into one accurate sentence.",
    "learningObjectives": [
      {
        "text": "Apply the signature-first reading order at speed across unfamiliar functions",
        "bloomLevel": "apply"
      },
      {
        "text": "Compress a function's actual behavior into one accurate sentence, distinguishing it from a vague or name-trusting one",
        "bloomLevel": "analyze"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "Eight drills",
        "podNumber": 1,
        "duration": 30
      },
      {
        "type": "hook",
        "body": "Nothing new here. Eight drills across the module's skills, ordered from where the lessons ended to one step past it. Do them in one sitting or spread across a few — nothing here expires."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC1-P01"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC1-P02"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC1-P03"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC1-P04"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC1-P05"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC1-P06"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC1-P07"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC1-P08"
      },
      {
        "type": "takeaway",
        "body": "If a drill needed a hint, that's what to watch for in the checkpoint — not a reason to redo the set."
      }
    ]
  },
  {
    "id": "aod-rc1-lab",
    "moduleId": "aod-rc1",
    "order": 7,
    "duration": 50,
    "difficulty": "hard",
    "title": "Lab — Reverse-Engineer the Spec of an Undocumented Module",
    "description": "Take a real module nobody documented and write down what it's actually for, using every technique from this module — including naming exactly what you couldn't verify.",
    "learningObjectives": [
      {
        "text": "Apply signature-first reading, name/comment verification, and constant reconstruction to a real, unfamiliar module",
        "bloomLevel": "apply"
      },
      {
        "text": "Produce a written specification of existing behavior that names every unverifiable assumption explicitly",
        "bloomLevel": "create"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "The lab",
        "podNumber": 1,
        "duration": 50
      },
      {
        "type": "hook",
        "body": "Every lesson this module used a single function at a time. Real undocumented code rarely comes in single functions — it comes in modules, where the functions reference each other, and the module's actual purpose only becomes clear once you've read several of them together. This lab is that: one real module, reverse-engineered into a spec, with every assumption you couldn't verify named as exactly that."
      },
      {
        "type": "lab-brief",
        "labId": "aod-rc1-reverse-spec",
        "title": "Reverse-engineer a module's spec",
        "brief": "Pick a real module from a codebase you have access to (your own project, an open-source repo, work code) that has little or no documentation — no README section, no doc comments beyond the occasional stray note, nobody left to ask. If nothing fits, use a module you generated with an AI tool for a side project and never wrote a spec for afterward — that's a legitimate and common real case for this exact skill. Apply this module's techniques to reconstruct what it's actually for.",
        "deliverable": "A written spec of the module's existing behavior: what each public function does (one accurate sentence each, per the last lesson), the module's overall purpose in a short paragraph, and an explicit list of every assumption you made that you could not verify from the code alone.",
        "stages": [
          {
            "title": "Inventory and signature pass",
            "minutes": 15,
            "instructions": "List every public function in the module. For each, read the signature and every return statement first — before the body — and write down your hypothesis for what it does, per the signature-first technique. Don't read bodies yet.",
            "checkpoint": "Every public function has a signature-based hypothesis written down, before you've read any bodies."
          },
          {
            "title": "Body verification and evidence check",
            "minutes": 20,
            "instructions": "Now read each body against your hypothesis. Check every name, comment, and specific constant against what the logic actually does — note every mismatch you find, the way the lesson's examples did. For constants and branch conditions with no documented reason, write down your best-supported reconstruction of the requirement, flagged clearly as inference rather than fact.",
            "checkpoint": "Every function's hypothesis has been checked against its body, every name/comment mismatch is noted, and every unexplained constant has a reconstructed-but-flagged reason or an explicit 'unknown' if you can't support one."
          },
          {
            "title": "Write the spec and self-check",
            "minutes": 15,
            "instructions": "Compress everything into the deliverable: one accurate sentence per public function, a short paragraph on the module's overall purpose, and an explicit list of unverifiable assumptions. Then self-check against the rubric below.",
            "checkpoint": "The spec is written, every assumption is listed explicitly rather than folded silently into the descriptions, and a colleague reading it cold would know exactly which parts are confirmed and which are your best guess."
          }
        ],
        "rubric": [
          {
            "criterion": "Every public function has a genuinely one-sentence, specific description",
            "meets": "The sentence names the normal case and at least one exception or edge case, not a vague restatement of the function's name."
          },
          {
            "criterion": "At least one name/comment mismatch is found and named, or their absence is explicitly noted",
            "meets": "If the module has zero mismatches, the spec says so explicitly rather than silently omitting the check — 'no name or comment mismatches found' is a real finding, not nothing."
          },
          {
            "criterion": "Every reconstructed requirement is flagged as inference, not fact",
            "meets": "A reader can tell, for each claim in the spec, whether it's confirmed from the code or your best-supported guess about an unstated requirement."
          },
          {
            "criterion": "The list of unverifiable assumptions is genuinely populated, not empty for a real module",
            "meets": "Real undocumented modules almost always have at least one thing you can't confirm without asking someone. An empty list is worth double-checking rather than treating as a clean result."
          },
          {
            "criterion": "The module's overall purpose paragraph is consistent with all the individual function sentences",
            "meets": "Nothing in the per-function descriptions contradicts the module-level summary — if it does, that's worth resolving before submitting, not leaving as a loose thread."
          }
        ]
      },
      {
        "type": "takeaway",
        "body": "A spec reconstructed from real code and honestly marked for what's confirmed versus inferred is a genuinely useful artefact — often more useful than an outdated written spec would have been."
      }
    ]
  },
  {
    "id": "aod-rc1-cp",
    "moduleId": "aod-rc1",
    "order": 8,
    "duration": 10,
    "difficulty": "easy",
    "title": "Checkpoint — Reading for Intent",
    "description": "Ten questions: four from this module, four from Knowing When to Take Over, two from further back. Ungraded, untimed.",
    "learningObjectives": [
      {
        "text": "Retrieve this module's core distinctions alongside earlier modules', without looking them up",
        "bloomLevel": "remember"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "Let's see what stuck",
        "podNumber": 1,
        "duration": 10
      },
      {
        "type": "retrieval-check",
        "title": "Checkpoint — Reading for Intent",
        "questions": [
          {
            "from": "This module — Reading for Intent",
            "prompt": "What's the recommended reading order for an unfamiliar function?",
            "options": [
              {
                "value": "a1",
                "label": "Signature, then every return, then the body"
              },
              {
                "value": "a2",
                "label": "Top to bottom, the order it's written in"
              },
              {
                "value": "a3",
                "label": "The body first, to see what it actually computes, then the signature to confirm"
              }
            ],
            "correctValue": "a1",
            "explanation": "Signature and returns are the densest, fastest source of intent — reading them first turns the body into a check against a specific hypothesis instead of an open-ended search."
          },
          {
            "from": "This module — Reading for Intent",
            "prompt": "A function named `is_authorized` only checks `user is not None`. What does this illustrate?",
            "options": [
              {
                "value": "b1",
                "label": "A name is a claim made by whoever wrote the code, and it can be wrong even when the docstring agrees with it"
              },
              {
                "value": "b2",
                "label": "The function has a syntax error"
              },
              {
                "value": "b3",
                "label": "Boolean-returning functions should always be named 'is_X'"
              }
            ],
            "correctValue": "b1",
            "explanation": "Name and docstring agreeing with each other is not evidence either is true — both are outputs of the same process as the logic they're supposedly describing."
          },
          {
            "from": "This module — Reading for Intent",
            "prompt": "A comment claims a function retries 3 times with exponential backoff; the code retries once with no backoff. What's the most accurate way to treat this comment?",
            "options": [
              {
                "value": "c1",
                "label": "As a lead pointing at a real gap between what was intended and what shipped"
              },
              {
                "value": "c2",
                "label": "As documentation to trust instead of reading the code"
              },
              {
                "value": "c3",
                "label": "As irrelevant noise, since comments can't be verified anyway"
              }
            ],
            "correctValue": "c1",
            "explanation": "A specific, checkable comment that contradicts its code is often the closest thing you have to a written requirement — worth chasing, not dismissing."
          },
          {
            "from": "This module — Reading for Intent",
            "prompt": "An unexplained cap of exactly `0.25` in a fee calculation, with no comment. What's the right move?",
            "options": [
              {
                "value": "d1",
                "label": "Treat it as a strong signal of a real, external requirement worth confirming — not as an arbitrary detail to skip past"
              },
              {
                "value": "d2",
                "label": "Assume it's arbitrary, since there's no comment explaining it"
              },
              {
                "value": "d3",
                "label": "Change it to a rounder number for readability"
              }
            ],
            "correctValue": "d1",
            "explanation": "Specific, non-round constants and fixed caps are expensive to arrive at by accident — they're evidence of a decision made somewhere else, worth naming as an open question."
          },
          {
            "from": "Previous module — Knowing When to Take Over",
            "prompt": "What's the shape of a situation where directing an AI tool costs more than just writing the code yourself?",
            "options": [
              {
                "value": "e1",
                "label": "A stalled prompt loop with no closer, a problem whose hard part is the thinking rather than the typing, or a domain you can't review well enough to judge the output"
              },
              {
                "value": "e2",
                "label": "Any task that takes more than ten minutes"
              },
              {
                "value": "e3",
                "label": "Any task involving a database"
              }
            ],
            "correctValue": "e1",
            "explanation": "Take-over situations share a shape: the tool isn't converging, the actual difficulty is deciding rather than translating, or you can't tell correct from incorrect in the domain — not a fixed time or technology boundary."
          },
          {
            "from": "Previous module — Knowing When to Take Over",
            "prompt": "Why is a small, precise edit sometimes faster to make by hand than to prompt for?",
            "options": [
              {
                "value": "f1",
                "label": "Because describing the exact edit precisely enough to prompt for it can take longer than just making it directly"
              },
              {
                "value": "f2",
                "label": "Because AI tools can't make small edits at all"
              },
              {
                "value": "f3",
                "label": "Because manual edits are always more reliable than generated ones"
              }
            ],
            "correctValue": "f1",
            "explanation": "For a genuinely small, precise change, the overhead of writing a prompt precise enough to get it right can exceed the time to just make the edit — a real, situational trade-off, not a general claim about reliability."
          },
          {
            "from": "Previous module — Knowing When to Take Over",
            "prompt": "What does a personal 'take-over rule,' built from evidence rather than instinct, actually look for?",
            "options": [
              {
                "value": "g1",
                "label": "Recurring, nameable situations from your own logged experience where directing cost more than doing it yourself"
              },
              {
                "value": "g2",
                "label": "A fixed time limit that applies to every task equally"
              },
              {
                "value": "g3",
                "label": "Whether a task feels hard at first glance"
              }
            ],
            "correctValue": "g1",
            "explanation": "A rule built from a logged decision history names specific, recurring situations — not a generic feeling or a one-size time limit — which is what makes it something you can actually apply consistently next time."
          },
          {
            "from": "Previous module — Knowing When to Take Over",
            "prompt": "A developer is asked to implement something in a domain they don't understand well enough to review the AI's output. What's the accurate framing?",
            "options": [
              {
                "value": "h1",
                "label": "This is exactly the situation where directing is risky, because you can't tell correct output from confidently wrong output"
              },
              {
                "value": "h2",
                "label": "AI tools are more reliable in unfamiliar domains, since they've seen more examples than any one person has"
              },
              {
                "value": "h3",
                "label": "It doesn't matter, since the code will be reviewed by someone else eventually"
              }
            ],
            "correctValue": "h1",
            "explanation": "Review requires knowing what correct looks like. In a domain you can't evaluate, fluent-and-wrong output is indistinguishable from fluent-and-right — the risk that motivated most of this module's earlier lessons on reading code carefully."
          },
          {
            "from": "Three modules back — What Actually Changes",
            "prompt": "What actually predicts whether a task is a good fit to delegate to an AI tool?",
            "options": [
              {
                "value": "i1",
                "label": "Pattern density and whether the result is checkable — not how difficult the task looks to a human"
              },
              {
                "value": "i2",
                "label": "Whether the task involves a well-known algorithm"
              },
              {
                "value": "i3",
                "label": "How many lines of code the task is likely to require"
              }
            ],
            "correctValue": "i1",
            "explanation": "A hard-looking regex can be a great fit; a trivial-sounding, unwritten business rule can be a terrible one — human difficulty doesn't track the real predictor."
          },
          {
            "from": "Four modules back — The Executable Spec",
            "prompt": "Why does \"make it better\" fail as a specification?",
            "options": [
              {
                "value": "j1",
                "label": "It names no state the code could fail to be in, so nothing can actually be checked against it"
              },
              {
                "value": "j2",
                "label": "It's grammatically incomplete"
              },
              {
                "value": "j3",
                "label": "It doesn't specify a programming language"
              }
            ],
            "correctValue": "j1",
            "explanation": "A spec only does work if some output could fail it — this is the same falsifiability idea that makes a one-sentence function description useful only when it's specific enough to be wrong."
          }
        ]
      }
    ]
  },
  {
    "id": "aod-rc2-l01",
    "moduleId": "aod-rc2",
    "order": 1,
    "duration": 20,
    "difficulty": "easy",
    "title": "Tracing a happy path by hand",
    "description": "Before you can find the case a function got wrong, you need to be able to walk one case through it correctly — with real values, not a vibe.",
    "learningObjectives": [
      {
        "text": "Trace a function's happy path step by step with concrete example values, tracking every variable's value as it changes",
        "bloomLevel": "apply"
      },
      {
        "text": "Distinguish an actual hand trace from a plausible-sounding summary of what the code probably does",
        "bloomLevel": "understand"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "One concrete input, all the way through",
        "podNumber": 1,
        "duration": 10
      },
      {
        "type": "hook",
        "body": "A generated function for splitting a restaurant bill:\n\n```python\ndef split_bill(total, people, tip_pct):\n    tip = total * (tip_pct / 100)\n    grand_total = total + tip\n    per_person = grand_total / len(people)\n    return {p: round(per_person, 2) for p in people}\n```\n\n\"Looks right\" is not the same claim as \"I traced it.\" Pick real numbers — `total=100`, `people=['A', 'B', 'C']`, `tip_pct=20` — and walk them through: `tip = 100 * 0.2 = 20`. `grand_total = 120`. `per_person = 120 / 3 = 40.0`. Result: `{'A': 40.0, 'B': 40.0, 'C': 40.0}`. That took fifteen seconds and it's the difference between believing the function is right and knowing one specific case of it is."
      },
      {
        "type": "text",
        "body": "Tracing by hand means picking concrete values, then updating every variable's actual value line by line as the code executes — not summarizing what a section \"does\" in prose, which is where skimming quietly substitutes for tracing. The test of whether you actually traced something: could you write down the value of every variable at the point the function returns, without re-reading the code to check?\n\nThis matters more before you go looking for bugs, not after. Reviewers who jump straight to \"what could go wrong\" without first confirming the normal case actually works end up debating hypothetical edge cases against a mental model of the function that was never verified to be correct even for the input it was obviously built for. One traced happy path anchors everything else you check against it.\n\nGenerated code raises the stakes slightly: a function can look identical in structure to a hundred correct examples the model was trained on and still have one arithmetic detail wrong — a wrong divisor, an operator flipped — that only shows up once you actually compute the numbers rather than pattern-match the shape of the code."
      },
      {
        "type": "worked-example",
        "problem": "Trace this generated discount function by hand for a concrete cart.\n\n```python\ndef price_cart(items, member_discount_pct):\n    subtotal = sum(item['price'] * item['qty'] for item in items)\n    discount = subtotal * (member_discount_pct / 100)\n    return round(subtotal - discount, 2)\n```\n\nInput: `items = [{'price': 12.50, 'qty': 2}, {'price': 5.00, 'qty': 1}]`, `member_discount_pct = 10`.",
        "steps": [
          {
            "label": "Compute subtotal",
            "work": "`12.50 * 2 = 25.00`, `5.00 * 1 = 5.00`, sum = `30.00`.",
            "why": "Tracing the generator expression term by term is the only way to be sure `qty` is actually being multiplied in, not just added."
          },
          {
            "label": "Compute discount",
            "work": "`30.00 * (10 / 100) = 30.00 * 0.1 = 3.00`.",
            "why": "This is where a wrong divisor would show up — if the code had said `member_discount_pct / 1000` by mistake, this step is exactly where you'd catch it, and only because you're using real numbers instead of eyeballing the expression."
          },
          {
            "label": "Compute and round the result",
            "work": "`30.00 - 3.00 = 27.00`, rounded to `27.0`.",
            "why": "The final value is now a fact you derived, not an impression you formed — you could defend `27.0` to someone else with the arithmetic, not just the shape of the code."
          }
        ],
        "faded": {
          "problem": "Trace this generated shipping-tier function for a concrete order.\n\n```python\ndef shipping_tier(order_total, is_member):\n    threshold = 35 if is_member else 50\n    if order_total >= threshold:\n        return 'free'\n    return 'standard'\n```\n\nInput: `order_total = 40`, `is_member = True`.",
          "blanks": [
            {
              "prompt": "What value does `threshold` take, and why?",
              "answer": "35, because `is_member` is `True`, taking the first branch of the conditional expression.",
              "accept": [
                "35"
              ],
              "why": "Conditional expressions (`x if cond else y`) still need to be traced with the actual truth value, not assumed from reading the shape of the line."
            },
            {
              "prompt": "Does `order_total >= threshold` evaluate to True or False here, with the real numbers?",
              "answer": "True — `40 >= 35`.",
              "accept": [
                "true",
                "yes"
              ],
              "why": "This is the step a skim skips: confirming the comparison with the actual numbers rather than the general sense that '40 seems like it should qualify.'"
            },
            {
              "prompt": "What does the function return for this input?",
              "answer": "'free'",
              "accept": [
                "free"
              ],
              "why": "A traced happy path ends in a specific, defensible value — not 'probably free' but 'free, because 40 >= 35 with threshold correctly resolved to the member rate.'"
            }
          ]
        },
        "procedure": "Pick concrete values for every input. Update every variable's actual value at each line, including inside comprehensions and conditional expressions. Arrive at a specific final value you derived, not one you're pattern-matching from having seen similar code before."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC2-001"
      },
      {
        "type": "takeaway",
        "body": "A happy-path trace ends in a specific value you can defend with arithmetic, not a summary you're pattern-matching from code that looks familiar."
      },
      {
        "type": "pod-header",
        "title": "Why the happy path comes first",
        "podNumber": 2,
        "duration": 10
      },
      {
        "type": "text",
        "body": "It's tempting to skip straight to hunting for the broken case, especially once you already suspect a function has a bug — that instinct is usually right to have, but wrong to act on first. If you haven't confirmed the ordinary case with real numbers, you don't actually have a baseline to compare the suspicious case against, and it becomes easy to misdiagnose a real bug as expected behavior, or invent a bug that isn't there because your mental model of the 'correct' output was never checked either.\n\nThe happy-path trace is also what makes the rest of this module's techniques fast rather than slow. Once you know exactly how one concrete input flows through the function — which branch it takes, which variables it touches — every other case you check is a diff against that baseline: same input with one thing changed, tracing only what's different. That's a much smaller task than re-deriving the whole function from scratch for every case you check."
      },
      {
        "type": "interactive-guess",
        "question": "A reviewer skips tracing the happy path and jumps straight to checking what happens with an empty `items` list in `price_cart`. They report: \"looks fine, `sum()` of an empty list is `0`, so subtotal is `0`, discount is `0`, result is `0`.\" What did skipping the happy-path trace cost them here?",
        "hint": "The edge-case trace they did is arithmetically correct. What haven't they established about the function that the empty-list case alone can't tell them?",
        "answer": "The edge case they checked is correct, but they still don't know whether the *normal*, non-empty case computes the right number — the case that will run on almost every real order. A reviewer who never traced a normal cart with real prices and quantities has approved the function's most common path purely on the strength of the code's shape looking plausible, which is exactly the gap a happy-path trace closes and an edge-case-only check doesn't."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC2-002"
      },
      {
        "type": "takeaway",
        "body": "Trace the ordinary case first, with real numbers, before hunting for the broken one — it gives you a verified baseline instead of an assumed one."
      }
    ]
  },
  {
    "id": "aod-rc2-l02",
    "moduleId": "aod-rc2",
    "order": 2,
    "duration": 25,
    "difficulty": "medium",
    "title": "Every branch is a promise about a case that exists",
    "description": "An if/elif/else chain is a claim about how many distinct situations the author believed could occur. Reading the branches tells you what they believed — and whether they were right.",
    "learningObjectives": [
      {
        "text": "Enumerate the distinct cases a branching structure claims to handle, and check that claim against what can actually occur",
        "bloomLevel": "analyze"
      },
      {
        "text": "Identify a branch condition inherited from a similar pattern that doesn't actually apply to the real domain of values",
        "bloomLevel": "analyze"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "Count the branches, name the case each one claims",
        "podNumber": 1,
        "duration": 13
      },
      {
        "type": "hook",
        "body": "```python\ndef shipping_label(order):\n    if order.destination_country == 'US':\n        return 'domestic'\n    elif order.destination_country in ('CA', 'MX'):\n        return 'nafta'\n    else:\n        return 'international'\n```\n\nThree branches, three claims: shipments to the US are domestic, shipments to Canada or Mexico get a distinct label, everything else is international. That third branch, `else`, isn't neutral — it's a promise that every remaining country code the system will ever see belongs to exactly one bucket: 'international.' Is that actually true for this business, or did whoever wrote this just not think about, say, US territories that ship with different customs paperwork than the mainland?"
      },
      {
        "type": "text",
        "body": "Every branch in a conditional chain is a claim that a specific case exists and needs to be handled differently from the others. Reading a branch structure for intent means reading it as a list of claims, not just a list of code paths: \"this author believed there are exactly these N distinct situations.\" The `else` or final `elif` is the least examined branch in most reviews and often the most important, because it's a catch-all claim about everything not explicitly named — and catch-all claims are exactly where an incomplete understanding of the real domain hides.\n\nThis is a specific risk with generated branches: a model producing an `if/elif/else` chain is often pattern-completing from similar branching structures it's seen, which reliably produces syntactically clean chains that don't always reflect the actual, current shape of your domain's data. A three-way country split that's correct for a business that ships in North America only can be quietly wrong the day that business starts shipping to the EU, and nothing about the code itself signals that the branch structure hasn't been re-examined against the new reality.\n\nThe habit: for every branch, name the specific case it claims to handle, out loud or in writing. For the `else`, name what it's implicitly claiming is true about everything not named above it. If you can't confidently say that claim is still accurate, that's the branch to go verify first, not the one to skim past because it's the shortest line in the function."
      },
      {
        "type": "concept-flow",
        "scenario": "A generated function classifying support tickets:\n\n```python\ndef ticket_priority(ticket):\n    if ticket.customer_tier == 'enterprise':\n        return 'urgent'\n    elif ticket.customer_tier == 'pro':\n        return 'high'\n    else:\n        return 'normal'\n```\n\nThe company's actual customer tiers, per their billing system, are: `free`, `pro`, `enterprise`, and a newer `enterprise_plus` tier added six months ago for the largest accounts.",
        "question": "What does the `else` branch's claim get wrong here, and what's the concrete consequence?",
        "options": [
          {
            "label": "It silently claims `enterprise_plus` customers — the highest tier — belong in the same bucket as `free` customers, giving the company's biggest accounts 'normal' priority instead of at least 'urgent'",
            "value": "enterprise-plus-miscategorized"
          },
          {
            "label": "Nothing is wrong — `else` correctly catches every tier not explicitly named, which is exactly what a catch-all should do",
            "value": "else-is-fine"
          },
          {
            "label": "The bug is that `free` tier isn't given its own explicit branch",
            "value": "free-tier-missing"
          }
        ],
        "correctValue": "enterprise-plus-miscategorized",
        "feedback": {
          "enterprise-plus-miscategorized": "Exactly this. The `else` branch's implicit claim — 'everything not enterprise or pro is normal priority' — was true when the function was written against a three-tier world, and became false the moment a fourth, higher tier was added, without a single line of this function changing. `enterprise_plus` customers, the company's most valuable accounts, now get the same 'normal' priority as customers on the free tier, and nothing about running the code raises an error to reveal it.",
          "else-is-fine": "A catch-all branch correctly catching everything unnamed isn't the problem — whether its *implicit claim about what those unnamed cases deserve* is still accurate is the problem. Here it isn't: it lumps the highest tier in with the lowest under a claim that made sense for an older, smaller set of tiers.",
          "free-tier-missing": "`free` tier customers are in fact correctly handled by the `else` branch — the miscategorization isn't about `free`, it's about a newer tier the branch structure was never updated to know exists at all."
        },
        "concept": "A branch chain's `else` is an implicit claim about everything not named above it, and that claim can go stale the moment the domain grows a new case — silently, with no error, because the code still runs without complaint against the new value."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC2-003"
      },
      {
        "type": "takeaway",
        "body": "Name the case each branch claims to handle, including what the final `else` implicitly claims about everything else — that catch-all claim is where an outdated understanding of the domain hides longest."
      },
      {
        "type": "pod-header",
        "title": "A branch that can never fire is also a claim worth checking",
        "podNumber": 2,
        "duration": 12
      },
      {
        "type": "text",
        "body": "The opposite failure is just as common and just as easy to miss: a branch condition that looks meaningful but can structurally never be reached, because an earlier branch already claims every case it would have handled. This isn't a cosmetic issue — dead branches are frequently the sign of a requirement the author had in mind but implemented in the wrong place, meaning the actual requirement isn't met anywhere in the function even though it visibly appears to be handled.\n\nSpotting this requires reading branches in relation to each other, not one at a time: does an earlier condition's range of matched values already fully cover a later condition's range? If so, the later branch's claim is never tested against real input, no matter how sensible it looks sitting there in the code."
      },
      {
        "type": "spot-flaw",
        "code": "def refund_window(order):\n    if order.days_since_purchase <= 30:\n        return 'eligible'\n    elif order.days_since_purchase <= 14:\n        return 'expedited_only'\n    else:\n        return 'expired'",
        "question": "The second branch is meant to handle a specific case. What's actually wrong?",
        "options": [
          {
            "value": "unreachable-branch",
            "label": "The `expedited_only` branch can never execute — any order with `days_since_purchase <= 14` already satisfies `<= 30` and returns 'eligible' first, so the second condition is dead code"
          },
          {
            "value": "wrong-comparison-operator",
            "label": "The comparisons should use `<` instead of `<=`"
          },
          {
            "value": "missing-negative-check",
            "label": "The function needs a check for negative `days_since_purchase` values"
          }
        ],
        "correctValue": "unreachable-branch",
        "feedback": {
          "unreachable-branch": "Right. The branches are checked in order, and `<= 30` is a strictly wider condition than `<= 14` — every value that would satisfy the second condition already satisfied the first one and returned. Whatever business rule the author intended for orders 0-14 days old (an 'expedited' refund path, presumably faster or different from the general 30-day window) is never actually applied by this code, despite the branch sitting right there looking like it handles it.",
          "wrong-comparison-operator": "Switching `<=` to `<` doesn't fix the ordering problem — the second branch would still be a subset of the first regardless of whether the boundary is inclusive or exclusive, because 30 is still a wider window than 14.",
          "missing-negative-check": "A negative `days_since_purchase` is a real, separate concern worth flagging in its own right, but it's not what's wrong with the specific branch this question is about, and it doesn't explain why the `expedited_only` case never fires for any input."
        },
        "flawExplanation": "The branches needed to be ordered narrowest-condition-first for this pattern to work: check `<= 14` before `<= 30`, so the tighter case gets a chance to match before the wider one swallows it. As written, the intended three-tier structure (0-14 days: expedited, 15-30 days: eligible, 31+: expired) collapses into an effective two-tier structure (0-30: eligible, 31+: expired) with a completely dead branch in the middle — a real gap in the refund policy that looks, on a casual read, like it's already covered."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC2-004"
      },
      {
        "type": "takeaway",
        "body": "A branch that looks like it handles a case can still be structurally unreachable if an earlier, wider condition already claims every value it would have matched — check branch order, not just branch content."
      }
    ]
  },
  {
    "id": "aod-rc2-l03",
    "moduleId": "aod-rc2",
    "order": 3,
    "duration": 20,
    "difficulty": "medium",
    "title": "Early returns, guard clauses, and the paths they hide",
    "description": "A guard clause that quietly returns a default value handles the case without ever telling anyone it happened — which is fine for some cases and a hidden failure for others.",
    "learningObjectives": [
      {
        "text": "Distinguish a guard clause that correctly short-circuits a real edge case from one that silently absorbs an input the caller needed to know about",
        "bloomLevel": "analyze"
      },
      {
        "text": "Trace what a function's caller actually receives when a guard clause fires, not just whether the function itself avoids crashing",
        "bloomLevel": "analyze"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "A guard clause is a decision about what the caller finds out",
        "podNumber": 1,
        "duration": 10
      },
      {
        "type": "hook",
        "body": "```python\ndef get_active_subscription(user):\n    if not user:\n        return None\n    if not user.subscriptions:\n        return None\n    active = [s for s in user.subscriptions if s.status == 'active']\n    if not active:\n        return None\n    return active[0]\n```\n\nThree guard clauses, three `return None`s. From the caller's side, `get_active_subscription(user)` returning `None` could mean: there was no user object at all, the user exists but has never subscribed to anything, or the user has subscriptions but none are currently active. Those are three very different situations for a billing page to be in, and this function collapses all three into one indistinguishable signal."
      },
      {
        "type": "text",
        "body": "Guard clauses are a genuinely good pattern — checking a precondition and returning early keeps a function's main logic flat and readable instead of nested three levels deep inside `if` blocks. The risk isn't the pattern, it's what happens to the information at the moment a guard clause fires: does the caller still get to know *which* precondition failed, or does everything collapse into one generic signal that erases the distinction?\n\nThis matters most when the different failure cases actually call for different behavior downstream. A checkout page that gets `None` back from `get_active_subscription` can't tell the difference between 'show a sign-up prompt' (no user, or never subscribed) and 'show a reactivate-your-subscription prompt' (had one, it lapsed) — the guard clauses threw that distinction away on the way out, even though the function's own logic clearly knew which case it was in at the moment each guard fired.\n\nThe habit: for every early return in a guard clause, ask what the caller actually receives, and whether that's enough information for the caller to do the right thing next. A guard clause that returns the same generic value for meaningfully different situations is a place where real information is being discarded inside the function, invisibly to anyone just reading the call site."
      },
      {
        "type": "diagnose-mechanism",
        "scenario": "A generated checkout function:\n\n```javascript\nfunction canCheckout(cart) {\n  if (!cart.items.length) return false;\n  if (cart.items.some(i => i.outOfStock)) return false;\n  if (!cart.shippingAddress) return false;\n  return true;\n}\n```\n\nA product manager files a bug: \"users report the checkout button is disabled and don't know why.\"",
        "question": "Given the code, what's the actual root cause of the vague user complaint?",
        "options": [
          {
            "value": "collapsed-boolean",
            "label": "All three distinct failure reasons — empty cart, an out-of-stock item, a missing address — collapse into the same `false`, so the UI built on top of this function has no way to tell the user which one applies"
          },
          {
            "value": "logic-is-wrong",
            "label": "The boolean logic itself is incorrect and sometimes returns `false` for carts that should be checkoutable"
          },
          {
            "value": "needs-loading-state",
            "label": "The function needs to show a loading spinner while checking stock"
          }
        ],
        "correctValue": "collapsed-boolean",
        "feedback": {
          "collapsed-boolean": "Right. The function's internal logic is fine at distinguishing the three cases — each guard clause fires on a specific, distinct condition — but the moment each one returns bare `false`, that distinction is gone. Whatever UI calls `canCheckout(cart)` can only ever know 'not checkoutable,' never 'because of what.' The fix isn't the logic, it's the return shape: something like a reason code or a result object that preserves which guard fired.",
          "logic-is-wrong": "Nothing in the given code suggests the three conditions are evaluated incorrectly — each one does correctly detect its specific case. The bug isn't in *whether* it's false, it's in the total loss of information about *why* once it is.",
          "needs-loading-state": "A loading state addresses a different problem (asynchronous stock checks) that isn't described anywhere in this scenario — the bug report is specifically about users not knowing *why* the button is disabled, which points at the collapsed return value, not at timing."
        },
        "mechanism": "Each guard clause correctly detects its own specific precondition failure, but returning the same generic `false` (or `None`, or any single default) for all of them destroys the distinction the moment execution leaves the function. The caller inherits a strictly poorer signal than the function itself actually had internally at the point each guard fired — and that loss is invisible unless you specifically trace what value survives to the return."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC2-005"
      },
      {
        "type": "takeaway",
        "body": "A guard clause knows exactly which precondition failed at the moment it fires — check whether that knowledge survives into what the caller actually receives, or gets flattened into one generic signal."
      },
      {
        "type": "pod-header",
        "title": "When a default is a case being silently accepted, not rejected",
        "podNumber": 2,
        "duration": 10
      },
      {
        "type": "text",
        "body": "There's a second guard-clause risk, the mirror image of the first: a guard clause that returns a plausible-looking default value instead of surfacing that something unexpected happened at all. `if not price: return 0` looks like defensive programming — it prevents a crash on missing data — but from the caller's side, a `0` that means 'this item is free' and a `0` that means 'the price was missing and we made one up' are indistinguishable, and only one of those is actually true.\n\nThe question to ask of every guard clause that returns a default rather than raising or signaling an error: is this default a legitimate value for the normal case, or is it standing in for 'something's wrong and I didn't want to crash'? The first is a real design choice. The second is a bug quietly wearing the guard-clause pattern as camouflage, and it's specifically dangerous because the code runs without complaint — there's no stack trace pointing at it, no test failure, just a wrong number flowing silently downstream."
      },
      {
        "type": "evidence-stack",
        "scenario": "Four guard clauses from a generated inventory system.",
        "question": "Select every one where the default returned is standing in for a real error, rather than a legitimate normal-case value.",
        "items": [
          {
            "value": "missing-price-zero",
            "label": "`if item.price is None: return 0` — treats a missing price the same as a genuinely free item",
            "applicable": true
          },
          {
            "value": "empty-cart-zero-total",
            "label": "`if not cart.items: return 0` for a function computing cart total — an empty cart legitimately costs $0",
            "applicable": false
          },
          {
            "value": "missing-quantity-one",
            "label": "`if item.quantity is None: return 1` — silently assumes a quantity of exactly one whenever the field wasn't set, with no signal that it was assumed rather than specified",
            "applicable": true
          },
          {
            "value": "unauthenticated-empty-list",
            "label": "`if not user.is_authenticated: return []` for a function listing a user's saved items — a logged-out user genuinely has no saved items to show",
            "applicable": false
          }
        ],
        "explanation": {
          "missing-price-zero": "A missing price and an intentionally free item are different facts about the world, and this guard clause makes them indistinguishable — `0` downstream could mean either, and anything doing math on prices (revenue reports, discount calculations) would silently treat missing data as a real $0 sale.",
          "empty-cart-zero-total": "This one's legitimate: an empty cart really does cost nothing, and `0` is the mathematically correct total, not a stand-in for missing information — there's no ambiguity about what `0` means here.",
          "missing-quantity-one": "Same shape as the missing-price case: `1` is a plausible-looking normal value, but it's standing in for 'this field wasn't set,' and a caller has no way to tell 'quantity is genuinely 1' from 'quantity was never recorded and we guessed.'",
          "unauthenticated-empty-list": "Also legitimate — a logged-out user has no saved items in any meaningful sense, so an empty list isn't hiding a missing fact, it's accurately describing the actual state."
        },
        "synthesis": "The distinguishing question is whether the returned value would still be true if you had complete information, or whether it's filling a gap where information is actually missing. `0` for an empty cart is true either way; `0` for a missing price is only true if the price actually is zero, which the guard clause has no way of confirming and no way of telling anyone it didn't confirm."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC2-006"
      },
      {
        "type": "takeaway",
        "body": "A default value returned by a guard clause is only safe when it would be true regardless of missing information — otherwise it's an error wearing the shape of a normal case, silently, with nothing downstream able to tell the difference."
      }
    ]
  },
  {
    "id": "aod-rc2-l04",
    "moduleId": "aod-rc2",
    "order": 4,
    "duration": 25,
    "difficulty": "medium",
    "title": "Mutable state and who else can change it",
    "description": "A function that reads or writes shared state is only as understandable as the answer to one question: what else, anywhere in the system, can also touch that same state?",
    "learningObjectives": [
      {
        "text": "Identify shared mutable state a function depends on or modifies, distinct from its own local variables",
        "bloomLevel": "analyze"
      },
      {
        "text": "Trace a concrete scenario where two callers touching the same mutable state produce a wrong result neither caller's code alone would suggest",
        "bloomLevel": "analyze"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "Local variables are safe. Shared state is a question about everyone else",
        "podNumber": 1,
        "duration": 13
      },
      {
        "type": "hook",
        "body": "```python\n_rate_limit_cache = {}\n\ndef check_rate_limit(user_id):\n    count = _rate_limit_cache.get(user_id, 0)\n    if count >= 10:\n        return False\n    _rate_limit_cache[user_id] = count + 1\n    return True\n```\n\nRead this function on its own and the logic is fine: read a counter, compare it, increment it. But `_rate_limit_cache` is a module-level dictionary — every request handler in the process shares the exact same object. If two requests from the same user arrive close enough together that both read `count` before either writes it back, both can read `9`, both increment to `10`, and both get approved — the eleventh and twelfth request both slip through a limit that was supposed to stop at ten."
      },
      {
        "type": "text",
        "body": "A function's own local variables are safe to reason about in isolation — nothing outside the function can see or touch them while it runs. Shared mutable state is a completely different category: a module-level variable, a class attribute, an object passed by reference and mutated in place, a row in a database read and written without a lock. Understanding a function that touches shared state requires answering a question the function's own code never fully answers on its own: who else can read or write this same state, and when, relative to this function's own reads and writes?\n\nThis is exactly the kind of correctness bug that a happy-path trace, done by one caller calling the function once, cannot catch — tracing `check_rate_limit(user_id=42)` in isolation produces a perfectly correct-looking result every time, because the bug only exists in the gap *between* two overlapping calls, a scenario a single trace structurally can't represent. Finding this class of bug means specifically asking: could this function be called again, by something else, before this call finishes updating the state it depends on?\n\nFor generated code, this risk is easy to underweight, because a function like `check_rate_limit` reads as clean, idiomatic, textbook rate-limiting logic — the kind of code that appears correct in isolation in thousands of tutorials that never show it under concurrent load. Fluency in the single-caller case says nothing about safety in the multiple-caller case."
      },
      {
        "type": "sequence-it",
        "question": "Two nearly-simultaneous requests for the same `user_id`, both starting when `count` is already 9. Put the four operations in an interleaved order that results in BOTH requests being approved — a violation of the intended 10-request limit.",
        "steps": [
          "Request A reads `_rate_limit_cache.get(user_id, 0)` → gets 9",
          "Request B reads `_rate_limit_cache.get(user_id, 0)` → also gets 9, before A has written anything back",
          "Request A writes `_rate_limit_cache[user_id] = 10` and returns True (approved)",
          "Request B writes `_rate_limit_cache[user_id] = 10` and returns True (approved)"
        ],
        "explanation": "Both requests read the same stale value (9) before either one's write is visible to the other — that's the entire mechanism of the bug. Neither request's own code is wrong in isolation; each one correctly reads, compares, and increments. The bug lives entirely in the gap between two callers' reads and writes overlapping in time, which is exactly why a single-caller trace can never surface it: there's no interleaving to trace when only one caller exists. This is the concrete shape of 'who else can touch this state' — the answer here is 'another request for the same user, arriving close enough in time,' and that answer only becomes visible once you deliberately consider more than one caller at once."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC2-007"
      },
      {
        "type": "takeaway",
        "body": "A function's own logic can be correct in isolation and still be wrong under shared state — the bug lives in the gap between two callers' reads and writes, which a single trace structurally cannot show you."
      },
      {
        "type": "pod-header",
        "title": "Mutation you didn't mean to share",
        "podNumber": 2,
        "duration": 12
      },
      {
        "type": "text",
        "body": "A second, quieter version of the same risk: a function that mutates an object it was merely handed, rather than one it owns, without anyone at the call site expecting that. In languages where objects, lists, and dicts are passed by reference, a function that looks purely computational — takes an input, returns an output — can still leave the caller's original data changed underneath them, because 'in' and 'mutated' aren't mutually exclusive.\n\nA specific, well-known trap in this family: a mutable default argument. `def add_tag(item, tags=[])` looks like it gives every call a fresh empty list to work with — it doesn't. The default list is created exactly once, when the function is defined, and every call that doesn't pass its own `tags` argument shares and mutates that same one list, across every caller, for the lifetime of the program. This reads as ordinary, idiomatic-looking code, and it's one of the most consistently reported real bugs in generated Python for exactly that reason: it's syntactically unremarkable and semantically wrong."
      },
      {
        "type": "spot-flaw",
        "code": "def add_tag(item, tags=[]):\n    tags.append(item)\n    return tags",
        "question": "Called twice, once as `add_tag('urgent')` and once as `add_tag('billing')`, with no `tags` argument passed either time — what's actually wrong?",
        "options": [
          {
            "value": "shared-default-mutated",
            "label": "The default list `tags=[]` is created once, at function definition time, and shared across every call that omits the argument — the second call returns `['urgent', 'billing']`, not a fresh `['billing']`, because both calls mutated the same underlying list"
          },
          {
            "value": "return-type-inconsistent",
            "label": "The bug is that the function sometimes returns a list and sometimes returns nothing"
          },
          {
            "value": "append-is-slow",
            "label": "`list.append` is inefficient and should be replaced with a different data structure"
          }
        ],
        "correctValue": "shared-default-mutated",
        "feedback": {
          "shared-default-mutated": "Right. Python evaluates default argument values exactly once, when the `def` statement runs — not once per call. `tags=[]` creates a single list object that becomes the default for every future call omitting that argument, and because `.append()` mutates it in place, every call that relies on the default is silently accumulating into the same shared list rather than getting an isolated fresh one, which is exactly the kind of thing a single-call trace won't reveal.",
          "return-type-inconsistent": "The function does consistently return a list on every call shown here — the actual defect is about what's *in* that list across separate calls, not about the type of what's returned.",
          "append-is-slow": "Performance isn't the issue in this snippet at all — `list.append` is the normal, efficient way to add to a Python list. The defect is a correctness bug, not a speed one."
        },
        "flawExplanation": "This is one of Python's most well-documented traps for exactly this reason: the code reads as completely idiomatic, and the bug only shows up across multiple calls, never within a single one — a single-call trace, done in isolation the way the first lesson's happy-path technique teaches, would show `add_tag('urgent')` correctly returning `['urgent']` and reveal nothing wrong. The fix is the standard one: default to `None` and create a fresh list inside the function body when it's `None`, so every call that doesn't pass its own list actually gets an isolated one."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC2-008"
      },
      {
        "type": "takeaway",
        "body": "A function that mutates an object it was handed — including its own default arguments — can leave state changed for callers who never expected to share anything. Trace more than one call before trusting a function that touches anything mutable."
      }
    ]
  },
  {
    "id": "aod-rc2-l05",
    "moduleId": "aod-rc2",
    "order": 5,
    "duration": 20,
    "difficulty": "medium",
    "title": "Loops that terminate, and loops that only usually terminate",
    "description": "A loop's exit condition is a claim about what will eventually become true. The claim can be wrong, or true only for the inputs it was tested against.",
    "learningObjectives": [
      {
        "text": "Trace a loop's exit condition against a concrete input to confirm it will actually be reached, not just that it's syntactically present",
        "bloomLevel": "analyze"
      },
      {
        "text": "Identify a loop that terminates for typical inputs but has a reachable input for which it wouldn't",
        "bloomLevel": "analyze"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "\"It has a break statement\" is not the same as \"it terminates\"",
        "podNumber": 1,
        "duration": 10
      },
      {
        "type": "hook",
        "body": "```python\ndef find_available_username(base_name, is_taken_fn):\n    candidate = base_name\n    suffix = 1\n    while is_taken_fn(candidate):\n        candidate = f\"{base_name}{suffix}\"\n        suffix += 1\n    return candidate\n```\n\nThis has no explicit `break` — it relies entirely on `is_taken_fn(candidate)` eventually returning `False`. That's a real, load-bearing assumption: it terminates if and only if there exists some suffix for which the username is available. For a real user-facing signup system, is that guaranteed? What if `is_taken_fn` is buggy and always returns `True`? What if every one of the first ten thousand suffixes is genuinely already taken by bots?"
      },
      {
        "type": "text",
        "body": "A loop terminates when its exit condition becomes true, and 'has an exit condition' is not the same claim as 'the exit condition is guaranteed to become true.' Reading a loop for correctness means tracing not just what the loop does on each pass, but what specifically would have to be true about the real world for the loop to ever stop — and then asking whether that thing is actually guaranteed, or just usually the case in the inputs anyone happened to test with.\n\nThis distinction matters because a loop that terminates for every test case anyone ran can still be an infinite loop waiting for the one input where the exit condition's assumption doesn't hold. `find_available_username` will terminate instantly in every manual test, because manually you'll never generate ten thousand taken usernames — but it has no upper bound, no maximum attempt count, and no fallback if the assumption 'eventually some suffix is free' turns out false or just extremely slow to become true in a specific adversarial or degenerate case.\n\nThe habit: for any loop without an explicit, counted bound (a `for i in range(n)`, a fixed retry count), name out loud what has to become true for the loop to exit, and ask specifically whether that's guaranteed by something in the code, or just an assumption about the data the loop expects to see."
      },
      {
        "type": "predict-number",
        "scenario": "A generated retry function:\n\n```python\ndef fetch_with_retry(url, fetch_fn):\n    response = None\n    while response is None or response.status_code >= 500:\n        response = fetch_fn(url)\n    return response\n```\n\nNo maximum attempt count anywhere in the function.",
        "question": "If the remote server has an outage and returns a 503 status on every single request indefinitely, how many times will this loop call `fetch_fn` before returning — give your best estimate as a specific number.",
        "unit": "calls",
        "actualValue": 999999999,
        "explanation": "There's no finite correct number — that's the point of the exercise. The loop's exit condition, `response.status_code < 500`, depends entirely on the remote server eventually returning a non-5xx status, which is exactly the thing that isn't guaranteed during a real outage. With no maximum attempt count and no growing delay between attempts, a genuine prolonged outage turns this into an unbounded busy-loop, hammering the failing server with retries as fast as the loop can execute, for as long as the outage lasts — potentially forever, and actively making the outage worse by adding retry traffic to an already-failing service. A defensible version needs a maximum attempt count, and normally an increasing delay between attempts (the actual 'exponential backoff' the earlier module's stale comment claimed but never implemented)."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC2-009"
      },
      {
        "type": "takeaway",
        "body": "Name what specifically has to become true for a loop to exit, and check whether that's actually guaranteed by the code — a loop that terminates in every test you happened to run can still have no real upper bound."
      },
      {
        "type": "pod-header",
        "title": "Mutating the thing you're iterating over",
        "podNumber": 2,
        "duration": 10
      },
      {
        "type": "text",
        "body": "A second, more specific way a loop's termination assumption can quietly break: the loop mutates the very collection it's iterating over, changing the thing the loop's own progress is measured against while it's still measuring it. This can produce loops that skip elements, process elements twice, or in some languages and cases, throw an error mid-iteration or loop far longer than the original collection's length would suggest.\n\nThis is worth specifically watching for in generated code because 'iterate over a list, removing items that match some condition' is a common enough task that a model reaches for the intuitive-looking version — a `for` loop with a `.remove()` or `del` inside it — which looks correct, runs without crashing in simple manual tests, and quietly skips elements the moment two matching items sit next to each other in the list."
      },
      {
        "type": "diagnose-mechanism",
        "scenario": "```python\ndef remove_expired(items):\n    for item in items:\n        if item.is_expired:\n            items.remove(item)\n    return items\n```\n\nCalled with a list where the first two items, in order, both have `is_expired = True`, followed by one non-expired item.",
        "question": "What actually happens, and why does it happen specifically when two expired items are adjacent?",
        "options": [
          {
            "value": "index-shift-skip",
            "label": "Removing an item shifts every later item one position earlier in the list, but the loop's internal position counter keeps advancing regardless — so the item that shifted into the just-vacated slot gets skipped entirely on the next pass"
          },
          {
            "value": "raises-exception",
            "label": "Python always raises a RuntimeError immediately when you call `.remove()` inside a `for` loop over the same list"
          },
          {
            "value": "works-fine-here",
            "label": "This works correctly for this input — removal during iteration is only a problem when removing from the end of the list"
          }
        ],
        "correctValue": "index-shift-skip",
        "feedback": {
          "index-shift-skip": "Right. When the first expired item is removed, everything after it shifts one index to the left — the second expired item, previously at index 1, is now at index 0. But the loop's internal iterator has already advanced to look at index 1 next, which is now the *non-expired* item that shifted into that slot. The second expired item, now sitting at index 0, is never visited again. It survives the function despite matching the exact condition the loop was written to remove.",
          "raises-exception": "Python doesn't reliably raise an error for `list.remove()` during a `for` loop over that same list the way it does for some other mutate-while-iterating patterns — this specific case fails silently, by skipping, which is arguably worse than an exception because nothing signals that anything went wrong at all.",
          "works-fine-here": "This is exactly backwards — adjacency is precisely what triggers the bug. A single isolated expired item surrounded by non-expired items would in fact get removed correctly; it's specifically two (or more) expired items next to each other where the index shift causes one of them to be skipped."
        },
        "mechanism": "Iteration position and list contents are two separate things that a `for` loop over a mutable list assumes stay in sync — remove an item mid-iteration and they don't. The loop's notion of 'next position' doesn't know the list just got shorter, so it silently skips whatever slid into the gap. The safe pattern is to iterate over a copy (`for item in list(items):`) or build a new list of survivors (`items = [i for i in items if not i.is_expired]`) rather than mutating the collection the loop itself is walking."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC2-010"
      },
      {
        "type": "takeaway",
        "body": "A loop's progress and the collection it's iterating over need to stay in sync for the loop's own bookkeeping to mean what it claims — mutating the collection mid-iteration is a common, quiet way that assumption breaks."
      }
    ]
  },
  {
    "id": "aod-rc2-p01",
    "moduleId": "aod-rc2",
    "order": 6,
    "duration": 30,
    "difficulty": "medium",
    "title": "Practice — Enumerate the Untested Paths in Seven Functions",
    "description": "Seven drills, no new concepts: tracing happy paths, naming what each branch claims, checking what guard clauses discard, and finding the input where a loop or shared state breaks.",
    "learningObjectives": [
      {
        "text": "Apply hand-tracing, branch-claim analysis, and termination checking to unfamiliar functions at speed",
        "bloomLevel": "apply"
      },
      {
        "text": "Identify the specific input or interleaving that exposes a control-flow or state bug the code passes for on typical inputs",
        "bloomLevel": "analyze"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "Seven drills",
        "podNumber": 1,
        "duration": 30
      },
      {
        "type": "hook",
        "body": "Nothing new here. Seven drills across the module's skills, ordered from where the lessons ended to one step past it. Do them in one sitting or spread across a few — nothing here expires."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC2-P01"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC2-P02"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC2-P03"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC2-P04"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC2-P05"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC2-P06"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC2-P07"
      },
      {
        "type": "takeaway",
        "body": "If a drill needed a hint, that's what to watch for in the checkpoint — not a reason to redo the set."
      }
    ]
  },
  {
    "id": "aod-rc2-lab",
    "moduleId": "aod-rc2",
    "order": 7,
    "duration": 50,
    "difficulty": "hard",
    "title": "Lab — Path-Map a Generated Function and Find the Case It Forgot",
    "description": "Build a real path table for a function with real logic, then use it to find the input the function's author never considered.",
    "learningObjectives": [
      {
        "text": "Enumerate the full set of distinct execution paths through a function with branches, loops, and shared state",
        "bloomLevel": "apply"
      },
      {
        "text": "Produce a path table with concrete inputs and expected outputs, and use it to identify a genuinely uncovered case",
        "bloomLevel": "create"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "The lab",
        "podNumber": 1,
        "duration": 50
      },
      {
        "type": "hook",
        "body": "Every lesson this module worked one technique at a time on one function. A real generated function usually needs all five at once: trace the happy path, name what the branches claim, check what guard clauses discard, ask about shared state, confirm loops terminate. This lab asks you to do all of it, systematically, against a real function — and to write the result down as a path table, the artefact that makes 'I checked it' a specific, defensible claim instead of an impression."
      },
      {
        "type": "lab-brief",
        "labId": "aod-rc2-path-map",
        "title": "Path-map a function and find its gap",
        "brief": "Pick a real function with meaningful branching, at least one loop or piece of shared state, and no existing test suite you'd trust — from your own project, a generated piece of code you haven't reviewed carefully yet, or (if nothing fits) a function you generate now for a plausible task with at least three distinct branches. Build a complete path table for it, then use the table to find at least one input the function handles incorrectly or doesn't account for at all.",
        "deliverable": "A path table: every distinct execution path through the function, a concrete input that exercises each one, the output you traced by hand, and a written statement of the specific gap you found — the input, what the function actually does with it, and what it should do instead.",
        "stages": [
          {
            "title": "Enumerate the paths",
            "minutes": 10,
            "instructions": "List every distinct path through the function's branches — every combination of conditions that leads to a different outcome, the way this module named what each branch claims. Don't pick inputs yet; just enumerate the paths themselves as English descriptions.",
            "checkpoint": "You have a list of paths, each one a distinct combination of branch outcomes, covering every branch in the function including the final else."
          },
          {
            "title": "Trace a concrete input per path",
            "minutes": 15,
            "instructions": "For each path on your list, pick a specific, concrete input that would actually take that path, and trace it by hand — real values, every variable's value at each step, ending in a specific output you can defend.",
            "checkpoint": "Every path has a concrete input and a hand-traced, specific output — not a description of what the path 'probably' does."
          },
          {
            "title": "Check state and loop termination",
            "minutes": 10,
            "instructions": "If the function touches any shared mutable state, name what else could touch it and whether that could produce a wrong result. If it has any loop, name what has to become true for it to exit and whether that's actually guaranteed.",
            "checkpoint": "Shared state and loop termination are addressed explicitly — with a written 'not applicable' if genuinely neither is present, not a silent skip."
          },
          {
            "title": "Find the gap and write the deliverable",
            "minutes": 15,
            "instructions": "Using the path table, find at least one input the function handles wrong — a case none of your traced paths actually covers correctly, a missing precondition, an unguaranteed loop exit, a shared-state race. Compress the whole thing into the deliverable and self-check against the rubric.",
            "checkpoint": "The path table and the gap are both written down, and the gap is described specifically enough that someone else could reproduce it without re-deriving your reasoning."
          }
        ],
        "rubric": [
          {
            "criterion": "Every branch, including the final else, has a path in the table",
            "meets": "No branch in the function is left unrepresented — the else's implicit claim gets its own row, not a silent omission."
          },
          {
            "criterion": "Every traced output is a specific, defensible value",
            "meets": "Each row shows real input values and a real computed output, not a summary of what the path 'does.'"
          },
          {
            "criterion": "Shared state and loop termination are explicitly addressed",
            "meets": "If present, the specific external assumption is named. If absent, that's stated rather than left implicit."
          },
          {
            "criterion": "A genuine gap is found and described with a concrete input",
            "meets": "The gap names a specific input, the function's actual (wrong or missing) behavior on it, and what correct behavior would look like."
          },
          {
            "criterion": "The gap is something a real user or caller could actually trigger",
            "meets": "Not a contrived, impossible-in-practice input — a case that could plausibly occur given how the function is actually called."
          }
        ]
      },
      {
        "type": "takeaway",
        "body": "A path table turns 'I reviewed this function' from an impression into a specific, checkable artefact — and building one is usually how the actual gap gets found, not an afterthought once you already suspect one."
      }
    ]
  },
  {
    "id": "aod-rc2-cp",
    "moduleId": "aod-rc2",
    "order": 8,
    "duration": 10,
    "difficulty": "easy",
    "title": "Checkpoint — Control Flow and State",
    "description": "Ten questions: four from this module, four from Reading for Intent, two from further back. Ungraded, untimed.",
    "learningObjectives": [
      {
        "text": "Retrieve this module's core distinctions alongside earlier modules', without looking them up",
        "bloomLevel": "remember"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "Let's see what stuck",
        "podNumber": 1,
        "duration": 10
      },
      {
        "type": "retrieval-check",
        "title": "Checkpoint — Control Flow and State",
        "questions": [
          {
            "from": "This module — Control Flow and State",
            "prompt": "What should a happy-path trace end in?",
            "options": [
              {
                "value": "a1",
                "label": "A specific, defensible value derived from concrete inputs, not a summary of what the code 'probably' does"
              },
              {
                "value": "a2",
                "label": "A confirmation that the code compiles or runs without an error"
              },
              {
                "value": "a3",
                "label": "A list of every variable name the function declares"
              }
            ],
            "correctValue": "a1",
            "explanation": "Tracing means updating every variable's real value line by line for a concrete input, ending in a specific number or result you can defend with arithmetic — not a plausible-sounding impression of the code's shape."
          },
          {
            "from": "This module — Control Flow and State",
            "prompt": "A branch chain's final `else` is best understood as...",
            "options": [
              {
                "value": "b1",
                "label": "An implicit claim about everything not named above it — a claim that can go stale the moment the domain grows a new case"
              },
              {
                "value": "b2",
                "label": "Dead code that should generally be removed"
              },
              {
                "value": "b3",
                "label": "Evidence the author didn't think carefully about the earlier branches"
              }
            ],
            "correctValue": "b1",
            "explanation": "The customer-tier example showed this precisely: an else clause correct for a three-tier system silently mishandled a fourth tier added later, with no error to reveal it."
          },
          {
            "from": "This module — Control Flow and State",
            "prompt": "A guard clause returns the same generic default (like `None`) for three different precondition failures. What's the risk?",
            "options": [
              {
                "value": "c1",
                "label": "The caller loses the ability to distinguish which precondition failed, even though the function's own logic knew at the moment each guard fired"
              },
              {
                "value": "c2",
                "label": "The function is more likely to crash"
              },
              {
                "value": "c3",
                "label": "There's no risk, as long as the function doesn't throw an error"
              }
            ],
            "correctValue": "c1",
            "explanation": "The `canCheckout` example collapsed three distinct, meaningful failure reasons into one indistinguishable `false`, leaving the caller unable to tell the user what was actually wrong."
          },
          {
            "from": "This module — Control Flow and State",
            "prompt": "Why can a function be correct when traced with a single caller and still be wrong under shared mutable state?",
            "options": [
              {
                "value": "d1",
                "label": "Because the bug can live entirely in the gap between two overlapping callers' reads and writes — a gap a single-caller trace has no way to represent"
              },
              {
                "value": "d2",
                "label": "Because shared state is always slower than local state"
              },
              {
                "value": "d3",
                "label": "Because Python and JavaScript don't support shared state safely under any circumstances"
              }
            ],
            "correctValue": "d1",
            "explanation": "The rate-limiter example traced correctly for one caller every time — the bug only appears once you deliberately interleave two overlapping calls, which is exactly what a single trace can't show."
          },
          {
            "from": "Previous module — Reading for Intent",
            "prompt": "What's the recommended reading order for an unfamiliar function's signature versus its body?",
            "options": [
              {
                "value": "e1",
                "label": "Signature, then every return, then the body — so the body read becomes a check against a specific hypothesis"
              },
              {
                "value": "e2",
                "label": "Top to bottom, the order it's written in"
              },
              {
                "value": "e3",
                "label": "The body first, since that's where the real logic lives"
              }
            ],
            "correctValue": "e1",
            "explanation": "Signature and return types carry disproportionate intent for very little reading time — reading them first turns the body into verification rather than open-ended discovery."
          },
          {
            "from": "Previous module — Reading for Intent",
            "prompt": "Why can a function's name and its docstring agree with each other and still both be wrong about what the code does?",
            "options": [
              {
                "value": "f1",
                "label": "Both are outputs of the same generation process as the logic they describe, with nothing independently checking either against the actual implementation"
              },
              {
                "value": "f2",
                "label": "Docstrings are only ever written after the code is deployed, so they're always slightly out of date"
              },
              {
                "value": "f3",
                "label": "They can't both be wrong — agreement between them is strong evidence of correctness"
              }
            ],
            "correctValue": "f1",
            "explanation": "The `is_authorized` example had a name and a docstring in full agreement, both claiming a resource-specific permission check the code never actually performed."
          },
          {
            "from": "Previous module — Reading for Intent",
            "prompt": "An unexplained, non-round constant like `0.0825` in a tax calculation is best treated as...",
            "options": [
              {
                "value": "g1",
                "label": "A strong signal of a real external requirement worth confirming, not an arbitrary detail to skip past"
              },
              {
                "value": "g2",
                "label": "A likely typo that should be rounded"
              },
              {
                "value": "g3",
                "label": "Irrelevant unless a comment explains it"
              }
            ],
            "correctValue": "g1",
            "explanation": "Specific, non-round numbers are expensive to arrive at by accident — they're evidence of a decision made somewhere else, worth naming as an open question rather than treated as noise."
          },
          {
            "from": "Previous module — Reading for Intent",
            "prompt": "What does the difficulty of compressing a function into one accurate sentence usually signal, once every attempt needs another unrelated 'and'?",
            "options": [
              {
                "value": "h1",
                "label": "The function is doing more than one job, stitched together, and should probably be split"
              },
              {
                "value": "h2",
                "label": "The function's variable names need improvement"
              },
              {
                "value": "h3",
                "label": "The description should be allowed to run to two or three sentences instead"
              }
            ],
            "correctValue": "h1",
            "explanation": "The `handleSignup` example — validate, create, email, log, provision — resisted one-sentence compression because it genuinely was five separable responsibilities, not one function that was merely hard to describe."
          },
          {
            "from": "Two modules back — Knowing When to Take Over",
            "prompt": "What's the shape of a situation where directing an AI tool costs more than writing the code yourself?",
            "options": [
              {
                "value": "i1",
                "label": "A stalled prompt loop with no closer, a problem whose hard part is deciding rather than translating, or a domain you can't review well enough to judge the output"
              },
              {
                "value": "i2",
                "label": "Any task that takes more than ten minutes to prompt for"
              },
              {
                "value": "i3",
                "label": "Any task touching a database"
              }
            ],
            "correctValue": "i1",
            "explanation": "Take-over situations share a recognizable shape — non-convergence, decision-heavy work, or an unreviewable domain — not a fixed time or technology boundary."
          },
          {
            "from": "Three modules back — Reading the First Draft",
            "prompt": "What's the fastest reliable signal that a generated diff needs a full rewrite rather than a quick fix?",
            "options": [
              {
                "value": "j1",
                "label": "The core approach itself is wrong for the problem, not just a detail within an otherwise sound approach"
              },
              {
                "value": "j2",
                "label": "The diff is longer than about 50 lines"
              },
              {
                "value": "j3",
                "label": "The diff doesn't include any comments"
              }
            ],
            "correctValue": "j1",
            "explanation": "Line count and comment presence are surface features; whether the underlying approach actually fits the problem is the distinction that determines whether refining is salvageable or wasted effort."
          }
        ]
      }
    ]
  },
  {
    "id": "aod-rc3-l01",
    "moduleId": "aod-rc3",
    "order": 1,
    "duration": 25,
    "difficulty": "medium",
    "title": "What the function believes about its input",
    "description": "Every line of a function's body encodes a belief about the shape of its arguments — that a list has at least one item, that a key exists, that a string is already trimmed. None of those beliefs are checked unless the code checks them.",
    "learningObjectives": [
      {
        "text": "Identify the unstated assumptions a function's body makes about the shape of its input, distinct from what its signature declares",
        "bloomLevel": "analyze"
      },
      {
        "text": "Trace a concrete input that violates one of those assumptions and predict what actually happens",
        "bloomLevel": "analyze"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "A signature declares a type. It doesn't declare a shape.",
        "podNumber": 1,
        "duration": 13
      },
      {
        "type": "hook",
        "body": "```python\ndef top_scorer(players: list[dict]) -> dict:\n    return max(players, key=lambda p: p['score'])\n```\n\nThe type hint says `list[dict]` in, `dict` out. It says nothing about whether the list can be empty, whether every dict actually has a `'score'` key, or whether `score` is always a number rather than sometimes `None`. Call this with `players=[]` and it raises `ValueError: max() arg is an empty sequence` — not because the type hint was violated, but because the function's body silently assumed at least one player would always be present, a belief the signature never stated and nothing checked."
      },
      {
        "type": "text",
        "body": "A type annotation, in Python or TypeScript or any gradually-typed language, describes a *category* of value — a list, a dict, a string — without describing the specific *shape* a value in that category needs to have for the function's logic to actually work. `list[dict]` is satisfied by an empty list, a list of one dict, a list of a thousand dicts each missing the `score` key — the type system has nothing more to say once the outer container and its element type check out.\n\nEvery function body, reading through it, is making a series of narrower claims than the signature does: this list has at least one element, this dict has this specific key, this number is never negative, this string never contains a certain character. None of those claims are visible anywhere except in how the code would behave if they were false — which means finding them requires reading the body specifically for what it assumes, not just what it declares.\n\nThe habit for this module: read a function's body once for what it *does*, then read it again asking, for each piece of input it touches, 'what would have to be true about this value for this line not to blow up or silently misbehave?' That second read surfaces the function's actual, narrower beliefs — the ones a type signature never had the vocabulary to express in the first place."
      },
      {
        "type": "concept-flow",
        "scenario": "A generated function for computing a shipping discount:\n\n```python\ndef apply_bulk_discount(order: dict) -> float:\n    items = order['items']\n    total_qty = sum(i['quantity'] for i in items)\n    unit_price = items[0]['unit_price']\n    if total_qty >= 10:\n        return unit_price * 0.9\n    return unit_price\n```",
        "question": "Which of these is the narrowest, most load-bearing assumption this function makes about `order` that its type hint (`dict`) says nothing about?",
        "options": [
          {
            "label": "That `order['items']` is a non-empty list, and specifically that every item shares the exact same `unit_price` as `items[0]` — the function only ever reads the first item's price and silently ignores the rest",
            "value": "same-price-assumption"
          },
          {
            "label": "That `order` has a `'total'` key",
            "value": "total-key"
          },
          {
            "label": "That `total_qty` is always an integer rather than a float",
            "value": "qty-type"
          }
        ],
        "correctValue": "same-price-assumption",
        "feedback": {
          "same-price-assumption": "Right. `items[0]['unit_price']` reads exactly one item's price and treats it as *the* price for the whole discount calculation — a belief that only holds if every item in the order shares the same unit price, which is never stated, never checked, and false for any order mixing different products. `dict` as a type says nothing about this at all; it's a belief buried in which specific field got read.",
          "total-key": "The function never reads `order['total']` anywhere — there's no evidence it depends on that key existing at all.",
          "qty-type": "`sum()` over a generator works fine whether the individual quantities are ints or floats; nothing in this function actually requires `total_qty` to be an integer specifically."
        },
        "concept": "A function's real, load-bearing beliefs about its input often live in which specific field or index gets read, not in the type of the container as a whole — `items[0]['unit_price']` is a much narrower and much more fragile claim than 'items is a list of dicts,' and it's invisible until you ask what would have to be true for that specific line to be correct."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC3-001"
      },
      {
        "type": "takeaway",
        "body": "A type signature describes a category of value. A function's body encodes narrower, unstated beliefs about the specific shape within that category — find them by asking what would have to be true for each line to behave correctly."
      },
      {
        "type": "pod-header",
        "title": "Trace the input that breaks the belief",
        "podNumber": 2,
        "duration": 12
      },
      {
        "type": "text",
        "body": "Once you've named a function's unstated belief, the next step is the same hand-tracing discipline from the previous module: construct a concrete input that violates the belief, and trace what actually happens — not what you'd guess happens, what the code, read line by line, actually does. Sometimes it raises a clear, immediate exception, which is at least honest about failing. Sometimes it does something much quieter: returns a wrong-but-plausible-looking value, corrupts a downstream calculation, or silently drops data.\n\nThe quiet failures are the ones worth specifically hunting for, because a loud, immediate crash tends to get caught in testing or in the first real use — someone notices, someone fixes it. A function that returns `0.0` instead of raising when handed something it didn't expect can pass every manual smoke test and still be wrong in production for months, because nothing about running it looks broken."
      },
      {
        "type": "worked-example",
        "problem": "Trace `apply_bulk_discount` from the concept-flow above with an order that violates its unstated same-price assumption.\n\n```python\ndef apply_bulk_discount(order: dict) -> float:\n    items = order['items']\n    total_qty = sum(i['quantity'] for i in items)\n    unit_price = items[0]['unit_price']\n    if total_qty >= 10:\n        return unit_price * 0.9\n    return unit_price\n```\n\nInput: `order = {'items': [{'quantity': 8, 'unit_price': 5.00}, {'quantity': 4, 'unit_price': 40.00}]}` — 8 units of a $5 item and 4 units of a $40 item.",
        "steps": [
          {
            "label": "Compute total_qty",
            "work": "`sum(i['quantity'] for i in items) = 8 + 4 = 12`.",
            "why": "This part of the function is genuinely correct for any mix of items — quantity summing doesn't depend on the same-price belief."
          },
          {
            "label": "Read unit_price",
            "work": "`items[0]['unit_price'] = 5.00` — the price of the *first* item only, ignoring the $40 item entirely.",
            "why": "This is the exact line where the unstated belief gets exercised. The function has no mechanism for handling two different prices, so it silently picks one and discards the other."
          },
          {
            "label": "Apply the discount and return",
            "work": "`total_qty (12) >= 10`, so return `5.00 * 0.9 = 4.50`.",
            "why": "The function returns `4.50` as \"the\" discounted price for an order that actually contains two different products at two different prices — a specific, wrong, plausible-looking number that gives no indication anything went wrong."
          }
        ],
        "faded": {
          "problem": "Trace `top_scorer` from the hook, with an input that violates its unstated non-empty-list belief.\n\n```python\ndef top_scorer(players: list[dict]) -> dict:\n    return max(players, key=lambda p: p['score'])\n```\n\nInput: `players = []`.",
          "blanks": [
            {
              "prompt": "What does `max()` do when called on an empty sequence?",
              "answer": "It raises a ValueError — 'max() arg is an empty sequence'.",
              "accept": [
                "raises an error",
                "valueerror",
                "throws an exception"
              ],
              "why": "Unlike the discount example, this particular unstated belief fails loudly — which, per the lesson, is actually the easier failure mode to catch, since it can't be silently ignored in testing."
            },
            {
              "prompt": "Is this failure mode (loud exception) more or less dangerous in production than the discount function's silent wrong-answer failure?",
              "answer": "Less dangerous — a raised exception is visible immediately, gets logged, and typically triggers an alert or a failed request, unlike a silently wrong number that looks like a valid result.",
              "accept": [
                "less dangerous",
                "less risky",
                "easier to catch"
              ],
              "why": "This is the core distinction the lesson draws: a function that fails loudly on a bad assumption is a smaller risk than one that fails quietly, even though both stem from the same root cause — an unchecked belief about input shape."
            }
          ]
        },
        "procedure": "Name the function's narrowest unstated belief about its input's shape. Construct a concrete input that violates it. Trace the actual behavior line by line — and specifically classify whether the failure is loud (an exception, easy to catch) or quiet (a plausible-looking wrong value, easy to miss)."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC3-002"
      },
      {
        "type": "takeaway",
        "body": "Trace a concrete input that violates the function's unstated belief, and classify the failure as loud or quiet — quiet, plausible-looking wrong answers are the ones worth hunting for specifically, because nothing about running the code reveals them."
      }
    ]
  },
  {
    "id": "aod-rc3-l02",
    "moduleId": "aod-rc3",
    "order": 2,
    "duration": 20,
    "difficulty": "medium",
    "title": "Null, empty, missing and zero are four different things",
    "description": "'No value,' 'a value that happens to be empty,' 'the field isn't there at all,' and 'a real value that's falsy' are four distinct facts about the world, and code that treats them as one is throwing information away.",
    "learningObjectives": [
      {
        "text": "Distinguish null, empty, missing, and falsy-but-valid as four distinct real-world facts a field can represent",
        "bloomLevel": "analyze"
      },
      {
        "text": "Identify code that conflates these into a single check and trace the specific case where that conflation produces a wrong result",
        "bloomLevel": "analyze"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "Four different facts, one lazy check",
        "podNumber": 1,
        "duration": 10
      },
      {
        "type": "hook",
        "body": "```python\ndef needs_reminder(subscription):\n    if not subscription.get('trial_end_date'):\n        return False\n    return subscription['trial_end_date'] < today()\n```\n\n`not subscription.get('trial_end_date')` is `True` in at least three genuinely different situations: the key `'trial_end_date'` isn't in the dict at all (this subscription was never a trial), the key is present but explicitly `None` (the trial ended and someone cleared the field), or the key holds an empty string from a bad upstream import. Those are three different facts about this subscription's history, and this line treats all three, plus one more — a `trial_end_date` that happens to fall exactly on the epoch or some other falsy-adjacent edge — identically."
      },
      {
        "type": "text",
        "body": "Four states get routinely collapsed into one in code that reaches for a bare truthiness check: **missing** (the field doesn't exist in this record at all — never set, never applicable), **null/None** (the field exists and was explicitly set to 'no value'), **empty** (the field exists and holds a real but zero-length value — `''`, `[]`, `{}`), and **falsy-but-meaningful** (a real value that happens to be `0`, `False`, or otherwise evaluates as falsy in the language's truthiness rules, like a discount of exactly `0%` or a quantity of exactly `0` items still on backorder).\n\nThese are four different facts about the world, and a real system usually needs to respond to them differently. 'This user never set a middle name' (missing) is a different situation from 'this user explicitly cleared their middle name' (null) is a different situation from 'this user has a middle name that's an empty string because of a bad CSV import' (empty, garbage) — but `if not user.middle_name` treats all three, and also a middle name that's the literal string `'0'` if the language's truthiness rules apply to strings differently than expected, as one undifferentiated case.\n\nThe practical habit: whenever you see a bare truthiness check on a value that could plausibly be zero, empty, or genuinely absent for different real reasons, ask which of the four states the check is actually trying to catch — and whether it's accidentally catching the other three along with it."
      },
      {
        "type": "evidence-stack",
        "scenario": "Four fields from a generated e-commerce order model, each checked with `if not order.<field>:`.",
        "question": "Select every field where a bare truthiness check plausibly conflates a real, meaningful falsy value with 'no value.'",
        "items": [
          {
            "value": "discount-pct",
            "label": "`discount_pct` — a real order can legitimately have a discount of exactly `0`, meaning 'no discount applied,' which is meaningfully different from 'discount was never calculated'",
            "applicable": true
          },
          {
            "value": "customer-notes",
            "label": "`customer_notes` — a genuinely empty string here just means the customer didn't write anything, which is the same real-world fact whether it's `''`, `None`, or a missing key",
            "applicable": false
          },
          {
            "value": "gift-wrap-quantity",
            "label": "`gift_wrap_quantity` — `0` legitimately means 'no gift wrap requested,' distinct from `None` meaning 'this order predates the gift-wrap feature and the field was never backfilled'",
            "applicable": true
          },
          {
            "value": "is-cancelled",
            "label": "`is_cancelled` — a boolean where `False` genuinely and unambiguously means 'not cancelled,' with no other real-world state it could be confused with",
            "applicable": false
          }
        ],
        "explanation": {
          "discount-pct": "`0` here is a real, valid, meaningful value — 'this order has no discount' — and it's falsy in Python. A check meant to catch 'discount was never set' will also silently catch every order with a legitimate zero discount, potentially skipping logic that should run for both.",
          "customer-notes": "An empty notes field and a missing notes field both cash out to the same real-world fact — the customer left no note — so there's no meaningful distinction being lost by treating them the same. This is exactly the case where a bare truthiness check is fine.",
          "gift-wrap-quantity": "Same shape as the discount field: `0` is a legitimate, common, meaningful value (no gift wrap wanted), and conflating it with `None` (the field never existed for old orders) could mean old orders are silently treated as 'no gift wrap' when the honest answer is 'unknown, this predates the feature.'",
          "is-cancelled": "A boolean flag genuinely only has two real states, and `False` correctly and unambiguously represents one of them — there's no third or fourth real-world fact being collapsed here."
        },
        "synthesis": "The fields worth scrutinizing are the ones where a legitimate, real value (a `0`, an empty collection) exists alongside a legitimately different 'this was never set' state. Fields where every falsy value maps to the same real-world meaning — a boolean, free-text notes — don't have this risk; a bare truthiness check there is genuinely fine, not just lucky."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC3-003"
      },
      {
        "type": "takeaway",
        "body": "A bare truthiness check is only safe when every falsy value it could match means the same real-world thing. Wherever a legitimate zero, empty value, or false could plausibly coexist with a genuine 'never set' state, name which one the check is actually supposed to catch."
      },
      {
        "type": "pod-header",
        "title": "Reading JSON specifically widens the risk",
        "podNumber": 2,
        "duration": 10
      },
      {
        "type": "text",
        "body": "This risk sharpens specifically around JSON, which most AI-generated backend and integration code spends its life reading and writing. JSON has exactly one way to represent 'no value' (`null`), and no built-in distinction at all between 'this key is present with value null' and 'this key is absent entirely' — most JSON parsers in most languages quietly merge those two into the same result once parsed, unless the code goes out of its way to check for key presence separately from value.\n\nThis means a generated handler reading an incoming JSON payload is working with a format that has already thrown away one of the four distinctions before the code even runs — missing and explicit-null frequently arrive indistinguishable, and the code has to actively decide to preserve or recover that distinction rather than getting it for free."
      },
      {
        "type": "spot-flaw",
        "code": "def update_profile(request_json, user):\n    if request_json.get('bio'):\n        user.bio = request_json['bio']\n    if request_json.get('display_name'):\n        user.display_name = request_json['display_name']\n    return user",
        "question": "A client sends `{\"bio\": null, \"display_name\": \"Alex\"}`, wanting to explicitly clear their bio and update their display name. What's actually wrong here?",
        "options": [
          {
            "value": "get-conflates-clear-with-skip",
            "label": "`request_json.get('bio')` returns `None` for both 'the key is missing entirely' and 'the key is present with an explicit null value,' so the client's explicit request to clear their bio is silently ignored — treated identically to not having mentioned bio at all"
          },
          {
            "value": "should-reject-null",
            "label": "The function should reject `null` values entirely and require clients to send an empty string instead"
          },
          {
            "value": "display-name-is-the-bug",
            "label": "The real bug is in the `display_name` line, not the `bio` line"
          }
        ],
        "correctValue": "get-conflates-clear-with-skip",
        "feedback": {
          "get-conflates-clear-with-skip": "Right. `.get('bio')` returns `None` whether `'bio'` is absent from `request_json` or present with an explicit `null` value — and `if None:` is `False` either way, so the update is skipped in both cases. The client's explicit 'clear my bio' request produces no visible error and no change at all, which from the client's side looks like the request silently failed.",
          "should-reject-null": "Rejecting `null` outright would remove the client's only way to express 'clear this field' as opposed to 'leave it alone,' which is a real and common requirement for a profile-editing API — the fix is to distinguish presence from truthiness, not to forbid the value that's being mishandled.",
          "display-name-is-the-bug": "`display_name` happens to work correctly for this specific request only because `\"Alex\"` is a non-empty, truthy string — the identical bug is lurking in that line too (a client trying to explicitly clear their display name with `null` would hit the same silent-skip problem), but the concrete failure described in the question is about `bio`."
        },
        "flawExplanation": "The fix is to check key presence rather than truthiness: `if 'bio' in request_json:` distinguishes 'the client didn't mention bio' (skip) from 'the client explicitly sent `null`' (clear it) — the same JSON-specific missing-versus-null recovery the lesson text describes. `.get()` alone throws that distinction away, silently, for every field on this handler, not just `bio`."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC3-004"
      },
      {
        "type": "takeaway",
        "body": "JSON collapses 'missing' and 'null' into the same parsed value unless the code specifically checks for key presence rather than truthiness — a generated handler reading JSON needs to actively recover a distinction the format itself doesn't preserve."
      }
    ]
  },
  {
    "id": "aod-rc3-l03",
    "moduleId": "aod-rc3",
    "order": 3,
    "duration": 25,
    "difficulty": "medium",
    "title": "Boundaries: where validation actually has to happen",
    "description": "Validated data and unvalidated data look identical once they're sitting in a variable — the only way to know which one you have is to trace back to whether anything actually checked it, at a point where checking was still possible.",
    "learningObjectives": [
      {
        "text": "Identify the trust boundary a piece of data actually crossed — the point where it moved from a source that could lie to code that assumes it doesn't",
        "bloomLevel": "apply"
      },
      {
        "text": "Recognize when internal code trusts validation that only happens on some, not all, of its call paths",
        "bloomLevel": "analyze"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "A trust boundary is a place, not a feeling",
        "podNumber": 1,
        "duration": 13
      },
      {
        "type": "hook",
        "body": "```python\ndef process_transfer(amount, from_account, to_account):\n    from_account.balance -= amount\n    to_account.balance += amount\n```\n\nThis function has no validation at all — no check that `amount` is positive, no check that `from_account` has sufficient balance, no check that `from_account` and `to_account` aren't the same account. That's not automatically a bug. It's only a bug if nothing *upstream*, on every path that can reach this function, actually validated those things first. Whether it's safe depends entirely on something this function's own code can't tell you: who calls it, and what they checked before calling."
      },
      {
        "type": "text",
        "body": "A trust boundary is the specific point in a system where data crosses from a source that might be wrong, malicious, or malformed into code that's going to treat it as reliable. Outside an API handler, a form submission, a webhook payload, a file upload, a value some other, less-trusted internal service sent you — all untrusted. Inside your own business logic, operating on values your own code already constructed and checked — trusted, at least with respect to shape and type.\n\nValidation only does real work at the boundary where untrusted data first enters trusted code. Once a value has crossed that boundary and been checked, re-checking it deeper in the call stack is redundant, not wrong exactly, but validating it *deeper* than the boundary — inside a function that's only ever called with already-checked data — creates a specific risk: the validation looks like it's happening somewhere, so a reviewer skimming the codebase can convince themselves the system is safe, without confirming that *every* path into the sensitive operation actually passes through the validated entry point.\n\nThe practical question for reading `process_transfer`: is this function only ever called from one place, immediately after validation? Or is it also callable from an admin script, a batch job, a test fixture repurposed in production, a second API route someone added later without realizing the first route's validation wasn't shared? The function's own code can't answer that — you have to trace every actual caller."
      },
      {
        "type": "case-sim",
        "title": "The second call site",
        "opening": "You're reviewing `process_transfer` from the hook. It's called from `POST /transfers`, which does validate: checks `amount > 0`, checks sufficient balance, checks the two accounts differ. The validation looks solid at that one call site.",
        "start": "start",
        "nodes": {
          "start": {
            "situation": "Before approving the PR, you decide to check whether `process_transfer` has any other callers.",
            "question": "What's the first thing to check?",
            "choices": [
              {
                "value": "trust-the-route",
                "label": "Nothing further — the one call site you've seen validates correctly, and that's the code under review",
                "consequence": "You approve. Three weeks later, someone adds an internal `POST /admin/reverse-transfer` endpoint that calls `process_transfer` directly, reusing it for convenience, without realizing it needed to duplicate the original validation. A negative-amount reversal silently credits the wrong account.",
                "ending": "missed"
              },
              {
                "value": "search-for-callers",
                "label": "Search the codebase for every place `process_transfer` is called, not just the one route in this diff",
                "consequence": "You find it's also called from a nightly reconciliation job, which passes amounts computed from a report — never validated the same way the API route validates user input.",
                "nextNode": "found-second"
              }
            ]
          },
          "found-second": {
            "situation": "The reconciliation job's amounts come from internal report data, not directly from a user — lower risk than the API route, but the report-generation code has had bugs before, including one last year that produced a negative total.",
            "question": "Given that a second, differently-trusted caller exists, what's the right move?",
            "choices": [
              {
                "value": "leave-unvalidated",
                "label": "Leave `process_transfer` itself unvalidated, since the risk from the batch job seems lower than from direct user input",
                "consequence": "Six months later the report-generation bug resurfaces in a different form, and a negative amount flows through the reconciliation job into `process_transfer` uncaught, because the only validation in the system still lives at the one call site that isn't this one.",
                "ending": "recurred"
              },
              {
                "value": "move-validation-into-function",
                "label": "Move the core validation (positive amount, sufficient balance, distinct accounts) into `process_transfer` itself, so every caller gets it regardless of what they remembered to check first",
                "consequence": "The API route's validation becomes redundant but harmless — it can stay as an early, user-friendly error message, while `process_transfer` is now safe regardless of who calls it or what they forgot to check.",
                "ending": "resolved"
              }
            ]
          }
        },
        "endings": {
          "missed": {
            "verdict": "costly",
            "summary": "Trusting one validated call site without checking for others let a second, unvalidated caller reach the same sensitive operation.",
            "lesson": "Validation reviewed at one call site tells you nothing about a call site that doesn't exist yet. The safe assumption about a function with no internal validation is that it's one new caller away from being reached without any."
          },
          "recurred": {
            "verdict": "mixed",
            "summary": "The second caller was found, but the fix left the underlying function still trusting whoever calls it, so the same class of bug came back through a different door.",
            "lesson": "Finding an extra caller is only half the job — the fix that actually closes the gap moves validation to the function every caller has to pass through, not to yet another individual call site."
          },
          "resolved": {
            "verdict": "good",
            "summary": "Validation moved to the function itself, so every current and future caller is protected regardless of what they remember to check first.",
            "lesson": "The most durable place to validate is the function every path is forced to go through, not the specific call site you happened to be reviewing — a boundary is only real if nothing can get past it."
          }
        }
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC3-005"
      },
      {
        "type": "takeaway",
        "body": "Validation at one call site says nothing about a function's safety in general — a function with no internal validation of its own is only as safe as every current and future caller remembering to check first, which is a promise the function's own code can't enforce."
      },
      {
        "type": "pod-header",
        "title": "The boundary can move without anyone deciding it should",
        "podNumber": 2,
        "duration": 12
      },
      {
        "type": "text",
        "body": "There's a version of this risk specific to AI-assisted development worth naming directly: when a tool is asked to add a new entry point to an existing system — a new API route, a new CLI command, a new scheduled job — it's generating new code that needs to reach an existing, previously-internal function, and it has no way of knowing, just from the function's signature, whether that function expects to be called with already-validated data or not. A function named `process_transfer(amount, from_account, to_account)` gives no signal in its name or type hints that it's relying on validation the tool can't see happening thirty lines away in a different file.\n\nThis means every new call site an AI tool adds to an existing sensitive function is a small, specific moment where the trust boundary could silently move — not because anyone decided it should, but because the new caller simply didn't know the old assumption existed. Reading a diff that adds a new caller to an existing internal function is exactly the moment to ask the boundary question explicitly, since it's precisely the situation where nobody involved, including the tool, necessarily thought to ask it."
      },
      {
        "type": "interactive-guess",
        "question": "A generated diff adds a new `POST /internal/bulk-transfer` endpoint that loops over a CSV upload and calls `process_transfer(amount, from_account, to_account)` for each row, with no validation of the CSV's amount column beyond confirming it parses as a number. Should this specific addition raise the boundary question, even though `process_transfer` already has a validated caller elsewhere?",
        "hint": "Does the existence of one safe caller anywhere in the codebase make a brand-new caller automatically safe too?",
        "answer": "Yes, and specifically because of what this lesson names: `process_transfer`'s own code has no validation, so its safety was always contingent on every caller independently getting it right. A CSV upload is exactly the kind of untrusted-input source that needs the same checks the original API route had — positive amount, sufficient balance, distinct accounts — and 'confirms it parses as a number' is a much weaker bar than 'confirms it's a valid transfer amount.' This is the trust boundary silently moving: a new, less-careful caller now reaches a function whose safety was never actually its own, and nothing about the diff itself flags that the assumption is being relied on again in a new place."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC3-006"
      },
      {
        "type": "takeaway",
        "body": "Every new call site a diff adds to an existing internal function is a moment the trust boundary could silently shift — check explicitly, because a tool generating the new caller has no visibility into an assumption the old code was quietly relying on."
      }
    ]
  },
  {
    "id": "aod-rc3-l04",
    "moduleId": "aod-rc3",
    "order": 4,
    "duration": 20,
    "difficulty": "medium",
    "title": "Types that lie, and types that only describe the happy case",
    "description": "A type annotation is a claim the compiler enforces at compile time, about values the compiler never actually sees running through your program at runtime — and those two things can quietly diverge.",
    "learningObjectives": [
      {
        "text": "Identify a point where a type annotation stops being enforced — a type assertion, an external boundary, a parsed-but-unvalidated payload",
        "bloomLevel": "analyze"
      },
      {
        "text": "Distinguish a type that's actually verified at runtime from one that's merely asserted or assumed",
        "bloomLevel": "analyze"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "A type assertion is a promise, not a check",
        "podNumber": 1,
        "duration": 10
      },
      {
        "type": "hook",
        "body": "```typescript\nasync function getUser(id: string): Promise<User> {\n  const response = await fetch(`/api/users/${id}`);\n  const data = await response.json();\n  return data as User;\n}\n```\n\n`data as User` is a TypeScript type assertion — it tells the compiler \"trust me, this is a `User`,\" and the compiler believes it, at compile time, with zero runtime consequence. `response.json()` returns `any`; TypeScript has genuinely no way to know what shape the actual response body has. If the API returns `{\"error\": \"not found\"}` instead of a user object — a completely normal thing for an API to do — `data as User` happily lies about it, and every line of code downstream that trusts `user.email` being a string will find out the hard way, at runtime, with a type system that promised this could never happen."
      },
      {
        "type": "text",
        "body": "TypeScript's type system, and every gradually- or statically-typed language's type system operating at a network or file-system boundary, has a specific limit worth naming precisely: it can only reason about values the compiler can trace, at compile time, through code it controls. The moment a value comes from outside that — a network response, a file read, a database query, user input — the type system is being told what shape to assume, not verifying that shape is actually there. `as User`, a type parameter on a fetch call, a `JSON.parse()` result cast to an interface — all of these are the type system taking your word for it, not checking.\n\nThis creates a specific reading skill: distinguish a type that's been *verified* — actually checked at runtime, by a parsing library that validates shape and rejects what doesn't match, by an explicit set of `if` checks — from a type that's merely *asserted*, where a value is declared to be a certain shape with nothing behind the declaration except the hope that it's true. Code that reads as fully type-safe, with types everywhere, can still have every single one of those types resting on an unverified assertion at the one place data actually entered the system from outside.\n\nThis is a common pattern in AI-generated integration code specifically: a model asked to fetch and use data from an API will often produce clean, fully-typed code for everything *after* the fetch, while the fetch itself returns an untyped or `any` value that gets cast once, early, and never actually checked — the types look complete and rigorous while resting on nothing."
      },
      {
        "type": "diagnose-mechanism",
        "scenario": "The `getUser` function from the hook is called from a profile page. In production, a downstream service occasionally times out and the `/api/users/:id` endpoint, on that specific failure, returns `{\"error\": \"upstream timeout\"}` with a 200 status instead of an error code (a real, common misconfiguration). The profile page crashes with `Cannot read properties of undefined (reading 'toUpperCase')`.",
        "question": "What's the actual mechanism connecting the type assertion to this crash?",
        "options": [
          {
            "value": "assertion-hides-shape-mismatch",
            "label": "`data as User` told the compiler the response was a `User` without checking, so `data.name` — actually `undefined`, since the real payload was `{error: ...}`, not a user — passed the type checker with no complaint, and the crash only happened later, wherever the code called `.toUpperCase()` on what it believed was a guaranteed string"
          },
          {
            "value": "network-issue-unrelated",
            "label": "This is purely a network reliability issue, unrelated to the type assertion — better retry logic would have been the real fix"
          },
          {
            "value": "should-use-any",
            "label": "The function should have typed `data` as `any` instead of asserting `User`, which would have prevented the crash"
          }
        ],
        "correctValue": "assertion-hides-shape-mismatch",
        "feedback": {
          "assertion-hides-shape-mismatch": "Right. The type assertion is exactly what let a shape mismatch travel silently from the fetch call to wherever it eventually crashed — potentially far from `getUser` itself, in whatever component first tried to use a field the actual response never had. TypeScript's type checker had no chance to catch this, because the assertion told it there was nothing to check.",
          "network-issue-unrelated": "The upstream timeout is the trigger, but the reason it turned into a crash rather than a handled error is specifically the unchecked type assertion — a version that validated the response shape before trusting it would have caught the mismatch immediately, at the fetch call, with a clear error instead of a downstream crash on an unrelated line.",
          "should-use-any": "Typing `data` as `any` would remove the type checker's (already limited) ability to catch anything at all in the code that uses it — it doesn't fix the underlying problem, which is that nothing runtime-checks the actual shape of the response; it just stops pretending there's a check."
        },
        "mechanism": "A type assertion moves the point of failure from 'right where the untrustworthy data entered the system' (which would be an easy, precise place to catch and handle a shape mismatch) to 'wherever downstream code first touches the specific field that turned out to be missing' (which can be arbitrarily far away, in a completely unrelated file, with an error message that gives no hint the real problem was an unvalidated API response several layers up)."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC3-007"
      },
      {
        "type": "takeaway",
        "body": "A type assertion at a network or file boundary is a claim the compiler trusts and never checks — the failure it enables shows up downstream, wherever code first touches the field that turned out not to exist, far from where the actual, unchecked data entered."
      },
      {
        "type": "pod-header",
        "title": "Types that describe only the case someone tested",
        "podNumber": 2,
        "duration": 10
      },
      {
        "type": "text",
        "body": "A second, subtler version of the same risk: a type that's technically accurate for the data it was written against, but describes only the happy-path shape, because whoever wrote it — human or model — was working from one example response rather than the full range of shapes the real system actually produces. An interface with `email: string` is a completely reasonable type to write while looking at a handful of successful signups, and it's still wrong the day a user signs up via a legacy import path that leaves `email` genuinely `null`.\n\nThe way to catch this while reading: ask where the type definition actually came from. A type inferred from one sample JSON response, or hand-written to match the fields visible in a single test fixture, has a real chance of being narrower than the domain it's meant to describe. A type that comes from an API's own published schema, or that's been deliberately widened with optional fields and unions based on documented edge cases, is a stronger claim — though even those are worth spot-checking against a genuinely messy real record if one exists."
      },
      {
        "type": "predict-number",
        "scenario": "A TypeScript interface, generated by inferring types from one example API response the model was shown:\n\n```typescript\ninterface Order {\n  id: string;\n  total: number;\n  discountCode: string;\n}\n```\n\nThe real API, sampled across a large batch of actual production orders, sometimes omits `discountCode` entirely for orders where no code was used, rather than sending an empty string.",
        "question": "Out of 1,000 real production orders sampled, roughly what percentage would you guess have no `discountCode` field at all — enough to make `discountCode: string` (rather than `discountCode?: string`) a meaningfully wrong type, not just a rare edge case?",
        "unit": "%",
        "actualValue": 70,
        "explanation": "Most orders, in most real e-commerce systems, don't use a discount code at all — code usage is typically the minority case, not the majority, so a plausible real figure is well over half, easily 70% or more depending on the business. The exact number matters less than the shape of the mistake: a type inferred from one example that happened to include a discount code makes the *common* case — no code used — the one the type doesn't actually describe correctly. Every line of code written against `order.discountCode: string` will assume a string is always there, and the majority of real orders will violate that assumption, not a rare edge case a team could reasonably deprioritize."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC3-008"
      },
      {
        "type": "takeaway",
        "body": "A type inferred from one example response describes that response, not necessarily the domain — ask where a type definition actually came from, and treat one sampled the way a single trace is treated: informative, not comprehensive."
      }
    ]
  },
  {
    "id": "aod-rc3-l05",
    "moduleId": "aod-rc3",
    "order": 5,
    "duration": 20,
    "difficulty": "medium",
    "title": "Serialisation, encoding, and the data that changes shape in transit",
    "description": "A value doesn't have to be wrong to change meaning — converting it to text and back can quietly lose precision, order, or type in ways that are only visible if you check both ends against each other.",
    "learningObjectives": [
      {
        "text": "Identify a value whose precision, type, or ordering is at risk of changing across a serialization boundary",
        "bloomLevel": "analyze"
      },
      {
        "text": "Trace a specific value through a serialize/deserialize round trip and predict where it diverges from the original",
        "bloomLevel": "apply"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "JSON has fewer number types than your code does",
        "podNumber": 1,
        "duration": 10
      },
      {
        "type": "hook",
        "body": "```python\nfrom decimal import Decimal\nimport json\n\nprice = Decimal('19.99')\npayload = json.dumps({'price': price})\n```\n\nThis raises `TypeError: Object of type Decimal is not JSON serializable` — Python's `json` module has no idea what a `Decimal` is. A generated fix, entirely plausible-looking, might convert it: `json.dumps({'price': float(price)})`. That runs without error. It also silently converts an exact decimal value into a binary floating-point approximation — `float(Decimal('19.99'))` is not exactly `19.99`, it's the nearest value a float can actually represent, which for many decimal fractions is a value that's off by a tiny amount that can compound across enough arithmetic to become a real, visible cent discrepancy in a financial report."
      },
      {
        "type": "text",
        "body": "Serialization — converting an in-memory value into text (JSON, XML, CSV, a query string) to send it somewhere, and deserialization — converting it back — is a boundary where a value's *meaning* can change even though something that looks like 'the same value' comes out the other side. JSON specifically has exactly one numeric type, and it's not decimal-exact; representing money, exact scientific measurements, or any value where precision genuinely matters as a JSON number is inviting the float-approximation risk from the hook, silently, on every single request.\n\nDates and times have their own version of this: an ISO-8601 string round-trips cleanly only if both ends agree on time zone handling, and a value serialized without an explicit offset (`2024-03-15T09:00:00` versus `2024-03-15T09:00:00Z` versus `2024-03-15T09:00:00-05:00`) can be silently reinterpreted in a different time zone by whatever deserializes it, producing a value hours off from the original with no error anywhere in the process — both ends behaved exactly as documented, and the meaning still changed.\n\nEven something as basic as key order or duplicate keys can carry meaning that a round trip discards: a JSON object with a duplicate key is technically permitted by the spec, and different parsers resolve the duplicate differently (first wins, or last wins) — a value that means one thing when serialized by one library can mean something else when deserialized by another, purely because of which implementation's convention won."
      },
      {
        "type": "worked-example",
        "problem": "Trace what happens to this value across a generated serialize/deserialize round trip.\n\n```python\n# Service A serializes:\norder_total = Decimal('100.10')\npayload = json.dumps({'total': float(order_total)})\n# payload is sent over the network as text, then...\n\n# Service B deserializes:\ndata = json.loads(payload)\nreceived_total = data['total']\n```",
        "steps": [
          {
            "label": "Convert Decimal to float",
            "work": "`float(Decimal('100.10'))` — binary floating point cannot represent `100.10` exactly, since `0.10` has no exact finite binary fraction, the same way `1/3` has no exact finite decimal. The actual stored value is something microscopically off, like `100.09999999999999`.",
            "why": "This is the exact moment the precision loss happens — before the network is even involved. The Decimal was exact; the float is an approximation, and JSON only knows how to carry the float."
          },
          {
            "label": "Serialize to JSON text",
            "work": "`json.dumps` writes out a text representation of that float — Python's JSON encoder is usually smart enough to print `100.1` rather than the ugly internal representation, which can mask the fact that precision was already lost a step earlier.",
            "why": "The text `\"100.1\"` looks completely clean and correct — nothing about the payload itself signals that a lossy conversion already happened before this point."
          },
          {
            "label": "Deserialize on the receiving end",
            "work": "`json.loads` parses `\"100.1\"` back into a Python float, `100.1` (or its nearest binary approximation) — a float again, not a Decimal, and Service B's code now has to remember to convert it back to `Decimal` explicitly if it needs exact decimal arithmetic.",
            "why": "Even in the best case, where the specific value happens to round-trip losslessly, the *type* information (this was originally a Decimal, meant to be handled with exact arithmetic) is gone — Service B has no way to know from the payload alone that it shouldn't do further float arithmetic on this value."
          }
        ],
        "faded": {
          "problem": "Trace this generated datetime serialization for a meeting scheduled at 9am in New York.\n\n```python\nmeeting_time = datetime(2024, 3, 15, 9, 0, 0)  # naive, no timezone attached\npayload = json.dumps({'starts_at': meeting_time.isoformat()})\n# starts_at in the payload: \"2024-03-15T09:00:00\"\n```\n\nA client in London deserializes `\"2024-03-15T09:00:00\"` and, per its own application's convention, interprets any timestamp with no offset as UTC.",
          "blanks": [
            {
              "prompt": "What time does the London client display to its user, interpreting the string as UTC?",
              "answer": "9:00 AM UTC, which the London client (in GMT, same as UTC in March before British Summer Time starts) would display as 9:00 AM local London time.",
              "accept": [
                "9am utc",
                "9:00 utc"
              ],
              "why": "The string carries no timezone information at all, so any receiver is free to apply its own convention for interpreting it — nothing in the payload constrains that choice."
            },
            {
              "prompt": "Was the meeting actually scheduled for 9am UTC, or 9am New York time? What's the actual gap in hours between what was meant and what London displays?",
              "answer": "It was 9am New York (Eastern) time, which in March is UTC-4 or UTC-5 depending on daylight saving — so the real UTC time was 1-2 PM, not 9 AM. London's display of 9:00 AM is 4-5 hours off from the meeting's actual UTC time.",
              "accept": [
                "4-5 hours off",
                "4 to 5 hours"
              ],
              "why": "This is the mechanism, made concrete: a naive datetime with no offset attached made a completely reasonable-looking round trip through serialization and deserialization, and came out meaning something 4-5 hours different from what was intended — with no error, no exception, and a string that looks perfectly well-formed at every step."
            }
          ]
        },
        "procedure": "Trace a specific value's type and precision at each step of a serialize/deserialize round trip: what it was before, what representation it's converted to, what comes out the other side. A value that looks unchanged as text can have silently lost precision, type information, or timezone context — and the only way to catch it is checking the actual bits, not the string's apparent correctness."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC3-009"
      },
      {
        "type": "takeaway",
        "body": "A value that looks correct as serialized text can have silently lost precision, type, or timezone context in the conversion — trace what a value actually was before serialization and what it actually is after deserialization, not just whether the text looks plausible."
      },
      {
        "type": "pod-header",
        "title": "The boundary that matters most is the one you don't control",
        "podNumber": 2,
        "duration": 10
      },
      {
        "type": "text",
        "body": "The riskiest version of this class of bug is a serialization boundary between two systems maintained by different teams, or a system you're integrating with rather than one you wrote — because you can verify your own serialize step and your own deserialize step by reading both, but you can't always verify what a third-party API's serializer actually did before the data reached you, only what you receive.\n\nThe practical habit closing out this module: when reading code that consumes an external API's response, ask specifically what type and precision guarantees that API's documentation actually makes — not what shape a single sample response happened to have. An API that returns prices as JSON numbers, with no stated precision guarantee, is handing you a float-shaped risk regardless of how clean the sample response looked; an API that deliberately returns prices as strings (`\"19.99\"` rather than `19.99`) is very often doing that specifically to sidestep the float-precision problem, and code that 'helpfully' converts that string to a float on the way in is quietly reintroducing the exact risk the API's own design was trying to avoid."
      },
      {
        "type": "interactive-guess",
        "question": "A payments API's documentation states: \"Amounts are always returned as strings, e.g. `\\\"19.99\\\"`, to preserve exact decimal precision.\" A generated integration includes this line: `amount = float(response['amount'])`. Is this line likely to introduce the exact risk the API's design was trying to prevent?",
        "hint": "What does converting a precision-preserving string back into a float actually do to the precision guarantee the API went out of its way to provide?",
        "answer": "Yes. The API deliberately serialized amounts as strings specifically to avoid the binary floating-point imprecision that a JSON number would introduce — and `float(response['amount'])` throws that protection away in one line, converting a precision-preserving `\"19.99\"` string into exactly the imprecise binary approximation the API's design was built to prevent. This is a specific, common failure mode in generated integration code: a model reaching for the intuitive-looking conversion (\"this is obviously a number, so convert it to a number type\") without registering that the API chose a string representation on purpose, for a stated reason, that the conversion directly undoes."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC3-010"
      },
      {
        "type": "takeaway",
        "body": "When an external API makes a deliberate, documented choice about how it represents a value — a string instead of a number, specifically to preserve precision — check whether the integration code respects that choice or quietly reverses it on the way in."
      }
    ]
  },
  {
    "id": "aod-rc3-p01",
    "moduleId": "aod-rc3",
    "order": 6,
    "duration": 30,
    "difficulty": "medium",
    "title": "Practice — Find the Unchecked Assumption in Eight Boundary Crossings",
    "description": "Eight drills, no new concepts: unstated input beliefs, null/empty/missing/zero, trust boundaries, unverified types, and serialization round trips.",
    "learningObjectives": [
      {
        "text": "Apply boundary-tracing and assumption-naming across unfamiliar data-handling code at speed",
        "bloomLevel": "apply"
      },
      {
        "text": "Identify the specific input or conversion that exposes a shape bug the code passes for on typical data",
        "bloomLevel": "analyze"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "Eight drills",
        "podNumber": 1,
        "duration": 30
      },
      {
        "type": "hook",
        "body": "Nothing new here. Eight drills across the module's skills, ordered from where the lessons ended to one step past it. Do them in one sitting or spread across a few — nothing here expires."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC3-P01"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC3-P02"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC3-P03"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC3-P04"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC3-P05"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC3-P06"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC3-P07"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC3-P08"
      },
      {
        "type": "takeaway",
        "body": "If a drill needed a hint, that's what to watch for in the checkpoint — not a reason to redo the set."
      }
    ]
  },
  {
    "id": "aod-rc3-lab",
    "moduleId": "aod-rc3",
    "order": 7,
    "duration": 50,
    "difficulty": "hard",
    "title": "Lab — Harden the Boundary of a Generated API Handler",
    "description": "Take a real handler that trusts its input, and produce both a hardened version and the specific list of inputs that used to slip through unnoticed.",
    "learningObjectives": [
      {
        "text": "Apply unstated-belief analysis, null/empty/missing distinctions, and boundary tracing to a real API handler",
        "bloomLevel": "apply"
      },
      {
        "text": "Produce a validated handler and a documented list of previously-accepted bad inputs, each with what it used to do",
        "bloomLevel": "create"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "The lab",
        "podNumber": 1,
        "duration": 50
      },
      {
        "type": "hook",
        "body": "Every lesson this module worked one boundary risk at a time. A real handler needs all of them checked together: what it assumes about its input's shape, whether it conflates null/empty/missing/zero, whether it's the actual trust boundary or is quietly relying on something else to have validated first, whether its type claims are checked or merely asserted, and whether anything crossing a serialization boundary loses precision on the way in. This lab is that, against one real handler, with the deliverable being both the fix and the specific evidence for why it was needed."
      },
      {
        "type": "lab-brief",
        "labId": "aod-rc3-harden-boundary",
        "title": "Harden a boundary handler",
        "brief": "Pick a real API handler, webhook receiver, or file/CSV import function — from your own project, an AI-generated one you haven't hardened yet, or one you generate now for a plausible task (a signup form handler, a payment webhook, a bulk-upload endpoint). It needs to read data from outside a trust boundary — a request body, a file, an external API response. Find the unchecked assumptions, then fix them.",
        "deliverable": "A hardened version of the handler with explicit validation added at the actual trust boundary, plus a list of at least four specific inputs that would have been silently accepted (or crashed unhelpfully) by the original version, each with what the original actually did and what the hardened version does instead.",
        "stages": [
          {
            "title": "Read for unstated beliefs",
            "minutes": 12,
            "instructions": "Read the handler and list every unstated belief it makes about its input's shape — missing keys, null-vs-empty-vs-missing-vs-zero conflations, and any type claims (declared or asserted) that aren't actually runtime-checked.",
            "checkpoint": "You have a list of specific unstated beliefs, each tied to a specific line of the handler."
          },
          {
            "title": "Trace the failing inputs",
            "minutes": 13,
            "instructions": "For each belief on your list, construct a concrete input that violates it and trace, by hand, exactly what the current code does — a loud crash, or a quiet, plausible-looking wrong result. Note which is which.",
            "checkpoint": "Every listed belief has a concrete violating input and a traced, specific outcome — not a guess at what 'probably' happens."
          },
          {
            "title": "Check the trust boundary itself",
            "minutes": 10,
            "instructions": "Confirm this handler actually is the trust boundary — the first point where this data is checked — rather than a function that's implicitly relying on something upstream. If it calls into other internal functions with the same unvalidated data, note whether those functions have any validation of their own.",
            "checkpoint": "You've stated explicitly where the real trust boundary is, and whether this handler is it."
          },
          {
            "title": "Write the hardened version and the evidence list",
            "minutes": 15,
            "instructions": "Add explicit validation at the real boundary for every belief you found. Then write the deliverable: the hardened code, plus the list of specific bad inputs with before/after behavior. Self-check against the rubric.",
            "checkpoint": "The handler is hardened, the evidence list has at least four concrete entries, and each shows a real behavior change from before to after."
          }
        ],
        "rubric": [
          {
            "criterion": "At least four distinct unstated-belief violations are documented",
            "meets": "Each entry names a specific concrete input, what the original code actually did with it, and what the hardened version does instead."
          },
          {
            "criterion": "At least one entry addresses a quiet, plausible-looking failure, not just a loud crash",
            "meets": "A crash is the easier failure to find; the lab specifically wants at least one case where the original code returned a wrong-but-plausible value with no error."
          },
          {
            "criterion": "The null/empty/missing/zero distinction is addressed if the handler has any field where it plausibly applies",
            "meets": "If a relevant field exists, the hardened version explicitly distinguishes the states that need distinguishing rather than using a bare truthiness check."
          },
          {
            "criterion": "The trust boundary question is answered explicitly",
            "meets": "The write-up states plainly whether this handler is the real boundary or is relying on an upstream or downstream check, with reasoning."
          },
          {
            "criterion": "The hardened version fails loudly and specifically, not silently, for every input on the evidence list",
            "meets": "Each previously-silent bad input now produces a clear, specific error or rejection rather than either crashing unhelpfully or continuing with bad data."
          }
        ]
      },
      {
        "type": "takeaway",
        "body": "The list of inputs that used to get through is the actual evidence that hardening did something — a fix with no documented list of what it fixed is a claim as unverifiable as 'I read through it and it looked fine.'"
      }
    ]
  },
  {
    "id": "aod-rc3-cp",
    "moduleId": "aod-rc3",
    "order": 8,
    "duration": 10,
    "difficulty": "easy",
    "title": "Checkpoint — Data Shapes and Boundaries",
    "description": "Ten questions: four from this module, four from Control Flow and State, two from Reading for Intent. Ungraded, untimed.",
    "learningObjectives": [
      {
        "text": "Retrieve this module's core distinctions alongside earlier modules', without looking them up",
        "bloomLevel": "remember"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "Let's see what stuck",
        "podNumber": 1,
        "duration": 10
      },
      {
        "type": "retrieval-check",
        "title": "Checkpoint — Data Shapes and Boundaries",
        "questions": [
          {
            "from": "This module — Data Shapes and Boundaries",
            "prompt": "A function's type signature says `list[dict]`. What does that guarantee about the list's contents?",
            "options": [
              {
                "value": "a1",
                "label": "Very little beyond the outer container and element type — nothing about non-emptiness, required keys, or value ranges, which is where a function's real, narrower beliefs live"
              },
              {
                "value": "a2",
                "label": "That every dict has the same set of keys"
              },
              {
                "value": "a3",
                "label": "That the list has at least one element"
              }
            ],
            "correctValue": "a1",
            "explanation": "`apply_bulk_discount` showed this precisely: the type hint said nothing about needing every item to share the same price, which is exactly what the function's logic silently assumed."
          },
          {
            "from": "This module — Data Shapes and Boundaries",
            "prompt": "Why is `if not order.get('discount_pct'):` risky as a check for 'no discount was calculated'?",
            "options": [
              {
                "value": "b1",
                "label": "A discount of exactly 0% is a legitimate, meaningful value that's falsy, so it gets silently treated the same as 'never calculated'"
              },
              {
                "value": "b2",
                "label": "`.get()` always raises an error on a missing key"
              },
              {
                "value": "b3",
                "label": "Percentages should never be stored as numbers"
              }
            ],
            "correctValue": "b1",
            "explanation": "This is the null/empty/missing/zero distinction — a real, valid zero value gets conflated with a genuinely different 'not set' state by a bare truthiness check."
          },
          {
            "from": "This module — Data Shapes and Boundaries",
            "prompt": "A sensitive function has no internal validation and one currently-validated caller. What does this module say about its actual safety?",
            "options": [
              {
                "value": "c1",
                "label": "Its safety is entirely borrowed from that one caller, and it will remain safe only as long as every future caller independently re-implements the same checks"
              },
              {
                "value": "c2",
                "label": "It's safe permanently, since the current caller was reviewed and approved"
              },
              {
                "value": "c3",
                "label": "It doesn't matter, since validation should always happen deep inside business logic, not at entry points"
              }
            ],
            "correctValue": "c1",
            "explanation": "The `process_transfer` case-sim showed this directly — a second caller (the reconciliation job, then the CSV upload) reached the same unvalidated function without the original route's checks."
          },
          {
            "from": "This module — Data Shapes and Boundaries",
            "prompt": "Why does `data as User` (a TypeScript type assertion) provide no real protection against a malformed API response?",
            "options": [
              {
                "value": "d1",
                "label": "A type assertion is a compile-time-only instruction with zero runtime effect — nothing actually inspects the real value's shape"
              },
              {
                "value": "d2",
                "label": "TypeScript assertions are slower than runtime checks, but otherwise equivalent"
              },
              {
                "value": "d3",
                "label": "It works correctly as long as the API is generally reliable"
              }
            ],
            "correctValue": "d1",
            "explanation": "The `getUser` example traced this exactly — the assertion told the compiler to stop checking, and the resulting crash happened downstream, far from where the actual mismatch occurred."
          },
          {
            "from": "Previous module — Control Flow and State",
            "prompt": "What should a happy-path trace end in?",
            "options": [
              {
                "value": "e1",
                "label": "A specific, defensible value derived from concrete inputs, not a summary of what the code 'probably' does"
              },
              {
                "value": "e2",
                "label": "Confirmation that the function compiles without error"
              },
              {
                "value": "e3",
                "label": "A list of every branch the function contains"
              }
            ],
            "correctValue": "e1",
            "explanation": "Tracing means updating every variable's real value for a concrete input, arriving at a specific number or result you can defend with arithmetic."
          },
          {
            "from": "Previous module — Control Flow and State",
            "prompt": "A branch chain's final `else` is best understood as...",
            "options": [
              {
                "value": "f1",
                "label": "An implicit claim about every case not named above it — a claim that can go stale the moment the domain grows a new case"
              },
              {
                "value": "f2",
                "label": "Dead code that should generally be removed"
              },
              {
                "value": "f3",
                "label": "A performance optimization"
              }
            ],
            "correctValue": "f1",
            "explanation": "The customer-tier example showed an `else` correct for a three-tier system silently mishandling a fourth tier added later."
          },
          {
            "from": "Previous module — Control Flow and State",
            "prompt": "Why can a function be correct when traced by a single caller and still be wrong under shared mutable state?",
            "options": [
              {
                "value": "g1",
                "label": "The bug can live entirely in the gap between two overlapping callers' reads and writes — a gap a single-caller trace can't represent"
              },
              {
                "value": "g2",
                "label": "Shared state always runs slower than local state"
              },
              {
                "value": "g3",
                "label": "It can't — a function correct for one caller is always correct for multiple callers"
              }
            ],
            "correctValue": "g1",
            "explanation": "The rate-limiter example traced correctly for one caller every time — the bug only appears once two overlapping calls are deliberately interleaved."
          },
          {
            "from": "Previous module — Control Flow and State",
            "prompt": "A guard clause returns the same generic default for three different precondition failures. What's the risk?",
            "options": [
              {
                "value": "h1",
                "label": "The caller loses the ability to tell which precondition failed, even though the function's own logic knew at the moment each guard fired"
              },
              {
                "value": "h2",
                "label": "The function becomes slower to execute"
              },
              {
                "value": "h3",
                "label": "There's no risk as long as the function never throws an exception"
              }
            ],
            "correctValue": "h1",
            "explanation": "The `canCheckout` example collapsed three distinct, meaningful failure reasons into one indistinguishable `false`."
          },
          {
            "from": "Two modules back — Reading for Intent",
            "prompt": "What's the recommended reading order for an unfamiliar function's signature versus its body?",
            "options": [
              {
                "value": "i1",
                "label": "Signature, then every return, then the body — turning the body read into a check against a specific hypothesis"
              },
              {
                "value": "i2",
                "label": "Top to bottom, the order it's written in"
              },
              {
                "value": "i3",
                "label": "The body first, since the logic matters more than the interface"
              }
            ],
            "correctValue": "i1",
            "explanation": "Signature and returns carry disproportionate intent for very little reading time, turning the body into verification rather than open-ended discovery."
          },
          {
            "from": "Two modules back — Reading for Intent",
            "prompt": "What does the difficulty of compressing a function into one accurate sentence usually signal, once every attempt needs another unrelated 'and'?",
            "options": [
              {
                "value": "j1",
                "label": "The function is doing more than one job, stitched together, and should probably be split"
              },
              {
                "value": "j2",
                "label": "The description just needs to be allowed to run longer than one sentence"
              },
              {
                "value": "j3",
                "label": "The function's variable names are the actual problem"
              }
            ],
            "correctValue": "j1",
            "explanation": "The `handleSignup` example — validate, create, email, log, provision — resisted one-sentence compression because it genuinely was five separable responsibilities."
          }
        ]
      }
    ]
  },
  {
    "id": "aod-rc4-l01",
    "moduleId": "aod-rc4",
    "order": 1,
    "duration": 20,
    "difficulty": "easy",
    "title": "Why tools add libraries you didn't ask for",
    "description": "A model reaches for a familiar library the same way it reaches for a familiar variable name — because it's a common pattern in the code it learned from, not because it weighed the alternative of six lines of standard-library code.",
    "learningObjectives": [
      {
        "text": "Explain why a generated diff reaches for a dependency for a task that standard-library code could handle in a few lines",
        "bloomLevel": "understand"
      },
      {
        "text": "Recognize the specific shape of an unnecessary dependency addition in a diff",
        "bloomLevel": "apply"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "One function call, one new dependency",
        "podNumber": 1,
        "duration": 10
      },
      {
        "type": "hook",
        "body": "A ticket asks for a function that picks three fields off a user object. A generated diff:\n\n```javascript\nimport _ from 'lodash';\n\nfunction publicProfile(user) {\n  return _.pick(user, ['id', 'displayName', 'avatarUrl']);\n}\n```\n\nPlus a new line in `package.json`, plus lodash's own dependency tree, for a task that's three lines of plain JavaScript:\n\n```javascript\nfunction publicProfile(user) {\n  const { id, displayName, avatarUrl } = user;\n  return { id, displayName, avatarUrl };\n}\n```\n\nNeither version is wrong. But one of them added a new external package — with its own install size, its own update cadence, its own transitive dependencies — to solve a problem the language's own destructuring syntax already solves in three lines."
      },
      {
        "type": "text",
        "body": "A model generating code is pattern-completing from an enormous amount of real code it's seen, and `import _ from 'lodash'` followed by a `_.pick` call is an extremely common pattern in that training data — lodash has been one of the most-imported packages in the JavaScript ecosystem for years, used constantly, including for tasks the language itself has since grown a native way to do. The model isn't weighing 'dependency versus six lines of standard library' as a tradeoff the way a careful human reviewer would; it's producing the statistically familiar shape of code for this kind of task, and the familiar shape, for a lot of common utility work, happens to include a well-known library.\n\nThis matters because the choice to add a dependency is a real decision with real, ongoing cost — install size, attack surface, a version to keep updated, a license to track — and a model reaching for a library isn't making that tradeoff consciously any more than it's consciously deciding a variable should be named `result` versus `output`. It's the most likely next token given everything it's seen, and 'the most statistically common way to write this' and 'the way that's actually best for this specific project' are not guaranteed to be the same thing, especially for a project that cares about bundle size, or already has a policy against adding new dependencies for one-off utility functions.\n\nThe practical habit: when a diff adds a new dependency for a task that's genuinely small, ask what the standard library or the language's own newer features would take to do the same thing — not because dependencies are always wrong, but because the model didn't actually make that comparison, and someone needs to."
      },
      {
        "type": "concept-flow",
        "scenario": "Four generated diffs, each adding exactly one new npm dependency for one specific task.",
        "question": "Which of these is the clearest case of a dependency added for a task the language's own current features already handle well, with no real benefit from the package?",
        "options": [
          {
            "label": "`import isEqual from 'lodash.isequal'` to deep-compare two plain objects, where `structuredClone` and manual comparison, or even `JSON.stringify` equality for simple cases, were viable, well-understood native alternatives for this specific use",
            "value": "deep-equal-native-alternative"
          },
          {
            "label": "`import { z } from 'zod'` to validate the shape of an incoming webhook payload with several required and optional fields, nested objects, and specific string formats",
            "value": "zod-for-validation"
          },
          {
            "label": "`import dayjs from 'dayjs'` to handle timezone-aware date arithmetic across a dozen different call sites in a scheduling application",
            "value": "dayjs-for-scheduling"
          }
        ],
        "correctValue": "deep-equal-native-alternative",
        "feedback": {
          "deep-equal-native-alternative": "Right — a deep-equality check for plain objects is a small, well-scoped, one-purpose task that JavaScript's own tools (or even a short hand-written recursive function) cover reasonably well for most cases, without pulling in a new external dependency and its own maintenance burden for something this narrow.",
          "zod-for-validation": "Runtime schema validation with nested shapes, optional fields, and format checks is exactly the kind of task that's genuinely painful and error-prone to hand-roll correctly — a validation library earns its place here by doing something meaningfully harder than a few lines of manual `if` checks would reliably do.",
          "dayjs-for-scheduling": "Timezone-aware date arithmetic, used repeatedly across many call sites, is a classic case where JavaScript's native `Date` is genuinely difficult to use correctly and consistently — a well-chosen date library is solving a real, recurring, error-prone problem, not just avoiding six lines of code."
        },
        "concept": "The question isn't 'does this task have a library for it' — almost everything does. It's whether the task is genuinely hard or error-prone to do correctly by hand, repeated often enough to be worth abstracting, versus a small, one-off utility a few lines of plain language code would handle just as well, where the dependency is pure overhead the model added out of statistical habit rather than necessity."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC4-001"
      },
      {
        "type": "takeaway",
        "body": "A model reaches for a familiar library because it's statistically common in code like this, not because it weighed the dependency's cost against a few lines of standard-library code — that comparison is still someone's job."
      },
      {
        "type": "pod-header",
        "title": "The tell in the diff",
        "podNumber": 2,
        "duration": 10
      },
      {
        "type": "text",
        "body": "There's a specific, reliable tell for spotting an unnecessary dependency in a generated diff: a new import used exactly once, for a single function call, doing something the surrounding language already has a built-in or near-trivial way to express. A library genuinely earning its place in a project tends to show up in multiple places, or handle something with real edge-case complexity underneath a simple-looking call — date libraries handling timezone and daylight-saving edge cases, validation libraries handling nested optional fields, HTTP client libraries handling retries and cancellation.\n\nThe other tell worth watching for: a dependency added to solve a problem that's actually about *unfamiliarity* with a language feature that already shipped, rather than a genuine gap. JavaScript's `structuredClone`, optional chaining, nullish coalescing, and array methods like `.flatMap()` and `.at()` are all relatively recent enough that a lot of training data predates them, which means a model can reach for a small utility library to do something a current runtime's native syntax already covers cleanly — not because the library is wrong, but because the built-in alternative simply wasn't as common in what it learned from."
      },
      {
        "type": "spot-flaw",
        "code": "import { v4 as uuidv4 } from 'uuid';\nimport isEmpty from 'lodash.isempty';\n\nfunction createDraftOrder(items) {\n  if (isEmpty(items)) {\n    throw new Error('Cannot create an empty order');\n  }\n  return { id: uuidv4(), items, status: 'draft' };\n}",
        "question": "One of these two imports is well-justified; the other is a single-use import for something the language already handles natively. Which is which, and why?",
        "options": [
          {
            "value": "uuid-justified-isempty-not",
            "label": "`uuid` is justified — generating a cryptographically sound, correctly-formatted v4 UUID by hand is genuinely fiddly and easy to get subtly wrong. `lodash.isempty` is not — `items.length === 0` (for an array, which this clearly is) does the identical job in one native, obvious expression"
          },
          {
            "value": "both-justified",
            "label": "Both are justified — UUIDs and emptiness checks are both common enough operations to warrant a dedicated, well-tested library each"
          },
          {
            "value": "neither-justified",
            "label": "Neither is justified — both should be replaced with hand-written equivalents"
          }
        ],
        "correctValue": "uuid-justified-isempty-not",
        "feedback": {
          "uuid-justified-isempty-not": "Right. `uuid` is solving something with real, easy-to-get-wrong complexity underneath a simple call — correct entropy, correct version bits, correct formatting — where a hand-rolled version risks subtly non-random or malformed IDs. `isEmpty` on a known array is doing the job of `items.length === 0`, one native property check, with zero benefit from the abstraction and one more dependency (plus lodash's broader ecosystem baggage, even in its modular per-function form) for no real gain.",
          "both-justified": "Treating every utility call as equally justified misses the actual distinguishing question this lesson is built around: is the underlying task genuinely hard to get right by hand, or is the library just standing in for a native one-liner?",
          "neither-justified": "Correct UUID generation is exactly the kind of task worth pulling in a dedicated, audited library for — hand-rolling randomness and version-bit formatting correctly is a real source of subtle bugs, unlike checking whether an array's length is zero."
        },
        "flawExplanation": "The `isEmpty` import is the unjustified one: for a known array (`items`), `items.length === 0` is exactly as correct, more explicit about what's actually being checked, and removes a dependency whose only job here was standing in for a property access. `lodash.isempty`'s real value — handling emptiness checks uniformly across arrays, objects, strings, Maps, and Sets — isn't being used at all in a context where the input type is already known and fixed."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC4-002"
      },
      {
        "type": "takeaway",
        "body": "A single-use import doing something the language already expresses natively in one line is the clearest tell of an unnecessary dependency — check what the built-in alternative would actually cost before accepting the library."
      }
    ]
  },
  {
    "id": "aod-rc4-l02",
    "moduleId": "aod-rc4",
    "order": 2,
    "duration": 25,
    "difficulty": "medium",
    "title": "Reading a package before you install it",
    "description": "A package's README tells you what it claims to do. Its download count, publish history, maintainer count, and dependency tree tell you what happens if that claim turns out to matter less than whether it's still cared for.",
    "learningObjectives": [
      {
        "text": "Identify the specific, checkable signals that predict a package's ongoing reliability and maintenance, beyond its README",
        "bloomLevel": "evaluate"
      },
      {
        "text": "Distinguish a package worth trusting for a given task from one that merely looks polished",
        "bloomLevel": "evaluate"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "The README is marketing. The registry page is evidence.",
        "podNumber": 1,
        "duration": 13
      },
      {
        "type": "hook",
        "body": "A generated diff adds `import { parseCSV } from 'csv-magic'` for a one-time data-import script. The README, if you were to look, reads well: clean examples, a friendly tone, a badge claiming 100% test coverage. None of that tells you what actually matters before you run `npm install` and let this package's code execute in your project: when was it last published, how many other packages does it pull in transitively, is there more than one person who could plausibly fix a bug in it, and how many people are actually depending on it in production right now, which is a rough proxy for how many eyes would notice if something in it broke or turned malicious."
      },
      {
        "type": "text",
        "body": "A README is written by the package's author to make a case for using it — it's fair to read, but it's not neutral evidence, the same way a function's docstring is a claim rather than a fact. The registry page (npm, PyPI, crates.io, whichever ecosystem) carries a different, more checkable kind of information, most of it visible in under a minute: **last publish date** (a package untouched for three years might be stable and finished, or might be abandoned — worth checking which by skimming its issue tracker for anything alarming and unaddressed), **weekly download count** (a rough, imperfect proxy for how battle-tested it is against real-world usage, and how many other projects would notice and report a regression), **number of maintainers** (a project with one listed maintainer is one person's account compromise, one person's burnout, or one person's changed priorities away from a maintenance gap), and **transitive dependency count** (every dependency this package pulls in is a dependency your project now has too, with its own maintenance status you didn't individually check).\n\nNone of these signals is decisive alone. A single-maintainer package can be perfectly fine for years; a heavily-downloaded package can still have a supply-chain incident. The habit is to actually look at more than one signal together before installing, rather than judging a package purely by whether its README reads well and its API looks clean — polish in documentation and reliability in maintenance are produced by different things and don't reliably correlate."
      },
      {
        "type": "evidence-stack",
        "scenario": "You're evaluating `csv-magic` before adding it for a one-time import script. Its registry page shows: last published 3 years ago, 4,200 weekly downloads, one listed maintainer, 14 transitive dependencies, and an open issue from 18 months ago titled 'crashes on files with BOM' with no maintainer reply.",
        "question": "Select every signal here that's genuinely relevant to whether you should trust this specific package for this specific task.",
        "items": [
          {
            "value": "stale-unaddressed-issue",
            "label": "The unaddressed, 18-month-old bug report — a specific, concrete signal that a real, plausible failure mode (BOM handling, common in real-world CSV files) exists and nobody's fixing it",
            "applicable": true
          },
          {
            "value": "single-maintainer",
            "label": "One listed maintainer, combined with the stale publish date — together they suggest this package may no longer be actively watched, which matters if you hit a bug or a security issue",
            "applicable": true
          },
          {
            "value": "download-count-alone",
            "label": "4,200 weekly downloads in isolation, without comparing it to any alternative package's download count",
            "applicable": false
          },
          {
            "value": "transitive-deps-for-oneoff",
            "label": "14 transitive dependencies, for a script that will run once, by you, on your own machine, and never again",
            "applicable": false
          }
        ],
        "explanation": {
          "stale-unaddressed-issue": "This is the single most concrete, task-relevant piece of evidence here — a specific, plausible failure mode, already reported, already unaddressed for over a year. For a CSV import task, BOM handling is a genuinely common real-world edge case, not an obscure one.",
          "single-maintainer": "Combined with a three-year-old last publish, this is a real signal about ongoing support, not a standalone red flag — a lot of genuinely fine packages have one maintainer and haven't needed a publish in years because they're simply done. It's the combination with the unaddressed issue that's the actual concern.",
          "download-count-alone": "4,200 weekly downloads means very little without a comparison point — is that high or low for this category of package? Without knowing what a typical CSV-parsing library's download count looks like, this number alone doesn't tell you much either way.",
          "transitive-deps-for-oneoff": "14 transitive dependencies matters a lot for a package that's going into a production application's long-term dependency tree — it matters far less for a script you're going to run once, locally, and delete. Context about how the package will actually be used changes whether a real signal is actually relevant here."
        },
        "synthesis": "The signals that matter are the ones connected to the actual, specific risk for this specific use — a concrete unaddressed bug that's plausible for your data is real evidence; a raw download number with no comparison point isn't; and a signal's weight changes with context, like transitive dependency count mattering far less for a disposable script than for a long-lived service."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC4-003"
      },
      {
        "type": "takeaway",
        "body": "A README is a claim made by the package's author. Publish recency, maintainer count, and unaddressed issues relevant to your actual use case are checkable evidence — read both, and weight the evidence by how relevant it is to your specific situation."
      },
      {
        "type": "pod-header",
        "title": "Bus factor and the single point of failure",
        "podNumber": 2,
        "duration": 12
      },
      {
        "type": "text",
        "body": "\"Bus factor\" — how many people would need to become unavailable before a project stalls — is worth naming explicitly for any dependency doing something load-bearing in a project meant to last. A package maintained by one person, however skilled and well-intentioned, is one account compromise, one burnout, one shift in priorities away from a maintenance gap nobody in your project controls or can predict. This isn't a criticism of solo maintainers, many of whom do excellent, careful work — it's a fact about risk distribution that's worth weighing against how load-bearing the package actually is in your system.\n\nThe practical calibration: a single-maintainer package doing something small, replaceable, and non-critical (a formatting utility, a one-off script dependency) carries low real risk even with a bus factor of one, because if it stalls, swapping it out is cheap. The same bus factor on a package your authentication flow, your payment processing, or your core data pipeline depends on is a meaningfully bigger risk, worth either a more heavily-maintained alternative or, at minimum, a deliberate note that this dependency's health is worth periodically checking."
      },
      {
        "type": "diagnose-mechanism",
        "scenario": "Two generated diffs on the same day. Diff A adds a single-maintainer package for pretty-printing debug output during local development only, never shipped to production. Diff B adds a different single-maintainer package for parsing and validating incoming webhook signatures — a security-critical step that runs on every production request.",
        "question": "Why does the same bus-factor risk (one maintainer) warrant very different levels of concern across these two diffs?",
        "options": [
          {
            "value": "criticality-differs",
            "label": "The risk from a stalled maintainer is proportional to how load-bearing and how hard-to-replace the dependency is — a dev-only formatting tool is cheap to swap or drop if it stalls, while an unmaintained webhook-signature validator sitting on the security-critical path of every production request is a much more consequential single point of failure"
          },
          {
            "value": "bus-factor-irrelevant",
            "label": "Bus factor doesn't actually matter for either — what matters is only whether the package currently works correctly"
          },
          {
            "value": "always-avoid-single-maintainer",
            "label": "Both diffs should be rejected outright, since single-maintainer packages should never be used for anything"
          }
        ],
        "correctValue": "criticality-differs",
        "feedback": {
          "criticality-differs": "Right. The same underlying risk factor — one person's availability — has a very different consequence depending on what the package is actually load-bearing for. A dev-only tool stalling is an inconvenience; a security-critical validation library stalling, with an undiscovered bug or a missed CVE patch, is a real production risk. Bus factor isn't a fixed verdict, it's an input that needs to be weighed against what the dependency is actually doing.",
          "bus-factor-irrelevant": "Whether a package currently works is a snapshot in time — bus factor is specifically about the risk that it *stops* being maintained, patched, or fixed going forward, which matters more the more critical and long-lived the thing depending on it is.",
          "always-avoid-single-maintainer": "This ignores that a huge amount of genuinely excellent, widely-used open-source software is maintained by one person — the point isn't to categorically avoid solo-maintained packages, it's to weigh that risk against how critical and hard-to-replace the specific use is."
        },
        "mechanism": "Bus factor is a probability of future disruption, not a verdict about current quality — and the actual cost of that disruption scales with how deeply embedded and how safety-critical the dependency is. The same input (one maintainer) produces a low-stakes risk for a swappable dev tool and a genuinely serious one for something sitting on a security-critical, hard-to-quickly-replace path."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC4-004"
      },
      {
        "type": "takeaway",
        "body": "Bus factor is a real signal, but its weight depends entirely on what the dependency is actually load-bearing for — the same single-maintainer risk is trivial for a swappable dev tool and serious for something on a critical, hard-to-replace path."
      }
    ]
  },
  {
    "id": "aod-rc4-l03",
    "moduleId": "aod-rc4",
    "order": 3,
    "duration": 20,
    "difficulty": "medium",
    "title": "Licences, and the ones that follow your code out the door",
    "description": "A permissive license lets you use a package and forget about it. A copyleft license is a condition attached to your own code, triggered by how you use theirs — and it doesn't announce itself in the README's code examples.",
    "learningObjectives": [
      {
        "text": "Distinguish permissive licenses from copyleft licenses by what obligation each places on code that depends on them",
        "bloomLevel": "evaluate"
      },
      {
        "text": "Identify a license whose obligations are triggered by the specific way a dependency is used in a given project",
        "bloomLevel": "evaluate"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "Two questions a license actually answers",
        "podNumber": 1,
        "duration": 10
      },
      {
        "type": "hook",
        "body": "A generated diff adds a well-regarded image-processing library to a proprietary SaaS product's backend. It works perfectly, passes every test, ships. Nobody on the team checked the license field in its `package.json` — `AGPL-3.0`. The AGPL's specific, unusual condition: if you run AGPL-licensed code as part of a service other people interact with over a network, you're obligated to make the *complete source code of your whole service* available to those users, not just the library itself. A perfectly working, well-tested feature just became a legal question for the company's lawyers, and the answer to 'should we have used this' was fully knowable before a single line was merged — it just needed someone to read one field."
      },
      {
        "type": "text",
        "body": "A software license answers two questions worth separating clearly: what are you allowed to *do* with this code, and what does using it *obligate you to do* with your own code as a result. Permissive licenses (MIT, Apache 2.0, BSD variants) answer the second question with essentially nothing — use it, modify it, ship it inside proprietary software, no obligation to share your own source in return, beyond keeping the license notice attached to the library itself. This is why permissive-licensed packages are the overwhelming majority of what shows up in most commercial codebases without anyone thinking twice.\n\nCopyleft licenses (GPL, LGPL, and especially AGPL) answer the second question very differently: using the code under certain conditions obligates you to release your own code — sometimes just the parts that directly link to the library, sometimes, in the AGPL's case, your whole running service — under the same license. The specific condition that triggers this obligation varies by license family and is exactly the kind of detail that doesn't show up in a code example or a README's quick-start section, only in the actual license text.\n\nThe practical habit: check the license field before merging a new dependency, not after — it's usually one line in a `package.json`, `pyproject.toml`, or the package's registry page, and it's the cheapest check in this entire module relative to how expensive getting it wrong can be for a company."
      },
      {
        "type": "concept-flow",
        "scenario": "Four generated diffs, each adding a package with a different license, to a proprietary, closed-source commercial product.",
        "question": "Which license triggers a real, concrete obligation on the rest of this proprietary product's source code, given that the product is a network service other people log into and use?",
        "options": [
          {
            "label": "`MIT` for a small string-formatting utility",
            "value": "mit-formatting"
          },
          {
            "label": "`AGPL-3.0` for a data-visualization component rendered directly in the product's web dashboard, which every user's browser loads and runs as part of using the service",
            "value": "agpl-dashboard"
          },
          {
            "label": "`Apache-2.0` for a logging library used internally",
            "value": "apache-logging"
          }
        ],
        "correctValue": "agpl-dashboard",
        "feedback": {
          "agpl-dashboard": "Right. The AGPL's defining, unusual clause specifically covers software made available as a network service — exactly this product's situation — and its condition is triggered by users interacting with the software over a network, not by whether the code is literally redistributed as a file. A component embedded in the dashboard every user's browser runs is squarely inside that condition.",
          "mit-formatting": "MIT places essentially no obligation on the code that uses it — it can be embedded in fully proprietary, closed-source software with nothing more required than keeping the MIT license notice attached to the library's own files.",
          "apache-logging": "Apache 2.0 is also permissive, similar in practical effect to MIT for this purpose (with some added patent-grant language) — using it in proprietary internal tooling carries no obligation to release any of your own code."
        },
        "concept": "The AGPL is specifically designed to close a gap the regular GPL doesn't cover: software run only as a network service, never literally distributed to end users as a copy of the code, was historically outside the GPL's trigger condition entirely — which the AGPL was written specifically to fix. It's the license most worth checking for explicitly in any product that's a service someone logs into, which describes most modern SaaS products, including most AI-assisted-development output."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC4-005"
      },
      {
        "type": "takeaway",
        "body": "Permissive licenses (MIT, Apache 2.0, BSD) place essentially no obligation on your own code. Copyleft licenses, especially AGPL, can — and the trigger condition is specific enough that it's worth actually reading, not assuming from a package's popularity or polish."
      },
      {
        "type": "pod-header",
        "title": "A model has no way to know your license policy",
        "podNumber": 2,
        "duration": 10
      },
      {
        "type": "text",
        "body": "A generated diff choosing between two functionally similar packages has no visibility into which one is licensed permissively and which isn't, unless it's specifically asked to check — and even if it were shown both licenses, it has no way to know your company's actual policy (many companies flatly disallow GPL-family dependencies in proprietary products regardless of the specific trigger condition, as a simplifying, risk-averse rule). This is a decision that requires context the generation step structurally doesn't have: your product's business model, whether it's open-source itself, and your legal team's actual risk tolerance.\n\nThe practical review habit closing this lesson: treat a new dependency's license field as a required line item to check on every diff that adds one, the same reflexive way you'd check for a hardcoded secret — not because most dependencies turn out to be a problem (most are permissively licensed and entirely fine), but because the ones that aren't are expensive enough, and invisible enough in a typical code review, that skipping the check is a bad trade even at low odds."
      },
      {
        "type": "predict-number",
        "scenario": "A study of the most popular npm packages found the overwhelming majority use permissive licenses (MIT, ISC, Apache-2.0, BSD), with copyleft-family licenses (GPL, LGPL, AGPL) making up a small minority.",
        "question": "Out of the top 1,000 most-downloaded npm packages, roughly what percentage would you guess use a copyleft license (GPL/LGPL/AGPL family) rather than a permissive one?",
        "unit": "%",
        "actualValue": 3,
        "explanation": "The real figure is small — commonly cited estimates put copyleft-licensed packages in the low single digits among the most popular npm packages, with the overwhelming majority permissively licensed. The point of the exercise isn't the exact number — it's that a low base rate is exactly why the check gets skipped by habit ('it's probably fine, most packages are') and exactly why skipping it is still a bad trade: a rare event with a large cost, that takes seconds to check for, doesn't stop being worth checking just because it's rare. The AGPL invoice-processing scenario from the hook is a low-probability, high-cost event — precisely the shape of risk a cheap, reflexive check is designed to catch."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC4-006"
      },
      {
        "type": "takeaway",
        "body": "Most packages are permissively licensed, which is exactly why the license check gets skipped out of habit — and exactly why a rare, low-cost check against a rare, high-cost outcome is still worth doing on every new dependency, not just the ones that feel risky."
      }
    ]
  },
  {
    "id": "aod-rc4-l04",
    "moduleId": "aod-rc4",
    "order": 4,
    "duration": 25,
    "difficulty": "medium",
    "title": "Packages that don't exist: hallucinated imports and the squatters waiting for them",
    "description": "A model can generate a plausible-sounding package name for a real need, with a confident import statement and realistic-looking usage — and nothing about how fluent it looks tells you whether the package is real.",
    "learningObjectives": [
      {
        "text": "Explain the mechanism by which a model produces a confident, plausible-looking import for a package that doesn't exist",
        "bloomLevel": "understand"
      },
      {
        "text": "Identify the specific security risk created when a hallucinated package name is later published by someone else",
        "bloomLevel": "analyze"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "A plausible name is not a real package",
        "podNumber": 1,
        "duration": 13
      },
      {
        "type": "hook",
        "body": "A generated diff, asked to add retry logic with exponential backoff to an HTTP client:\n\n```python\nfrom requests_retry_toolkit import RetryingSession\n\nsession = RetryingSession(max_retries=3, backoff_factor=2)\n```\n\n`requests_retry_toolkit` sounds exactly like a real package — it's named the way real packages in this space are named, the API reads like a real API, and it's used with total confidence, no hedging, no comment saying 'verify this exists.' It doesn't exist. Running `pip install requests_retry_toolkit` today either fails outright, or — and this is the actual danger — succeeds, because someone registered that exact name after noticing models kept hallucinating it, and published something under it."
      },
      {
        "type": "text",
        "body": "A model generating an import statement is doing the same thing it does generating a function name or a docstring: producing the most statistically plausible continuation, given the surrounding context, of what a package for this task would probably be called. For extremely common, real packages, this works fine — the model has seen `import requests` and `import numpy` so many times that it reliably reproduces them correctly. For less common needs, or oddly specific combinations, the model can produce a name that fits the *pattern* of real package names in that ecosystem perfectly, with fluent, confident usage code around it, without that specific package actually existing anywhere — the same fluency-without-correctness gap the earlier module on reading generated code for intent identified in comments and docstrings, here applied to an import statement instead.\n\nThis is a known, named phenomenon in AI-assisted development, sometimes called *package hallucination* or, once the security angle is included, *slopsquatting* — a play on 'typosquatting,' the older, well-known attack where someone registers `reqeusts` (a common typo of `requests`) hoping for accidental installs. Slopsquatting is the same idea with a different source of the mistake: instead of hoping for a human's typo, an attacker registers a name that AI tools have been observed hallucinating repeatedly, and waits for someone to run the generated install command without checking first.\n\nThe practical stakes: a package name that doesn't exist yet is not automatically safe just because installing it currently fails. It's a name that a real attacker can watch for — hallucination patterns are researchable and somewhat predictable — and claim at any point, turning a future `pip install` or `npm install` of the exact command a generated diff suggested into a supply-chain compromise, with malicious code executing inside your project the moment the install finishes."
      },
      {
        "type": "diagnose-mechanism",
        "scenario": "Six months after a generated diff suggested `pip install requests_retry_toolkit`, a security researcher discovers a package by that exact name was published to PyPI two months ago, containing code that exfiltrates environment variables on import.",
        "question": "What's the actual mechanism that made this attack possible, distinct from an ordinary malicious-package upload?",
        "options": [
          {
            "value": "hallucination-created-predictable-target",
            "label": "The model's hallucinated package name — reused across many developers' AI-assisted sessions asking for similar functionality — created a predictable, discoverable target name that an attacker could specifically watch for and register, rather than needing to guess or typo-squat an existing name"
          },
          {
            "value": "pypi-has-no-review",
            "label": "This happened purely because PyPI doesn't review package contents before publishing, which is true of any malicious package regardless of how its name was chosen"
          },
          {
            "value": "user-should-have-known-python",
            "label": "The developer should have simply known Python well enough to recognize the package name was fake on sight"
          }
        ],
        "correctValue": "hallucination-created-predictable-target",
        "feedback": {
          "hallucination-created-predictable-target": "Right. The distinguishing mechanism of slopsquatting specifically is that the *target name itself* is discoverable in advance, because models tend to hallucinate the same or similar plausible names repeatedly for similar requests — an attacker doesn't need to guess a typo, they can research what AI tools actually suggest and register exactly that.",
          "pypi-has-no-review": "Lack of pre-publish review is a real, general fact about most package registries and a necessary precondition for any malicious upload — but it doesn't explain what's specific to slopsquatting versus any other malicious package, which is the predictability of the target name.",
          "user-should-have-known-python": "Package names in a large, actively-growing ecosystem are genuinely hard to verify from familiarity alone — plenty of real, legitimate, useful packages have names an experienced developer wouldn't recognize on sight either, which is exactly why 'sounds plausible' isn't a reliable signal in either direction."
        },
        "mechanism": "The chain: a model hallucinates a plausible package name repeatedly, for a common enough request, that the specific name becomes a predictable, researchable target — an attacker identifies it, registers it with malicious code, and waits. The victim is anyone who ran the generated install command without first confirming the package exists on the real registry with a legitimate history — a verification step that takes seconds and that fluent, confident-looking generated code gives no visual signal is necessary."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC4-007"
      },
      {
        "type": "takeaway",
        "body": "A confident, fluent import statement is exactly as easy for a model to generate for a package that doesn't exist as for one that does — verify a new package actually exists, on the real registry, with a real history, before running the install command a diff suggests."
      },
      {
        "type": "pod-header",
        "title": "The check that closes this gap",
        "podNumber": 2,
        "duration": 12
      },
      {
        "type": "text",
        "body": "The defense here is specific and cheap, worth stating as a concrete habit rather than a vague 'be careful': before running an install command a generated diff suggests for a package you haven't personally used before, open the actual registry page — npmjs.com, pypi.org, whichever ecosystem — and confirm the package exists, has a plausible publish history (not registered yesterday), and has a description and usage that actually match what the generated code is doing with it. This is the same registry-reading habit from earlier in this module, applied as a security gate rather than a quality gate — the two checks happen to be nearly the same motion.\n\nThe other half of the defense is noticing the shape of the risk in the first place: a hallucinated import is most likely for a less common task, an unusual combination of requirements, or a request phrased in a way that doesn't map cleanly onto one well-known package — exactly the situations where a generated diff is extrapolating furthest from its training data, and exactly where 'this looks fluent and confident' has the least actual connection to 'this is real.'"
      },
      {
        "type": "sequence-it",
        "question": "Put these steps in the order you'd actually want to take before running an install command a generated diff suggests for an unfamiliar package.",
        "steps": [
          "Notice the import is for a package you don't personally recognize or haven't used before",
          "Open the package's actual page on the real registry (PyPI, npm, etc.) rather than trusting the diff's usage as proof it exists",
          "Check that the package has a plausible publish history and isn't a brand-new, unknown registration",
          "Confirm the package's actual documented purpose matches what the generated code is using it for",
          "Only then run the install command"
        ],
        "explanation": "The check has to happen before the install, not after — once a malicious package's install script has run, the compromise has already happened, since install scripts can execute arbitrary code on your machine. Verifying existence, publish history, and actual documented purpose (in that order, since existence is the first thing to confirm and purpose-matching is a finer-grained check once you know it's real) is what turns 'this looks plausible' into 'this is confirmed,' cheaply, before the risky step."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC4-008"
      },
      {
        "type": "takeaway",
        "body": "Verify an unfamiliar package's existence and history on the real registry before installing, not after — the moment an install script runs is the moment a hallucinated-then-squatted package can compromise your machine."
      }
    ]
  },
  {
    "id": "aod-rc4-l05",
    "moduleId": "aod-rc4",
    "order": 5,
    "duration": 20,
    "difficulty": "medium",
    "title": "The maintenance you inherit with every import",
    "description": "Every dependency you add is a piece of future work you've committed your project to, sight unseen — an upgrade you'll eventually need to do, a breaking change you'll eventually need to absorb, a security patch you'll eventually need to apply, on someone else's timeline.",
    "learningObjectives": [
      {
        "text": "Name the specific, ongoing work a dependency commits a project to, beyond the moment it's installed",
        "bloomLevel": "understand"
      },
      {
        "text": "Weigh a dependency's ongoing maintenance cost against the cost of building and maintaining the equivalent functionality in-house",
        "bloomLevel": "evaluate"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "Installing is the cheapest moment a dependency ever costs you",
        "podNumber": 1,
        "duration": 10
      },
      {
        "type": "hook",
        "body": "`npm install some-package` takes about four seconds. What it actually commits a project to: a version to eventually bump, possibly across a breaking major-version change with its own migration guide to read; a security advisory to eventually respond to, on whatever schedule the maintainer discovers and discloses one; a transitive dependency tree that changes underneath you on every fresh install unless it's pinned; and, if the package is ever deprecated, abandoned, or removed from the registry entirely, a replacement to research and migrate to, on a timeline you didn't choose. The four-second install is the cheapest moment in that entire relationship."
      },
      {
        "type": "text",
        "body": "A dependency isn't a one-time cost paid at install — it's a small, recurring claim on future attention for as long as the project depending on it exists. Every dependency added is a promise, implicit and easy to forget having made, that someone will eventually: read a changelog before upgrading, decide whether a breaking change actually affects this codebase, apply a security patch promptly enough to matter, and, in the worst case, replace the package entirely if it's abandoned or its incident history becomes disqualifying.\n\nThis cost scales with how many dependencies a project has, not linearly but closer to compounding — more dependencies means more version-bump decisions per week, more surface area for one of them to have a security advisory, and more transitive dependency conflicts between packages that each want a different version of some shared underlying library. A generated diff adding one more small, convenient dependency is adding one more small, ongoing claim on the team's future attention, and it's genuinely difficult to feel that cost in the moment a diff is being reviewed for correctness, because the cost is deferred and diffuse rather than immediate and visible.\n\nThe practical habit: when evaluating whether a new dependency is worth adding, explicitly ask not just 'does this work correctly right now' but 'am I willing to be the person who eventually upgrades this, reads its next breaking-change migration guide, and responds if it has a security advisory' — because for most small projects and small teams, that person is whoever's reviewing the diff today, whether or not they've thought about it that way."
      },
      {
        "type": "evidence-stack",
        "scenario": "A small team is reviewing four dependencies a generated diff added across several PRs this month.",
        "question": "Select every one where the ongoing maintenance burden is a real, worth-naming cost — not necessarily a reason to reject it, but a cost that should be weighed, not ignored.",
        "items": [
          {
            "value": "auth-library",
            "label": "A third-party authentication library handling password hashing and session tokens — security-critical, meaning any advisory needs a fast, prioritized response, not a 'get to it eventually'",
            "applicable": true
          },
          {
            "value": "internal-cli-formatter",
            "label": "A small CLI output-formatting library used only in an internal developer tool, never shipped to any customer or production system",
            "applicable": false
          },
          {
            "value": "pdf-export",
            "label": "A PDF-generation library used in a customer-facing billing feature, where a future breaking change could silently alter invoice layout if the upgrade isn't tested carefully",
            "applicable": true
          },
          {
            "value": "test-only-mock-lib",
            "label": "A mocking library used exclusively in the test suite, with no code path that ever runs in production",
            "applicable": false
          }
        ],
        "explanation": {
          "auth-library": "Security-critical dependencies carry a genuinely different maintenance obligation than most — an advisory here needs prompt, prioritized attention, not the 'whenever someone gets to it' cadence that's fine for most routine dependency updates.",
          "internal-cli-formatter": "Low real cost: it's not customer-facing, not security-relevant, and if it's ever abandoned, swapping it for another formatting library or a hand-rolled version is cheap and low-risk, with no customer-facing consequence either way.",
          "pdf-export": "A breaking change here has a real, customer-visible failure mode (a malformed invoice) that could be easy to miss in casual testing — worth flagging as a dependency that needs deliberate, careful testing on every upgrade, not an automatic version bump.",
          "test-only-mock-lib": "Test-only dependencies that never run in production carry meaningfully lower risk — a security advisory in a test-only mocking library has no production attack surface, and a breaking change only affects developer workflow, not customers."
        },
        "synthesis": "The maintenance cost that actually matters to name explicitly is concentrated in dependencies that are either security-relevant or customer-facing in a way where a breaking change could cause a silent, hard-to-notice regression — dev-only and test-only dependencies carry the same nominal 'someone will need to update this eventually' cost, but at meaningfully lower real stakes."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC4-009"
      },
      {
        "type": "takeaway",
        "body": "A dependency's real cost is a small, recurring, easy-to-forget claim on future attention — weigh it heaviest for anything security-critical or customer-facing, where a missed update or an unnoticed breaking change actually costs something."
      },
      {
        "type": "pod-header",
        "title": "What a supply-chain incident actually looks like",
        "podNumber": 2,
        "duration": 10
      },
      {
        "type": "text",
        "body": "The sharpest version of this risk isn't a package quietly going stale — it's a widely-trusted, actively-used package's publishing account or maintainer getting compromised, and a malicious update going out under the same trusted name millions of projects already depend on. This has happened to real, extremely popular packages across ecosystems: a maintainer's credentials phished, a malicious version published, and every project that auto-updates or installs fresh without pinning a specific version pulling in the compromised code within hours, often before anyone notices.\n\nThis is precisely why the earlier lessons' evidence — maintainer count, publish history, how heavily-used and -watched a package is — matters beyond just predicting abandonment. A heavily-used package with an active community is more likely to have the compromise noticed and reverted quickly by someone else before you ever pull the malicious version; a small, quiet package with one maintainer and few users has no such safety net, and a compromise there could sit undiscovered far longer. None of this means dependencies are too risky to use — nearly all software depends on them, including software written by hand — it means the evaluation habits from this whole module (existence, maintenance signals, license, and now this) are worth applying together, not selectively, on anything a project genuinely depends on."
      },
      {
        "type": "interactive-guess",
        "question": "A project pins every dependency to an exact version in its lockfile and only upgrades deliberately, reading changelogs first. A second, similar project auto-updates to the latest compatible version on every fresh install with no review step. Which is more exposed to a supply-chain compromise of a popular dependency, and why?",
        "hint": "Think about the gap in time between a malicious version being published and it actually reaching a project's running code, in each setup.",
        "answer": "The auto-updating project is more exposed — it pulls in a new version, potentially including a malicious one, the moment it's published and matches the version range, with no human review step in between to catch anything unusual before it's running in the codebase. The pinned project has a deliberate gap between a new version existing and it actually being adopted, during which a compromise is more likely to already be discovered, reported, and reverted by the wider community before this specific project ever installs it. Pinning isn't a complete defense — a deliberate upgrade can still adopt a compromised version if it happens to land during the review window — but it removes the fastest, most automatic path from 'malicious version published' to 'running in your project.'"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC4-010"
      },
      {
        "type": "takeaway",
        "body": "The evaluation habits from this whole module — existence, maintenance health, license, ongoing cost — exist together specifically because a supply-chain compromise is most dangerous for exactly the dependencies that skipped all of them: obscure, unpinned, unreviewed, and auto-updated."
      }
    ]
  },
  {
    "id": "aod-rc4-p01",
    "moduleId": "aod-rc4",
    "order": 6,
    "duration": 30,
    "difficulty": "medium",
    "title": "Practice — Accept or Reject Seven Dependencies With a Written Reason",
    "description": "Seven drills, no new concepts: necessity, registry evidence, licenses, hallucinated packages, and ongoing maintenance cost.",
    "learningObjectives": [
      {
        "text": "Apply dependency-evaluation habits across unfamiliar packages at speed",
        "bloomLevel": "apply"
      },
      {
        "text": "Produce a specific, evidence-based accept/reject call for a dependency rather than a general impression",
        "bloomLevel": "evaluate"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "Seven drills",
        "podNumber": 1,
        "duration": 30
      },
      {
        "type": "hook",
        "body": "Nothing new here. Seven drills across the module's skills, ordered from where the lessons ended to one step past it. Do them in one sitting or spread across a few — nothing here expires."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC4-P01"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC4-P02"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC4-P03"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC4-P04"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC4-P05"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC4-P06"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC4-P07"
      },
      {
        "type": "takeaway",
        "body": "If a drill needed a hint, that's what to watch for in the checkpoint — not a reason to redo the set."
      }
    ]
  },
  {
    "id": "aod-rc4-lab",
    "moduleId": "aod-rc4",
    "order": 7,
    "duration": 50,
    "difficulty": "hard",
    "title": "Lab — Audit the Dependency Additions in a Generated Pull Request",
    "description": "Take every new dependency a real or realistic PR adds, and produce a written keep/replace/remove call for each, backed by actual evidence.",
    "learningObjectives": [
      {
        "text": "Apply necessity, registry-health, license, existence, and maintenance-cost evaluation to a real set of dependency additions",
        "bloomLevel": "evaluate"
      },
      {
        "text": "Produce a dependency review with a specific call and cited evidence per package, not a general impression",
        "bloomLevel": "create"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "The lab",
        "podNumber": 1,
        "duration": 50
      },
      {
        "type": "hook",
        "body": "Every lesson this module worked one dependency-evaluation habit at a time. A real pull request that adds several new packages needs all of them applied together, per package: is it necessary, is it healthy, what does its license obligate, does it actually exist with a real history, and what ongoing cost does it commit the project to. This lab produces the artefact a real dependency review actually looks like — a specific call per package, with evidence, not a blanket approval or rejection of the PR as a whole."
      },
      {
        "type": "lab-brief",
        "labId": "aod-rc4-dependency-audit",
        "title": "Audit a PR's dependency additions",
        "brief": "Find a real pull request — your own project's history, an open-source project's PR history, or one you generate now for a plausible feature (a CSV export, a notification system, an image upload pipeline) — that adds at least three new dependencies. If you can't find one with real dependency additions, construct a plausible one: list three to five packages a generated diff might add for a specific feature, with enough realistic detail (purpose, rough download count, license, last publish) to actually evaluate.",
        "deliverable": "A dependency review: for each new package, a keep/replace/remove call, the specific evidence behind it (necessity, registry health, license, existence check, ongoing cost), and for anything marked replace or remove, what you'd use instead.",
        "stages": [
          {
            "title": "List every new dependency and check necessity",
            "minutes": 10,
            "instructions": "List every package the PR adds. For each, ask whether the task is genuinely hard enough to justify a dependency versus a few lines of code — mark any that fail this check as a likely 'replace with hand-written code' candidate before doing further evaluation on them.",
            "checkpoint": "Every new dependency is listed, with an initial necessity call and a one-line reason."
          },
          {
            "title": "Check registry health and existence",
            "minutes": 12,
            "instructions": "For each package that passed the necessity check, confirm it actually exists with a real publish history, and note its maintenance signals — last publish, maintainer count, any relevant open issues.",
            "checkpoint": "Every remaining package has a documented existence check and maintenance-health summary."
          },
          {
            "title": "Check license and ongoing cost",
            "minutes": 13,
            "instructions": "For each package, note its license and whether it creates any obligation given how this specific product runs (network service, distributed binary, internal tool). Then name the ongoing maintenance stakes — how critical is the path it sits on, who's responsible for upgrading it.",
            "checkpoint": "Every package has a license note and an ongoing-cost assessment tied to its actual criticality."
          },
          {
            "title": "Write the final calls and the deliverable",
            "minutes": 15,
            "instructions": "For each package, make a final keep/replace/remove call backed by the evidence gathered. For anything not kept as-is, state a specific alternative. Compress into the deliverable and self-check against the rubric.",
            "checkpoint": "Every package has a specific call, cited evidence, and — where relevant — a named alternative, not a vague 'this seems risky.'"
          }
        ],
        "rubric": [
          {
            "criterion": "Every new dependency in the PR has an individual call, not a blanket verdict on the PR as a whole",
            "meets": "Each package is evaluated and called separately, even if several end up with the same verdict."
          },
          {
            "criterion": "Each call is backed by specific, named evidence",
            "meets": "The reasoning cites an actual fact — a publish date, a license name, a specific missing standard-library alternative — not a general impression."
          },
          {
            "criterion": "At least one necessity call is made explicitly, even if every package passes",
            "meets": "The 'could this be a few lines of code instead' question is asked and answered for every package, not skipped for the ones that seem obviously fine."
          },
          {
            "criterion": "License is addressed for every package, even briefly",
            "meets": "Each package has a stated license and a one-line note on whether it's relevant given how this product runs — 'permissive, no concern' is a valid, sufficient answer when true."
          },
          {
            "criterion": "Anything called replace or remove has a specific alternative named",
            "meets": "A rejection without a stated alternative isn't a complete review — the deliverable names what to use instead."
          }
        ]
      },
      {
        "type": "takeaway",
        "body": "A dependency review that says 'looks fine' about a whole PR is exactly as unfalsifiable as a code review that says the same about a whole diff — the useful version names each package, cites the specific evidence, and makes a call someone else could check."
      }
    ]
  },
  {
    "id": "aod-rc4-cp",
    "moduleId": "aod-rc4",
    "order": 8,
    "duration": 10,
    "difficulty": "easy",
    "title": "Checkpoint — Dependencies and What They Cost",
    "description": "Ten questions: four from this module, four from Data Shapes and Boundaries, two from Control Flow and State. Ungraded, untimed.",
    "learningObjectives": [
      {
        "text": "Retrieve this module's core distinctions alongside earlier modules', without looking them up",
        "bloomLevel": "remember"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "Let's see what stuck",
        "podNumber": 1,
        "duration": 10
      },
      {
        "type": "retrieval-check",
        "title": "Checkpoint — Dependencies and What They Cost",
        "questions": [
          {
            "from": "This module — Dependencies and What They Cost",
            "prompt": "Why does a model reach for a library like lodash for a task a few lines of standard-library code could handle?",
            "options": [
              {
                "value": "a1",
                "label": "It's producing the statistically common shape of code for this task from its training data — not weighing this project's specific bundle-size or dependency-count concerns"
              },
              {
                "value": "a2",
                "label": "Because dependencies always make code run faster"
              },
              {
                "value": "a3",
                "label": "Because standard-library code is deprecated in most modern languages"
              }
            ],
            "correctValue": "a1",
            "explanation": "The `_.pick` example showed this — a model reaches for a familiar library out of statistical habit, not a deliberate tradeoff calculation this specific project needed made."
          },
          {
            "from": "This module — Dependencies and What They Cost",
            "prompt": "What does the AGPL license's distinguishing clause specifically cover, that GPL alone doesn't?",
            "options": [
              {
                "value": "b1",
                "label": "Software run as a network service, even when never literally distributed as a copy to users"
              },
              {
                "value": "b2",
                "label": "Software installed on a user's personal computer"
              },
              {
                "value": "b3",
                "label": "Software used only for internal, non-customer-facing tooling"
              }
            ],
            "correctValue": "b1",
            "explanation": "The AGPL was specifically written to close the network-service gap the regular GPL doesn't cover — relevant to nearly any modern SaaS product."
          },
          {
            "from": "This module — Dependencies and What They Cost",
            "prompt": "What's the mechanism that makes 'slopsquatting' a distinct risk from an ordinary malicious package upload?",
            "options": [
              {
                "value": "c1",
                "label": "The target package name is predictable in advance, because models tend to hallucinate similar plausible names repeatedly for similar requests, giving an attacker a name to specifically watch for and register"
              },
              {
                "value": "c2",
                "label": "It only affects packages that are already extremely popular"
              },
              {
                "value": "c3",
                "label": "It requires the attacker to guess a common human typo"
              }
            ],
            "correctValue": "c1",
            "explanation": "The `requests_retry_toolkit` scenario showed this — a hallucinated name reused across many sessions becomes a discoverable, researchable target, distinct from typosquatting's reliance on human typing mistakes."
          },
          {
            "from": "This module — Dependencies and What They Cost",
            "prompt": "What's the actual ongoing cost a project takes on the moment a new dependency is added?",
            "options": [
              {
                "value": "d1",
                "label": "A small, recurring, easy-to-forget claim on future attention — eventual upgrades, breaking-change migrations, and security response, on someone else's timeline"
              },
              {
                "value": "d2",
                "label": "Nothing beyond the initial install time"
              },
              {
                "value": "d3",
                "label": "A one-time licensing fee"
              }
            ],
            "correctValue": "d1",
            "explanation": "The install is the cheapest moment in the whole relationship — the real cost is deferred and diffuse, which is exactly why it's easy to underweight when reviewing a diff for correctness alone."
          },
          {
            "from": "Previous module — Data Shapes and Boundaries",
            "prompt": "A function's type signature says `list[dict]`. What does that guarantee about the list's contents?",
            "options": [
              {
                "value": "e1",
                "label": "Very little beyond the outer container and element type — nothing about non-emptiness or required keys, which is where a function's real, narrower beliefs live"
              },
              {
                "value": "e2",
                "label": "That every dict shares the same set of keys"
              },
              {
                "value": "e3",
                "label": "That the list is never empty"
              }
            ],
            "correctValue": "e1",
            "explanation": "The bulk-discount example showed a function's real, narrower belief — that every item shares one price — living entirely outside what its type signature actually declared."
          },
          {
            "from": "Previous module — Data Shapes and Boundaries",
            "prompt": "Why is `if not order.get('discount_pct'):` risky as a check for 'no discount was calculated'?",
            "options": [
              {
                "value": "f1",
                "label": "A discount of exactly 0% is a legitimate, meaningful value that's falsy, so it gets silently conflated with 'never calculated'"
              },
              {
                "value": "f2",
                "label": "`.get()` always raises an error on a missing key"
              },
              {
                "value": "f3",
                "label": "Percentages should never be stored as floats"
              }
            ],
            "correctValue": "f1",
            "explanation": "This is the null/empty/missing/zero distinction — a real, valid zero value gets conflated with a genuinely different 'not set' state by a bare truthiness check."
          },
          {
            "from": "Previous module — Data Shapes and Boundaries",
            "prompt": "A sensitive function has no internal validation and one currently-validated caller. What does this module say about its actual safety?",
            "options": [
              {
                "value": "g1",
                "label": "Its safety is entirely borrowed from that one caller, and remains safe only as long as every future caller independently re-implements the same checks"
              },
              {
                "value": "g2",
                "label": "It's safe permanently, since the current caller was already reviewed"
              },
              {
                "value": "g3",
                "label": "Validation should never happen at entry points, only deep in business logic"
              }
            ],
            "correctValue": "g1",
            "explanation": "The `process_transfer` case-sim showed a second caller (a reconciliation job, then a CSV upload) reaching the same unvalidated function without the original route's checks."
          },
          {
            "from": "Previous module — Data Shapes and Boundaries",
            "prompt": "Why does `data as User` (a TypeScript type assertion) provide no real protection against a malformed API response?",
            "options": [
              {
                "value": "h1",
                "label": "A type assertion is a compile-time-only instruction with zero runtime effect — nothing actually inspects the real value's shape"
              },
              {
                "value": "h2",
                "label": "TypeScript assertions are only valid for primitive types, not objects"
              },
              {
                "value": "h3",
                "label": "It works fine as long as the API is generally reliable"
              }
            ],
            "correctValue": "h1",
            "explanation": "The `getUser` example traced this exactly — the assertion told the compiler to stop checking, and the resulting crash happened downstream, far from the actual mismatch."
          },
          {
            "from": "Two modules back — Control Flow and State",
            "prompt": "What should a happy-path trace end in?",
            "options": [
              {
                "value": "i1",
                "label": "A specific, defensible value derived from concrete inputs, not a summary of what the code 'probably' does"
              },
              {
                "value": "i2",
                "label": "Confirmation the code compiles without error"
              },
              {
                "value": "i3",
                "label": "A performance benchmark"
              }
            ],
            "correctValue": "i1",
            "explanation": "Tracing means updating every variable's real value for a concrete input, arriving at a specific number or result you can defend with arithmetic."
          },
          {
            "from": "Two modules back — Control Flow and State",
            "prompt": "Why can a function be correct when traced by a single caller and still be wrong under shared mutable state?",
            "options": [
              {
                "value": "j1",
                "label": "The bug can live entirely in the gap between two overlapping callers' reads and writes — a gap a single-caller trace can't represent"
              },
              {
                "value": "j2",
                "label": "Shared state is always inherently slower"
              },
              {
                "value": "j3",
                "label": "It can't — a function correct for one caller is always correct for multiple"
              }
            ],
            "correctValue": "j1",
            "explanation": "The rate-limiter example traced correctly for one caller every time — the bug only appears once two overlapping calls are deliberately interleaved."
          }
        ]
      }
    ]
  },
  {
    "id": "aod-rc5-l01",
    "moduleId": "aod-rc5",
    "order": 1,
    "duration": 20,
    "difficulty": "easy",
    "title": "Tests as the only executable documentation you have",
    "description": "A comment can drift from the code without anyone noticing. A test that drifts from the code fails the build — which makes a passing test suite the one document in an undocumented codebase that's still provably true right now.",
    "learningObjectives": [
      {
        "text": "Explain why a passing test carries a stronger correctness guarantee than a comment or a README, given how each stays or fails to stay true over time",
        "bloomLevel": "understand"
      },
      {
        "text": "Read a test file as a source of intent when no other documentation exists",
        "bloomLevel": "apply"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "A comment can lie silently. A test can't.",
        "podNumber": 1,
        "duration": 10
      },
      {
        "type": "hook",
        "body": "No README, no ticket, no comment — just a function and its test file.\n\n```python\ndef compute_late_fee(balance, days_late):\n    ...\n\ndef test_compute_late_fee_caps_at_25_percent():\n    fee = compute_late_fee(balance=1000, days_late=90)\n    assert fee == 250\n```\n\nThe function's implementation might have a stale comment above it, or no comment at all. The test can't drift the same way — it either currently passes, meaning `compute_late_fee(1000, 90)` really does return `250` right now, or it currently fails, which the whole team would already know about. A passing test is the one artifact in this codebase that's *mechanically checked to still be true*, every time the suite runs, with no possibility of quietly going stale the way a comment can."
      },
      {
        "type": "text",
        "body": "The earlier module in this phase on reading for intent established that comments and docstrings are claims, not facts — written once, checked by nothing, free to drift from the code beneath them without anyone noticing. A test is a fundamentally different kind of claim: it's a claim the build actively re-verifies every time it runs, and if the claim stops being true, the test suite turns red and — in any team with even minimal process — someone notices before the code ships. This doesn't make a test a perfect description of intent (later lessons in this module cover exactly how a test can pass while checking almost nothing), but it does make a *passing* test meaningfully more trustworthy than a comment making the same claim, purely because of what would have to happen for it to be false: a comment can be false silently, forever; a test being false is, structurally, a build failure.\n\nIn an undocumented codebase — which describes a lot of real, especially newer AI-assisted codebases, where a comment explaining intent is easy to skip generating and a test is often the thing that actually got written to confirm a feature works — the test suite is frequently the *only* executable record of what the code is actually supposed to do. Reading test names, arrangements, and assertions as a primary source of intent, not an afterthought once you've already read the implementation, is a real, distinct skill from reading the implementation itself.\n\nThe habit this lesson opens with: when you land in unfamiliar code, check whether tests exist before assuming you need to read the implementation cold. A well-named test (`test_compute_late_fee_caps_at_25_percent`) can hand you the requirement — a fee that caps at 25% of the balance — faster than tracing the implementation's own branches would."
      },
      {
        "type": "concept-flow",
        "scenario": "You've just opened an unfamiliar module with no README and one stale, contradictory comment above a function. The module does have a test file alongside it.",
        "question": "What's the most efficient first move, given the tools available?",
        "options": [
          {
            "label": "Read the test file's test names and assertions first, to form a hypothesis about the requirement, before diving into the implementation's logic",
            "value": "tests-first"
          },
          {
            "label": "Trust the stale comment, since it's presumably describing the original intent even if slightly outdated",
            "value": "trust-comment"
          },
          {
            "label": "Skip both and trace the implementation cold, since tests could also be wrong",
            "value": "implementation-cold"
          }
        ],
        "correctValue": "tests-first",
        "feedback": {
          "tests-first": "Right. A test that's currently passing is mechanically confirmed true right now, in a way a comment structurally can't be — reading it first gives you a verified starting hypothesis about the requirement, which then makes reading the implementation a confirmation pass rather than starting from nothing, the same efficiency gain the earlier module found in reading a function's signature before its body.",
          "trust-comment": "The lesson this hook opened with is specifically about why a comment can't be trusted the way a passing test can — a stale, contradictory comment is exactly the kind of claim that's had every opportunity to silently drift from the truth, unlike a test the build re-checks on every run.",
          "implementation-cold": "Tests can be wrong too, and this module's later lessons cover exactly how — but 'tests can also be imperfect' isn't a reason to skip the fastest, most-verified starting point available; it's a reason to read them critically rather than not at all."
        },
        "concept": "A passing test is the one document in an otherwise undocumented codebase that's continuously, mechanically re-verified — reading it first, before the implementation, gives you a checked starting hypothesis instead of an unverified one, the same efficiency principle behind reading a signature before a function body."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC5-001"
      },
      {
        "type": "takeaway",
        "body": "A passing test is re-verified every time the build runs, which a comment never is — read a test suite as your primary source of intent in undocumented code, before the implementation, not as an afterthought once you've already formed a guess."
      },
      {
        "type": "pod-header",
        "title": "What a test file's shape tells you before you read a single assertion",
        "podNumber": 2,
        "duration": 10
      },
      {
        "type": "text",
        "body": "Before reading individual assertions, a test file's overall shape is itself informative: how many tests exist for a given function, what their names describe, and which cases are covered by name alone. A function with one test named `test_compute_late_fee` tells you less than a function with four tests named `test_caps_at_25_percent`, `test_zero_for_paid_invoice`, `test_zero_when_not_yet_due`, and `test_rate_applies_per_day` — the second set is effectively a table of contents for the function's actual behavior, readable before a single line of the implementation.\n\nThis is worth checking specifically because the number and naming of tests for a piece of AI-generated code is itself a signal about how carefully it was built out, in a way that's cheap to read: a function with no tests at all is a function whose behavior you'll need to establish entirely through implementation-reading and your own manual tracing; a function with several specifically-named tests is one where someone (or some prompt) already thought through several of its distinct cases, and you can use their thinking as a starting map rather than building one from nothing."
      },
      {
        "type": "interactive-guess",
        "question": "You find a function `apply_late_fee` with zero tests anywhere in the codebase, and a second function `calculate_shipping_cost` in the same file with six specifically-named tests covering different weight tiers, international shipping, and free-shipping thresholds. Before reading either implementation, which one would you expect to take longer to fully understand, and why?",
        "hint": "One of these hands you a partial map of its behavior for free; the other hands you nothing.",
        "answer": "`apply_late_fee`, almost certainly — with zero tests, there's no executable documentation at all, and understanding its full behavior requires reading the implementation cold and manually tracing every case yourself, the way the earlier module's techniques work without any test-derived head start. `calculate_shipping_cost`'s six specifically-named tests hand you a partial map of its behavior before you've read a line of its body: weight tiers, international handling, and a free-shipping threshold are all named cases you can now specifically look for in the implementation, turning that read into confirmation rather than open discovery — the same efficiency gain reading a signature and its returns provides, here scaled up by an entire test file's worth of named cases."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC5-002"
      },
      {
        "type": "takeaway",
        "body": "A test file's names and count, read before any individual assertion, are a table of contents for a function's known behavior — a rich, specifically-named test file hands you a head start; an empty one hands you nothing, and tells you so honestly."
      }
    ]
  },
  {
    "id": "aod-rc5-l02",
    "moduleId": "aod-rc5",
    "order": 2,
    "duration": 25,
    "difficulty": "medium",
    "title": "Reading a test to find the requirement behind it",
    "description": "A test's arrangement, action, and assertion are three separate claims — what situation was set up, what was actually done, and what was checked afterward — and the real requirement usually lives in the gap between the test's name and what it actually asserts.",
    "learningObjectives": [
      {
        "text": "Decompose a test into its arrange/act/assert structure to extract the specific requirement it's checking",
        "bloomLevel": "analyze"
      },
      {
        "text": "Identify when a test's name claims more than its actual assertion checks",
        "bloomLevel": "analyze"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "Arrange, act, assert — read all three separately",
        "podNumber": 1,
        "duration": 13
      },
      {
        "type": "hook",
        "body": "```python\ndef test_refund_within_window():\n    order = Order(total=100, purchased_at=days_ago(10))\n    result = process_refund(order)\n    assert result.approved\n    assert result.amount == 100\n```\n\nRead as one block, this looks like a simple confirmation. Decomposed: the *arrangement* fixes one specific fact — this order is exactly 10 days old — which is itself a claim about the boundary this test is probing, not an arbitrary number. The *action* calls the one function under test. The *assertion* checks two separate things: that a refund was approved at all, and that the amount is the full $100, not a partial refund. That second assertion is easy to skim past, and it's actually encoding a real requirement: a refund within the window is full, not prorated."
      },
      {
        "type": "text",
        "body": "Every test worth reading for intent decomposes into three parts, and reading them as one undifferentiated block is how a real requirement hides in plain sight. The **arrangement** — the setup, the specific input values chosen — is itself a claim: a test that arranges `purchased_at=days_ago(10)` rather than `days_ago(1)` or `days_ago(29)` is very likely probing something specific about a boundary, not picking an arbitrary number, the same way this phase's earlier module treated a specific, non-round constant in implementation code as evidence of a deliberate decision. The **action** is usually the least informative part — it's just the call to the function under test. The **assertion** is where the actual requirement is stated, and it's worth reading assertion by assertion, not just noticing that assertions exist, because — as the hook showed — a second or third assertion can carry a distinct, easy-to-skim-past requirement of its own.\n\nThe specific habit for this lesson: for each assertion in a test, ask what real-world requirement would make this specific check necessary — not just 'the code returns true here,' but 'why does the test author care that it returns true specifically here, with these specific values.' A test asserting `result.amount == 100` isn't just confirming 'a number came back' — given a $100 order, it's confirming the refund is full-value, a genuine business rule (full refunds within the window, not prorated ones) that the assertion states more precisely than the test's own name does."
      },
      {
        "type": "worked-example",
        "problem": "Decompose this test and extract the specific requirement it encodes.\n\n```python\ndef test_bulk_order_discount():\n    cart = Cart(items=[Item(price=10, qty=15)])\n    total = calculate_total(cart)\n    assert total == pytest.approx(127.50)\n```",
        "steps": [
          {
            "label": "Read the arrangement",
            "work": "One item, price $10, quantity 15. Fifteen units of a single $10 item — a specific, deliberately round-but-not-trivial quantity.",
            "why": "15 units suggests this is testing a bulk-discount threshold specifically — a quantity high enough to plausibly cross into a discount tier, not just an arbitrary cart contents choice."
          },
          {
            "label": "Compute what the total would be with NO discount",
            "work": "15 × $10 = $150 undiscounted.",
            "why": "This baseline is essential to interpreting the assertion — without it, `127.50` is just a number; with it, the gap between $150 and $127.50 becomes the actual finding."
          },
          {
            "label": "Read the assertion against that baseline",
            "work": "$127.50 is $150 minus $22.50, which is exactly 15% off $150.",
            "why": "The requirement this test actually encodes: a 15-unit order of this item receives a 15% discount. The test's name says 'bulk order discount' generically; the assertion, read against a computed baseline, states the specific rate."
          }
        ],
        "faded": {
          "problem": "Decompose this second test the same way.\n\n```python\ndef test_free_shipping_threshold():\n    cart = Cart(items=[Item(price=25, qty=2)], destination='domestic')\n    shipping = calculate_shipping(cart)\n    assert shipping == 0\n```",
          "blanks": [
            {
              "prompt": "What's the cart's total value, computed from the arrangement?",
              "answer": "$50 — 2 items at $25 each.",
              "accept": [
                "50",
                "$50"
              ],
              "why": "As in the worked example, the assertion is only interpretable against a value computed from the arrangement, not read in isolation."
            },
            {
              "prompt": "What specific requirement does `shipping == 0` encode, given that computed total?",
              "answer": "Domestic orders totaling $50 or more (or possibly exactly $50, depending on whether the threshold is inclusive) qualify for free shipping.",
              "accept": [
                "free shipping at $50",
                "orders over 50 get free shipping"
              ],
              "why": "The test's name says 'free shipping threshold' generically; the specific number, $50, is the actual requirement, extracted only by computing the arrangement's total and reading it against the assertion."
            },
            {
              "prompt": "What's one thing this single test does NOT tell you about the threshold, that you'd need a second test to confirm?",
              "answer": "Whether $50 is exactly the boundary (inclusive) or whether the real threshold is lower and $50 just clearly qualifies — a test with cart total exactly at $49.99 or some other near-boundary value would be needed to pin down the exact cutoff.",
              "accept": [
                "exact boundary",
                "whether 50 is inclusive"
              ],
              "why": "This is the same open-question habit from the earlier module's reconstruction lesson — a single data point supports an inference, but the exact boundary needs a boundary-probing test to actually confirm, not just imply."
            }
          ]
        },
        "procedure": "Read the arrangement for specific, deliberately-chosen values. Compute a baseline where relevant (what would this be without the behavior under test). Read the assertion against that baseline to extract the actual numeric or logical requirement — which is usually more specific than the test's name alone states."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC5-003"
      },
      {
        "type": "takeaway",
        "body": "A test's arrangement, action, and assertion are three separate claims — the real requirement usually lives in reading the assertion against a computed baseline from the arrangement, not in the test's name alone."
      },
      {
        "type": "pod-header",
        "title": "When the name promises more than the assertion checks",
        "podNumber": 2,
        "duration": 12
      },
      {
        "type": "text",
        "body": "The gap runs the other direction too, and it's the more consequential one to catch: a test named for a broad requirement whose actual assertion checks something much narrower. `test_validates_email` that only asserts a function returns `False` for one specific malformed input tells you the function rejects *that* input — it says nothing about whether the function correctly accepts genuinely valid emails, or correctly rejects other malformed ones, despite a name that reads as a general claim about validation working correctly.\n\nThis is the test-reading version of the earlier module's name-versus-body distinction applied to comments and function names — a test's name is a claim made by whoever wrote it, generated by the same process (human or model) as the assertion itself, with nothing forcing the two to actually match in scope. Reading a test's name as proof the requirement is fully covered, without checking what the assertion specifically verifies, is exactly the kind of name-trusting mistake this whole phase has been building the habit of catching."
      },
      {
        "type": "spot-flaw",
        "code": "def test_handles_invalid_input():\n    result = parse_user_age(\"abc\")\n    assert result is None",
        "question": "The test name claims to cover 'invalid input' generally. What's the actual, narrower scope of what this test verifies?",
        "options": [
          {
            "value": "one-case-only",
            "label": "It verifies exactly one thing: that the specific string `\"abc\"` returns `None`. It says nothing about negative numbers, empty strings, floats, extremely large numbers, or any other input that might also be 'invalid' but isn't `\"abc\"`"
          },
          {
            "value": "name-matches-scope",
            "label": "The name is accurate — testing one clearly-invalid string is a representative, sufficient check for 'handles invalid input' generally"
          },
          {
            "value": "assertion-too-strict",
            "label": "The problem is that the assertion should check for a raised exception instead of a returned None"
          }
        ],
        "correctValue": "one-case-only",
        "feedback": {
          "one-case-only": "Right. This is a single data point wearing the clothing of a general claim. A non-numeric string is one specific kind of invalid input among many plausible ones — negative ages, ages of 200, empty strings, `None` itself passed in directly — and this test's actual, narrow assertion says nothing about how the function handles any of those.",
          "name-matches-scope": "One example is evidence for that one example, not for the general category — this is precisely the over-generalization the lesson is warning about, treating a test's name as though it guarantees coverage its actual assertion doesn't provide.",
          "assertion-too-strict": "Whether `None` or an exception is the right response design is a separate, legitimate question from this exercise's actual point, which is about the gap between the test's broad name and its narrow actual scope — that gap exists regardless of which specific failure behavior the function chooses."
        },
        "flawExplanation": "A test named for a general category, backed by exactly one example, tells you the function handles that one example — full stop. Treating the name as proof the whole category is covered is exactly the kind of name-trusting error the earlier reading-for-intent module warned against for function names and comments, applied here to test names: a name is a claim by whoever wrote the test, not a guarantee about what the assertion beneath it actually checks."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC5-004"
      },
      {
        "type": "takeaway",
        "body": "A test's name can claim a broader requirement than its assertion actually checks — read what's specifically asserted, against what specific arrangement, before trusting a name's implied scope."
      }
    ]
  },
  {
    "id": "aod-rc5-l03",
    "moduleId": "aod-rc5",
    "order": 3,
    "duration": 20,
    "difficulty": "medium",
    "title": "Tests that pass because they assert nothing",
    "description": "A green checkmark next to a test's name means the assertions inside it didn't fail — it says nothing about whether those assertions were actually capable of catching a real bug.",
    "learningObjectives": [
      {
        "text": "Identify a vacuous or tautological assertion that would pass regardless of whether the code under test is correct",
        "bloomLevel": "analyze"
      },
      {
        "text": "Distinguish a test that exercises real behavior from one that only confirms the code ran without crashing",
        "bloomLevel": "analyze"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "\"It ran without crashing\" is not the same claim as \"it's correct\"",
        "podNumber": 1,
        "duration": 10
      },
      {
        "type": "hook",
        "body": "```python\ndef test_calculate_total():\n    cart = Cart(items=[Item(price=10, qty=3)])\n    total = calculate_total(cart)\n    assert total is not None\n```\n\nThis test is green. It will stay green if `calculate_total` returns `30` (correct), `0` (wrong), `-5` (very wrong), or `\"thirty\"` (a completely different type than expected) — every one of those values is 'not None,' so every one of them passes this assertion. The test ran the function, confirmed it didn't crash and didn't return the specific value `None`, and checked absolutely nothing about whether the actual computed total is right. A reviewer glancing at a green test suite and a passing CI badge has no way to see this from the outside — the checkmark looks identical whether the assertion is meaningful or vacuous."
      },
      {
        "type": "text",
        "body": "A vacuous, or tautological, assertion is one that's structurally guaranteed — or near-guaranteed — to pass regardless of whether the code under test actually behaves correctly. `assert result is not None`, `assert response.status_code < 500` (passes for a 200, a 400, a 404 — nearly every plausible response short of a server error), `assert isinstance(result, dict)` (checks the type, never the contents), and `assert True` written as a placeholder that never got filled in are all common, real shapes this takes. Every one of them produces a genuine green checkmark, contributes to a genuine 'tests passing' CI status, and catches essentially no real bugs, because the space of wrong answers that would still satisfy the assertion is enormous.\n\nThis specific failure is worth naming as a distinct risk in AI-generated test suites: a model asked to 'add tests' for a function has a strong incentive, structurally, to produce something that passes — and the cheapest, most reliable way to guarantee a passing test is a weak assertion that's hard to violate, whether or not that was anyone's intention. A generated test suite that's 100% green can create a false sense of security disproportionate to how much it actually verified, precisely because the failure is invisible from the outside — a red test suite announces a problem; a suite full of vacuous-but-green tests announces nothing at all.\n\nThe reading habit: for every assertion, ask the specific question 'what wrong value would this assertion fail to catch?' If the honest answer is 'almost anything except a crash or a literal None,' the test is close to vacuous regardless of how confident its name sounds."
      },
      {
        "type": "diagnose-mechanism",
        "scenario": "A generated function `apply_tax` has a bug: it multiplies by the tax rate as a percentage (e.g. `8.5`) instead of converting it to a decimal first (`0.085`), producing wildly inflated totals. Its accompanying test:\n\n```python\ndef test_apply_tax():\n    result = apply_tax(price=100, rate=8.5)\n    assert result > 0\n```\n\nThis test is green in CI.",
        "question": "What's the actual mechanism by which this genuine, serious bug ships with a fully green, passing test suite?",
        "options": [
          {
            "value": "vacuous-assertion-cant-catch-it",
            "label": "`assert result > 0` is satisfied by the correct answer (`$108.50`) and by the buggy answer (`$950`, from multiplying by 8.5 as a whole number) equally — the assertion's scope is so wide it can't distinguish correct from badly wrong, so the bug simply never has a chance to fail the test"
          },
          {
            "value": "test-runner-broken",
            "label": "This must mean the test runner itself has a configuration problem, since a real bug should always be caught by its accompanying test"
          },
          {
            "value": "tax-rate-ambiguous",
            "label": "The bug is actually in how `rate=8.5` is documented, not in the multiplication logic or the test"
          }
        ],
        "correctValue": "vacuous-assertion-cant-catch-it",
        "feedback": {
          "vacuous-assertion-cant-catch-it": "Right. `result > 0` is true for the correct total and for a total roughly nine times too large — the assertion's pass condition is so wide that a genuinely severe bug (charging customers ten times the intended amount) sails through it without any resistance. This is what makes vacuous assertions dangerous rather than just unhelpful: they don't merely fail to add confidence, they actively manufacture false confidence in the form of a green checkmark.",
          "test-runner-broken": "There's no evidence of a runner or configuration problem here — the test genuinely, correctly executes, and its assertion genuinely, correctly evaluates to `True` for the buggy result. The failure is in what the test chose to check, not in any tooling malfunction.",
          "tax-rate-ambiguous": "The multiplication bug (treating a percentage like `8.5` as though it were already a decimal fraction) is a real, specific, and common category of bug — the point of this exercise isn't to relitigate the source of the bug, it's to see exactly why the test failed to catch a bug that specific and that severe."
        },
        "mechanism": "A vacuous assertion has a pass condition wide enough to include both the correct behavior and a large space of incorrect behavior. The bug doesn't evade the test through any special cleverness — it simply falls inside the enormous region of results the assertion was never narrow enough to exclude in the first place. The fix is always the same: assert the actual expected value (`assert result == 108.50`), which a nine-times-too-large result would immediately and obviously fail."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC5-005"
      },
      {
        "type": "takeaway",
        "body": "A vacuous assertion doesn't fail to help — it actively manufactures a false green checkmark, because its pass condition is wide enough to include both the correct answer and a large space of badly wrong ones."
      },
      {
        "type": "pod-header",
        "title": "The other shape: tests that never reach the assertion at all",
        "podNumber": 2,
        "duration": 10
      },
      {
        "type": "text",
        "body": "A second, related shape worth naming separately: a test that wraps its call in a broad `try/except` and only asserts inside the success branch, silently passing (or reporting nothing meaningful) if an exception is raised instead. This produces green not because the code is correct, but because the test's own structure never gives a wrong result the chance to be checked — it either checks a correct result, or it doesn't check anything at all, and both outcomes look identical from outside the test file.\n\nThe practical version of this to watch for in generated test code specifically: a broad exception handler added defensively 'just in case,' around test logic that was meant to be a strict check. It's an easy pattern for a model to produce, because 'handle exceptions gracefully' is a common, generally-good instinct in application code — applied to test code, where the entire point is that a failure should be loud and visible, the same instinct becomes actively counterproductive."
      },
      {
        "type": "evidence-stack",
        "scenario": "Four generated test snippets.",
        "question": "Select every one where the test's structure would allow a real, wrong result to pass silently — not just a weak assertion, but a test that might never actually reach a check at all.",
        "items": [
          {
            "value": "try-except-pass",
            "label": "```python\ntry:\n    result = risky_calculation()\n    assert result == 42\nexcept Exception:\n    pass\n```",
            "applicable": true
          },
          {
            "value": "strict-equality",
            "label": "```python\nresult = calculate_discount(100, 0.1)\nassert result == 90\n```",
            "applicable": false
          },
          {
            "value": "assert-not-raises-only",
            "label": "```python\ntry:\n    process_payment(order)\nexcept Exception:\n    pytest.fail('should not raise')\n# no assertion on the actual outcome after the try block\n```",
            "applicable": true
          },
          {
            "value": "type-and-value-check",
            "label": "```python\nresult = get_user_status(user_id)\nassert isinstance(result, str)\nassert result in ('active', 'suspended', 'deleted')\n```",
            "applicable": false
          }
        ],
        "explanation": {
          "try-except-pass": "If `risky_calculation()` raises an exception for any reason — including a genuine bug that should be caught — the `except Exception: pass` silently swallows it, and the test reports as passed with the actual assertion (`result == 42`) never having been evaluated at all.",
          "strict-equality": "This asserts a specific, exact expected value with no escape hatch — any wrong result from `calculate_discount` fails this test loudly and specifically, exactly the shape of a meaningful assertion.",
          "assert-not-raises-only": "This test only ever checks that no exception was raised — it never asserts anything about whether the payment was actually processed correctly (the right amount, the right account, success recorded properly). A function that silently does nothing at all, without raising, would pass this test just as cleanly as a function that processes the payment perfectly.",
          "type-and-value-check": "This checks both the type and that the value is one of a specific, finite, meaningful set of valid outcomes — narrow enough that most wrong results (a typo'd status string, a None, a wrong type) would genuinely fail it, even though it doesn't pin down which exact status is expected for this specific user."
        },
        "synthesis": "The dangerous shape isn't 'has a try/except' or 'checks a type' in isolation — it's a test structure where a real, wrong outcome has no path to actually failing the test, whether because an exception silently escapes the check, or because the only thing verified is 'didn't crash' rather than 'did the right thing.'"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC5-006"
      },
      {
        "type": "takeaway",
        "body": "A test that swallows exceptions before its assertion runs, or that only ever checks 'didn't crash,' can pass for a function that's silently doing nothing or doing the wrong thing — check whether a wrong outcome has any actual path to failing the test."
      }
    ]
  },
  {
    "id": "aod-rc5-l04",
    "moduleId": "aod-rc5",
    "order": 4,
    "duration": 25,
    "difficulty": "medium",
    "title": "Mocks that make the test agree with the code, whatever the code does",
    "description": "A mock that stands in for a real dependency is only useful if it can disagree with the code under test — one that's built to always agree isn't testing anything, it's just watching itself nod.",
    "learningObjectives": [
      {
        "text": "Identify a mock configured to always match whatever the code under test expects, regardless of whether that expectation is correct",
        "bloomLevel": "analyze"
      },
      {
        "text": "Distinguish a test verifying real behavior from one verifying only that a mock was called in a particular way",
        "bloomLevel": "analyze"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "A mock that mirrors the code isn't checking the code",
        "podNumber": 1,
        "duration": 13
      },
      {
        "type": "hook",
        "body": "```python\ndef test_get_user_display_name(mocker):\n    mock_db = mocker.patch('app.db.fetch_user')\n    mock_db.return_value = {'first_name': 'Jordan', 'last_name': 'Lee'}\n    result = get_user_display_name(user_id=1)\n    assert result == 'Jordan Lee'\n```\n\nThis looks like a normal, reasonable use of mocking — stub out the database, check the function's logic in isolation. Whether it's actually a good test depends entirely on one thing this snippet alone can't show you: does `get_user_display_name`'s real implementation genuinely combine `first_name` and `last_name` with a space, correctly, or did whoever wrote the mock's return value already know what the implementation does and construct a mock specifically shaped to make it pass? A mock that's built by reading the implementation first, rather than by reading the actual requirement first, risks becoming a mirror the code checks itself against rather than an independent check."
      },
      {
        "type": "text",
        "body": "Mocking exists for a good reason: isolating a function from a real database, a real network call, or a real external service makes tests fast, deterministic, and independent of things outside your control. The risk this lesson is about isn't mocking itself — it's a mock configured with values chosen specifically to make the code under test pass, rather than values chosen to represent what a real dependency would actually, plausibly return, including its edge cases and its failure modes.\n\nThis risk is sharper in AI-generated test code for a specific, structural reason: a model asked to write a test for a function it just generated (or that's shown in the same context) has full visibility into that function's implementation, and producing a mock return value that happens to make the assertion pass is a much easier generation task than independently deriving what a real dependency's response actually looks like from its own documentation or contract. The result can be a test that's internally consistent — the mock and the code agree perfectly — while telling you nothing about whether either one is actually correct against the real world.\n\nThe reading habit: for any test using a mock, ask where the mock's return value came from. If it's traceable to the actual, documented shape of the real dependency's response — a real API's documented schema, a real database's actual column types — the mock is doing genuine work. If the mock's value seems to exist purely because it's exactly what makes this specific implementation's assertion pass, the test is closer to checking that the code agrees with itself than checking that the code is correct."
      },
      {
        "type": "diagnose-mechanism",
        "scenario": "A generated function has a real bug: it expects a payment gateway's response to have a field called `transaction_id`, but the actual gateway's real, documented API returns the field named `txn_id`. Its test:\n\n```python\ndef test_process_payment(mocker):\n    mock_gateway = mocker.patch('app.gateway.charge')\n    mock_gateway.return_value = {'transaction_id': 'tx_123', 'status': 'success'}\n    result = process_payment(order)\n    assert result.transaction_id == 'tx_123'\n```\n\nThis test passes. The function fails in production against the real gateway.",
        "question": "What's the actual mechanism by which this test passes despite a genuine, production-breaking bug?",
        "options": [
          {
            "value": "mock-shaped-to-match-bug",
            "label": "The mock's return value uses the field name `transaction_id` — matching the buggy code's expectation exactly, rather than `txn_id`, the real gateway's actual documented field name — so the test verifies the code agrees with a mock shaped like the bug, not that the code agrees with the real dependency"
          },
          {
            "value": "gateway-changed-recently",
            "label": "This must mean the real payment gateway recently changed its API and the test was written correctly against an older version"
          },
          {
            "value": "assertion-checks-wrong-field",
            "label": "The bug is that the assertion checks `.transaction_id` instead of checking the raw dictionary directly"
          }
        ],
        "correctValue": "mock-shaped-to-match-bug",
        "feedback": {
          "mock-shaped-to-match-bug": "Right. The mock's field name was set to match what the buggy code expects, not what the real gateway actually returns — whether that happened because the mock was written by reading the (buggy) implementation rather than the gateway's real documentation, or because a model generating both the code and its test made the identical field-naming assumption in both places. Either way, the test verifies internal agreement between two things that share the same wrong assumption, which is worse than not testing at all, because it produces false confidence specifically at the exact point where the real bug lives.",
          "gateway-changed-recently": "Nothing in the scenario suggests an API version change — the scenario states this as the gateway's actual, current, documented field name, which the code simply never matched to begin with.",
          "assertion-checks-wrong-field": "The assertion syntax itself isn't the problem — `result.transaction_id` is a completely reasonable way to check a result object's attribute. The actual defect is upstream: the mock supplies a field name that doesn't match reality, and the code that reads it was never checked against what the real dependency provides."
        },
        "mechanism": "When a mock's shape is derived from the code under test rather than from the real dependency's actual contract, the mock and the code can share the exact same wrong assumption — and a test comparing two things that agree with each other, rather than comparing the code against an independent, accurate stand-in for reality, cannot detect a bug that both of them share. This is the test-suite version of the earlier module's finding that a name and its docstring agreeing with each other proves nothing, since both come from the same generation pass."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC5-007"
      },
      {
        "type": "takeaway",
        "body": "A mock's return value needs to come from the real dependency's actual, documented contract — not from reading the implementation and reverse-engineering whatever would make its assertion pass — or the test can only ever catch bugs the mock doesn't also share."
      },
      {
        "type": "pod-header",
        "title": "Testing that a mock was called, versus testing the outcome",
        "podNumber": 2,
        "duration": 12
      },
      {
        "type": "text",
        "body": "A related, narrower risk: a test that verifies a mock was *called*, with certain arguments, but never verifies anything about the actual downstream outcome or the function's return value. This is sometimes genuinely the right thing to check — confirming a specific side effect happened is a legitimate test, when the side effect itself is the entire point (did we call the payment gateway at all, did we log the event). It becomes a problem when it's the *only* thing checked, standing in for verifying the function's actual observable behavior.\n\nThe distinguishing question: is verifying the mock call a check on a genuine, meaningful side effect (an email really was requested to be sent, a payment really was requested to be charged), or is it a check on an implementation detail that happens to be how this particular version of the code is currently structured (this specific internal helper function was called, in this specific order) — something that would break the test on a legitimate refactor that changes *how* the function achieves the same correct outcome, without the outcome itself ever being wrong."
      },
      {
        "type": "spot-flaw",
        "code": "def test_apply_discount(mocker):\n    mock_round = mocker.patch('app.pricing._round_currency')\n    apply_discount(cart, 0.1)\n    mock_round.assert_called_once()",
        "question": "What's the actual gap in what this test verifies?",
        "options": [
          {
            "value": "checks-internal-call-not-outcome",
            "label": "It only confirms that an internal helper function was called once — it never checks the actual discounted total that `apply_discount` produces, so a version of the function that calls `_round_currency` correctly but computes a completely wrong discount amount would still pass this test"
          },
          {
            "value": "mock-called-once-is-wrong-assertion",
            "label": "`assert_called_once()` is the wrong method — it should be `assert_called()` instead"
          },
          {
            "value": "test-is-fine",
            "label": "This is a perfectly sufficient test of the discount logic"
          }
        ],
        "correctValue": "checks-internal-call-not-outcome",
        "feedback": {
          "checks-internal-call-not-outcome": "Right. This test checks a private implementation detail — that a specific internal helper got called exactly once — and says nothing whatsoever about the actual value `apply_discount` produces. A version of the function with a completely wrong discount percentage, or a wrong base price, would call `_round_currency` exactly once regardless, and this test would still pass.",
          "mock-called-once-is-wrong-assertion": "The specific assertion method isn't the problem — whether it's `assert_called()` or `assert_called_once()`, both are checking the same category of thing (was an internal function invoked), not the actual computed result, which is the real gap.",
          "test-is-fine": "This test provides essentially no coverage of the discount calculation's correctness — it's checking an implementation detail (a private helper was invoked) rather than the actual, observable outcome (the discounted total), which is what a caller of `apply_discount` actually cares about."
        },
        "flawExplanation": "A meaningful version of this test would call `apply_discount(cart, 0.1)` and assert on its actual return value or the cart's resulting total — not on whether an internal, private helper function happened to be invoked. Testing the internal call instead of the outcome also makes the test brittle in the wrong direction: a legitimate refactor that computes the same correct discount a different way (without calling `_round_currency` at all) would break this test despite the actual behavior being perfectly correct — testing implementation structure instead of observable behavior."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC5-008"
      },
      {
        "type": "takeaway",
        "body": "Verifying a mock was called is only meaningful when the call itself is the actual point (a real side effect that matters) — when it stands in for checking the function's real output, it can pass for a badly wrong result and break on a harmless refactor."
      }
    ]
  },
  {
    "id": "aod-rc5-l05",
    "moduleId": "aod-rc5",
    "order": 5,
    "duration": 20,
    "difficulty": "medium",
    "title": "Coverage as a map of what nobody checked",
    "description": "Coverage percentage measures which lines executed during the test suite, not which lines had their behavior actually verified — the number is more reliably read backward than forward.",
    "learningObjectives": [
      {
        "text": "Explain precisely what a coverage percentage measures and what it doesn't",
        "bloomLevel": "understand"
      },
      {
        "text": "Use an uncovered line as a reliable signal, and a covered line's coverage status as an unreliable one, about test quality",
        "bloomLevel": "evaluate"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "What 'covered' actually means, mechanically",
        "podNumber": 1,
        "duration": 10
      },
      {
        "type": "hook",
        "body": "A generated PR proudly reports \"98% test coverage\" on a new payment-processing module. Inside that module, this exact line — from the mock-agreement scenario earlier in this module — is marked covered, green, executed:\n\n```python\nresult = process_payment(order)\nassert result.transaction_id == 'tx_123'\n```\n\nThat line ran. The coverage tool sees it ran and marks it green. The coverage tool has no way to know, and does not check, whether the assertion beneath it was vacuous, whether the mock feeding it was shaped to agree with a bug, or whether `'tx_123'` is even a value that could plausibly differ from what a wrong implementation would also produce. \"98% covered\" and \"98% correctly verified\" are different claims, and a coverage report only makes the first one."
      },
      {
        "type": "text",
        "body": "Code coverage is a mechanical measurement: which lines, branches, or statements were *executed* at least once while the test suite ran. That's genuinely useful information, read correctly — but 'executed' and 'checked' are different properties, and coverage tooling only measures the first one. A line can execute and be followed by an assertion that's vacuous (this module's third lesson), or by a mock so tightly shaped to the code's own assumptions that it can't disagree (the fourth lesson), and the coverage tool marks that line green regardless, because from its perspective, execution happened — it has no visibility into whether anything meaningful was verified about what happened.\n\nThis asymmetry is the single most important thing to internalize about a coverage number: an **uncovered** line is a reliable, strong signal — it means, unambiguously, that nothing in the test suite ever exercised that code path at all, full stop, no exceptions. A **covered** line is a much weaker signal — it means the line ran during some test, and says nothing about whether that test's assertions were capable of catching a wrong result if that line had produced one. Coverage percentage is far more trustworthy read as 'here is a map of what's definitely never been tested at all' than as 'here is a map of what's been properly verified.'\n\nThis matters specifically for AI-generated test suites because a high coverage number is a genuinely easy target for a model to hit — coverage-maximizing test generation, given a function, tends to produce tests that execute every branch (satisfying the coverage tool) without necessarily producing assertions specific enough to catch a wrong result on any of those branches, especially when combined with the vacuous-assertion and mirror-mock patterns from the last two lessons. A 95%+ coverage number on generated code is worth treating as 'nearly everything ran at least once,' not as 'nearly everything was actually verified.'"
      },
      {
        "type": "predict-number",
        "scenario": "A generated test suite for a 40-line pricing module reports 100% line coverage. On closer reading, 6 of its 12 tests use assertions this module's third and fourth lessons would flag as vacuous or mirror-mocked (checking `is not None`, checking a mock was called without checking the outcome, or using mock values derived from the implementation itself).",
        "question": "Given that half the tests are weak in this specific way, what's your best guess for what percentage of this module's actual, meaningful behavioral requirements (correct values under real conditions, not just 'ran without crashing') this suite genuinely verifies — despite the 100% line-coverage number?",
        "unit": "%",
        "actualValue": 50,
        "explanation": "There's no single correct number here, and that's the point — a reasonable estimate is well under the reported 100%, roughly in the neighborhood of what the strong half of the tests actually cover, since the weak half contributes execution (satisfying the coverage tool) without contributing much real verification. The exercise isn't about landing on an exact figure; it's about registering that '100% coverage' and 'this module's behavior is 100% verified' are simply different claims, and a large gap between them is entirely possible — even likely — in a suite where half the assertions are close to vacuous, regardless of what the coverage percentage reports."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC5-009"
      },
      {
        "type": "takeaway",
        "body": "An uncovered line is a strong, reliable signal that nothing was tested there at all. A covered line only means something executed — it says nothing about whether what ran was actually checked meaningfully, which is a completely separate question the coverage tool can't see."
      },
      {
        "type": "pod-header",
        "title": "Reading coverage alongside the assertions, not instead of them",
        "podNumber": 2,
        "duration": 10
      },
      {
        "type": "text",
        "body": "None of this makes coverage numbers worthless — used correctly, as a map of what's definitely never been touched, a coverage report is one of the fastest ways to find the parts of a generated module that have zero test evidence behind them at all, which is exactly where to start reading the implementation cold, with none of this module's earlier head-start techniques available. The mistake is treating a high coverage number as a substitute for actually reading a sample of the assertions themselves, the way this module's third and fourth lessons practiced.\n\nThe practical habit closing this module, and this whole phase: when handed a generated PR reporting high coverage, treat the number as an invitation to sample-check, not a reason to skip reading. Pick a handful of the most consequential covered lines — the ones handling money, security, or a genuinely tricky edge case — and read their actual backing assertions with the specific, narrow-vacuous-detecting eye this module has been building. A coverage report tells you where to look for the *absence* of testing reliably. It cannot tell you, on its own, where testing that exists is actually any good."
      },
      {
        "type": "case-sim",
        "title": "The 100% coverage report",
        "opening": "A generated PR for a discount-code redemption feature reports 100% line and branch coverage. It's late in a sprint, everyone's tired, and the coverage badge is green.",
        "start": "start",
        "nodes": {
          "start": {
            "situation": "You're the reviewer. The coverage report is the most visible piece of evidence in the PR.",
            "question": "What's your first move?",
            "choices": [
              {
                "value": "approve-on-coverage",
                "label": "Approve based on the 100% coverage figure — full coverage on a well-scoped feature is a strong signal",
                "consequence": "Two weeks later, a customer reports a discount code that should have been single-use got applied twice on the same order. The test suite's coverage was real — every line ran — but the assertion checking 'redeemed' status used `is not None` instead of checking the actual boolean value, missing the double-redemption bug entirely.",
                "ending": "missed"
              },
              {
                "value": "sample-the-money-path",
                "label": "Spend five minutes reading the actual assertions on the lines that touch money and redemption state specifically, ignoring the coverage number itself",
                "consequence": "You find exactly the vacuous `is not None` assertion on the redemption-state check, before merge.",
                "nextNode": "found-it"
              }
            ]
          },
          "found-it": {
            "situation": "You've found one vacuous assertion on a consequential line. The rest of the suite's coverage is real, but you now have reason to wonder about the rest.",
            "question": "What's the proportionate next step?",
            "choices": [
              {
                "value": "fix-just-this-one",
                "label": "Ask for just this one assertion to be strengthened, and approve the rest as-is based on the coverage number",
                "consequence": "The fix goes in, but a second vacuous assertion elsewhere in the same suite — on a related discount-stacking check — ships unnoticed, because the review stopped after finding one.",
                "ending": "partial"
              },
              {
                "value": "sample-more-broadly",
                "label": "Use the one finding as a reason to specifically sample a few more of the money- and state-related assertions across the same suite, not just fix the one found and stop",
                "consequence": "You find the second vacuous assertion on the discount-stacking check before merge, and both get fixed together.",
                "ending": "resolved"
              }
            ]
          }
        },
        "endings": {
          "missed": {
            "verdict": "costly",
            "summary": "A 100% coverage number was trusted as a substitute for reading assertions, and a genuinely serious bug — a discount code usable twice — shipped inside a fully green test suite.",
            "lesson": "Coverage measures execution, not verification. A green coverage badge on a suite full of vacuous assertions is a confident, entirely unearned signal."
          },
          "partial": {
            "verdict": "mixed",
            "summary": "The one vacuous assertion that was found got fixed, but finding one weak assertion is evidence the same author's pattern likely repeats elsewhere in the same suite — and it did.",
            "lesson": "One vacuous assertion found is a sample result, not a complete audit — the same weak pattern that produced it is likely to have produced others nearby, worth checking rather than assuming it was isolated."
          },
          "resolved": {
            "verdict": "good",
            "summary": "The initial finding prompted a broader, still-proportionate sample of the suite's most consequential assertions, catching a second real gap before it shipped.",
            "lesson": "A coverage number tells you where nothing was tested. Reading the actual assertions, especially once one weak pattern is found, is what tells you whether what was tested actually verifies anything — and finding one instance of a pattern is a reason to look for more, not a reason to stop."
          }
        }
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC5-010"
      },
      {
        "type": "takeaway",
        "body": "Use coverage to find what's definitely untested, then read the assertions on the most consequential covered lines directly — a coverage number alone can't tell you whether what ran was actually checked for anything real."
      }
    ]
  },
  {
    "id": "aod-rc5-p01",
    "moduleId": "aod-rc5",
    "order": 6,
    "duration": 30,
    "difficulty": "medium",
    "title": "Practice — Spot the Tautological Assertion in Eight Generated Tests",
    "description": "Eight drills, no new concepts: reading tests for the requirement behind them, spotting vacuous assertions, mirror-mocks, and what coverage does and doesn't tell you.",
    "learningObjectives": [
      {
        "text": "Apply test-reading and vacuous-assertion detection across unfamiliar generated tests at speed",
        "bloomLevel": "apply"
      },
      {
        "text": "Identify the specific wrong result an assertion, mock, or coverage number would fail to catch",
        "bloomLevel": "analyze"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "Eight drills",
        "podNumber": 1,
        "duration": 30
      },
      {
        "type": "hook",
        "body": "Nothing new here. Eight drills across the module's skills, ordered from where the lessons ended to one step past it. Do them in one sitting or spread across a few — nothing here expires."
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC5-P01"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC5-P02"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC5-P03"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC5-P04"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC5-P05"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC5-P06"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC5-P07"
      },
      {
        "type": "exercise",
        "exerciseRef": "AOD-RC5-P08"
      },
      {
        "type": "takeaway",
        "body": "If a drill needed a hint, that's what to watch for in the checkpoint — not a reason to redo the set."
      }
    ]
  },
  {
    "id": "aod-rc5-lab",
    "moduleId": "aod-rc5",
    "order": 7,
    "duration": 50,
    "difficulty": "hard",
    "title": "Lab — Review a Generated Test Suite for Tests That Cannot Fail",
    "description": "Take a real test suite and find every test that would pass regardless of whether the code is correct — then write down what should replace each one.",
    "learningObjectives": [
      {
        "text": "Apply vacuous-assertion detection, mirror-mock detection, and coverage-reading discipline to a real test suite",
        "bloomLevel": "evaluate"
      },
      {
        "text": "Produce a review naming every vacuous test with a specific, stronger replacement assertion",
        "bloomLevel": "create"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "The lab",
        "podNumber": 1,
        "duration": 50
      },
      {
        "type": "hook",
        "body": "Every lesson this module worked one test-reading risk at a time. A real generated test suite usually has several of these mixed together — a genuinely solid test next to a vacuous one next to a mirror-mocked one, all reporting the same green checkmark. This lab is a full pass over one real suite, specifically hunting for every test that cannot fail, and closing this whole phase's arc: from reading a single function for intent, to tracing its control flow, to checking the shape of its data, to weighing its dependencies, to — finally — checking whether the tests that are supposed to back all of it up actually verify anything at all."
      },
      {
        "type": "lab-brief",
        "labId": "aod-rc5-vacuous-test-review",
        "title": "Review a test suite for tests that cannot fail",
        "brief": "Pick a real test file — from your own project, an AI-generated one you haven't scrutinized, or an open-source project's test suite — with at least eight tests covering real logic (not pure configuration or trivial getters). If nothing fits, generate a small module and its test suite now for a plausible feature (an order-total calculator, a permissions checker, a scheduling function) and treat the generated tests as your subject.",
        "deliverable": "A review naming every test in the suite that would pass regardless of whether the underlying code is correct — vacuous assertions, mirror-mocked dependencies, or tests that only check 'didn't crash' — with the specific, stronger assertion that should replace each one.",
        "stages": [
          {
            "title": "Map the suite and extract requirements",
            "minutes": 15,
            "instructions": "For each test, decompose its arrangement and assertion the way this module practiced, and write down the specific requirement it appears to encode. Note where a test's name claims more than its assertion actually checks.",
            "checkpoint": "Every test has a one-line extracted requirement, and any name-versus-scope gaps are flagged."
          },
          {
            "title": "Hunt for vacuous assertions and mirror-mocks",
            "minutes": 20,
            "instructions": "For each assertion, ask what wrong value would still satisfy it — flag anything close to vacuous. For any mocks, trace whether their values come from a real, independent source or from the code under test itself.",
            "checkpoint": "Every test is marked as either genuinely verifying something, or flagged with a specific reason it's weak — not a vague 'this seems off.'"
          },
          {
            "title": "Write the deliverable and self-check",
            "minutes": 15,
            "instructions": "For every flagged test, write the specific stronger assertion that should replace it — a real expected value, a check on an actual side effect rather than an internal call, or a mock value sourced correctly. Compress into the deliverable and self-check against the rubric.",
            "checkpoint": "Every flagged test has a specific, concrete replacement assertion, not just a note that it's weak."
          }
        ],
        "rubric": [
          {
            "criterion": "Every test in the suite is accounted for, not just a sample",
            "meets": "The review covers the whole file, even if most entries are quick 'this one's fine, here's why' notes."
          },
          {
            "criterion": "Each flagged test names the specific wrong value or bug it would fail to catch",
            "meets": "The flag isn't just 'this is vacuous' — it names a concrete wrong result that would still pass."
          },
          {
            "criterion": "Mirror-mocks are checked for, not just vacuous assertions",
            "meets": "Any mocked dependency has its value's source explicitly traced, even if the conclusion is 'this one is sourced correctly.'"
          },
          {
            "criterion": "Every flagged test has a specific replacement assertion",
            "meets": "The fix names an actual expected value or check, not a general instruction like 'make this stronger.'"
          },
          {
            "criterion": "The review distinguishes genuinely solid tests from flagged ones explicitly",
            "meets": "A reader can tell, for every single test, whether it passed the review or why it didn't — nothing is left ambiguous."
          }
        ]
      },
      {
        "type": "takeaway",
        "body": "A green test suite and a correct codebase are two different claims — the gap between them is exactly the set of tests that were never actually capable of failing, and finding them is the last, and maybe most consequential, reading skill this phase built."
      }
    ]
  },
  {
    "id": "aod-rc5-cp",
    "moduleId": "aod-rc5",
    "order": 8,
    "duration": 10,
    "difficulty": "easy",
    "title": "Checkpoint — Tests as a Reading Tool",
    "description": "Ten questions: four from this module, four from Dependencies and What They Cost, two from Data Shapes and Boundaries. Ungraded, untimed.",
    "learningObjectives": [
      {
        "text": "Retrieve this module's core distinctions alongside earlier modules', without looking them up",
        "bloomLevel": "remember"
      }
    ],
    "contentSections": [
      {
        "type": "pod-header",
        "title": "Let's see what stuck",
        "podNumber": 1,
        "duration": 10
      },
      {
        "type": "retrieval-check",
        "title": "Checkpoint — Tests as a Reading Tool",
        "questions": [
          {
            "from": "This module — Tests as a Reading Tool",
            "prompt": "Why is a passing test structurally more trustworthy than an accurate-looking comment, at the moment you read either one?",
            "options": [
              {
                "value": "a1",
                "label": "A test is re-verified every time the build runs and fails loudly if it stops being true; a comment can silently drift from the code with nothing checking it"
              },
              {
                "value": "a2",
                "label": "Comments are always written before the code, while tests are always written after"
              },
              {
                "value": "a3",
                "label": "Tests are shorter than comments, so they're less likely to contain an error"
              }
            ],
            "correctValue": "a1",
            "explanation": "This is the module's opening distinction — a test's relationship to ongoing truth is mechanically different from a comment's, regardless of how each currently reads."
          },
          {
            "from": "This module — Tests as a Reading Tool",
            "prompt": "A test named `test_handles_invalid_input` asserts on exactly one malformed string. What does this test actually establish?",
            "options": [
              {
                "value": "b1",
                "label": "Only that this one specific input is handled as expected — nothing about the broader category the name implies"
              },
              {
                "value": "b2",
                "label": "That the function handles all forms of invalid input correctly, since the name states it generally"
              },
              {
                "value": "b3",
                "label": "That the function's error handling is broken"
              }
            ],
            "correctValue": "b1",
            "explanation": "A test's name is a claim, not a guarantee about its assertion's actual scope — one example only establishes that one example."
          },
          {
            "from": "This module — Tests as a Reading Tool",
            "prompt": "Why is `assert result > 0` dangerous rather than just unhelpful, for a function whose bug produces a wildly inflated (but still positive) number?",
            "options": [
              {
                "value": "c1",
                "label": "Its pass condition is wide enough to include both the correct answer and a large space of badly wrong ones, so it produces a false, unearned green checkmark rather than just adding no value"
              },
              {
                "value": "c2",
                "label": "It's dangerous because it will always fail even for correct code"
              },
              {
                "value": "c3",
                "label": "It only becomes dangerous when combined with a try/except block"
              }
            ],
            "correctValue": "c1",
            "explanation": "The tax-calculation example showed a nearly-tenfold overcharge sailing through `result > 0` untouched — the assertion manufactures false confidence, not just a missed opportunity."
          },
          {
            "from": "This module — Tests as a Reading Tool",
            "prompt": "What does an uncovered line in a coverage report tell you, compared to a covered line?",
            "options": [
              {
                "value": "d1",
                "label": "An uncovered line reliably means nothing was tested there at all; a covered line only means something executed, saying nothing about whether it was checked meaningfully"
              },
              {
                "value": "d2",
                "label": "Both are equally reliable indicators of test quality"
              },
              {
                "value": "d3",
                "label": "A covered line is always backed by a meaningful assertion, by definition of the coverage tool"
              }
            ],
            "correctValue": "d1",
            "explanation": "This asymmetry is the module's closing point — uncovered is a strong, nearly unambiguous signal; covered is a much weaker one that requires actually reading the assertion to interpret."
          },
          {
            "from": "Previous module — Dependencies and What They Cost",
            "prompt": "Why does a model reach for a library like lodash for a task a few lines of standard-library code could handle?",
            "options": [
              {
                "value": "e1",
                "label": "It's producing the statistically common shape of code for this task from its training data — not weighing this project's specific concerns"
              },
              {
                "value": "e2",
                "label": "Because dependencies always make code run faster"
              },
              {
                "value": "e3",
                "label": "Because using the standard library is considered deprecated practice"
              }
            ],
            "correctValue": "e1",
            "explanation": "The `_.pick` example showed a model reaching for a familiar library out of statistical habit, not a deliberate tradeoff this specific project needed made."
          },
          {
            "from": "Previous module — Dependencies and What They Cost",
            "prompt": "What's the mechanism that makes 'slopsquatting' a distinct risk from an ordinary malicious package upload?",
            "options": [
              {
                "value": "f1",
                "label": "The target package name is predictable in advance, because models tend to hallucinate similar plausible names repeatedly, giving an attacker a name to specifically watch for and register"
              },
              {
                "value": "f2",
                "label": "It only ever affects already-popular packages"
              },
              {
                "value": "f3",
                "label": "It requires guessing a common human typo"
              }
            ],
            "correctValue": "f1",
            "explanation": "The `requests_retry_toolkit` scenario showed a hallucinated name becoming a discoverable, researchable target — distinct from typosquatting's reliance on typing mistakes."
          },
          {
            "from": "Previous module — Dependencies and What They Cost",
            "prompt": "What does the AGPL license's distinguishing clause specifically cover that GPL alone doesn't?",
            "options": [
              {
                "value": "g1",
                "label": "Software run as a network service, even when never literally distributed as a copy to users"
              },
              {
                "value": "g2",
                "label": "Software installed only on a user's personal device"
              },
              {
                "value": "g3",
                "label": "Internal-only developer tooling"
              }
            ],
            "correctValue": "g1",
            "explanation": "The AGPL specifically closes the network-service gap the regular GPL doesn't cover — relevant to nearly any modern SaaS product."
          },
          {
            "from": "Previous module — Dependencies and What They Cost",
            "prompt": "What's the actual ongoing cost a project takes on the moment a new dependency is added?",
            "options": [
              {
                "value": "h1",
                "label": "A small, recurring, easy-to-forget claim on future attention — eventual upgrades, breaking-change migrations, and security response"
              },
              {
                "value": "h2",
                "label": "Nothing beyond the initial install time"
              },
              {
                "value": "h3",
                "label": "A one-time cost that's fully paid off once the code passes review"
              }
            ],
            "correctValue": "h1",
            "explanation": "The install is the cheapest moment in the whole relationship — the real cost is deferred and diffuse, which is exactly why it's easy to underweight during a correctness-focused review."
          },
          {
            "from": "Two modules back — Data Shapes and Boundaries",
            "prompt": "A sensitive function has no internal validation and one currently-validated caller. What does this module say about its actual safety?",
            "options": [
              {
                "value": "i1",
                "label": "Its safety is entirely borrowed from that one caller, and remains safe only as long as every future caller independently re-implements the same checks"
              },
              {
                "value": "i2",
                "label": "It's permanently safe, since the current caller was already reviewed"
              },
              {
                "value": "i3",
                "label": "Validation should never happen at entry points"
              }
            ],
            "correctValue": "i1",
            "explanation": "The `process_transfer` case-sim showed a second caller reaching the same unvalidated function without the original route's checks."
          },
          {
            "from": "Two modules back — Data Shapes and Boundaries",
            "prompt": "Why does `data as User` (a TypeScript type assertion) provide no real protection against a malformed API response?",
            "options": [
              {
                "value": "j1",
                "label": "A type assertion is a compile-time-only instruction with zero runtime effect — nothing actually inspects the real value's shape"
              },
              {
                "value": "j2",
                "label": "TypeScript assertions only work for primitive types"
              },
              {
                "value": "j3",
                "label": "It works fine as long as the API is generally reliable"
              }
            ],
            "correctValue": "j1",
            "explanation": "The `getUser` example traced this exactly — the assertion told the compiler to stop checking, and the resulting crash happened downstream, far from the actual mismatch."
          }
        ]
      }
    ]
  }
]
