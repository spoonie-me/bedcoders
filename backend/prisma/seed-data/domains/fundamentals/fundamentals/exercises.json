[
  {
    "ref": "FUNDAMENTALS-FN-001",
    "moduleId": "fund-m1",
    "lessonId": "fund-l01",
    "type": "MULTIPLE_CHOICE",
    "prompt": "What does DRY stand for, and why does it matter?",
    "config": {
      "options": [
        {
          "text": "Don't Repeat Yourself — reduces bugs and makes code easier to maintain",
          "correct": true
        },
        {
          "text": "Define Reusable Yields — a way to write async functions",
          "correct": false
        },
        {
          "text": "Don't Run Yourself — let the computer do the work",
          "correct": false
        },
        {
          "text": "Dynamic Runtime Yield — a performance optimization",
          "correct": false
        }
      ]
    },
    "hints": [
      "Think about what happens when you copy code and later find a bug"
    ],
    "explanation": "DRY = Don't Repeat Yourself. When you copy-paste code, you create multiple versions to maintain. When a bug exists in that logic, you have to find and fix every copy. A function means you fix it once.",
    "bloomLevel": "remember",
    "difficulty": 1,
    "xpReward": 10,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FUNDAMENTALS-FN-002",
    "moduleId": "fund-m1",
    "lessonId": "fund-l01",
    "type": "OPEN_ENDED",
    "prompt": "Sarah has this code appearing 3 times in her app:\n```js\nconst discounted = price - (price * 0.15);\nconst formatted = `€${discounted.toFixed(2)}`;\n```\n\nRefactor this into a single reusable function called `applyDiscount`. It should:\n- Take `price` as the first parameter\n- Take `rate` as the second parameter (default: 0.15)\n- Return the formatted discounted price string\n\nThen show how you'd call it.",
    "hints": [
      "Arrow function syntax: const name = (param1, param2 = default) => { ... }",
      "Use a template literal with .toFixed(2) for the formatted price",
      "The function should return the string, not console.log it"
    ],
    "explanation": "```js\nconst applyDiscount = (price, rate = 0.15) => {\n  const discounted = price - (price * rate);\n  return `€${discounted.toFixed(2)}`;\n};\n\napplyDiscount(100);       // '€85.00'\napplyDiscount(100, 0.20); // '€80.00'\n```\n\nNow when the discount logic changes, you update one function. All 3 call sites automatically get the fix.",
    "bloomLevel": "apply",
    "difficulty": 2,
    "xpReward": 25,
    "isKnowledgeCheck": false
  },
  {
    "ref": "FUNDAMENTALS-FN-003",
    "moduleId": "fund-m1",
    "lessonId": "fund-l02",
    "type": "MULTIPLE_CHOICE",
    "prompt": "What's the output of this code?\n```js\nconst x = 10;\nconst double = (x) => x * 2;\nconsole.log(double(5));\nconsole.log(x);\n```",
    "config": {
      "options": [
        {
          "text": "20, then 10",
          "correct": false
        },
        {
          "text": "10, then 10",
          "correct": false
        },
        {
          "text": "10, then 20",
          "correct": false
        },
        {
          "text": "10, then 10 — but the parameter `x` inside double shadows the outer `x`",
          "correct": true
        }
      ]
    },
    "hints": [
      "The `x` in `double(x)` is a parameter — it's a local variable inside the function",
      "What happens to the outer `x` when the function modifies its own `x`?"
    ],
    "explanation": "`double(5)` returns 10 (5 * 2). The `x` parameter inside `double` is separate from the outer `const x = 10`. The function parameter *shadows* the outer variable within the function scope. Outer `x` remains 10 throughout.",
    "bloomLevel": "understand",
    "difficulty": 2,
    "xpReward": 15,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FUNDAMENTALS-FN-004",
    "moduleId": "fund-m1",
    "lessonId": "fund-l02",
    "type": "OPEN_ENDED",
    "prompt": "Alex's AI pipeline processes messages. Write a function called `processMessage` that:\n- Takes an options object with: `{ message, model = 'claude-3', maxTokens = 1000, temperature = 0.7 }`\n- Validates that `message` is not empty (return `null` if it is)\n- Returns an object with the processed config: `{ message, model, maxTokens, temperature, timestamp: new Date().toISOString() }`\n\nUse destructuring in the parameters.",
    "hints": [
      "Destructure the options object in the function signature: `({ message, model = 'claude-3', ... })`",
      "Check if message is empty: `!message || message.trim() === ''`",
      "Return null early if invalid, then return the config object"
    ],
    "explanation": "```js\nconst processMessage = ({ message, model = 'claude-3', maxTokens = 1000, temperature = 0.7 }) => {\n  if (!message || message.trim() === '') return null;\n  return {\n    message,\n    model,\n    maxTokens,\n    temperature,\n    timestamp: new Date().toISOString()\n  };\n};\n\nprocessMessage({ message: 'Hello' });\n// { message: 'Hello', model: 'claude-3', maxTokens: 1000, temperature: 0.7, timestamp: '...' }\n```\n\nDestructuring + defaults in the parameter list is a clean pattern for option-heavy functions.",
    "bloomLevel": "apply",
    "difficulty": 3,
    "xpReward": 25,
    "isKnowledgeCheck": false
  },
  {
    "ref": "FUNDAMENTALS-DEBUG-001",
    "moduleId": "fund-m2",
    "lessonId": "fund-l03",
    "type": "MULTIPLE_CHOICE",
    "prompt": "You see this error:\n```\nReferenceError: Cannot access 'total' before initialization\n```\nWhat most likely caused it?",
    "config": {
      "options": [
        {
          "text": "The variable `total` was never declared",
          "correct": false
        },
        {
          "text": "`total` is declared with `const` or `let` but used before the declaration line runs",
          "correct": true
        },
        {
          "text": "The variable `total` is spelled incorrectly somewhere",
          "correct": false
        },
        {
          "text": "`total` is inside a function and can't be accessed outside",
          "correct": false
        }
      ]
    },
    "hints": [
      "'Before initialization' is a specific clue — the variable exists but hasn't been set yet",
      "This happens with const/let but NOT with var (var is hoisted differently)"
    ],
    "explanation": "This is the 'temporal dead zone' error. `const` and `let` declarations are hoisted to the top of their scope, but they're not initialized until the declaration line runs. Accessing them before that line causes this error. Fix: move the declaration before the first use.",
    "bloomLevel": "understand",
    "difficulty": 2,
    "xpReward": 15,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FUNDAMENTALS-DEBUG-002",
    "moduleId": "fund-m2",
    "lessonId": "fund-l03",
    "type": "OPEN_ENDED",
    "prompt": "This code has a bug. Read the error, identify the problem, and fix it:\n\n```js\nconst users = [\n  { id: 1, name: 'Sarah', plan: 'pro' },\n  { id: 2, name: 'Marcus', plan: 'free' }\n];\n\nconst proUser = users.find(u => u.plan === 'pro');\nconsole.log(`Pro user: ${proUser.name}, ID: ${proUser.id}`);\n\nconst freeUser = users.find(u => u.plan === 'team');\nconsole.log(`Team user: ${freeUser.name}`);\n// TypeError: Cannot read properties of undefined (reading 'name')\n```\n\nExplain: (1) why the error occurs, (2) how to fix it safely.",
    "hints": [
      ".find() returns undefined when no match is found — there's no 'team' plan user",
      "Always check if .find() returned a result before accessing properties",
      "Use optional chaining (?.) or an if check"
    ],
    "explanation": "`users.find(u => u.plan === 'team')` returns `undefined` because no user has plan 'team'. Then `undefined.name` throws TypeError.\n\nFix:\n```js\nconst freeUser = users.find(u => u.plan === 'team');\nif (freeUser) {\n  console.log(`Team user: ${freeUser.name}`);\n} else {\n  console.log('No team user found');\n}\n// Or with optional chaining:\nconsole.log(`Team user: ${freeUser?.name ?? 'not found'}`);\n```\n\nAlways guard against `.find()` returning `undefined`.",
    "bloomLevel": "apply",
    "difficulty": 3,
    "xpReward": 25,
    "isKnowledgeCheck": false
  },
  {
    "ref": "FUNDAMENTALS-DEBUG-003",
    "moduleId": "fund-m2",
    "lessonId": "fund-l04",
    "type": "SEQUENCING",
    "prompt": "Put the binary search debugging steps in the correct order:",
    "config": {
      "items": [
        "Identify the broken behavior (what should happen vs. what does happen)",
        "Add a console.log at the midpoint of suspicious code",
        "Based on the result, eliminate half the code from suspicion",
        "Repeat until you've isolated the exact line",
        "Fix the bug and remove debug logs"
      ],
      "correctOrder": [
        0,
        1,
        2,
        3,
        4
      ]
    },
    "hints": [
      "You need to know what's broken before you can search for it"
    ],
    "explanation": "Binary search debugging always starts with a clear problem statement. Then: find middle, check there, eliminate half, repeat. The last step (remove debug logs) is critical — shipping code full of console.logs is unprofessional and can leak sensitive data.",
    "bloomLevel": "apply",
    "difficulty": 2,
    "xpReward": 15,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FUNDAMENTALS-DEBUG-004",
    "moduleId": "fund-m2",
    "lessonId": "fund-l04",
    "type": "OPEN_ENDED",
    "prompt": "Marcus has a function that should calculate a running total for a shopping cart, but it always returns the wrong number. Show what strategic console.logs you'd add to debug this function:\n\n```js\nconst calculateTotal = (items) => {\n  let total = 0;\n  for (const item of items) {\n    total += item.price * item.quantity;\n  }\n  return total;\n};\n```\n\nDon't fix the bug — just show what logging you'd add and what you'd look for in the output.",
    "hints": [
      "Check the input first — is `items` what you expect?",
      "Log inside the loop to see each iteration",
      "Check the running total after each item"
    ],
    "explanation": "```js\nconst calculateTotal = (items) => {\n  console.log('INPUT items:', JSON.stringify(items, null, 2));\n  let total = 0;\n  for (const item of items) {\n    console.log(`Processing: ${item.name}, price: ${item.price}, qty: ${item.quantity}, type: ${typeof item.price}`);\n    total += item.price * item.quantity;\n    console.log(`Running total: ${total}`);\n  }\n  console.log('FINAL total:', total);\n  return total;\n};\n```\n\nCommon bugs this would reveal: `item.price` is a string (typeof shows 'string'), `item.quantity` is undefined, or an item is missing the `price` property.",
    "bloomLevel": "apply",
    "difficulty": 3,
    "xpReward": 25,
    "isKnowledgeCheck": false
  },
  {
    "ref": "FUNDAMENTALS-ALGO-001",
    "moduleId": "fund-m3",
    "lessonId": "fund-l05",
    "type": "MATCHING",
    "prompt": "Match each problem to the array method pattern it uses:",
    "config": {
      "pairs": [
        {
          "left": "Find the user with email 'sarah@test.com'",
          "right": ".find()"
        },
        {
          "left": "Convert all prices from USD to EUR",
          "right": ".map()"
        },
        {
          "left": "Keep only active subscriptions",
          "right": ".filter()"
        },
        {
          "left": "Calculate the total price of all items",
          "right": ".reduce()"
        }
      ]
    },
    "hints": [
      "find = one item, map = transform all, filter = keep some, reduce = combine into one value"
    ],
    "explanation": "The four fundamental array operations: find (get one matching item), map (transform every item), filter (keep items that match), reduce (combine all items into a single value). Recognizing which pattern fits a problem is a core skill.",
    "bloomLevel": "understand",
    "difficulty": 2,
    "xpReward": 15,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FUNDAMENTALS-ALGO-002",
    "moduleId": "fund-m3",
    "lessonId": "fund-l05",
    "type": "OPEN_ENDED",
    "prompt": "Alex has a list of API response times (in ms) and needs to:\n1. Filter out responses that took more than 2000ms (outliers)\n2. Calculate the average response time of the remaining\n3. Log: 'Average response time: Xms (based on N requests)'\n\nData: `const times = [120, 340, 89, 2400, 567, 1890, 234, 3200, 445, 678]`\n\nWrite this using `.filter()` and `.reduce()`. No for loops.",
    "hints": [
      "Filter first: keep only times <= 2000",
      "Sum with reduce: (total, time) => total + time, starting at 0",
      "Average = sum / count",
      "Use .length on the filtered array for N"
    ],
    "explanation": "```js\nconst times = [120, 340, 89, 2400, 567, 1890, 234, 3200, 445, 678];\n\nconst valid = times.filter(t => t <= 2000);\nconst sum = valid.reduce((total, t) => total + t, 0);\nconst avg = Math.round(sum / valid.length);\n\nconsole.log(`Average response time: ${avg}ms (based on ${valid.length} requests)`);\n// Average response time: 595ms (based on 8 requests)\n```\n\nThe chain filter → reduce is a very common data processing pattern. Real-world analytics pipelines use this constantly.",
    "bloomLevel": "apply",
    "difficulty": 3,
    "xpReward": 30,
    "isKnowledgeCheck": false
  },
  {
    "ref": "FUNDAMENTALS-DATA-001",
    "moduleId": "fund-m3",
    "lessonId": "fund-l06",
    "type": "MULTIPLE_CHOICE",
    "prompt": "You need to store 500 users and frequently look them up by their user ID. Which data structure is most efficient?",
    "config": {
      "options": [
        {
          "text": "An array: users = [{ id: 1, name: 'Sarah' }, ...]",
          "correct": false
        },
        {
          "text": "An object keyed by ID: users = { '1': { name: 'Sarah' }, ... }",
          "correct": true
        },
        {
          "text": "A string with all users serialized as JSON",
          "correct": false
        },
        {
          "text": "Nested arrays: [[1, 'Sarah'], [2, 'Marcus']]",
          "correct": false
        }
      ]
    },
    "hints": [
      "With an array, how do you find user with ID 487 efficiently?",
      "Objects allow instant lookup by key — no searching needed"
    ],
    "explanation": "Object keyed by ID gives O(1) lookup — instant, regardless of how many users exist. Array lookup requires scanning (O(n) — slower as the list grows). When you need fast lookup by a unique identifier, an object (or Map) is almost always better.",
    "bloomLevel": "analyze",
    "difficulty": 2,
    "xpReward": 15,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FUNDAMENTALS-DATA-002",
    "moduleId": "fund-m3",
    "lessonId": "fund-l06",
    "type": "OPEN_ENDED",
    "prompt": "You receive this API response:\n```js\nconst orders = [\n  { id: 'o1', user: 'Sarah', items: 3, total: 89.99, status: 'delivered' },\n  { id: 'o2', user: 'Marcus', items: 1, total: 12.00, status: 'pending' },\n  { id: 'o3', user: 'Sarah', items: 5, total: 234.50, status: 'delivered' },\n  { id: 'o4', user: 'Alex', items: 2, total: 45.00, status: 'pending' }\n];\n```\n\nWrite code that:\n1. Filters to only delivered orders\n2. Gets the total revenue from delivered orders\n3. Finds Sarah's orders specifically\n4. Logs a summary: 'Delivered: N orders, €X revenue. Sarah: N orders.'",
    "hints": [
      "Step 1: .filter(o => o.status === 'delivered')",
      "Step 2: .reduce() to sum the totals",
      "Step 3: .filter(o => o.user === 'Sarah') — filter original or delivered array?",
      "Use .toFixed(2) for the revenue amount"
    ],
    "explanation": "```js\nconst delivered = orders.filter(o => o.status === 'delivered');\nconst revenue = delivered.reduce((sum, o) => sum + o.total, 0);\nconst sarahOrders = orders.filter(o => o.user === 'Sarah');\n\nconsole.log(`Delivered: ${delivered.length} orders, €${revenue.toFixed(2)} revenue. Sarah: ${sarahOrders.length} orders.`);\n// Delivered: 2 orders, €324.49 revenue. Sarah: 2 orders.\n```\n\nThis pattern — filter, then aggregate — is at the core of every reporting and analytics feature you'll ever build.",
    "bloomLevel": "apply",
    "difficulty": 3,
    "xpReward": 30,
    "isKnowledgeCheck": false
  },
  {
    "ref": "FND-BB1-001",
    "moduleId": "fnd-bb1",
    "lessonId": "fnd-bb1-l01",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nlet spoons = 12;\nspoons = 9;\nconsole.log(spoons);\n```\nWhat prints?",
    "config": {
      "options": [
        {
          "text": "12",
          "correct": false
        },
        {
          "text": "9",
          "correct": true
        },
        {
          "text": "12 9",
          "correct": false
        },
        {
          "text": "An error — you can't set spoons twice",
          "correct": false
        }
      ]
    },
    "hints": [
      "`console.log` runs last, after both lines above it have already happened.",
      "The second line doesn't add a second value next to the first — it replaces what `spoons` was holding."
    ],
    "explanation": "9. The first line creates `spoons` holding 12. The second line reassigns it to 9 — the 12 isn't kept anywhere; it's simply overwritten. By the time `console.log` runs, `spoons` has only ever been 9 as far as that line can see. `let` explicitly allows a variable to be reassigned like this — that's the entire point of the keyword.",
    "bloomLevel": "apply",
    "difficulty": 1,
    "xpReward": 8,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB1-002",
    "moduleId": "fnd-bb1",
    "lessonId": "fnd-bb1-l01",
    "type": "TRUE_FALSE_JUSTIFY",
    "prompt": "True or false: once you write `let total = 20;`, the number 20 is permanently glued to the name `total` for the rest of the program.",
    "config": {
      "statement": "Once a variable is declared with a value, that value never changes for the rest of the program.",
      "correctAnswer": false,
      "justificationRequired": true,
      "rubric": "A full answer states false and explains that `total` is a name pointing at a current value, which a later line can replace with `=`."
    },
    "hints": [
      "Is `total` the number itself, or a label you can point at a different number later?",
      "What would `let` even be for, if a variable's first value were permanent?"
    ],
    "explanation": "False. `total` is a name that currently points at 20 — it isn't the number 20 welded in place. Any later line can reassign it with `=`, and from that point on, `total` points at the new value instead. If a value really needed to be permanent, JavaScript has a separate keyword for that (`const`) — the fact that `let` exists specifically to allow change is the tell that a `let` variable's value was never meant to be frozen.",
    "bloomLevel": "evaluate",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB1-003",
    "moduleId": "fnd-bb1",
    "lessonId": "fnd-bb1-l02",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nlet age = 32;\nlet ageText = \"32\";\nconsole.log(age === ageText);\n```\nWhat prints?",
    "config": {
      "options": [
        {
          "text": "true — they're both 32",
          "correct": false
        },
        {
          "text": "false — one is a number and one is text",
          "correct": true
        },
        {
          "text": "32",
          "correct": false
        },
        {
          "text": "An error, because you can't compare a number to text",
          "correct": false
        }
      ]
    },
    "hints": [
      "Look closely at `ageText` — is there anything around the 32 that changes what kind of value it is?",
      "`===` checks type as well as value. Same digits, different type, counts as not equal."
    ],
    "explanation": "false. `age` is the number 32. `ageText` is the *text* \"32\" — the quotes make it a string, which JavaScript treats as a completely different kind of value from a number, even though it looks identical on screen. `===` (triple equals) checks both value and type, so a number and a string holding the \"same\" digits are still not equal. This is exactly the kind of thing that looks like a bug in your reading, not your code, until you know quotes are the tell.",
    "bloomLevel": "analyze",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB1-004",
    "moduleId": "fnd-bb1",
    "lessonId": "fnd-bb1-l02",
    "type": "MULTIPLE_CHOICE",
    "prompt": "Which of these creates a boolean (true/false) value?",
    "config": {
      "options": [
        {
          "text": "let loggedIn = \"true\";",
          "correct": false
        },
        {
          "text": "let loggedIn = true;",
          "correct": true
        },
        {
          "text": "let loggedIn = 1;",
          "correct": false
        },
        {
          "text": "let loggedIn = \"yes\";",
          "correct": false
        }
      ]
    },
    "hints": [
      "Quotes turn anything inside them into text, including the words true and false.",
      "1 is a real, valid value — but which of the four types is it?"
    ],
    "explanation": "`let loggedIn = true;` — with no quotes. `\"true\"` in quotes is a three-letter string that happens to spell the word true; it isn't the boolean value at all. `1` is a legitimate number, sometimes used to *mean* true in other contexts, but it isn't literally a boolean either. Only the bare word `true` (or `false`), with no quotes, is JavaScript's actual boolean type.",
    "bloomLevel": "understand",
    "difficulty": 2,
    "xpReward": 10,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB1-005",
    "moduleId": "fnd-bb1",
    "lessonId": "fnd-bb1-l03",
    "type": "CATEGORIZATION",
    "prompt": "Sort each variable name by whether JavaScript actually allows it, or rejects it outright as invalid.",
    "config": {
      "categories": [
        "Valid name",
        "Invalid — JavaScript rejects this"
      ],
      "items": [
        {
          "text": "spoonsRemaining",
          "category": "Valid name"
        },
        {
          "text": "spoons_remaining",
          "category": "Valid name"
        },
        {
          "text": "2ndDose",
          "category": "Invalid — JavaScript rejects this"
        },
        {
          "text": "dose count",
          "category": "Invalid — JavaScript rejects this"
        },
        {
          "text": "doseCount",
          "category": "Valid name"
        },
        {
          "text": "_privateNote",
          "category": "Valid name"
        }
      ]
    },
    "hints": [
      "Names can't start with a digit — check which one does.",
      "Spaces aren't allowed anywhere in a variable name — camelCase or an underscore replaces the space instead."
    ],
    "explanation": "`spoonsRemaining`, `spoons_remaining`, `doseCount`, and `_privateNote` are all valid — letters, digits (not leading), and underscores are allowed, and case matters. `2ndDose` is rejected because a name can't start with a digit. `dose count` is rejected because of the space in the middle — JavaScript reads that as two separate things, not one name. Valid isn't the same as *good*, though — the next pod is about picking between several valid options.",
    "bloomLevel": "understand",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB1-006",
    "moduleId": "fnd-bb1",
    "lessonId": "fnd-bb1-l03",
    "type": "TRUE_FALSE_JUSTIFY",
    "prompt": "True or false: `data` is just as good a variable name as `spoonsRemaining`, as long as the code still runs correctly.",
    "config": {
      "statement": "A name like `data` is just as good as a more specific name, as long as the program still runs.",
      "correctAnswer": false,
      "justificationRequired": true,
      "rubric": "A full answer says false and explains that running correctly and being readable later are two separate requirements, not one."
    },
    "hints": [
      "Would `data` still run? Almost certainly. Is \"runs\" the only thing a name is judged on?",
      "Picture reopening this file in three weeks. Which name tells you, at a glance, what's actually being stored?"
    ],
    "explanation": "False. `data` runs exactly as well as `spoonsRemaining` — JavaScript genuinely doesn't care what you call anything, as long as it's a valid name. The problem shows up later, for the human reopening the file: `data` could be anything, and finding out means reading surrounding code to reconstruct what should have been obvious from the name itself. `spoonsRemaining` answers the question \"what is this?\" on sight. Correctness and readability are two different requirements a name has to meet, and JavaScript only ever checks the first one.",
    "bloomLevel": "evaluate",
    "difficulty": 2,
    "xpReward": 13,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB1-007",
    "moduleId": "fnd-bb1",
    "lessonId": "fnd-bb1-l04",
    "type": "MULTIPLE_CHOICE",
    "prompt": "A form reads someone's age as text input, so it arrives in code as `\"32\"`, not `32`. This line runs:\n\n```\nlet nextYear = \"32\" + 1;\nconsole.log(nextYear);\n```\nWhat actually prints?",
    "config": {
      "options": [
        {
          "text": "33",
          "correct": false
        },
        {
          "text": "\"321\"",
          "correct": true
        },
        {
          "text": "An error, because you can't add text and a number",
          "correct": false
        },
        {
          "text": "NaN",
          "correct": false
        }
      ]
    },
    "hints": [
      "`+` behaves differently depending on what's on either side of it. What's on the left side here?",
      "When one side of `+` is a string, JavaScript doesn't do arithmetic — it glues the two values together as text instead."
    ],
    "explanation": "\"321\". `\"32\"` is text, not the number 32, and `+` between a string and anything else means \"stick them together as text\" rather than \"add them\" — so `\"32\"` and `1` become the text `\"321\"`, not the number 33. This doesn't throw an error, which is exactly what makes it dangerous: the program keeps running quietly with a wrong value, instead of stopping to tell you something's off. Form inputs are text by default, which is why this specific surprise shows up constantly with real user data.",
    "bloomLevel": "analyze",
    "difficulty": 3,
    "xpReward": 15,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB1-008",
    "moduleId": "fnd-bb1",
    "lessonId": "fnd-bb1-l04",
    "type": "OPEN_ENDED",
    "prompt": "```\nlet score = \"10\";\nlet bonus = 5;\nlet total = score - bonus;\nconsole.log(total);\n```\nWhat does this actually print, and why does it behave differently from the `\"32\" + 1` example above? Explain the difference in your own words.",
    "config": {},
    "hints": [
      "`-` only ever means subtraction in JavaScript — there's no \"glue text together\" meaning for it the way there is for `+`.",
      "If `-` only means subtraction, what does JavaScript have to do to `\"10\"` before it can subtract anything from it?",
      "Compare this directly to `\"32\" + 1`: same string-plus-number setup, different operator, different outcome."
    ],
    "explanation": "It prints 5. Unlike `+`, the `-` operator has no \"combine as text\" meaning at all — it can only mean subtraction. So instead of gluing the values together, JavaScript automatically converts `\"10\"` into the number 10 first, then subtracts: 10 − 5 = 5. This is the same underlying habit (JavaScript quietly converting types instead of raising an error) producing the opposite-feeling result from `\"32\" + 1`: with `+`, a string on either side means \"glue as text\"; with `-`, `*`, or `/`, a string gets converted to a number first if it looks like one. Two operators, two different automatic behaviors, and neither one warns you it happened.",
    "bloomLevel": "analyze",
    "difficulty": 3,
    "xpReward": 16,
    "isKnowledgeCheck": false
  },
  {
    "ref": "FND-BB1-009",
    "moduleId": "fnd-bb1",
    "lessonId": "fnd-bb1-l05",
    "type": "MULTIPLE_CHOICE",
    "prompt": "You have `let ageText = \"32\";` from a form field, and you need real arithmetic on it — adding 1 to get next year's age. What's the correct fix?",
    "config": {
      "options": [
        {
          "text": "let age = ageText + 0;",
          "correct": false
        },
        {
          "text": "let age = Number(ageText);",
          "correct": true
        },
        {
          "text": "let age = \"\" + ageText;",
          "correct": false
        },
        {
          "text": "Just use ageText directly — JavaScript will figure it out eventually",
          "correct": false
        }
      ]
    },
    "hints": [
      "`+` glues text together — adding 0 to a string doesn't convert it, it just produces more text.",
      "`Number(...)` is a function whose entire job is converting its input into a real number, on purpose."
    ],
    "explanation": "`Number(ageText)`. `Number(...)` explicitly converts its argument into a real number — `Number(\"32\")` produces the actual number 32, ready for arithmetic. `ageText + 0` looks like it should work but doesn't: with a string on the left, `+` joins as text, producing `\"320\"`, not the number 32. `\"\" + ageText` does the opposite of what's needed — it forces a value *into* a string, not out of one. Converting deliberately, with a named function, beats hoping an operator will happen to do it for you.",
    "bloomLevel": "apply",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB1-010",
    "moduleId": "fnd-bb1",
    "lessonId": "fnd-bb1-l05",
    "type": "OPEN_ENDED",
    "prompt": "A settings page stores whether dark mode is on as the string `\"true\"` (it came from a saved file, which only stores text). Write the one line of code that converts it into a real boolean, and explain in one sentence why `let darkMode = \"true\";` on its own wouldn't already do the job.",
    "config": {},
    "hints": [
      "JavaScript has a function for converting a value into a boolean, the same way `Number(...)` converts into a number.",
      "The trap here is the same shape as the earlier `\"32\"` example: something that *looks* like the right type on screen still isn't, until you convert it."
    ],
    "explanation": "`let darkMode = Boolean(\"true\");` isn't actually the standard fix here — `Boolean(\"true\")` and even `Boolean(\"false\")` both come out `true`, because any non-empty string counts as truthy to `Boolean(...)`, which is a real trap of its own. The genuinely correct fix compares the stored text directly: `let darkMode = savedValue === \"true\";` — this produces a real boolean (`true` only when the saved text is exactly \"true\", `false` otherwise) instead of relying on `Boolean(...)`'s much looser rule. The one-sentence reason `\"true\"` on its own doesn't already work: it's a four-letter string, not the boolean value, so anything that checks `if (darkMode)` would treat it as true regardless of which word it actually said.",
    "bloomLevel": "apply",
    "difficulty": 3,
    "xpReward": 16,
    "isKnowledgeCheck": false
  },
  {
    "ref": "FND-BB1-P01",
    "moduleId": "fnd-bb1",
    "lessonId": "fnd-bb1-p01",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nlet price = \"9\";\nlet total = price + 1;\nconsole.log(total);\n```\nWhat prints?",
    "config": {
      "options": [
        {
          "text": "10",
          "correct": false
        },
        {
          "text": "\"91\"",
          "correct": true
        },
        {
          "text": "9",
          "correct": false
        },
        {
          "text": "An error",
          "correct": false
        }
      ]
    },
    "hints": [
      "`price` is text, not a number — check for quotes.",
      "With a string on the left of `+`, JavaScript joins rather than adds."
    ],
    "explanation": "\"91\". `price` holds the string \"9\", and `+` with a string on either side means join, not add — so \"9\" and 1 become \"91\". No error, no warning, just a quietly wrong-shaped result.",
    "bloomLevel": "apply",
    "difficulty": 1,
    "xpReward": 8,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB1-P02",
    "moduleId": "fnd-bb1",
    "lessonId": "fnd-bb1-p01",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nlet price = \"9\";\nlet total = price - (-1);\nconsole.log(total);\n```\nWhat prints?",
    "config": {
      "options": [
        {
          "text": "10",
          "correct": true
        },
        {
          "text": "\"9-1\"",
          "correct": false
        },
        {
          "text": "\"9--1\"",
          "correct": false
        },
        {
          "text": "NaN",
          "correct": false
        }
      ]
    },
    "hints": [
      "`-` never joins text, no matter what's on either side of it — it only ever means subtraction.",
      "Subtracting a negative number is the same as adding — work out what price actually becomes as a number first."
    ],
    "explanation": "10. `-` has no text-joining behavior at all, so JavaScript converts \"9\" into the number 9 before doing anything. Subtracting -1 from 9 is the same as adding 1: 9 - (-1) = 10.",
    "bloomLevel": "apply",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB1-P03",
    "moduleId": "fnd-bb1",
    "lessonId": "fnd-bb1-p01",
    "type": "TRUE_FALSE_JUSTIFY",
    "prompt": "True or false: `console.log(typeof \"12\")` prints `\"number\"`, since \"12\" is made entirely of digits.",
    "config": {
      "statement": "typeof \"12\" is \"number\", because the string is made entirely of digits.",
      "correctAnswer": false,
      "justificationRequired": true,
      "rubric": "A full answer says false and explains that quotes determine type, regardless of what characters are inside them."
    },
    "hints": [
      "What does typeof actually check — the characters inside a value, or the value's type as JavaScript stores it?",
      "\"12\" has quotes around it. Does that change anything about its type?"
    ],
    "explanation": "False. `typeof \"12\"` is `\"string\"` — the quotes make it text, and being made entirely of digits doesn't change that. `typeof` reports the actual stored type, not what the characters happen to look like.",
    "bloomLevel": "understand",
    "difficulty": 1,
    "xpReward": 8,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB1-P04",
    "moduleId": "fnd-bb1",
    "lessonId": "fnd-bb1-p01",
    "type": "MULTIPLE_CHOICE",
    "prompt": "A tip calculator has this bug: `let tip = billText * 0.18;` gives `NaN` (Not a Number) instead of a real result. `billText` came from a text input and holds `\"abc\"` because the user typo'd. What's the most accurate description of why this happens?",
    "config": {
      "options": [
        {
          "text": "* doesn't work on any strings at all",
          "correct": false
        },
        {
          "text": "\"abc\" can't be converted into a meaningful number, so the conversion itself fails and produces NaN",
          "correct": true
        },
        {
          "text": "NaN means the program crashed",
          "correct": false
        },
        {
          "text": "0.18 is too small a number to multiply by",
          "correct": false
        }
      ]
    },
    "hints": [
      "`*` does convert numeric-looking strings, like \"9\" — the difference here is what's inside the string.",
      "NaN doesn't stop the program. It's a real value that means \"this was supposed to be a number, but isn't one.\""
    ],
    "explanation": "\"abc\" can't be converted into a meaningful number — unlike \"9\", there's no numeric value hiding in it, so the conversion itself fails, producing the special value `NaN`. Crucially, `NaN` does **not** crash the program — it's a real, valid value that means \"this was supposed to be a number but the conversion didn't work,\" and it will happily keep flowing through the rest of your math, quietly turning every later calculation that touches it into `NaN` too.",
    "bloomLevel": "analyze",
    "difficulty": 3,
    "xpReward": 15,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB1-P05",
    "moduleId": "fnd-bb1",
    "lessonId": "fnd-bb1-p01",
    "type": "SEQUENCING",
    "prompt": "Put these four lines in the order that correctly converts a form's text input (\"7\") into a number, doubles it, and prints the result as a labeled sentence.\n\n(Lines: print the sentence, double the number, convert to a number, store the raw text)",
    "config": {
      "items": [
        "let doubled = doseCount * 2;",
        "console.log(`Doubled: ${doubled}`);",
        "let doseCount = Number(rawInput);",
        "let rawInput = \"7\";"
      ],
      "correctOrder": [
        3,
        2,
        0,
        1
      ]
    },
    "hints": [
      "Each line needs whatever the line before it created — trace which variable each line reads.",
      "The raw text has to exist before it can be converted. The number has to exist before it can be doubled. The doubled value has to exist before it can be printed."
    ],
    "explanation": "Correct order: store the raw text (`rawInput`), convert it to a number (`doseCount`), double it (`doubled`), then print it. Each line depends on a variable the previous line created — this is the same top-to-bottom dependency rule from Module 1's first lesson, applied to a small conversion pipeline.",
    "bloomLevel": "apply",
    "difficulty": 2,
    "xpReward": 13,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB1-P06",
    "moduleId": "fnd-bb1",
    "lessonId": "fnd-bb1-p01",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nlet steps = \"5000\";\nlet goal = 6000;\nconsole.log(steps < goal);\n```\nWhat prints?",
    "config": {
      "options": [
        {
          "text": "true",
          "correct": true
        },
        {
          "text": "false",
          "correct": false
        },
        {
          "text": "An error, because you can't compare a string to a number",
          "correct": false
        },
        {
          "text": "\"5000\" (unchanged)",
          "correct": false
        }
      ]
    },
    "hints": [
      "`<` isn't `+` — check whether it has a text-joining meaning at all, the same way `-` doesn't.",
      "If \"5000\" gets converted to the number 5000 first, is 5000 less than 6000?"
    ],
    "explanation": "true. Like `-`, `*`, and `/`, the comparison operators (`<`, `>`) have no text-joining meaning — they convert \"5000\" into the real number 5000 first, then compare: 5000 < 6000 is true. Comparisons follow the same numeric-conversion rule as arithmetic, not the join-as-text rule that's specific to `+`.",
    "bloomLevel": "apply",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB1-P07",
    "moduleId": "fnd-bb1",
    "lessonId": "fnd-bb1-p01",
    "type": "OPEN_ENDED",
    "prompt": "A settings file stores whether reminders are on as the text \"false\". Someone writes `let remindersOn = Boolean(savedText);` to convert it. Explain, in your own words, what's wrong with this line and what should replace it.",
    "config": {},
    "hints": [
      "This is the exact trap from lesson 5 — check what Boolean(...) actually looks at when given a string.",
      "The fix compares the saved text to the exact word \"true\", rather than converting it with Boolean(...)."
    ],
    "explanation": "`Boolean(\"false\")` evaluates to `true`, because `Boolean(...)` only checks whether a string is empty or not — any non-empty string, including the word \"false\" itself, converts to `true`. This line would silently turn reminders on regardless of what the saved file actually said. The fix is a direct comparison instead: `let remindersOn = savedText === \"true\";`, which produces `true` only when the saved text is exactly \"true\".",
    "bloomLevel": "evaluate",
    "difficulty": 3,
    "xpReward": 16,
    "isKnowledgeCheck": false
  },
  {
    "ref": "FND-BB1-P08",
    "moduleId": "fnd-bb1",
    "lessonId": "fnd-bb1-p01",
    "type": "MULTIPLE_CHOICE",
    "prompt": "Which line correctly converts the number 42 into the string \"Age: 42\" for display?",
    "config": {
      "options": [
        {
          "text": "let label = 42 + \"Age: \";",
          "correct": false
        },
        {
          "text": "let label = `Age: ${42}`;",
          "correct": true
        },
        {
          "text": "let label = Boolean(42);",
          "correct": false
        },
        {
          "text": "let label = \"Age: \" - 42;",
          "correct": false
        }
      ]
    },
    "hints": [
      "A template literal (backticks with ${...}) is built for exactly this — inserting a value into a piece of text in a specific order.",
      "42 + \"Age: \" would still join as text, but check the order it produces — does it read correctly?"
    ],
    "explanation": "`` `Age: ${42}` `` — the template literal, which reads as \"Age: 42\" with the pieces in the order you wrote them. `42 + \"Age: \"` would technically also join as text (since one side is a string), but it produces \"42Age: \", with the number stuck to the front instead of the label — order matters with `+`, and it's easy to get backwards. `Boolean(42)` converts to a boolean, not a label, and `-` doesn't join text at all.",
    "bloomLevel": "apply",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB2-001",
    "moduleId": "fnd-bb2",
    "lessonId": "fnd-bb2-l01",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nlet painLevel = 5;\nif (painLevel > 7) {\n  console.log(\"Flare day — log it and rest.\");\n} else {\n  console.log(\"Manageable today.\");\n}\n```\nWhat prints?",
    "config": {
      "options": [
        {
          "text": "Flare day — log it and rest.",
          "correct": false
        },
        {
          "text": "Manageable today.",
          "correct": true
        },
        {
          "text": "Both lines print",
          "correct": false
        },
        {
          "text": "Nothing — 5 isn't a valid comparison",
          "correct": false
        }
      ]
    },
    "hints": [
      "Check the condition first: is 5 actually greater than 7?",
      "Only one branch of an if/else ever runs — never both, and never neither."
    ],
    "explanation": "Manageable today. `painLevel > 7` checks whether 5 is greater than 7, which is false, so the `if` block is skipped entirely and the `else` block runs instead. Exactly one of the two blocks runs, based on whether the condition in parentheses evaluates to true or false — never both, never neither.",
    "bloomLevel": "apply",
    "difficulty": 1,
    "xpReward": 8,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB2-002",
    "moduleId": "fnd-bb2",
    "lessonId": "fnd-bb2-l01",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nlet spoonsLeft = 3;\nif (spoonsLeft < 2) {\n  console.log(\"Stop for today.\");\n}\nconsole.log(\"Session logged.\");\n```\nWhat prints, in total?",
    "config": {
      "options": [
        {
          "text": "Stop for today.\\nSession logged.",
          "correct": false
        },
        {
          "text": "Session logged.",
          "correct": true
        },
        {
          "text": "Nothing prints at all",
          "correct": false
        },
        {
          "text": "Stop for today.",
          "correct": false
        }
      ]
    },
    "hints": [
      "There's no `else` here at all — what happens to an `if` block when its condition is false and there's nothing to fall back to?",
      "The line after the if block isn't inside it — does the condition being false stop it from running too?"
    ],
    "explanation": "Session logged. `spoonsLeft < 2` is false (3 is not less than 2), so the `if` block is simply skipped — with no `else`, a false condition just means \"do nothing here\" and move on. The `console.log(\"Session logged.\")` line isn't inside the `if` block at all (there's no `{` including it), so it always runs regardless of the condition above it.",
    "bloomLevel": "analyze",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB2-003",
    "moduleId": "fnd-bb2",
    "lessonId": "fnd-bb2-l02",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nlet dose = 8;\nconsole.log(dose >= 8);\nconsole.log(dose > 8);\n```\nWhat prints, across both lines?",
    "config": {
      "options": [
        {
          "text": "true, then true",
          "correct": false
        },
        {
          "text": "true, then false",
          "correct": true
        },
        {
          "text": "false, then true",
          "correct": false
        },
        {
          "text": "false, then false",
          "correct": false
        }
      ]
    },
    "hints": [
      "\">=\" includes the case where the two sides are exactly equal. \">\" doesn't.",
      "8 is equal to 8, not greater than it — that distinction is the entire difference between these two lines."
    ],
    "explanation": "true, then false. `dose >= 8` (\"greater than or equal to\") is true because 8 equals 8. `dose > 8` (strictly \"greater than\") is false, because 8 is not *greater* than 8 — it's equal to it. Mixing up `>` and `>=` at a boundary value like this is one of the most common off-by-one mistakes in conditions.",
    "bloomLevel": "apply",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB2-004",
    "moduleId": "fnd-bb2",
    "lessonId": "fnd-bb2-l02",
    "type": "MULTIPLE_CHOICE",
    "prompt": "A symptom form stores today's pain level as text, since it came from an input field: `let painInput = \"7\";`. This runs:\n\n```\nconsole.log(painInput == 7);\nconsole.log(painInput === 7);\n```\nWhat prints, across both lines?",
    "config": {
      "options": [
        {
          "text": "true, then true",
          "correct": false
        },
        {
          "text": "true, then false",
          "correct": true
        },
        {
          "text": "false, then true",
          "correct": false
        },
        {
          "text": "false, then false",
          "correct": false
        }
      ]
    },
    "hints": [
      "`==` converts types before comparing. `===` refuses to — it checks type and value both.",
      "\"7\" and 7 hold the same digits but different types. Which operator lets that difference slide, and which one doesn't?"
    ],
    "explanation": "true, then false. `==` (\"loose equality\") converts types before comparing, so it silently turns \"7\" into 7 first — true. `===` (\"strict equality\") never converts anything; it requires both the value *and* the type to match, and a string is never the same type as a number, so it's false, even though the digits match. This is exactly why `==` catches almost every beginner at least once: it looks like it's being helpful by \"figuring out\" what you meant, and that's precisely the behavior that hides real bugs — form input, API responses, and saved files are text by default, and `==` will happily compare that text against a number as if the type difference never happened.",
    "bloomLevel": "analyze",
    "difficulty": 3,
    "xpReward": 16,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB2-005",
    "moduleId": "fnd-bb2",
    "lessonId": "fnd-bb2-l03",
    "type": "MULTIPLE_CHOICE",
    "prompt": "You want to log a flare day when pain is high **or** fever is present — either one alone should count. Which condition is correct?",
    "config": {
      "options": [
        {
          "text": "painLevel > 7 && hasFever",
          "correct": false
        },
        {
          "text": "painLevel > 7 || hasFever",
          "correct": true
        },
        {
          "text": "painLevel > 7 ! hasFever",
          "correct": false
        },
        {
          "text": "painLevel > 7, hasFever",
          "correct": false
        }
      ]
    },
    "hints": [
      "\"Either one alone should count\" — does that sound like \"both must be true,\" or \"at least one must be true\"?",
      "&& requires both sides true. || requires at least one side true."
    ],
    "explanation": "`painLevel > 7 || hasFever`. `||` (\"or\") is true when at least one side is true — exactly what \"either one alone should count\" means. `&&` (\"and\") would wrongly require *both* to be true at once, which would miss a flare day driven by fever alone, or pain alone.",
    "bloomLevel": "apply",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB2-006",
    "moduleId": "fnd-bb2",
    "lessonId": "fnd-bb2-l03",
    "type": "MULTIPLE_CHOICE",
    "prompt": "You want to suggest a coding session only when energy is decent **and** there's no appointment today — both conditions genuinely need to be true. Which is correct?",
    "config": {
      "options": [
        {
          "text": "energyIsOk || noAppointment",
          "correct": false
        },
        {
          "text": "energyIsOk && noAppointment",
          "correct": true
        },
        {
          "text": "!energyIsOk && noAppointment",
          "correct": false
        },
        {
          "text": "energyIsOk && !noAppointment",
          "correct": false
        }
      ]
    },
    "hints": [
      "\"Both need to be true\" is the definition of &&, not ||.",
      "Watch the ! (not) in the wrong options carefully — one flips energyIsOk, the other flips noAppointment. Neither belongs here."
    ],
    "explanation": "`energyIsOk && noAppointment`. `&&` (\"and\") is only true when both sides are true — matching \"both conditions genuinely need to be true\" exactly. `||` would wrongly suggest a session even when only one condition holds. The two `!` options each negate the wrong thing — flipping a condition that was never supposed to be negated at all changes the meaning of the rule entirely, not just its wording.",
    "bloomLevel": "apply",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB2-007",
    "moduleId": "fnd-bb2",
    "lessonId": "fnd-bb2-l04",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nlet energy = \"medium\";\nif (energy === \"low\") {\n  console.log(\"Rest day.\");\n} else if (energy === \"medium\") {\n  console.log(\"Light session.\");\n} else {\n  console.log(\"Full session.\");\n}\n```\nWhat prints?",
    "config": {
      "options": [
        {
          "text": "Rest day.",
          "correct": false
        },
        {
          "text": "Light session.",
          "correct": true
        },
        {
          "text": "Full session.",
          "correct": false
        },
        {
          "text": "All three lines print",
          "correct": false
        }
      ]
    },
    "hints": [
      "Go through the chain top to bottom: is energy === \"low\"? If not, move to the next check.",
      "Once one branch of an else-if chain matches, none of the ones below it are even checked."
    ],
    "explanation": "Light session. The chain checks each condition in order: `energy === \"low\"` is false (energy is \"medium\"), so it moves to `else if (energy === \"medium\")`, which is true — that branch runs, and everything after it in the chain (the final `else`) is skipped, even though it was never actually evaluated as false. Only one branch of an else-if chain ever runs, exactly like a plain if/else.",
    "bloomLevel": "apply",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB2-008",
    "moduleId": "fnd-bb2",
    "lessonId": "fnd-bb2-l04",
    "type": "OPEN_ENDED",
    "prompt": "Here's the same rule written two ways. Which is easier to read, and why — name the specific thing that makes one harder to follow than the other?\n\n**Version A (nested):**\n```\nif (energy !== \"low\") {\n  if (hasAppointment) {\n    console.log(\"Keep it light.\");\n  } else {\n    console.log(\"Full session.\");\n  }\n} else {\n  console.log(\"Rest day.\");\n}\n```\n\n**Version B (else-if chain):**\n```\nif (energy === \"low\") {\n  console.log(\"Rest day.\");\n} else if (hasAppointment) {\n  console.log(\"Keep it light.\");\n} else {\n  console.log(\"Full session.\");\n}\n```",
    "config": {},
    "hints": [
      "Count how many levels deep you have to track in each version to know which branch you're reading.",
      "Version A negates the first condition (!==) right away, which forces you to hold a double negative in your head while reading the rest."
    ],
    "explanation": "Version B is easier to follow, for two concrete reasons. First, it's flat — every branch sits at the same indentation level, so there's no need to track \"which nested level am I inside\" while reading. Second, Version A leads with a negated condition (`energy !== \"low\"`), which forces the reader to mentally flip it before they can even follow the logic, whereas Version B states the low-energy case directly and positively first. Both versions produce identical behavior — this is purely about which one a tired reader, including future you, can follow without re-reading it twice.",
    "bloomLevel": "evaluate",
    "difficulty": 3,
    "xpReward": 15,
    "isKnowledgeCheck": false
  },
  {
    "ref": "FND-BB2-009",
    "moduleId": "fnd-bb2",
    "lessonId": "fnd-bb2-l05",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nlet energy = \"high\";\nif (energy === \"low\") {\n  console.log(\"Rest day.\");\n} else if (energy === \"medium\") {\n  console.log(\"Light session.\");\n}\nconsole.log(\"Session logged.\");\n```\nWhat prints, in total, for energy = \"high\"?",
    "config": {
      "options": [
        {
          "text": "Rest day.\\nSession logged.",
          "correct": false
        },
        {
          "text": "Light session.\\nSession logged.",
          "correct": false
        },
        {
          "text": "Session logged.",
          "correct": true
        },
        {
          "text": "Nothing prints at all",
          "correct": false
        }
      ]
    },
    "hints": [
      "Neither \"low\" nor \"medium\" matches \"high\" — and there's no final else here to catch what's left over.",
      "The last console.log line isn't inside either branch, so check separately whether it always runs."
    ],
    "explanation": "Session logged. Neither condition matches \"high\" — there's no final `else`, so the chain simply produces no output for this case, a genuine gap in the logic that a missing case slipped through. `console.log(\"Session logged.\")` isn't part of the chain at all, so it still runs regardless. This is exactly the kind of bug that's invisible until someone hits the exact case that was never written for.",
    "bloomLevel": "analyze",
    "difficulty": 3,
    "xpReward": 15,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB2-010",
    "moduleId": "fnd-bb2",
    "lessonId": "fnd-bb2-l05",
    "type": "OPEN_ENDED",
    "prompt": "The chain from the exercise above only handles \"low\" and \"medium\" energy — \"high\" silently produces nothing. Fix it so every possible value of `energy` produces some message, and explain in one sentence why a final `else` (rather than another `else if`) is the right way to close this out.",
    "config": {},
    "hints": [
      "A final else, with no condition attached, catches everything that didn't match any branch above it — including values you didn't specifically anticipate.",
      "Compare: `else if (energy === \"high\")` only catches the exact string \"high\". A plain `else` catches anything else at all, including a typo or an unexpected value."
    ],
    "explanation": "Add a final `else { console.log(\"Full session.\"); }` after the medium branch, with no condition on it. A plain `else` (rather than one more `else if (energy === \"high\")`) is the safer close, because it catches *everything* that didn't match an earlier branch — including \"high\" but also anything unanticipated, like a typo'd value or an unexpected input the code wasn't specifically written to expect. An `else if` chasing one more exact string still leaves a gap for anything else that shows up; a bare `else` genuinely has no gap left.",
    "bloomLevel": "create",
    "difficulty": 3,
    "xpReward": 16,
    "isKnowledgeCheck": false
  },
  {
    "ref": "FND-BB2-P01",
    "moduleId": "fnd-bb2",
    "lessonId": "fnd-bb2-p01",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nlet temp = 38.5;\nif (temp >= 38) {\n  console.log(\"Fever.\");\n} else {\n  console.log(\"No fever.\");\n}\n```\nWhat prints?",
    "config": {
      "options": [
        {
          "text": "Fever.",
          "correct": true
        },
        {
          "text": "No fever.",
          "correct": false
        },
        {
          "text": "Both lines print",
          "correct": false
        },
        {
          "text": "Nothing prints",
          "correct": false
        }
      ]
    },
    "hints": [
      "Check 38.5 against 38 using >= specifically, not just >.",
      "Only one branch of an if/else runs."
    ],
    "explanation": "Fever. 38.5 >= 38 is true (38.5 is greater than 38), so the if block runs and the else is skipped.",
    "bloomLevel": "apply",
    "difficulty": 1,
    "xpReward": 8,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB2-P02",
    "moduleId": "fnd-bb2",
    "lessonId": "fnd-bb2-p01",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nlet steps = \"5000\";\nif (steps === 5000) {\n  console.log(\"Goal met.\");\n} else {\n  console.log(\"Not yet.\");\n}\n```\nWhat prints?",
    "config": {
      "options": [
        {
          "text": "Goal met.",
          "correct": false
        },
        {
          "text": "Not yet.",
          "correct": true
        },
        {
          "text": "An error, because steps is text",
          "correct": false
        },
        {
          "text": "5000",
          "correct": false
        }
      ]
    },
    "hints": [
      "steps holds \"5000\" (text). === refuses to convert types before comparing.",
      "\"5000\" and 5000 have matching digits but different types — does === treat that as equal?"
    ],
    "explanation": "Not yet. `steps` is the string \"5000\", not the number 5000. `===` checks type as well as value, and a string is never the same type as a number, so this is false regardless of the digits matching — the if block is skipped and the else runs.",
    "bloomLevel": "analyze",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB2-P03",
    "moduleId": "fnd-bb2",
    "lessonId": "fnd-bb2-p01",
    "type": "TRUE_FALSE_JUSTIFY",
    "prompt": "True or false: `energyIsOk || hasAppointment` is true only when both energyIsOk and hasAppointment are true.",
    "config": {
      "statement": "energyIsOk || hasAppointment is true only when both sides are true.",
      "correctAnswer": false,
      "justificationRequired": true,
      "rubric": "A full answer says false and correctly restates what || actually requires."
    },
    "hints": [
      "That description (\"only when both are true\") is the rule for &&, not ||.",
      "|| needs at least one side true — including the case where only one of them is."
    ],
    "explanation": "False. That's the rule for `&&`, not `||`. `||` is true when **at least one** side is true — it's only false when *both* sides are false. So `energyIsOk || hasAppointment` is true if either one alone is true, or if both are.",
    "bloomLevel": "understand",
    "difficulty": 1,
    "xpReward": 8,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB2-P04",
    "moduleId": "fnd-bb2",
    "lessonId": "fnd-bb2-p01",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nlet energy = \"medium\";\nlet hasAppointment = true;\nif (energy === \"low\" || hasAppointment) {\n  console.log(\"Keep it light.\");\n} else {\n  console.log(\"Full session.\");\n}\n```\nWhat prints?",
    "config": {
      "options": [
        {
          "text": "Keep it light.",
          "correct": true
        },
        {
          "text": "Full session.",
          "correct": false
        },
        {
          "text": "Both lines print",
          "correct": false
        },
        {
          "text": "Nothing prints",
          "correct": false
        }
      ]
    },
    "hints": [
      "energy === \"low\" is false here — but there's an || right after it. Check the other side too.",
      "|| only needs one side true. hasAppointment alone can make the whole condition true, regardless of energy."
    ],
    "explanation": "Keep it light. `energy === \"low\"` is false (energy is \"medium\"), but `hasAppointment` is true, and `||` only needs one side true — so the whole condition is true, and the if block runs.",
    "bloomLevel": "apply",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB2-P05",
    "moduleId": "fnd-bb2",
    "lessonId": "fnd-bb2-p01",
    "type": "CATEGORIZATION",
    "prompt": "Sort each condition by whether it correctly expresses \"energy is not low\" or gets it wrong.",
    "config": {
      "categories": [
        "Correctly means \"energy is not low\"",
        "Does not mean that"
      ],
      "items": [
        {
          "text": "energy !== \"low\"",
          "category": "Correctly means \"energy is not low\""
        },
        {
          "text": "!(energy === \"low\")",
          "category": "Correctly means \"energy is not low\""
        },
        {
          "text": "energy === \"low\"",
          "category": "Does not mean that"
        },
        {
          "text": "!energy === \"low\"",
          "category": "Does not mean that"
        }
      ]
    },
    "hints": [
      "!== is the negated version of === — read it as \"is not exactly equal to.\"",
      "!(energy === \"low\") wraps the whole comparison in a negation — the ! applies to the entire result, not just to energy."
    ],
    "explanation": "`energy !== \"low\"` and `!(energy === \"low\")` are equivalent and both correctly mean \"not low.\" `energy === \"low\"` is the un-negated original — the opposite of what's wanted. `!energy === \"low\"` is a common trap: `!` binds to `energy` alone here, not to the whole comparison, which produces something quite different from what it looks like it says.",
    "bloomLevel": "analyze",
    "difficulty": 3,
    "xpReward": 15,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB2-P06",
    "moduleId": "fnd-bb2",
    "lessonId": "fnd-bb2-p01",
    "type": "SEQUENCING",
    "prompt": "Put these into the order an else-if chain checks them, for `energy = \"medium\"`, to correctly identify which single branch ends up running.\n\n(Steps: check against \"low\", check against \"medium\", run the matching branch, skip everything after a match)",
    "config": {
      "items": [
        "Check whether energy === \"low\" — false, move on",
        "Check whether energy === \"medium\" — true",
        "Run the \"Light session\" branch",
        "Skip the final else entirely, since a branch already matched"
      ],
      "correctOrder": [
        0,
        1,
        2,
        3
      ]
    },
    "hints": [
      "The chain always checks top to bottom, stopping the instant something matches.",
      "Once a branch runs, nothing below it in the same chain is even evaluated."
    ],
    "explanation": "Top to bottom: check \"low\" (false), check \"medium\" (true, so this branch runs), then skip everything below it — the final else is never reached because a match already happened above it.",
    "bloomLevel": "apply",
    "difficulty": 2,
    "xpReward": 13,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB2-P07",
    "moduleId": "fnd-bb2",
    "lessonId": "fnd-bb2-p01",
    "type": "OPEN_ENDED",
    "prompt": "A chain checks `mood === \"good\"`, then `mood === \"okay\"`, with no final else. What happens for `mood = \"bad\"`, and what's the one-line fix?",
    "config": {},
    "hints": [
      "Neither condition matches \"bad\" — trace what an unmatched chain with no else actually does.",
      "The fix is the same catch-all pattern from lesson 5: one line, no condition attached."
    ],
    "explanation": "Nothing prints from the chain at all — \"bad\" doesn't match either condition, and with no final else, an unmatched chain just produces silence, not an error. The fix is adding a bare `else { ... }` at the end with no condition, so every value that doesn't match an earlier branch — \"bad\" or anything else — still produces some output.",
    "bloomLevel": "analyze",
    "difficulty": 3,
    "xpReward": 15,
    "isKnowledgeCheck": false
  },
  {
    "ref": "FND-BB2-P08",
    "moduleId": "fnd-bb2",
    "lessonId": "fnd-bb2-p01",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nlet loggedSymptom = false;\nif (!loggedSymptom) {\n  console.log(\"Reminder: log today's symptoms.\");\n}\n```\nWhat prints?",
    "config": {
      "options": [
        {
          "text": "Reminder: log today's symptoms.",
          "correct": true
        },
        {
          "text": "Nothing prints",
          "correct": false
        },
        {
          "text": "An error — ! can't be used on a variable directly",
          "correct": false
        },
        {
          "text": "false",
          "correct": false
        }
      ]
    },
    "hints": [
      "! flips a boolean. loggedSymptom is false — what does !false evaluate to?",
      "!loggedSymptom being true is what makes the if block run."
    ],
    "explanation": "Reminder: log today's symptoms. `loggedSymptom` is `false`, and `!false` is `true`, so the condition inside the `if` is true and the block runs. `!` is a completely normal, valid operator on any boolean variable.",
    "bloomLevel": "apply",
    "difficulty": 1,
    "xpReward": 8,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB3-001",
    "moduleId": "fnd-bb3",
    "lessonId": "fnd-bb3-l01",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nfor (let i = 0; i < 3; i++) {\n  console.log(\"Reminder\", i + 1);\n}\n```\nHow many times does console.log actually run?",
    "config": {
      "options": [
        {
          "text": "2 times",
          "correct": false
        },
        {
          "text": "3 times",
          "correct": true
        },
        {
          "text": "4 times",
          "correct": false
        },
        {
          "text": "Once, with i counting up inside the string",
          "correct": false
        }
      ]
    },
    "hints": [
      "i starts at 0 and the loop keeps going while i < 3 — list out the values i actually takes: 0, then 1, then 2.",
      "The loop stops the moment i < 3 becomes false, which happens right after i becomes 3 — but 3 never runs the body."
    ],
    "explanation": "3 times. `i` takes the values 0, 1, then 2 — for each of those, `i < 3` is still true, so the body runs. Once `i` becomes 3, `i < 3` is false, and the loop stops before running the body a fourth time. Three values, three runs of the body — one print per pass through the loop.",
    "bloomLevel": "apply",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB3-002",
    "moduleId": "fnd-bb3",
    "lessonId": "fnd-bb3-l01",
    "type": "MULTIPLE_CHOICE",
    "prompt": "You need to print a greeting for each of three medications: Ibuprofen, Omeprazole, Vitamin D. Copy-pasting `console.log(\"Take: \" + name)` three times, once per medication, works. What's the real cost of doing it that way instead of with a loop?",
    "config": {
      "options": [
        {
          "text": "There's no real cost — copy-pasting three times is completely fine",
          "correct": false
        },
        {
          "text": "If the format needs to change, or a fourth medication gets added, every copy has to be found and updated separately",
          "correct": true
        },
        {
          "text": "Copy-pasted code runs slower than a loop",
          "correct": false
        },
        {
          "text": "JavaScript doesn't allow the same console.log line to appear twice",
          "correct": false
        }
      ]
    },
    "hints": [
      "This is the exact DRY problem from earlier in the track — what happens the day the format needs to change?",
      "Three items feels small enough to copy by hand. What happens when it's thirty?"
    ],
    "explanation": "If the format needs to change (add the dose, change the wording) or a new medication gets added, every single copy has to be found and updated by hand — miss one and the output is inconsistent. A loop expresses \"do this for each one\" exactly once; adding a fourth medication or changing the message only means editing one place. Execution speed isn't meaningfully different at this scale — the real cost of copy-pasting is entirely about what happens the next time something needs to change.",
    "bloomLevel": "analyze",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB3-003",
    "moduleId": "fnd-bb3",
    "lessonId": "fnd-bb3-l02",
    "type": "MULTIPLE_CHOICE",
    "prompt": "You want to print exactly 5 reminders, numbered 1 through 5. Which loop is correct?",
    "config": {
      "options": [
        {
          "text": "for (let i = 0; i < 5; i++) { console.log(\"Reminder\", i); }",
          "correct": false
        },
        {
          "text": "for (let i = 1; i <= 5; i++) { console.log(\"Reminder\", i); }",
          "correct": true
        },
        {
          "text": "for (let i = 1; i < 5; i++) { console.log(\"Reminder\", i); }",
          "correct": false
        },
        {
          "text": "for (let i = 0; i <= 5; i++) { console.log(\"Reminder\", i); }",
          "correct": false
        }
      ]
    },
    "hints": [
      "You need the printed numbers themselves to run 1 through 5 — so where should the counter start?",
      "Count how many times each option's body actually runs, and what number it prints on the last pass."
    ],
    "explanation": "`for (let i = 1; i <= 5; i++)`. Starting `i` at 1 (not 0) means the first print is \"Reminder 1\" directly, with no extra math needed. `i <= 5` includes 5 itself, so the last print is \"Reminder 5\" — exactly five passes, numbered 1 through 5. The first option starts at 0 and stops before 5, printing 0 through 4. The third starts at 1 but stops before 5, printing only 1 through 4 — one short. The fourth starts at 0 and includes 5, printing 0 through 5 — six reminders, one too many.",
    "bloomLevel": "apply",
    "difficulty": 3,
    "xpReward": 15,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB3-004",
    "moduleId": "fnd-bb3",
    "lessonId": "fnd-bb3-l02",
    "type": "OPEN_ENDED",
    "prompt": "Explain, in your own words, why `for (let i = 0; i < 5; i++)` runs its body 5 times even though the counter only ever reaches 4 inside the loop, never 5.",
    "config": {},
    "hints": [
      "List out every value i actually takes while the condition i < 5 is still true: 0, 1, 2, 3, 4 — count them.",
      "The loop checks the condition before each pass, including the one that would make i equal to 5 — and stops there, before running the body again."
    ],
    "explanation": "i takes the values 0, 1, 2, 3, and 4 — that's five separate values, so the body runs five times, once per value. After the fifth pass, i becomes 5, the condition `i < 5` is checked again and comes out false, and the loop stops — the body never runs with i equal to 5. This is exactly why starting at 0 and using `<` (rather than `<=`) still produces the correct count: 0 through 4 is five distinct values, even though the highest one printed is 4, one less than the total count of passes.",
    "bloomLevel": "analyze",
    "difficulty": 3,
    "xpReward": 16,
    "isKnowledgeCheck": false
  },
  {
    "ref": "FND-BB3-005",
    "moduleId": "fnd-bb3",
    "lessonId": "fnd-bb3-l03",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nlet spoonsLeft = 3;\nwhile (spoonsLeft > 0) {\n  console.log(\"Spoons left:\", spoonsLeft);\n  spoonsLeft = spoonsLeft - 1;\n}\n```\nHow many lines does this print?",
    "config": {
      "options": [
        {
          "text": "2",
          "correct": false
        },
        {
          "text": "3",
          "correct": true
        },
        {
          "text": "4",
          "correct": false
        },
        {
          "text": "It never stops",
          "correct": false
        }
      ]
    },
    "hints": [
      "Trace spoonsLeft before each pass: 3, then 2, then 1 — check the condition each time before deciding whether to run again.",
      "The loop stops the moment spoonsLeft > 0 becomes false — spoonsLeft = 0 is the value that finally fails the check."
    ],
    "explanation": "3. The condition is checked before each pass: spoonsLeft is 3 (true, prints, becomes 2), then 2 (true, prints, becomes 1), then 1 (true, prints, becomes 0), then 0 (false — stop). Three passes, three prints, and the loop genuinely ends because `spoonsLeft` is being changed inside the body, moving it toward the point where the condition finally fails.",
    "bloomLevel": "apply",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB3-006",
    "moduleId": "fnd-bb3",
    "lessonId": "fnd-bb3-l03",
    "type": "OPEN_ENDED",
    "prompt": "```\nlet spoonsLeft = 3;\nwhile (spoonsLeft > 0) {\n  console.log(\"Spoons left:\", spoonsLeft);\n}\n```\nThis is missing one line from the working version above. What's missing, and what actually happens when you run it as written?",
    "config": {},
    "hints": [
      "Compare this to the working version — what line updates spoonsLeft inside the body, and is it here?",
      "If spoonsLeft never changes, does the condition spoonsLeft > 0 ever become false?"
    ],
    "explanation": "The line `spoonsLeft = spoonsLeft - 1;` is missing from inside the loop body. Without it, `spoonsLeft` stays 3 forever — the condition `spoonsLeft > 0` is checked before every pass and is true every single time, since nothing inside the loop ever changes it. This runs forever: an infinite loop, printing \"Spoons left: 3\" endlessly until the program is forcibly stopped or the browser tab becomes unresponsive. A while loop's condition has to have some real chance of becoming false eventually, and that only happens if something inside the body actually moves it there.",
    "bloomLevel": "analyze",
    "difficulty": 3,
    "xpReward": 16,
    "isKnowledgeCheck": false
  },
  {
    "ref": "FND-BB3-007",
    "moduleId": "fnd-bb3",
    "lessonId": "fnd-bb3-l04",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nlet meds = [\"Ibuprofen\", \"Omeprazole\", \"Vitamin D\"];\nfor (const name of meds) {\n  if (name === \"Omeprazole\") {\n    console.log(\"Found it.\");\n    break;\n  }\n  console.log(\"Checking:\", name);\n}\n```\nWhat prints, in order?",
    "config": {
      "options": [
        {
          "text": "Checking: Ibuprofen, Checking: Omeprazole, Checking: Vitamin D",
          "correct": false
        },
        {
          "text": "Checking: Ibuprofen, Found it.",
          "correct": true
        },
        {
          "text": "Found it.",
          "correct": false
        },
        {
          "text": "Checking: Ibuprofen, Checking: Omeprazole, Found it.",
          "correct": false
        }
      ]
    },
    "hints": [
      "\"Checking:\" only prints for names that DON'T match — trace what happens on the pass where name does match.",
      "break stops the loop immediately, before anything after it (including a later pass) runs."
    ],
    "explanation": "Checking: Ibuprofen, then Found it. On the first pass, name is \"Ibuprofen\" — it doesn't match, so \"Checking: Ibuprofen\" prints and the loop continues. On the second pass, name is \"Omeprazole\" — the if matches, so \"Found it.\" prints and `break` immediately exits the loop entirely, before \"Checking: Omeprazole\" would have printed and before Vitamin D is ever looked at.",
    "bloomLevel": "apply",
    "difficulty": 3,
    "xpReward": 15,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB3-008",
    "moduleId": "fnd-bb3",
    "lessonId": "fnd-bb3-l04",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nlet doses = [12, -1, 8, 400];\nfor (const dose of doses) {\n  if (dose < 0) {\n    continue;\n  }\n  console.log(\"Valid dose:\", dose);\n}\n```\nWhich doses actually get printed?",
    "config": {
      "options": [
        {
          "text": "12, -1, 8, 400 — all of them",
          "correct": false
        },
        {
          "text": "12, 8, 400 — everything except -1",
          "correct": true
        },
        {
          "text": "Just -1",
          "correct": false
        },
        {
          "text": "Nothing — continue stops the whole loop",
          "correct": false
        }
      ]
    },
    "hints": [
      "continue skips only the current pass, not the whole loop — the loop keeps going to the next item.",
      "-1 is the one dose that triggers the if — trace exactly what happens to that one pass, and nothing beyond it."
    ],
    "explanation": "12, 8, 400. `continue` skips the rest of the *current* pass only — when `dose` is -1, `continue` jumps straight to the next item without running `console.log` for that one pass, but the loop itself keeps going normally for every value after it. This is the key difference from `break`: `continue` skips one item and carries on; `break` exits the entire loop immediately, for good.",
    "bloomLevel": "analyze",
    "difficulty": 3,
    "xpReward": 15,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB3-009",
    "moduleId": "fnd-bb3",
    "lessonId": "fnd-bb3-l05",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nlet doses = [200, 400, 100];\nlet total = 0;\nfor (const dose of doses) {\n  total = total + dose;\n}\nconsole.log(total);\n```\nWhat prints?",
    "config": {
      "options": [
        {
          "text": "200",
          "correct": false
        },
        {
          "text": "700",
          "correct": true
        },
        {
          "text": "0",
          "correct": false
        },
        {
          "text": "[200, 400, 100]",
          "correct": false
        }
      ]
    },
    "hints": [
      "total starts at 0 and is reassigned on every pass — trace it after each one: 0, then 200, then 600, then 700.",
      "This is the same reassignment rule from Module 1 — total = total + dose always uses whatever total currently holds."
    ],
    "explanation": "700. `total` starts at 0. After the first pass (dose = 200), total becomes 200. After the second (dose = 400), total becomes 600. After the third (dose = 100), total becomes 700. Each pass adds the current dose to whatever total already held — this running-total pattern is exactly why total has to start at 0 outside the loop, before any dose has been added.",
    "bloomLevel": "apply",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB3-010",
    "moduleId": "fnd-bb3",
    "lessonId": "fnd-bb3-l05",
    "type": "OPEN_ENDED",
    "prompt": "Someone writes this to total up a list of doses, but it always prints just the last dose in the list, not the real total:\n\n```\nlet doses = [200, 400, 100];\nfor (const dose of doses) {\n  let total = 0;\n  total = total + dose;\n}\nconsole.log(total);\n```\n\nWhat's actually wrong, and where does `total` need to move to fix it?",
    "config": {},
    "hints": [
      "`let total = 0;` is inside the loop body here — think about what that means happens to total on every single pass.",
      "Compare to the working version: where is total declared relative to the loop's curly braces?"
    ],
    "explanation": "`let total = 0;` is inside the loop body, which means it gets **reset to 0 on every single pass**, right before adding the current dose — so total only ever ends up holding the most recently added dose, never a real running sum. It also wouldn't even run as written, since `total` declared inside the loop with `let` doesn't exist outside it, so the final `console.log(total)` would fail entirely. The fix is moving `let total = 0;` to before the loop starts, outside its braces, so it's initialized exactly once and then accumulates across every pass instead of restarting each time.",
    "bloomLevel": "analyze",
    "difficulty": 3,
    "xpReward": 16,
    "isKnowledgeCheck": false
  },
  {
    "ref": "FND-BB3-P01",
    "moduleId": "fnd-bb3",
    "lessonId": "fnd-bb3-p01",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nfor (let i = 0; i < 4; i++) {\n  console.log(i);\n}\n```\nWhat's the last value printed?",
    "config": {
      "options": [
        {
          "text": "3",
          "correct": true
        },
        {
          "text": "4",
          "correct": false
        },
        {
          "text": "5",
          "correct": false
        },
        {
          "text": "0",
          "correct": false
        }
      ]
    },
    "hints": [
      "List the actual values i takes while i < 4 stays true: 0, 1, 2, 3.",
      "The loop stops as soon as i becomes 4 — that value is never printed."
    ],
    "explanation": "3. i takes the values 0, 1, 2, 3 — the loop stops the moment i becomes 4, since 4 < 4 is false. The last printed value is 3, one less than the total number of passes (4).",
    "bloomLevel": "apply",
    "difficulty": 1,
    "xpReward": 8,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB3-P02",
    "moduleId": "fnd-bb3",
    "lessonId": "fnd-bb3-p01",
    "type": "MULTIPLE_CHOICE",
    "prompt": "You need a loop that runs exactly 10 times, counting from 1 to 10. Which is correct?",
    "config": {
      "options": [
        {
          "text": "for (let i = 1; i <= 10; i++)",
          "correct": true
        },
        {
          "text": "for (let i = 1; i < 10; i++)",
          "correct": false
        },
        {
          "text": "for (let i = 0; i < 10; i++)",
          "correct": false
        },
        {
          "text": "for (let i = 0; i <= 10; i++)",
          "correct": false
        }
      ]
    },
    "hints": [
      "You want the numbers printed to literally read 1 through 10 — where should the counter start?",
      "<=10 includes 10 itself. <10 stops one short of it."
    ],
    "explanation": "`for (let i = 1; i <= 10; i++)`. Starting at 1 makes the printed numbers match 1-10 directly, and `<= 10` includes 10 as the final pass. Starting at 0 would print 0-9, off by one from what's needed unless you added 1 when printing — starting at 1 with the right condition avoids that extra step entirely.",
    "bloomLevel": "apply",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB3-P03",
    "moduleId": "fnd-bb3",
    "lessonId": "fnd-bb3-p01",
    "type": "TRUE_FALSE_JUSTIFY",
    "prompt": "True or false: a while loop always runs a predictable, fixed number of times, the same way a for loop with a fixed count does.",
    "config": {
      "statement": "A while loop always runs a fixed, predictable number of times.",
      "correctAnswer": false,
      "justificationRequired": true,
      "rubric": "A full answer says false and explains that a while loop's pass count depends on how its condition changes, which isn't fixed in advance the way a counting for loop's is."
    },
    "hints": [
      "A for loop with i < 5 always runs exactly 5 times, known in advance. Is that true of a while loop in general?",
      "A while loop depends on when its condition becomes false — that can depend on user input, changing data, or anything else, not just a fixed count."
    ],
    "explanation": "False. A for loop with a fixed counter (`i < 5`) has a predictable pass count known before it even starts. A while loop runs for as long as its condition stays true, and that condition can depend on things that aren't known in advance — user input, changing data, external state — so the number of passes can vary or, if written carelessly, never end at all.",
    "bloomLevel": "understand",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB3-P04",
    "moduleId": "fnd-bb3",
    "lessonId": "fnd-bb3-p01",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nlet symptoms = [\"headache\", \"nausea\", \"fatigue\"];\nlet count = 0;\nfor (const s of symptoms) {\n  count = count + 1;\n}\nconsole.log(count);\n```\nWhat prints?",
    "config": {
      "options": [
        {
          "text": "0",
          "correct": false
        },
        {
          "text": "3",
          "correct": true
        },
        {
          "text": "[\"headache\", \"nausea\", \"fatigue\"]",
          "correct": false
        },
        {
          "text": "\"fatigue\"",
          "correct": false
        }
      ]
    },
    "hints": [
      "count starts outside the loop at 0 — trace it after each of the three passes.",
      "This is the accumulator pattern from lesson 5, counting instead of summing."
    ],
    "explanation": "3. `count` starts at 0, outside the loop, and gets incremented by 1 on each of the three passes — one per symptom in the list. This is the accumulator pattern used for counting rather than summing: same shape, `count = count + 1` instead of adding a variable value.",
    "bloomLevel": "apply",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB3-P05",
    "moduleId": "fnd-bb3",
    "lessonId": "fnd-bb3-p01",
    "type": "SEQUENCING",
    "prompt": "Put these lines in the order that correctly totals a list of pain-level entries and prints the result.\n\n(Lines: print the total, declare the accumulator, loop and add each entry, declare the list)",
    "config": {
      "items": [
        "let total = total + entry; // inside the loop",
        "console.log(total);",
        "let total = 0;",
        "let painEntries = [4, 6, 3, 5];"
      ],
      "correctOrder": [
        3,
        2,
        0,
        1
      ]
    },
    "hints": [
      "The list has to exist before you can loop over it. The accumulator has to be declared before the loop that uses it.",
      "Printing has to happen after the loop has fully finished updating total, not before."
    ],
    "explanation": "Correct order: declare the list, declare the accumulator at 0, loop and add each entry to it, then print the final total. Each step depends on the one before it — the same dependency chain that's shown up since Module 1's first lesson, just applied to a loop this time.",
    "bloomLevel": "apply",
    "difficulty": 2,
    "xpReward": 13,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB3-P06",
    "moduleId": "fnd-bb3",
    "lessonId": "fnd-bb3-p01",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nlet nums = [4, -2, 7, -1, 9];\nfor (const n of nums) {\n  if (n < 0) { continue; }\n  console.log(n);\n}\n```\nWhat prints, in order?",
    "config": {
      "options": [
        {
          "text": "4, -2, 7, -1, 9",
          "correct": false
        },
        {
          "text": "4, 7, 9",
          "correct": true
        },
        {
          "text": "-2, -1",
          "correct": false
        },
        {
          "text": "4",
          "correct": false
        }
      ]
    },
    "hints": [
      "continue skips just the current pass — negative numbers get skipped, but the loop still reaches every later item.",
      "Trace each value: which ones trigger the if, and which ones make it to console.log?"
    ],
    "explanation": "4, 7, 9. Negative values (-2 and -1) trigger `continue`, which skips `console.log` for just that pass but lets the loop carry on to the next item — so every non-negative value still gets printed, in the original order, with the negatives simply missing from the output.",
    "bloomLevel": "apply",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB3-P07",
    "moduleId": "fnd-bb3",
    "lessonId": "fnd-bb3-p01",
    "type": "OPEN_ENDED",
    "prompt": "Write, in plain English (no code needed), what would happen if the `break` in this snippet were changed to `continue` instead — be specific about which items would still print.\n\n```\nlet meds = [\"Ibuprofen\", \"Omeprazole\", \"Vitamin D\"];\nfor (const name of meds) {\n  if (name === \"Omeprazole\") { break; }\n  console.log(\"Checking:\", name);\n}\n```",
    "config": {},
    "hints": [
      "With break: the loop stops entirely the moment Omeprazole is reached — Vitamin D is never checked.",
      "With continue instead: the loop would skip printing for Omeprazole specifically, but keep going to check Vitamin D afterward."
    ],
    "explanation": "As written (with break), only \"Checking: Ibuprofen\" prints — the loop stops entirely the moment it reaches Omeprazole, so Vitamin D is never even looked at. If break were changed to continue, \"Checking: Ibuprofen\" would still print, then Omeprazole's print would be skipped (continue moves to the next item without running console.log for that pass), and then the loop would carry on to Vitamin D and print \"Checking: Vitamin D\" too — so continue would produce two lines of output instead of one, because the loop keeps running past the matched item instead of stopping there.",
    "bloomLevel": "analyze",
    "difficulty": 3,
    "xpReward": 16,
    "isKnowledgeCheck": false
  },
  {
    "ref": "FND-BB3-P08",
    "moduleId": "fnd-bb3",
    "lessonId": "fnd-bb3-p01",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nlet i = 0;\nwhile (i < 3) {\n  console.log(i);\n}\n```\nWhat happens when this runs?",
    "config": {
      "options": [
        {
          "text": "Prints 0, 1, 2 and stops",
          "correct": false
        },
        {
          "text": "Prints 0 forever — an infinite loop",
          "correct": true
        },
        {
          "text": "Prints nothing, because i starts too low",
          "correct": false
        },
        {
          "text": "Throws an error immediately",
          "correct": false
        }
      ]
    },
    "hints": [
      "Look inside the loop body — is there any line that changes i?",
      "If i never changes, does i < 3 ever become false?"
    ],
    "explanation": "Prints 0 forever. There's no `i++` or any other line updating `i` inside the body, so `i` stays 0 on every single pass, and `i < 3` never becomes false. This is a genuine infinite loop — it doesn't error, it just never stops on its own.",
    "bloomLevel": "analyze",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB4-001",
    "moduleId": "fnd-bb4",
    "lessonId": "fnd-bb4-l01",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nfunction sayHi() {\n  console.log(\"Hi there.\");\n}\n\nsayHi();\nsayHi();\n```\nWhat prints?",
    "config": {
      "options": [
        {
          "text": "\"Hi there.\" once",
          "correct": false
        },
        {
          "text": "\"Hi there.\" twice",
          "correct": true
        },
        {
          "text": "Nothing — sayHi is only defined, never called",
          "correct": false
        },
        {
          "text": "An error, because sayHi is called twice",
          "correct": false
        }
      ]
    },
    "hints": [
      "The function is defined once, but look at how many times it's actually called below the definition.",
      "Each call to sayHi() runs the whole body again, independently of the call before it."
    ],
    "explanation": "\"Hi there.\" twice. sayHi is defined once, then called twice — each call runs the function's body from the top, completely independently of any earlier call. Defining a function only teaches JavaScript what to do; it's each individual sayHi() that actually makes it run.",
    "bloomLevel": "apply",
    "difficulty": 1,
    "xpReward": 8,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB4-002",
    "moduleId": "fnd-bb4",
    "lessonId": "fnd-bb4-l01",
    "type": "TRUE_FALSE_JUSTIFY",
    "prompt": "True or false: writing `logCheckIn;` (with no parentheses) runs the logCheckIn function.",
    "config": {
      "statement": "Writing a function's name with no parentheses after it, like logCheckIn;, runs the function.",
      "correctAnswer": false,
      "justificationRequired": true,
      "rubric": "A full answer says false and explains that a bare function name refers to the function without calling it — only name() actually runs it."
    },
    "hints": [
      "Compare `logCheckIn;` to `logCheckIn();` — what's the one character difference, and what does it actually control?",
      "This fails silently — no error at all — which is itself a clue about what's happening."
    ],
    "explanation": "False. `logCheckIn` with no parentheses refers to the function itself — the same way a variable name refers to a value — without running it. Only `logCheckIn()`, with the parentheses, actually executes the body. Leaving off the () produces no error and no output, which is exactly what makes this mistake easy to miss.",
    "bloomLevel": "understand",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB4-003",
    "moduleId": "fnd-bb4",
    "lessonId": "fnd-bb4-l02",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nfunction logDose(medName, mg) {\n  console.log(`${medName}: ${mg}mg`);\n}\n\nlogDose(\"Melatonin\", 5);\n```\nWhat prints?",
    "config": {
      "options": [
        {
          "text": "\"Melatonin: 5mg\"",
          "correct": true
        },
        {
          "text": "\"medName: mgmg\"",
          "correct": false
        },
        {
          "text": "\"5: Melatoninmg\"",
          "correct": false
        },
        {
          "text": "An error, because medName and mg were never declared with let",
          "correct": false
        }
      ]
    },
    "hints": [
      "Arguments are matched to parameters by position — the first argument fills the first parameter, the second fills the second.",
      "medName and mg are parameter names, not text to print literally — they get replaced by whatever was actually passed in."
    ],
    "explanation": "\"Melatonin: 5mg\". \"Melatonin\" is the first argument, matched by position to the first parameter, medName. 5 is the second argument, matched to mg. Parameters don't need their own let or const — a function's parentheses are where they're declared, automatically, the moment the function is called.",
    "bloomLevel": "apply",
    "difficulty": 1,
    "xpReward": 8,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB4-004",
    "moduleId": "fnd-bb4",
    "lessonId": "fnd-bb4-l02",
    "type": "OPEN_ENDED",
    "prompt": "```\nfunction logDose(medName, mg) {\n  console.log(`${medName}: ${mg}mg`);\n}\n\nlogDose(\"Ibuprofen\");\n```\nWhat does this actually print, and why doesn't JavaScript throw an error for the missing second argument? Explain in your own words.",
    "config": {},
    "hints": [
      "medName does receive an argument here — check what happens to mg specifically, the one that never got one.",
      "JavaScript doesn't require every parameter to receive an argument at every call — it just fills in a specific value for the ones that don't."
    ],
    "explanation": "It prints \"Ibuprofen: undefinedmg\". medName receives \"Ibuprofen\" from the one argument supplied. mg never received an argument at all, so JavaScript doesn't error — it simply leaves mg as undefined and keeps running, and the template literal drops that undefined straight into the printed text. This is exactly the same kind of silent, no-error wrongness as a missing form value: nothing stops the program, so the bad output has to be noticed by whoever's watching, not flagged automatically.",
    "bloomLevel": "analyze",
    "difficulty": 3,
    "xpReward": 16,
    "isKnowledgeCheck": false
  },
  {
    "ref": "FND-BB4-005",
    "moduleId": "fnd-bb4",
    "lessonId": "fnd-bb4-l03",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nfunction doubleSpoons(count) {\n  console.log(count * 2);\n}\n\nlet result = doubleSpoons(4);\nconsole.log(result + 1);\n```\nWhat does the second console.log print?",
    "config": {
      "options": [
        {
          "text": "9",
          "correct": false
        },
        {
          "text": "NaN",
          "correct": true
        },
        {
          "text": "8",
          "correct": false
        },
        {
          "text": "An error, since result was never given a value",
          "correct": false
        }
      ]
    },
    "hints": [
      "doubleSpoons prints 8 from inside the function, correctly — but check whether it ever hands that 8 back with return.",
      "result holds whatever doubleSpoons(4) actually returns, not whatever it happened to print — what does a function with no return statement return?"
    ],
    "explanation": "NaN. doubleSpoons prints 8 correctly from inside the function — but it never returns anything, so result is undefined, not 8. undefined + 1 isn't text-joining (neither side is a string) and it isn't valid arithmetic either — the result is NaN, JavaScript's way of saying \"this was supposed to be a number, but it isn't one.\" The printed 8 and the returned value are two completely separate things.",
    "bloomLevel": "analyze",
    "difficulty": 3,
    "xpReward": 15,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB4-006",
    "moduleId": "fnd-bb4",
    "lessonId": "fnd-bb4-l03",
    "type": "TRUE_FALSE_JUSTIFY",
    "prompt": "True or false: a function that only prints something, with no return statement, is broken.",
    "config": {
      "statement": "A function with no return statement is always a mistake — every function should return something.",
      "correctAnswer": false,
      "justificationRequired": true,
      "rubric": "A full answer says false and explains that a function whose whole job is a side effect (like printing) is completely normal without a return statement."
    },
    "hints": [
      "logCheckIn from lesson 1 has no return statement — was that a mistake, or was it doing exactly its one job?",
      "The bug only shows up when someone tries to use a no-return function's result as if it were a real value."
    ],
    "explanation": "False. A function whose entire job is a side effect — printing, saving, alerting — is completely normal without a return statement; logCheckIn from lesson 1 is a genuine example. The real mistake isn't leaving out return, it's later treating that function's result (which will always be undefined) as if it were a real, usable value.",
    "bloomLevel": "evaluate",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB4-007",
    "moduleId": "fnd-bb4",
    "lessonId": "fnd-bb4-l04",
    "type": "CATEGORIZATION",
    "prompt": "Sort each piece of this repeated code by whether it should stay hardcoded in the function's body, or become a parameter.\n\n```\nconsole.log(`Took Ibuprofen at 9am`);\nconsole.log(`Took Melatonin at 10pm`);\n```",
    "config": {
      "categories": [
        "Stays hardcoded in the body",
        "Becomes a parameter"
      ],
      "items": [
        {
          "text": "The word \"Took\"",
          "category": "Stays hardcoded in the body"
        },
        {
          "text": "The word \"at\"",
          "category": "Stays hardcoded in the body"
        },
        {
          "text": "The medication name (\"Ibuprofen\", \"Melatonin\")",
          "category": "Becomes a parameter"
        },
        {
          "text": "The time (\"9am\", \"10pm\")",
          "category": "Becomes a parameter"
        }
      ]
    },
    "hints": [
      "Check each piece against both lines — is it identical in both, or different?",
      "\"Took\" and \"at\" appear unchanged on every line — that's the tell for something that belongs in the body text, not a parameter."
    ],
    "explanation": "\"Took\" and \"at\" are identical on both lines, so they stay written directly in the function's template literal. The medication name and the time are different on every line, which is exactly what makes them the two things that need to become parameters — one function, two parameters, called twice with different arguments.",
    "bloomLevel": "analyze",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB4-008",
    "moduleId": "fnd-bb4",
    "lessonId": "fnd-bb4-l04",
    "type": "OPEN_ENDED",
    "prompt": "Turn this repeated code into one function with parameters. Write the function definition and the two calls that replace the original lines.\n\n```\nconsole.log(`Steps today: 8200`);\nconsole.log(`Steps today: 6100`);\n```",
    "config": {},
    "hints": [
      "\"Steps today:\" is identical on both lines — only the number changes.",
      "One parameter is enough here, since only one value genuinely varies."
    ],
    "explanation": "One correct answer:\n\n```\nfunction logSteps(count) {\n  console.log(`Steps today: ${count}`);\n}\n\nlogSteps(8200);\nlogSteps(6100);\n```\n\n\"Steps today:\" is identical on both original lines, so it stays hardcoded in the template literal. Only the number varies, so a single parameter (count, or any similarly clear name) is enough — the two calls then supply 8200 and 6100 as arguments.",
    "bloomLevel": "create",
    "difficulty": 2,
    "xpReward": 13,
    "isKnowledgeCheck": false
  },
  {
    "ref": "FND-BB4-009",
    "moduleId": "fnd-bb4",
    "lessonId": "fnd-bb4-l05",
    "type": "MULTIPLE_CHOICE",
    "prompt": "Which of these function names is the strongest signal that it's doing more than one job?",
    "config": {
      "options": [
        {
          "text": "calculateTip(bill, rate)",
          "correct": false
        },
        {
          "text": "validateAndSaveEntry(entry)",
          "correct": true
        },
        {
          "text": "formatDate(date)",
          "correct": false
        },
        {
          "text": "isOverdue(dueDate)",
          "correct": false
        }
      ]
    },
    "hints": [
      "Look for a name with two separate verbs joined together — that's usually two separate jobs wearing one name.",
      "calculateTip, formatDate, and isOverdue each commit to exactly one verb, one job."
    ],
    "explanation": "validateAndSaveEntry(entry) — the \"and\" is doing real work here, honestly describing two separate jobs (validating, then saving) crammed into one function. calculateTip, formatDate, and isOverdue each name exactly one action, which is usually a real sign the function actually does just that one thing.",
    "bloomLevel": "evaluate",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB4-010",
    "moduleId": "fnd-bb4",
    "lessonId": "fnd-bb4-l05",
    "type": "OPEN_ENDED",
    "prompt": "This function does three separate jobs at once. Split it into three small, single-job functions, and name each one honestly.\n\n```\nfunction checkAndLogWater(cupsToday, goal) {\n  const metGoal = cupsToday >= goal;\n  console.log(`${cupsToday}/${goal} cups${metGoal ? \" — goal met\" : \"\"}`);\n  hydrationLog.push({ cupsToday, goal, metGoal });\n  return metGoal;\n}",
    "config": {},
    "hints": [
      "Look for the same three jobs the checkAndLogSymptom example had: deciding something, formatting/printing something, saving something.",
      "Each new function's name should describe exactly one of the three jobs, with no \"and\" needed."
    ],
    "explanation": "One correct split:\n\n```\nfunction metWaterGoal(cupsToday, goal) {\n  return cupsToday >= goal;\n}\n\nfunction formatWaterLog(cupsToday, goal, metGoal) {\n  return `${cupsToday}/${goal} cups${metGoal ? \" — goal met\" : \"\"}`;\n}\n\nfunction saveHydrationEntry(entry) {\n  hydrationLog.push(entry);\n}\n```\n\nEach function now does exactly one job — deciding, formatting, saving — and each name is specific enough to describe that job with no \"and\" needed. Nothing about the underlying behavior changed; it's just reusable and testable in pieces now instead of only as one undivided block.",
    "bloomLevel": "create",
    "difficulty": 3,
    "xpReward": 16,
    "isKnowledgeCheck": false
  },
  {
    "ref": "FND-BB4-P01",
    "moduleId": "fnd-bb4",
    "lessonId": "fnd-bb4-p01",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nfunction addSpoons(current, amount) {\n  return current + amount;\n}\n\nconsole.log(addSpoons(4, 3));\n```\nWhat prints?",
    "config": {
      "options": [
        {
          "text": "7",
          "correct": true
        },
        {
          "text": "\"43\"",
          "correct": false
        },
        {
          "text": "undefined",
          "correct": false
        },
        {
          "text": "NaN",
          "correct": false
        }
      ]
    },
    "hints": [
      "current and amount are both real numbers here — no quotes anywhere — so + means addition, not text-joining.",
      "The function does have a return statement, so its result is usable, not undefined."
    ],
    "explanation": "7. Both current (4) and amount (3) are plain numbers, with no quotes anywhere, so + means addition here, not the text-joining behavior — that only happens when at least one side is a string. return hands the result (7) back to console.log, which prints it directly.",
    "bloomLevel": "apply",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB4-P02",
    "moduleId": "fnd-bb4",
    "lessonId": "fnd-bb4-p01",
    "type": "TRUE_FALSE_JUSTIFY",
    "prompt": "True or false: if a function prints a value with console.log, any code that calls that function can use the printed value afterward.",
    "config": {
      "statement": "Printing a value with console.log inside a function makes that value available to whatever code called the function.",
      "correctAnswer": false,
      "justificationRequired": true,
      "rubric": "A full answer says false and explains that console.log only displays a value; only return hands a value back to the caller."
    },
    "hints": [
      "Printing and returning are two separate actions a function can take — check whether printing alone hands anything back.",
      "If the function has no return statement, what does calling it actually produce for the caller, regardless of what it printed?"
    ],
    "explanation": "False. console.log only displays a value in the console for a human to look at — it doesn't hand anything back to the code that called the function. Without an explicit return statement, the function's result is undefined to its caller, no matter what it printed along the way. Printing and returning are two separate, unrelated actions.",
    "bloomLevel": "evaluate",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB4-P03",
    "moduleId": "fnd-bb4",
    "lessonId": "fnd-bb4-p01",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nconsole.log(`Dose: 400mg at 9am`);\nconsole.log(`Dose: 200mg at 9pm`);\n```\nExtracting this into one function, how many parameters does it need?",
    "config": {
      "options": [
        {
          "text": "1 — only the dose amount changes",
          "correct": false
        },
        {
          "text": "2 — the dose amount and the time both change",
          "correct": true
        },
        {
          "text": "0 — the two lines are identical",
          "correct": false
        },
        {
          "text": "4 — every word could be a parameter",
          "correct": false
        }
      ]
    },
    "hints": [
      "Compare the two lines word by word — which pieces are literally the same both times, and which pieces differ?",
      "\"Dose:\" and \"at\" are identical on both lines — they don't need to be parameters at all."
    ],
    "explanation": "2. Comparing the lines directly: \"Dose: 400mg at 9am\" and \"Dose: 200mg at 9pm\" — the dose amount (400/200) differs, and the time (9am/9pm) differs. \"Dose:\" and \"at\" are identical both times, so they stay hardcoded in the function's body text. Two things vary, so the function needs two parameters.",
    "bloomLevel": "analyze",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB4-P04",
    "moduleId": "fnd-bb4",
    "lessonId": "fnd-bb4-p01",
    "type": "SEQUENCING",
    "prompt": "Put these four steps in the order you'd actually follow to turn two repeated console.log lines into one function.",
    "config": {
      "items": [
        "Write the function definition with the varying parts as parameters",
        "Compare the repeated lines and identify what's identical versus what varies",
        "Replace the original lines with calls to the new function, passing in the right arguments",
        "Run the code and confirm the output still matches what the original repeated lines produced"
      ],
      "correctOrder": [
        1,
        0,
        2,
        3
      ]
    },
    "hints": [
      "You can't write correct parameters without first knowing what they're actually standing in for.",
      "Nothing can be checked against the original output until the replacement is actually in place."
    ],
    "explanation": "Comparing the lines to find what's identical versus what varies has to come first — you can't write correct parameters without knowing what they stand in for. Writing the definition comes next, since the calls need it to already exist. Replacing the original lines with calls follows. Confirming the output matches has to be last, because there's nothing to check against until the replacement is actually in place.",
    "bloomLevel": "apply",
    "difficulty": 2,
    "xpReward": 13,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB4-P05",
    "moduleId": "fnd-bb4",
    "lessonId": "fnd-bb4-p01",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nfunction logSpoonUse(activity, count) {\n  console.log(`${activity} cost ${count} spoons`);\n}\n\nlogSpoonUse(\"Shower\");\n```\nWhat prints?",
    "config": {
      "options": [
        {
          "text": "\"Shower cost undefined spoons\"",
          "correct": true
        },
        {
          "text": "An error — count is required",
          "correct": false
        },
        {
          "text": "\"Shower cost 0 spoons\"",
          "correct": false
        },
        {
          "text": "\"undefined cost undefined spoons\"",
          "correct": false
        }
      ]
    },
    "hints": [
      "logSpoonUse is called with only one argument — what happens to the parameter that never received one?",
      "There's no default value written anywhere in this function's definition — so a missing argument doesn't fall back to 0, it becomes undefined."
    ],
    "explanation": "\"Shower cost undefined spoons\". activity receives \"Shower\" from the one argument supplied. count never received an argument at all, so it becomes undefined — with no default value written in the function's parentheses, JavaScript doesn't invent a 0 for you, it just leaves the parameter as undefined and keeps running, template literal and all.",
    "bloomLevel": "apply",
    "difficulty": 3,
    "xpReward": 15,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB4-P06",
    "moduleId": "fnd-bb4",
    "lessonId": "fnd-bb4-p01",
    "type": "OPEN_ENDED",
    "prompt": "A teammate writes this function:\n\n```\nfunction calculateTotal(items) {\n  let total = 0;\n  for (const item of items) {\n    total = total + item.price;\n  }\n  console.log(total);\n}\n```\nThey call it, expecting to store the total in a variable and use it later: `let total = calculateTotal(cart);`. It doesn't work. Explain what's wrong and how you'd fix it.",
    "config": {},
    "hints": [
      "calculateTotal does correctly compute the right number — check what it actually does with that number once it has it.",
      "console.log displays a value. It's a completely different action from return, which is the only thing that hands a value back to the caller."
    ],
    "explanation": "calculateTotal computes the correct total and prints it, but it never returns it — so `let total = calculateTotal(cart);` receives undefined, because that's what any function with no return statement always hands back, regardless of what it printed. The fix is to replace (or add to) `console.log(total);` with `return total;`, so the computed value is actually handed back to whatever called the function, instead of only being displayed.",
    "bloomLevel": "analyze",
    "difficulty": 3,
    "xpReward": 16,
    "isKnowledgeCheck": false
  },
  {
    "ref": "FND-BB4-P07",
    "moduleId": "fnd-bb4",
    "lessonId": "fnd-bb4-p01",
    "type": "MATCHING",
    "prompt": "Match each function name to the single job it's honestly describing.",
    "config": {
      "pairs": [
        {
          "left": "isSevereSymptom(severity)",
          "right": "Decides a yes/no answer from a number"
        },
        {
          "left": "formatSymptomLog(symptom, severity)",
          "right": "Builds a labeled piece of text to display"
        },
        {
          "left": "saveSymptomEntry(entry)",
          "right": "Writes data somewhere it will persist"
        }
      ],
      "distractors": {
        "right": [
          "Deletes every entry in the log at once"
        ]
      }
    },
    "hints": [
      "\"is\" at the start of a name is a strong signal the function returns a boolean.",
      "\"format\" and \"save\" describe two genuinely different single actions — text-building versus persisting data."
    ],
    "explanation": "isSevereSymptom(severity) decides a true/false answer from a number — the \"is\" prefix is a strong, honest signal of exactly that. formatSymptomLog(symptom, severity) builds a piece of readable text and hands it back, without touching storage or the console. saveSymptomEntry(entry) writes data somewhere durable. Each name commits to describing one specific action, which is exactly what makes single-responsibility functions easy to trust from their name alone.",
    "bloomLevel": "understand",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB4-P08",
    "moduleId": "fnd-bb4",
    "lessonId": "fnd-bb4-p01",
    "type": "MULTIPLE_CHOICE",
    "prompt": "Which of these function names is the strongest signal that the function is doing more than one job?",
    "config": {
      "options": [
        {
          "text": "calculateTip(bill, rate)",
          "correct": false
        },
        {
          "text": "validateAndSaveEntry(entry)",
          "correct": true
        },
        {
          "text": "formatDate(date)",
          "correct": false
        },
        {
          "text": "isOverdue(dueDate)",
          "correct": false
        }
      ]
    },
    "hints": [
      "Look for a name with two separate verbs joined together.",
      "calculateTip, formatDate, and isOverdue each commit to exactly one verb, one job."
    ],
    "explanation": "validateAndSaveEntry(entry) — the \"and\" honestly describes two separate jobs (validating, then saving) crammed into one function. The other three each name exactly one action, which is usually a real sign the function actually does just that one thing.",
    "bloomLevel": "evaluate",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB5-001",
    "moduleId": "fnd-bb5",
    "lessonId": "fnd-bb5-l01",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nlet spoonsPerDay = [10, 6, 8, 4, 9];\nconsole.log(spoonsPerDay[2]);\n```\nWhat prints?",
    "config": {
      "options": [
        {
          "text": "8",
          "correct": true
        },
        {
          "text": "6",
          "correct": false
        },
        {
          "text": "2",
          "correct": false
        },
        {
          "text": "undefined",
          "correct": false
        }
      ]
    },
    "hints": [
      "Indexes start at 0 — count positions from there, not from 1.",
      "Index 0 is 10, index 1 is 6 — count one more step to reach index 2."
    ],
    "explanation": "8. Indexing starts at 0: index 0 is 10, index 1 is 6, index 2 is 8. spoonsPerDay[2] reaches the third value in the array, not the value 2 itself.",
    "bloomLevel": "apply",
    "difficulty": 1,
    "xpReward": 8,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB5-002",
    "moduleId": "fnd-bb5",
    "lessonId": "fnd-bb5-l01",
    "type": "TRUE_FALSE_JUSTIFY",
    "prompt": "True or false: symptoms.push(\"nausea\") replaces the first item in the symptoms array with \"nausea\".",
    "config": {
      "statement": "Calling array.push(value) replaces the first item in the array with that value.",
      "correctAnswer": false,
      "justificationRequired": true,
      "rubric": "A full answer says false and explains that push adds a new item onto the end of the array, growing its length, without touching any existing items."
    },
    "hints": [
      "push means \"add\", not \"replace\" — where does the new item actually go?",
      "Check what happens to the array's length after a push — does it grow, or stay the same?"
    ],
    "explanation": "False. push(value) adds a new item onto the end of the array, growing its length by one — the existing items stay exactly where they were, at their original indexes. Nothing about push replaces or removes anything already in the array.",
    "bloomLevel": "understand",
    "difficulty": 1,
    "xpReward": 8,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB5-003",
    "moduleId": "fnd-bb5",
    "lessonId": "fnd-bb5-l02",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nlet steps = [8200, 6100, 9400, 5300];\nlet mostRecent = steps[steps.length - 1];\nconsole.log(mostRecent);\n```\nWhat prints?",
    "config": {
      "options": [
        {
          "text": "5300",
          "correct": true
        },
        {
          "text": "9400",
          "correct": false
        },
        {
          "text": "undefined",
          "correct": false
        },
        {
          "text": "4",
          "correct": false
        }
      ]
    },
    "hints": [
      "steps.length is 4. What's 4 - 1?",
      "The last valid index of a 4-item array is 3, and index 3 holds the fourth value."
    ],
    "explanation": "5300. steps.length is 4, so steps.length - 1 is 3 — the correct last index for a 4-item array. steps[3] is the fourth value, 5300, the actual last item written in the array.",
    "bloomLevel": "apply",
    "difficulty": 2,
    "xpReward": 10,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB5-004",
    "moduleId": "fnd-bb5",
    "lessonId": "fnd-bb5-l02",
    "type": "OPEN_ENDED",
    "prompt": "```\nlet symptoms = [\"fatigue\", \"brain fog\", \"joint pain\"];\nconsole.log(symptoms[symptoms.length]);\nconsole.log(symptoms[-1]);\n```\nWhat does each line actually print, and why does neither one give you \"joint pain\", the real last item? Explain both in your own words.",
    "config": {},
    "hints": [
      "symptoms.length is 3 — but is 3 a valid index for a 3-item array?",
      "-1 isn't a shortcut for \"the last item\" in plain JavaScript, the way it might be in some other languages."
    ],
    "explanation": "Both print undefined. symptoms[symptoms.length] is symptoms[3] — but a 3-item array only has valid indexes 0, 1, 2, so index 3 is one past the real last item and was never assigned anything. symptoms[-1] looks for a property literally named \"-1\", which was never created either — plain JavaScript arrays have no built-in \"count from the end\" behavior for negative indexes. The only reliable way to reach \"joint pain\" here is symptoms[symptoms.length - 1], which correctly lands on index 2.",
    "bloomLevel": "analyze",
    "difficulty": 3,
    "xpReward": 16,
    "isKnowledgeCheck": false
  },
  {
    "ref": "FND-BB5-005",
    "moduleId": "fnd-bb5",
    "lessonId": "fnd-bb5-l03",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nlet contact = { name: \"Dr. Reyes\", phone: \"555-0142\" };\nconsole.log(contact.phone);\n```\nWhat prints?",
    "config": {
      "options": [
        {
          "text": "\"555-0142\"",
          "correct": true
        },
        {
          "text": "\"phone\"",
          "correct": false
        },
        {
          "text": "undefined",
          "correct": false
        },
        {
          "text": "An error, since contact has no index 0",
          "correct": false
        }
      ]
    },
    "hints": [
      "Dot notation reaches a field by its name — phone is a real field on this object.",
      "Objects aren't read by index at all, so there's nothing wrong with there being no contact[0]."
    ],
    "explanation": "\"555-0142\". contact.phone uses dot notation to reach the field named phone directly, returning its value. Objects don't need or use numeric indexes the way arrays do — dot notation with the field's name is the normal, only way to read a field.",
    "bloomLevel": "apply",
    "difficulty": 1,
    "xpReward": 8,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB5-006",
    "moduleId": "fnd-bb5",
    "lessonId": "fnd-bb5-l03",
    "type": "OPEN_ENDED",
    "prompt": "```\nlet medication = { name: \"Ibuprofen\", doseMg: 400, takenToday: false };\nmedication.takenToday = true;\nmedication.refillDate = \"2026-09-01\";\nconsole.log(medication);\n```\nWhat does the final console.log actually print? Explain what each of the two lines above it changed.",
    "config": {},
    "hints": [
      "The first new line updates a field that already existed. The second adds one that didn't.",
      "Neither line touches name or doseMg at all."
    ],
    "explanation": "It prints `{ name: \"Ibuprofen\", doseMg: 400, takenToday: true, refillDate: \"2026-09-01\" }`. `medication.takenToday = true;` updates the existing takenToday field in place, leaving name and doseMg untouched. `medication.refillDate = \"2026-09-01\";` adds a brand-new fourth field to the same object — dot notation can both update an existing field and add a new one, with no special syntax needed for either.",
    "bloomLevel": "apply",
    "difficulty": 2,
    "xpReward": 13,
    "isKnowledgeCheck": false
  },
  {
    "ref": "FND-BB5-007",
    "moduleId": "fnd-bb5",
    "lessonId": "fnd-bb5-l04",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nlet books = [\n  { title: \"Sea Room\", read: true },\n  { title: \"The Undying\", read: false }\n];\nconsole.log(books[1].title);\n```\nWhat prints?",
    "config": {
      "options": [
        {
          "text": "\"The Undying\"",
          "correct": true
        },
        {
          "text": "\"Sea Room\"",
          "correct": false
        },
        {
          "text": "false",
          "correct": false
        },
        {
          "text": "{ title: \"The Undying\", read: false }",
          "correct": false
        }
      ]
    },
    "hints": [
      "books[1] reaches the second object in the array (index 1) — the one for \"The Undying\".",
      ".title after that goes one level further, into that specific object's title field."
    ],
    "explanation": "\"The Undying\". books[1] reaches the second object in the array — index 1, the one describing \"The Undying\". .title then reaches that specific object's title field, not the whole object and not the read field.",
    "bloomLevel": "apply",
    "difficulty": 2,
    "xpReward": 10,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB5-008",
    "moduleId": "fnd-bb5",
    "lessonId": "fnd-bb5-l04",
    "type": "OPEN_ENDED",
    "prompt": "```\nlet symptoms = [\n  { name: \"fatigue\", severity: 7 },\n  { name: \"brain fog\", severity: 5 }\n];\n\nfor (const symptom of symptoms) {\n  console.log(`${symptom.name}: ${symptom.severity}`);\n}\n```\nWhat does this print, across both passes of the loop? Explain what symptom holds on each pass.",
    "config": {},
    "hints": [
      "On each pass, symptom is one whole object from the array, not a plain value.",
      "symptom.name and symptom.severity reach into whichever object symptom currently holds."
    ],
    "explanation": "It prints \"fatigue: 7\" then \"brain fog: 5\" — one line per pass. On the first pass, symptom holds the first object, { name: \"fatigue\", severity: 7 }; on the second, it holds the second object. symptom.name and symptom.severity reach into whichever object symptom currently holds on that pass, the same way symptoms[0].name would have reached the first object's field directly, outside the loop.",
    "bloomLevel": "apply",
    "difficulty": 2,
    "xpReward": 13,
    "isKnowledgeCheck": false
  },
  {
    "ref": "FND-BB5-009",
    "moduleId": "fnd-bb5",
    "lessonId": "fnd-bb5-l05",
    "type": "MULTIPLE_CHOICE",
    "prompt": "You need to store one grocery order: the store name, the total cost, and whether it's been delivered yet. What's the best-fitting shape?",
    "config": {
      "options": [
        {
          "text": "A plain array: [\"Trader Joe's\", 42.50, false]",
          "correct": false
        },
        {
          "text": "An object: { store: \"Trader Joe's\", total: 42.50, delivered: false }",
          "correct": true
        },
        {
          "text": "Three separate variables with no connection between them",
          "correct": false
        },
        {
          "text": "An array of objects, one object per value",
          "correct": false
        }
      ]
    },
    "hints": [
      "This is one thing (one order) with several different kinds of information about it — which shape has named fields for exactly that?",
      "A plain array would technically work, but check what position 0, 1, 2 would actually tell a reader without checking the original order it was written in."
    ],
    "explanation": "The object. This is one thing (one order) described by several different, specifically-named facts about it — exactly what an object's fields are for. A plain array would technically hold the same three values, but store, total, and delivered would only be knowable by remembering position 0, 1, 2 — the object's field names make that explicit instead of implicit. Three unconnected variables lose the fact that all three describe the same order. An array of objects is overkill for describing just one thing.",
    "bloomLevel": "evaluate",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB5-010",
    "moduleId": "fnd-bb5",
    "lessonId": "fnd-bb5-l05",
    "type": "OPEN_ENDED",
    "prompt": "A teammate models a week of medication logs as three parallel arrays: doseDates, doseMeds, doseTaken. Explain, concretely, one specific way this can silently go wrong that an array of objects wouldn't.",
    "config": {},
    "hints": [
      "Think about what happens if one array gets a new item pushed to it, but the other two don't.",
      "Think about what happens if one of the three arrays gets sorted or filtered on its own."
    ],
    "explanation": "One concrete failure: if a new dose is logged and only doseDates.push(...) and doseMeds.push(...) are called — doseTaken.push(...) gets forgotten — the three arrays are now different lengths, and every position after that point describes the wrong day for at least one of the three facts, with nothing in the code catching it. Another: sorting doseDates alone (say, to display dates in order) leaves doseMeds and doseTaken in their original order, instantly breaking the pairing between date, medication, and taken-status for every entry. An array of objects makes both mistakes structurally impossible, because each day's date, medication, and taken-status live together in one object — there's no separate array to forget to update or accidentally sort out of sync.",
    "bloomLevel": "analyze",
    "difficulty": 3,
    "xpReward": 16,
    "isKnowledgeCheck": false
  },
  {
    "ref": "FND-BB5-P01",
    "moduleId": "fnd-bb5",
    "lessonId": "fnd-bb5-p01",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nlet spoonsPerDay = [10, 6, 8, 4, 9];\nconsole.log(spoonsPerDay[2]);\n```\nWhat prints?",
    "config": {
      "options": [
        {
          "text": "8",
          "correct": true
        },
        {
          "text": "6",
          "correct": false
        },
        {
          "text": "2",
          "correct": false
        },
        {
          "text": "undefined",
          "correct": false
        }
      ]
    },
    "hints": [
      "Indexes start at 0 — count positions from there, not from 1.",
      "Index 0 is 10, index 1 is 6 — count one more step to reach index 2."
    ],
    "explanation": "8. Indexing starts at 0: index 0 is 10, index 1 is 6, index 2 is 8. spoonsPerDay[2] reaches the third value in the array, not the value 2 itself.",
    "bloomLevel": "apply",
    "difficulty": 1,
    "xpReward": 8,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB5-P02",
    "moduleId": "fnd-bb5",
    "lessonId": "fnd-bb5-p01",
    "type": "TRUE_FALSE_JUSTIFY",
    "prompt": "True or false: for an array with 6 items, array[6] gives you the last item.",
    "config": {
      "statement": "For an array holding 6 items, array[6] reaches the last item in the array.",
      "correctAnswer": false,
      "justificationRequired": true,
      "rubric": "A full answer says false and explains that the last valid index is length - 1 (5, here), not length itself (6)."
    },
    "hints": [
      "An array with 6 items has valid indexes 0 through... how many, exactly?",
      "length counts items; the last valid index is always one less than that count."
    ],
    "explanation": "False. An array with 6 items has valid indexes 0 through 5 — six positions, starting at 0. array[6] reaches one position past the real last item, a spot that was never assigned anything, so it returns undefined instead of the last real value. The last valid index is always length - 1, here 5, not length itself.",
    "bloomLevel": "analyze",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB5-P03",
    "moduleId": "fnd-bb5",
    "lessonId": "fnd-bb5-p01",
    "type": "MULTIPLE_CHOICE",
    "prompt": "You need to store one grocery order: the store name, the total cost, and whether it's been delivered yet. What's the best-fitting shape?",
    "config": {
      "options": [
        {
          "text": "A plain array: [\"Trader Joe's\", 42.50, false]",
          "correct": false
        },
        {
          "text": "An object: { store: \"Trader Joe's\", total: 42.50, delivered: false }",
          "correct": true
        },
        {
          "text": "Three separate variables with no connection between them",
          "correct": false
        },
        {
          "text": "An array of objects, one object per value",
          "correct": false
        }
      ]
    },
    "hints": [
      "This is one thing (one order) with several different kinds of information about it — which shape has named fields for exactly that?",
      "A plain array would work, technically, but nothing about position 0, 1, 2 tells you what each value means without checking the original order it was written in."
    ],
    "explanation": "The object. This is one thing (one order) described by several different, specifically-named facts about it — exactly what an object's fields are for. A plain array would technically hold the same three values, but store, total, and delivered would only be knowable by remembering position 0, 1, 2 — the object's field names make that explicit instead of implicit. Three unconnected variables lose the fact that all three describe the same order. An array of objects is overkill for describing just one thing.",
    "bloomLevel": "evaluate",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB5-P04",
    "moduleId": "fnd-bb5",
    "lessonId": "fnd-bb5-p01",
    "type": "SEQUENCING",
    "prompt": "Put these steps in the order you'd follow to turn three parallel arrays (dates, painLevels, notes) into one array of objects.",
    "config": {
      "items": [
        "Loop over one of the arrays by index, building one object per position from all three arrays",
        "Replace the three original parallel arrays with the new array of objects",
        "Confirm the three arrays are the same length and lined up by position",
        "Push each newly built object into a new, single array"
      ],
      "correctOrder": [
        2,
        0,
        3,
        1
      ]
    },
    "hints": [
      "Restructuring data that's already desynced just locks in the mismatch — what needs checking before anything else?",
      "The originals can't be replaced with something that doesn't exist yet."
    ],
    "explanation": "Confirming the three arrays are actually the same length and still lined up has to come first — restructuring data that's already desynced just locks in the mismatch. Looping and building one object per position comes next, since that's the actual conversion. Pushing each object into a new array collects the result. Replacing the original three arrays is last, since there's nothing to replace them with until the new array actually exists.",
    "bloomLevel": "apply",
    "difficulty": 3,
    "xpReward": 15,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB5-P05",
    "moduleId": "fnd-bb5",
    "lessonId": "fnd-bb5-p01",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nlet med = { name: \"Melatonin\", doseMg: 5 };\nmed.doseMg = 10;\nconsole.log(med);\n```\nWhat does this print?",
    "config": {
      "options": [
        {
          "text": "{ name: \"Melatonin\", doseMg: 10 }",
          "correct": true
        },
        {
          "text": "{ name: \"Melatonin\", doseMg: 5 }",
          "correct": false
        },
        {
          "text": "10",
          "correct": false
        },
        {
          "text": "An error — doseMg was already set once",
          "correct": false
        }
      ]
    },
    "hints": [
      "Dot notation on the left of = updates that field in place — it doesn't create a second, separate object.",
      "Only doseMg is being changed here — name is never touched by this line."
    ],
    "explanation": "{ name: \"Melatonin\", doseMg: 10 }. med.doseMg = 10 updates the doseMg field of the existing object in place — name is completely untouched, and there's only ever one med object, not two. Objects can be reassigned field by field after they're created; nothing about the original 5 was \"locked in\".",
    "bloomLevel": "apply",
    "difficulty": 1,
    "xpReward": 8,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB5-P06",
    "moduleId": "fnd-bb5",
    "lessonId": "fnd-bb5-p01",
    "type": "CATEGORIZATION",
    "prompt": "Sort each real-world dataset by which shape actually fits it best.",
    "config": {
      "categories": [
        "Plain array",
        "Object",
        "Array of objects"
      ],
      "items": [
        {
          "text": "A week of daily step counts: 8200, 6100, 9400, ...",
          "category": "Plain array"
        },
        {
          "text": "One user's account: username, email, joinDate",
          "category": "Object"
        },
        {
          "text": "A grocery list where every item has a name, quantity, and price",
          "category": "Array of objects"
        },
        {
          "text": "Five reminder times for today's medications: \"9am\", \"1pm\", \"6pm\", \"9pm\", \"11pm\"",
          "category": "Plain array"
        },
        {
          "text": "A contact card: name, phone, relationship",
          "category": "Object"
        },
        {
          "text": "A log of this week's check-ins, each with a date, mood, and note",
          "category": "Array of objects"
        }
      ]
    },
    "hints": [
      "A plain array fits several values of the same kind, in order, with no need for named fields.",
      "An array of objects fits several things that each have the same set of named fields — not just several plain values."
    ],
    "explanation": "Step counts and reminder times are each a list of one plain kind of value, in order — plain arrays. An account and a contact card are each one thing with several different, named facts about it — objects. A grocery list and a week of check-ins are each several structurally similar things, each needing its own named fields — arrays of objects. The test throughout is the same: one thing with several fields is an object; several plain values of the same kind is an array; several structured things is an array of objects.",
    "bloomLevel": "understand",
    "difficulty": 2,
    "xpReward": 13,
    "isKnowledgeCheck": true
  },
  {
    "ref": "FND-BB5-P07",
    "moduleId": "fnd-bb5",
    "lessonId": "fnd-bb5-p01",
    "type": "OPEN_ENDED",
    "prompt": "A teammate models a week of medication logs as three parallel arrays: doseDates, doseMeds, doseTaken. Explain, concretely, one specific way this can silently go wrong that an array of objects wouldn't.",
    "config": {},
    "hints": [
      "Think about what happens if one array gets a new item pushed to it, but the other two don't.",
      "Think about what happens if one of the three arrays gets sorted or filtered on its own."
    ],
    "explanation": "One concrete failure: if a new dose is logged and only doseDates.push(...) and doseMeds.push(...) are called — doseTaken.push(...) gets forgotten — the three arrays are now different lengths, and every position after that point describes the wrong day for at least one of the three facts, with nothing in the code catching it. Another: sorting doseDates alone leaves doseMeds and doseTaken in their original order, instantly breaking the pairing for every entry. An array of objects makes both mistakes structurally impossible, because each day's facts live together in one object — there's no separate array to forget to update or accidentally sort out of sync.",
    "bloomLevel": "analyze",
    "difficulty": 3,
    "xpReward": 16,
    "isKnowledgeCheck": false
  },
  {
    "ref": "FND-BB5-P08",
    "moduleId": "fnd-bb5",
    "lessonId": "fnd-bb5-p01",
    "type": "MULTIPLE_CHOICE",
    "prompt": "```\nlet doses = [\n  { med: \"Ibuprofen\", mg: 400 },\n  { med: \"Melatonin\", mg: 5 }\n];\n\nfor (const dose of doses) {\n  console.log(dose.med);\n}\n```\nWhat prints, across both passes?",
    "config": {
      "options": [
        {
          "text": "\"Ibuprofen\" then \"Melatonin\"",
          "correct": true
        },
        {
          "text": "400 then 5",
          "correct": false
        },
        {
          "text": "The whole object, twice",
          "correct": false
        },
        {
          "text": "\"Ibuprofen\" then \"Ibuprofen\"",
          "correct": false
        }
      ]
    },
    "hints": [
      "dose.med reaches the med field specifically — not the mg field, and not the whole object.",
      "Each pass of for...of hands you the next object in the array, not the same one repeated."
    ],
    "explanation": "\"Ibuprofen\" then \"Melatonin\". Each pass of the loop hands dose one whole object from the array in turn — the first object, then the second — and dose.med reaches specifically the med field of whichever object dose currently holds, not mg and not the whole object.",
    "bloomLevel": "apply",
    "difficulty": 2,
    "xpReward": 12,
    "isKnowledgeCheck": true
  }
]
