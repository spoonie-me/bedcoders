{
  "buildCommand": "npm run build && cd backend && npx prisma generate",
  "outputDirectory": "dist",
  "framework": "vite",
  "functions": {
    "api/index.ts": {
      "maxDuration": 30,
      "runtime": "nodejs22.x",
      "includeFiles": "backend/src/**"
    }
  },
  "rewrites": [
    { "source": "/podcast.xml", "destination": "/api" },
    { "source": "/api/:path*", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
